/*        Generated by TAPENADE     (INRIA, Ecuador team)
    Tapenade 3.16 (develop) -  6 Jan 2026 19:07
*/
#include "DIFFSIZESC.inc"
#include <stdint.h>
#include <stdio.h>
#include <ctype.h>
#include "cblas_dv.h"
#include "cblas_f77_dv.h"
#include <stdc-predef.h>
#include "cblas_dv.h"
#include "cblas_f77_dv.h"

/* Declaration for differentiated Fortran routine */
/* void F77_sgbmv_dv_base(...); */
/* Note: This should match the signature of sgbmv_dv in Fortran */


/*
  Differentiation of cblas_sgbmv in forward (tangent) mode (with options multiDirectional):
   variations   of useful results: *Y
   with respect to varying inputs: alpha *A beta *X *Y
   RW status of diff variables: alpha:in A:(loc) *A:in beta:in
                X:(loc) *X:in Y:(loc) *Y:in-out
*/
void cblas_sgbmv_dv(const CBLAS_LAYOUT layout, const CBLAS_TRANSPOSE TransA, 
        const __int32_t M, const __int32_t N, const __int32_t KL, const 
        __int32_t KU, const float alpha, const float alphad[NBDirsMax], const 
        float *A, const float (*Ad)[NBDirsMax], const __int32_t lda, const 
        float *X, const float (*Xd)[NBDirsMax], const __int32_t incX, const 
        float beta, const float betad[NBDirsMax], float *Y, float (*Yd)[
        NBDirsMax], const __int32_t incY, int nbdirs) {
    char TA;
    int32_t F77_M;
    F77_M = M;
    int32_t F77_N;
    F77_N = N;
    int32_t F77_lda;
    F77_lda = lda;
    int32_t F77_incX;
    F77_incX = incX;
    int32_t F77_incY;
    F77_incY = incY;
    int32_t F77_KL;
    F77_KL = KL;
    int32_t F77_KU;
    F77_KU = KU;
    extern int CBLAS_CallFromC;
    extern int RowMajorStrg;
    RowMajorStrg = 0;
    CBLAS_CallFromC = 1;
    if (layout == CblasColMajor) {
        if (TransA == CblasNoTrans)
            TA = 'N';
        else if (TransA == CblasTrans)
            TA = 'T';
        else if (TransA == CblasConjTrans)
            TA = 'C';
        else {
            cblas_xerbla(2, "cblas_sgbmv", "Illegal TransA setting, %d\n", 
                         TransA);
            CBLAS_CallFromC = 0;
            RowMajorStrg = 0;
            return;
        }
        F77_sgbmv_dv(&TA, &F77_M, &F77_N, &F77_KL, &F77_KU, (float *)&alpha, (float *)alphad, (float *)A, (float *)Ad, &F77_lda, (float *)X, (float *)Xd, &F77_incX, (float *)&beta, (float *)betad, (float *)Y, (float *)Yd, &
                  F77_incY, &nbdirs, (size_t)1, (size_t)1);
    } else if (layout == CblasRowMajor) {
        RowMajorStrg = 1;
        if (TransA == CblasNoTrans)
            TA = 'T';
        else if (TransA == CblasTrans)
            TA = 'N';
        else if (TransA == CblasConjTrans)
            TA = 'N';
        else {
            cblas_xerbla(2, "cblas_sgbmv", "Illegal TransA setting, %d\n", 
                         TransA);
            CBLAS_CallFromC = 0;
            RowMajorStrg = 0;
            return;
        }
        F77_sgbmv_dv(&TA, &F77_N, &F77_M, &F77_KU, &F77_KL, (float *)&alpha, (float *)alphad, (float *)A, (float *)Ad, &F77_lda, (float *)X, (float *)Xd, &F77_incX, (float *)&beta, (float *)betad, (float *)Y, (float *)Yd, &
                  F77_incY, &nbdirs, (size_t)1, (size_t)1);
    } else
        cblas_xerbla(1, "cblas_sgbmv", "Illegal layout setting, %d\n", layout)
        ;
    CBLAS_CallFromC = 0;
    RowMajorStrg = 0;
    return;
}
