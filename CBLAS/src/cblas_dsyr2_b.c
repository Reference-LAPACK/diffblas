/*        Generated by TAPENADE     (INRIA, Ecuador team)
    Tapenade 3.16 (develop) -  6 Jan 2026 19:07
*/
#include <adStack.h>
#include <stdio.h>
#include <ctype.h>
#include "cblas_b.h"
#include "cblas_f77_b.h"
#include <adStack.h>
#include <stdc-predef.h>
#include "cblas_b.h"
#include "cblas_f77_b.h"

/* Declaration for differentiated Fortran routine */
/* void F77_dsyr2_b_base(...); */
/* Note: This should match the signature of dsyr2_b in Fortran */

/* F77_ macros for differentiated Fortran routines */
/* These macros handle name mangling for differentiated Fortran functions */
#define F77_dsyr2_b_base F77_GLOBAL_SUFFIX(dsyr2_b,DSYR2_B)
#define F77_dsyr2_b(...) F77_dsyr2_b_base(__VA_ARGS__)


/*
  Differentiation of cblas_dsyr2 in reverse (adjoint) mode:
   gradient     of useful results: *A
   with respect to varying inputs: alpha *A *X *Y
   RW status of diff variables: alpha:out A:(loc) *A:in-out X:(loc)
                *X:out Y:(loc) *Y:out
*/
void cblas_dsyr2_b(const CBLAS_LAYOUT layout, const CBLAS_UPLO Uplo, const 
        __int32_t N, const double alpha, double *alphab, const double *X, 
        double *Xb, const __int32_t incX, const double *Y, double *Yb, const 
        __int32_t incY, double *A, double *Ab, const __int32_t lda) {
    char UL;
    int32_t F77_N = N;
    int32_t F77_incX = incX;
    int32_t F77_incY = incY;
    int32_t F77_lda = lda;
    extern int CBLAS_CallFromC;
    extern int RowMajorStrg;
    int branch;
    if (layout == CblasColMajor) {
        if (Uplo == CblasLower) {
            pushControl1b(0);
            UL = 'L';
        } else if (Uplo == CblasUpper) {
            pushControl1b(1);
            UL = 'U';
        } else {
            if (alphab)
                *alphab = 0.0;
            if (Xb)
                *Xb = 0.0;
            if (Yb)
                *Yb = 0.0;
            goto label100;
        }
        F77_dsyr2_b(&UL, &F77_N, &alpha, &(*alphab), X, Xb, &F77_incX, Y, Yb, &
                 F77_incY, A, Ab, &F77_lda);
        popControl1b(&branch);
    } else if (layout == CblasRowMajor) {
        if (Uplo == CblasLower) {
            pushControl1b(0);
            UL = 'U';
        } else if (Uplo == CblasUpper) {
            pushControl1b(1);
            UL = 'L';
        } else {
            if (alphab)
                *alphab = 0.0;
            if (Xb)
                *Xb = 0.0;
            if (Yb)
                *Yb = 0.0;
            goto label100;
        }
        F77_dsyr2_b(&UL, &F77_N, &alpha, &(*alphab), X, Xb, &F77_incX, Y, Yb, &
                 F77_incY, A, Ab, &F77_lda);
        popControl1b(&branch);
    } else {
        if (alphab)
            *alphab = 0.0;
        if (Xb)
            *Xb = 0.0;
        if (Yb)
            *Yb = 0.0;
    }
  label100:
    ;
}
