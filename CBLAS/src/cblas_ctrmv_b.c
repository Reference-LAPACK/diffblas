/*        Generated by TAPENADE     (INRIA, Ecuador team)
    Tapenade 3.16 (develop) -  6 Jan 2026 19:07
*/
#include <complex.h>
#include <adStack.h>
#include <stdio.h>
#include <ctype.h>
#include "cblas_b.h"
#include "cblas_f77_b.h"
#include <adStack.h>
#include <stdc-predef.h>
#include "cblas_b.h"
#include "cblas_f77_b.h"

/* Declaration for differentiated Fortran routine */
/* void F77_ctrmv_b_base(..., (size_t)1, (size_t)1, (size_t)1); */
/* Note: This should match the signature of ctrmv_b in Fortran */

/* F77_ macros for differentiated Fortran routines */
/* These macros handle name mangling for differentiated Fortran functions */
#define F77_ctrmv_b_base F77_GLOBAL_SUFFIX(ctrmv_b,CTRMV_B)
#define F77_ctrmv_b(...) F77_ctrmv_b_base(__VA_ARGS__)


/*
  Differentiation of cblas_ctrmv in reverse (adjoint) mode:
   gradient     of useful results: *X
   with respect to varying inputs: *A *X
   RW status of diff variables: A:(loc) *A:out X:(loc) *X:in-out
   Plus diff mem management of: X:in
*/
void cblas_ctrmv_b(const CBLAS_LAYOUT layout, const CBLAS_UPLO Uplo, const 
        CBLAS_TRANSPOSE TransA, const CBLAS_DIAG Diag, const __int32_t N, 
        const void *A, void *Ab, const __int32_t lda, void *X, void *Xb, const
        __int32_t incX) {
    char TA;
    char UL;
    char DI;
    int32_t F77_N = N;
    int32_t F77_lda = lda;
    int32_t F77_incX = incX;
    int32_t n;
    int32_t i = 0;
    int32_t tincX;
    float *st = 0;
    float *x = (float *)X;
    float *xb = (float *)Xb;
    extern int CBLAS_CallFromC;
    extern int RowMajorStrg;
    int adCount;
    int i0;
    int adCount0;
    int i1;
    int branch;
    if (layout == CblasColMajor) {
        if (Uplo == CblasUpper) {
            pushControl1b(0);
            UL = 'U';
        } else if (Uplo == CblasLower) {
            pushControl1b(1);
            UL = 'L';
        } else {
            if (Ab)
                *((float complex *)Ab) = 0;
            goto label140;
        }
        if (TransA == CblasNoTrans) {
            pushControl2b(0);
            TA = 'N';
        } else if (TransA == CblasTrans) {
            pushControl2b(1);
            TA = 'T';
        } else if (TransA == CblasConjTrans) {
            pushControl2b(2);
            TA = 'C';
        } else {
            if (Ab)
                *((float complex *)Ab) = 0;
            goto label110;
        }
        if (Diag == CblasUnit) {
            pushControl1b(0);
            DI = 'U';
        } else if (Diag == CblasNonUnit) {
            pushControl1b(1);
            DI = 'N';
        } else {
            if (Ab)
                *((float complex *)Ab) = 0;
            goto label100;
        }
        F77_ctrmv_b(&UL, &TA, &DI, &F77_N, A, Ab, &F77_lda, X, Xb, &F77_incX, (size_t)1, (size_t)1, (size_t)1);
        popControl1b(&branch);
      label100:
        popControl2b(&branch);
      label110:
        popControl1b(&branch);
    } else if (layout == CblasRowMajor) {
        if (Uplo == CblasUpper) {
            pushControl1b(0);
            UL = 'L';
        } else if (Uplo == CblasLower) {
            pushControl1b(1);
            UL = 'U';
        } else {
            if (Ab)
                *((float complex *)Ab) = 0;
            goto label140;
        }
        if (TransA == CblasNoTrans) {
            pushControl2b(0);
            TA = 'T';
        } else if (TransA == CblasTrans) {
            pushControl2b(1);
            TA = 'N';
        } else if (TransA == CblasConjTrans) {
            TA = 'N';
            if (N > 0) {
                if (incX > 0)
                    tincX = incX;
                else
                    tincX = -incX;
                i = tincX << 1;
                n = i*N;
                st = x + n;
                adCount = 0;
                do {
                    x[1] = -x[1];
                    pushPointer8(xb);
                    xb = xb + i;
                    x = x + i;
                    adCount = adCount + 1;
                } while(x != st);
                pushInteger4(adCount);
                pushControl2b(2);
                xb = xb - n;
                x = x - n;
            } else
                pushControl2b(3);
        } else {
            if (Ab)
                *((float complex *)Ab) = 0;
            goto label130;
        }
        if (Diag == CblasUnit) {
            pushControl1b(0);
            DI = 'U';
        } else if (Diag == CblasNonUnit) {
            pushControl1b(1);
            DI = 'N';
        } else {
            if (Ab)
                *((float complex *)Ab) = 0;
            goto label120;
        }
        if (TransA == CblasConjTrans)
            if (N > 0) {
                adCount0 = 0;
                do {
                    pushPointer8(xb);
                    xb = xb + i;
                    pushPointer8(x);
                    x = x + i;
                    adCount0 = adCount0 + 1;
                } while(x != st);
                pushInteger4(adCount0);
                popInteger4(&adCount0);
                for (i1 = 1; i1 < adCount0+1; ++i1) {
                    popPointer8((void **)&x);
                    popPointer8((void **)&xb);
                    xb[1] = -xb[1];
                }
            }
        F77_ctrmv_b(&UL, &TA, &DI, &F77_N, A, Ab, &F77_lda, X, Xb, &F77_incX, (size_t)1, (size_t)1, (size_t)1);
        popControl1b(&branch);
      label120:
        popControl2b(&branch);
        if (branch >= 2)
            if (branch == 2) {
                popInteger4(&adCount);
                for (i0 = 1; i0 < adCount+1; ++i0) {
                    popPointer8((void **)&xb);
                    xb[1] = -xb[1];
                }
            }
      label130:
        popControl1b(&branch);
    } else if (Ab)
        *((float complex *)Ab) = 0;
  label140:
    ;
}
