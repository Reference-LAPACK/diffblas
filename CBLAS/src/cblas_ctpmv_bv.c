/*        Generated by TAPENADE     (INRIA, Ecuador team)
    Tapenade 3.16 (develop) -  6 Jan 2026 19:07
*/
#include <complex.h>
#include <adStack.h>
#include <stdio.h>
#include <ctype.h>
#include "cblas_bv.h"
#include "cblas_f77_bv.h"
#include <adStack.h>
#include <stdc-predef.h>
#include "cblas_bv.h"
#include "cblas_f77_bv.h"

/* Declaration for differentiated Fortran routine */
/* void F77_ctpmv_bv_base(...); */
/* Note: This should match the signature of ctpmv_bv in Fortran */

/* F77_ macros for differentiated Fortran routines */
/* These macros handle name mangling for differentiated Fortran functions */
#define F77_ctpmv_bv_base F77_GLOBAL_SUFFIX(ctpmv_bv,CTPMV_BV)
#define F77_ctpmv_bv(...) F77_ctpmv_bv_base(__VA_ARGS__)


/*
  Differentiation of cblas_ctpmv in reverse (adjoint) mode (with options multiDirectional):
   gradient     of useful results: *X
   with respect to varying inputs: *Ap *X
   RW status of diff variables: Ap:(loc) *Ap:out X:(loc) *X:in-out
   Plus diff mem management of: X:in
*/
void cblas_ctpmv_bv(const CBLAS_LAYOUT layout, const CBLAS_UPLO Uplo, const 
        CBLAS_TRANSPOSE TransA, const CBLAS_DIAG Diag, const __int32_t N, 
        const void *Ap, void *Apb, void *X, void *Xb, const __int32_t incX, 
        int nbdirs) {
    char TA;
    char UL;
    char DI;
    int32_t F77_N;
    int nd;
    int adCount;
    int i0;
    int adCount0;
    int i1;
    int branch;
    F77_N = N;
    int32_t F77_incX;
    F77_incX = incX;
    int32_t n;
    int32_t i;
    i = 0;
    int32_t tincX;
    float *st;
    st = 0;
    float *x;
    float *xb[NBDirsMax];
    for (nd = 0; nd < nbdirs; ++nd) xb[nd] = (float *)((float *)Xb + nd);
    x = (float *)X;
    extern int CBLAS_CallFromC;
    extern int RowMajorStrg;
    if (layout == CblasColMajor) {
        if (Uplo == CblasUpper) {
            pushControl1b(0);
            UL = 'U';
        } else if (Uplo == CblasLower) {
            pushControl1b(1);
            UL = 'L';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                ((float complex *)Apb)[nd] = 0.0;
            goto label140;
        }
        if (TransA == CblasNoTrans) {
            pushControl2b(0);
            TA = 'N';
        } else if (TransA == CblasTrans) {
            pushControl2b(1);
            TA = 'T';
        } else if (TransA == CblasConjTrans) {
            pushControl2b(2);
            TA = 'C';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                ((float complex *)Apb)[nd] = 0.0;
            goto label110;
        }
        if (Diag == CblasUnit) {
            pushControl1b(0);
            DI = 'U';
        } else if (Diag == CblasNonUnit) {
            pushControl1b(1);
            DI = 'N';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                ((float complex *)Apb)[nd] = 0.0;
            goto label100;
        }
        F77_ctpmv_bv(&UL, &TA, &DI, &F77_N, Ap, Apb, X, Xb, &F77_incX, &nbdirs);
        popControl1b(&branch);
      label100:
        popControl2b(&branch);
      label110:
        popControl1b(&branch);
    } else if (layout == CblasRowMajor) {
        if (Uplo == CblasUpper) {
            pushControl1b(0);
            UL = 'L';
        } else if (Uplo == CblasLower) {
            pushControl1b(1);
            UL = 'U';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                ((float complex *)Apb)[nd] = 0.0;
            goto label140;
        }
        if (TransA == CblasNoTrans) {
            pushControl2b(0);
            TA = 'T';
        } else if (TransA == CblasTrans) {
            pushControl2b(1);
            TA = 'N';
        } else if (TransA == CblasConjTrans) {
            TA = 'N';
            if (N > 0) {
                if (incX > 0)
                    tincX = incX;
                else
                    tincX = -incX;
                i = tincX << 1;
                n = i*N;
                xb[nd]++;
                x++;
                st = x + n;
                adCount = 0;
                do {
                    *x = -*x;
                    pushPointer8(xb);
                    xb[nd] = (xb+i)[nd];
                    x = x + i;
                    adCount = adCount + 1;
                } while(x != st);
                pushInteger4(adCount);
                pushControl2b(2);
                xb[nd] = (xb-n)[nd];
                x = x - n;
            } else
                pushControl2b(3);
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                ((float complex *)Apb)[nd] = 0.0;
            goto label130;
        }
        if (Diag == CblasUnit) {
            pushControl1b(0);
            DI = 'U';
        } else if (Diag == CblasNonUnit) {
            pushControl1b(1);
            DI = 'N';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                ((float complex *)Apb)[nd] = 0.0;
            goto label120;
        }
        if (TransA == CblasConjTrans)
            if (N > 0) {
                adCount0 = 0;
                do {
                    pushPointer8(xb);
                    xb[nd] = (xb+i)[nd];
                    pushPointer8(x);
                    x = x + i;
                    adCount0 = adCount0 + 1;
                } while(x != st);
                pushInteger4(adCount0);
                popInteger4(&adCount0);
                for (i1 = 1; i1 < adCount0+1; ++i1) {
                    popPointer8((void **)&x);
                    popPointer8((void **)&xb);
                    for (nd = 0; nd < nbdirs; ++nd)
                        ((float *)xb)[nd] = -((float *)xb)[nd];
                }
            }
        F77_ctpmv_bv(&UL, &TA, &DI, &F77_N, Ap, Apb, X, Xb, &F77_incX, &nbdirs);
        popControl1b(&branch);
      label120:
        popControl2b(&branch);
        if (branch >= 2)
            if (branch == 2) {
                popInteger4(&adCount);
                for (i0 = 1; i0 < adCount+1; ++i0) {
                    popPointer8((void **)&xb);
                    for (nd = 0; nd < nbdirs; ++nd)
                        ((float *)xb)[nd] = -((float *)xb)[nd];
                }
                --x;
                --xb[nd];
            }
      label130:
        popControl1b(&branch);
    } else
        for (nd = 0; nd < NBDirsMax; ++nd)
            ((float complex *)Apb)[nd] = 0.0;
  label140:
    ;
}
