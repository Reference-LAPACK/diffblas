/*        Generated by TAPENADE     (INRIA, Ecuador team)
    Tapenade 3.16 (develop) -  6 Jan 2026 19:07
*/
#include <adStack.h>
#include <stdio.h>
#include <ctype.h>
#include "cblas_bv.h"
#include "cblas_f77_bv.h"
#include <adStack.h>
#include <stdc-predef.h>
#include "cblas_bv.h"
#include "cblas_f77_bv.h"

/* Declaration for differentiated Fortran routine */
/* void F77_ssyr_bv_base(...); */
/* Note: This should match the signature of ssyr_bv in Fortran */

/* F77_ macros for differentiated Fortran routines */
/* These macros handle name mangling for differentiated Fortran functions */
#define F77_ssyr_bv_base F77_GLOBAL_SUFFIX(ssyr_bv,SSYR_BV)
#define F77_ssyr_bv(...) F77_ssyr_bv_base(__VA_ARGS__)


/*
  Differentiation of cblas_ssyr in reverse (adjoint) mode (with options multiDirectional):
   gradient     of useful results: *A
   with respect to varying inputs: alpha *A *X
   RW status of diff variables: alpha:out A:(loc) *A:in-out X:(loc)
                *X:out
*/
void cblas_ssyr_bv(const CBLAS_LAYOUT layout, const CBLAS_UPLO Uplo, const 
        __int32_t N, const float alpha, float (*alphab)[NBDirsMax], const 
        float *X, float (*Xb)[NBDirsMax], const __int32_t incX, float *A, 
        float *Ab, const __int32_t lda, int nbdirs) {
    char UL;
    int32_t F77_N;
    int branch;
    int nd;
    F77_N = N;
    int32_t F77_incX;
    F77_incX = incX;
    int32_t F77_lda;
    F77_lda = lda;
    extern int CBLAS_CallFromC;
    extern int RowMajorStrg;
    if (layout == CblasColMajor) {
        if (Uplo == CblasLower) {
            pushControl1b(0);
            UL = 'L';
        } else if (Uplo == CblasUpper) {
            pushControl1b(1);
            UL = 'U';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                (*alphab)[nd] = 0.0;
            for (nd = 0; nd < NBDirsMax; ++nd)
                *Xb[nd] = 0.0;
            goto label100;
        }
        F77_ssyr_bv(&UL, &F77_N, &alpha, &(*alphab), X, Xb, &F77_incX, A, Ab, &
                 F77_lda, &nbdirs);
        popControl1b(&branch);
    } else if (layout == CblasRowMajor) {
        if (Uplo == CblasLower) {
            pushControl1b(0);
            UL = 'U';
        } else if (Uplo == CblasUpper) {
            pushControl1b(1);
            UL = 'L';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                (*alphab)[nd] = 0.0;
            for (nd = 0; nd < NBDirsMax; ++nd)
                *Xb[nd] = 0.0;
            goto label100;
        }
        F77_ssyr_bv(&UL, &F77_N, &alpha, &(*alphab), X, Xb, &F77_incX, A, Ab, &
                 F77_lda, &nbdirs);
        popControl1b(&branch);
    } else {
        for (nd = 0; nd < NBDirsMax; ++nd)
            (*alphab)[nd] = 0.0;
        for (nd = 0; nd < NBDirsMax; ++nd)
            *Xb[nd] = 0.0;
    }
  label100:
    ;
}
