/*        Generated by TAPENADE     (INRIA, Ecuador team)
    Tapenade 3.16 (develop) -  6 Jan 2026 19:07
*/
#include <adStack.h>
#include <stdio.h>
#include <ctype.h>
#include "cblas_bv.h"
#include "cblas_f77_bv.h"
#include <adStack.h>
#include <stdc-predef.h>
#include "cblas_bv.h"
#include "cblas_f77_bv.h"

/* Declaration for differentiated Fortran routine */
/* void F77_dsbmv_bv_base(..., (size_t)1); */
/* Note: This should match the signature of dsbmv_bv in Fortran */

/* F77_ macros for differentiated Fortran routines */
/* These macros handle name mangling for differentiated Fortran functions */
#define F77_dsbmv_bv_base F77_GLOBAL_SUFFIX(dsbmv_bv,DSBMV_BV)
#define F77_dsbmv_bv(...) F77_dsbmv_bv_base(__VA_ARGS__)


/*
  Differentiation of cblas_dsbmv in reverse (adjoint) mode (with options multiDirectional):
   gradient     of useful results: *Y
   with respect to varying inputs: alpha *A beta *X *Y
   RW status of diff variables: alpha:out A:(loc) *A:out beta:out
                X:(loc) *X:out Y:(loc) *Y:in-out
*/
void cblas_dsbmv_bv(const CBLAS_LAYOUT layout, const CBLAS_UPLO Uplo, const 
        __int32_t N, const __int32_t K, const double alpha, double (*alphab)[
        NBDirsMax], const double *A, double *Ab, const __int32_t 
        lda, const double *X, double (*Xb)[NBDirsMax], const __int32_t incX, 
        const double beta, double (*betab)[NBDirsMax], double *Y, double (*Yb)
        [NBDirsMax], const __int32_t incY, int nbdirs) {
    char UL;
    int32_t F77_N;
    int branch;
    int nd;
    F77_N = N;
    int32_t F77_K;
    F77_K = K;
    int32_t F77_lda;
    F77_lda = lda;
    int32_t F77_incX;
    F77_incX = incX;
    int32_t F77_incY;
    F77_incY = incY;
    extern int CBLAS_CallFromC;
    extern int RowMajorStrg;
    if (layout == CblasColMajor) {
        if (Uplo == CblasUpper) {
            pushControl1b(0);
            UL = 'U';
        } else if (Uplo == CblasLower) {
            pushControl1b(1);
            UL = 'L';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                (*alphab)[nd] = 0.0;
            for (nd = 0; nd < NBDirsMax; ++nd)
                Ab[nd] = 0.0;
            for (nd = 0; nd < NBDirsMax; ++nd)
                (*betab)[nd] = 0.0;
            for (nd = 0; nd < NBDirsMax; ++nd)
                *Xb[nd] = 0.0;
            goto label100;
        }
        F77_dsbmv_bv(&UL, &F77_N, &F77_K, &alpha, &(*alphab), A, Ab, &F77_lda, X,
                  Xb, &F77_incX, &beta, &(*betab), Y, Yb, &F77_incY, &nbdirs, (size_t)1);
        popControl1b(&branch);
    } else if (layout == CblasRowMajor) {
        if (Uplo == CblasUpper) {
            pushControl1b(0);
            UL = 'L';
        } else if (Uplo == CblasLower) {
            pushControl1b(1);
            UL = 'U';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                (*alphab)[nd] = 0.0;
            for (nd = 0; nd < NBDirsMax; ++nd)
                Ab[nd] = 0.0;
            for (nd = 0; nd < NBDirsMax; ++nd)
                (*betab)[nd] = 0.0;
            for (nd = 0; nd < NBDirsMax; ++nd)
                *Xb[nd] = 0.0;
            goto label100;
        }
        F77_dsbmv_bv(&UL, &F77_N, &F77_K, &alpha, &(*alphab), A, Ab, &F77_lda, X,
                  Xb, &F77_incX, &beta, &(*betab), Y, Yb, &F77_incY, &nbdirs, (size_t)1);
        popControl1b(&branch);
    } else {
        for (nd = 0; nd < NBDirsMax; ++nd)
            (*alphab)[nd] = 0.0;
        for (nd = 0; nd < NBDirsMax; ++nd)
            Ab[nd] = 0.0;
        for (nd = 0; nd < NBDirsMax; ++nd)
            (*betab)[nd] = 0.0;
        for (nd = 0; nd < NBDirsMax; ++nd)
            *Xb[nd] = 0.0;
    }
  label100:
    ;
}
