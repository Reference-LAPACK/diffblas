/*        Generated by TAPENADE     (INRIA, Ecuador team)
    Tapenade 3.16 (develop) -  6 Jan 2026 19:07
*/
#include <complex.h>
#include <adStack.h>
#include <stdio.h>
#include <ctype.h>
#include "cblas_b.h"
#include "cblas_f77_b.h"
#include <adStack.h>
#include <stdc-predef.h>
#include <stdio.h>
#include <stdlib.h>
#include "cblas_b.h"
#include "cblas_f77_b.h"

/* Declaration for differentiated Fortran routine */
/* void F77_zgbmv_b_base(...); */
/* Note: This should match the signature of zgbmv_b in Fortran */

/* F77_ macros for differentiated Fortran routines */
/* These macros handle name mangling for differentiated Fortran functions */
#define F77_zgbmv_b_base F77_GLOBAL_SUFFIX(zgbmv_b,ZGBMV_B)
#define F77_zgbmv_b(...) F77_zgbmv_b_base(__VA_ARGS__)


/*
  Differentiation of cblas_zgbmv in reverse (adjoint) mode:
   gradient     of useful results: *Y
   with respect to varying inputs: *alpha *A *beta *X *Y
   RW status of diff variables: alpha:(loc) *alpha:out A:(loc)
                *A:out beta:(loc) *beta:out X:(loc) *X:out Y:(loc)
                *Y:in-out
   Plus diff mem management of: alpha:in beta:in X:in Y:in
*/
void cblas_zgbmv_b(const CBLAS_LAYOUT layout, const CBLAS_TRANSPOSE TransA, 
        const __int32_t M, const __int32_t N, const __int32_t KL, const 
        __int32_t KU, const void *alpha, void *alphab, const void *A, void *Ab
        , const __int32_t lda, const void *X, void *Xb, const __int32_t incX, 
        const void *beta, void *betab, void *Y, void *Yb, const __int32_t incY
) {
    char TA;
    int32_t F77_M = M;
    int32_t F77_N = N;
    int32_t F77_lda = lda;
    int32_t F77_incX = incX;
    int32_t F77_incY = incY;
    int32_t F77_KL = KL;
    int32_t F77_KU = KU;
    int32_t n;
    int32_t i = 0;
    int32_t incx = incX;
    const double *xx = (double *)X, *alp = (double *)alpha, *bet = (double *)
    beta;
    double ALPHA[2], BETA[2];
    double ALPHAb[2], BETAb[2];
    int32_t tincY, tincx;
    double *x = (void *)0;
    double *xb = (double *)Xb;
    double *y = (double *)Y;
    double *yb = (double *)Yb;
    double *st = 0;
    double *stb = (void *)0;
    double *tx;
    extern int CBLAS_CallFromC;
    extern int RowMajorStrg;
    double *alpb;
    double *betb;
    int ii1;
    double *xxb;
    double *txb;
    int adCount;
    int i0;
    int adCount0;
    int i1;
    int adCount1;
    int i2;
    int branch;
    if (layout == CblasColMajor) {
        if (TransA == CblasNoTrans) {
            pushControl2b(0);
            TA = 'N';
        } else if (TransA == CblasTrans) {
            pushControl2b(1);
            TA = 'T';
        } else if (TransA == CblasConjTrans) {
            pushControl2b(2);
            TA = 'C';
        } else {
            if (alphab)
                *((double complex *)alphab) = 0;
            if (Ab)
                *((double complex *)Ab) = 0;
            if (betab)
                *((double complex *)betab) = 0;
            if (Xb)
                *((double complex *)Xb) = 0;
            goto label110;
        }
        F77_zgbmv_b(&TA, &F77_M, &F77_N, &F77_KL, &F77_KU, alpha, alphab, A, Ab, 
                 &F77_lda, X, Xb, &F77_incX, beta, betab, Y, Yb, &F77_incY);
        popControl2b(&branch);
    } else if (layout == CblasRowMajor) {
        if (TransA == CblasNoTrans) {
            pushControl3b(0);
            TA = 'T';
        } else if (TransA == CblasTrans) {
            pushControl3b(1);
            TA = 'N';
        } else if (TransA == CblasConjTrans) {
            ALPHA[0] = *alp;
            ALPHA[1] = -alp[1];
            BETA[0] = *bet;
            BETA[1] = -bet[1];
            TA = 'N';
            if (M > 0) {
                n = M << 1;
                xb = (double *)malloc(n*sizeof(double));
                for (ii1 = 0; ii1 < n; ++ii1)
                    xb[ii1] = 0.0;
                x = (double *)malloc(n*sizeof(double));
                if (incX > 0) {
                    pushControl1b(1);
                    i = incX << 1;
                    tincx = 2;
                    st = x + n;
                } else {
                    i = incX*-2;
                    tincx = -2;
                    st = x - 2;
                    pushPointer8(xb);
                    xb = xb + (n - 2);
                    pushPointer8(x);
                    x = x + (n - 2);
                    pushControl1b(0);
                }
                adCount = 0;
                do {
                    *x = *xx;
                    x[1] = -xx[1];
                    pushPointer8(xb);
                    xb = xb + tincx;
                    pushPointer8(x);
                    x = x + tincx;
                    pushPointer8(xxb);
                    xxb = xxb + i;
                    xx = xx + i;
                    adCount = adCount + 1;
                } while(x != st);
                pushInteger4(adCount);
                pushPointer8(xb);
                xb = txb;
                F77_incX = 1;
                if (incY > 0)
                    tincY = incY;
                else
                    tincY = -incY;
                yb++;
                y++;
                if (N > 0) {
                    i = tincY << 1;
                    n = i*N;
                    st = y + n;
                    adCount0 = 0;
                    do {
                        *y = -*y;
                        pushPointer8(yb);
                        yb = yb + i;
                        y = y + i;
                        adCount0 = adCount0 + 1;
                    } while(y != st);
                    pushInteger4(adCount0);
                    pushControl3b(2);
                    yb = yb - n;
                    y = y - n;
                } else
                    pushControl3b(3);
            } else {
                pushControl3b(4);
                xb = (double *)Xb;
            }
        } else {
            if (alphab)
                *((double complex *)alphab) = 0;
            if (Ab)
                *((double complex *)Ab) = 0;
            if (betab)
                *((double complex *)betab) = 0;
            if (Xb)
                *((double complex *)Xb) = 0;
            goto label110;
        }
        if (TransA == CblasConjTrans)
            pushControl1b(0);
        else
            pushControl1b(1);
        if (TransA == CblasConjTrans)
            if (N > 0) {
                adCount1 = 0;
                do {
                    pushPointer8(yb);
                    yb = yb + i;
                    pushPointer8(y);
                    y = y + i;
                    adCount1 = adCount1 + 1;
                } while(y != st);
                pushInteger4(adCount1);
                popInteger4(&adCount1);
                for (i2 = 1; i2 < adCount1+1; ++i2) {
                    popPointer8((void **)&y);
                    popPointer8((void **)&yb);
                    *yb = -*yb;
                }
            }
        popControl1b(&branch);
        if (branch == 0) {
            F77_zgbmv_b(&TA, &F77_N, &F77_M, &F77_KU, &F77_KL, ALPHA, ALPHAb, A, 
                     Ab, &F77_lda, x, xb, &F77_incX, BETA, BETAb, Y, Yb, &
                     F77_incY);
            if (alphab)
                *((double complex *)alphab) = 0;
            if (betab)
                *((double complex *)betab) = 0;
        } else {
            F77_zgbmv_b(&TA, &F77_N, &F77_M, &F77_KU, &F77_KL, alpha, alphab, A, 
                     Ab, &F77_lda, x, xb, &F77_incX, beta, betab, Y, Yb, &
                     F77_incY);
            for (ii1 = 0; ii1 < 2; ++ii1)
                ALPHAb[ii1] = 0.0;
            for (ii1 = 0; ii1 < 2; ++ii1)
                BETAb[ii1] = 0.0;
        }
        popControl3b(&branch);
        if (branch >= 2) {
            if (branch == 2) {
                popInteger4(&adCount0);
                for (i1 = 1; i1 < adCount0+1; ++i1) {
                    popPointer8((void **)&yb);
                    *yb = -*yb;
                }
            } else if (branch != 3)
                goto label100;
            --y;
            --yb;
            popPointer8((void **)&xb);
            popInteger4(&adCount);
            for (i0 = 1; i0 < adCount+1; ++i0) {
                popPointer8((void **)&xxb);
                popPointer8((void **)&x);
                popPointer8((void **)&xb);
                xxb[1] = xxb[1] - xb[1];
                xb[1] = 0.0;
                *xxb = *xxb + *xb;
                *((double complex *)xb) = 0;
            }
            popControl1b(&branch);
            if (branch == 0) {
                popPointer8((void **)&x);
                popPointer8((void **)&xb);
            }
            free(x);
            free(xb);
          label100:
            betb[1] = betb[1] - BETAb[1];
            BETAb[1] = 0.0;
            *betb = *betb + BETAb[0];
            alpb[1] = alpb[1] - ALPHAb[1];
            ALPHAb[1] = 0.0;
            *alpb = *alpb + ALPHAb[0];
        }
    } else {
        if (alphab)
            *((double complex *)alphab) = 0;
        if (Ab)
            *((double complex *)Ab) = 0;
        if (betab)
            *((double complex *)betab) = 0;
        if (Xb)
            *((double complex *)Xb) = 0;
    }
  label110:
    ;
}
