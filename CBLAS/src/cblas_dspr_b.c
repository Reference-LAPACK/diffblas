/*        Generated by TAPENADE     (INRIA, Ecuador team)
    Tapenade 3.16 (develop) -  6 Jan 2026 19:07
*/
#include <adStack.h>
#include <stdio.h>
#include <ctype.h>
#include "cblas_b.h"
#include "cblas_f77_b.h"
#include <adStack.h>
#include <stdc-predef.h>
#include "cblas_b.h"
#include "cblas_f77_b.h"

/* Declaration for differentiated Fortran routine */
/* void F77_dspr_b_base(...); */
/* Note: This should match the signature of dspr_b in Fortran */

/* F77_ macros for differentiated Fortran routines */
/* These macros handle name mangling for differentiated Fortran functions */
#define F77_dspr_b_base F77_GLOBAL_SUFFIX(dspr_b,DSPR_B)
#define F77_dspr_b(...) F77_dspr_b_base(__VA_ARGS__)


/*
  Differentiation of cblas_dspr in reverse (adjoint) mode:
   gradient     of useful results: alpha *Ap *X
   with respect to varying inputs: alpha *Ap *X
   RW status of diff variables: alpha:incr Ap:(loc) *Ap:in-out
                X:(loc) *X:incr
*/
void cblas_dspr_b(const CBLAS_LAYOUT layout, const CBLAS_UPLO Uplo, const 
        __int32_t N, const double alpha, double *alphab, const double *X, 
        double *Xb, const __int32_t incX, double *Ap, double *Apb) {
    char UL;
    int32_t F77_N = N;
    int32_t F77_incX = incX;
    extern int CBLAS_CallFromC;
    extern int RowMajorStrg;
    int branch;
    if (layout == CblasColMajor) {
        if (Uplo == CblasLower) {
            pushControl1b(0);
            UL = 'L';
        } else if (Uplo == CblasUpper) {
            pushControl1b(1);
            UL = 'U';
        } else
            goto label100;
        F77_dspr_b(&UL, &F77_N, &alpha, &(*alphab), X, Xb, &F77_incX, Ap, Apb);
        popControl1b(&branch);
    } else if (layout == CblasRowMajor) {
        if (Uplo == CblasLower) {
            pushControl1b(0);
            UL = 'U';
        } else if (Uplo == CblasUpper) {
            pushControl1b(1);
            UL = 'L';
        } else
            goto label100;
        F77_dspr_b(&UL, &F77_N, &alpha, &(*alphab), X, Xb, &F77_incX, Ap, Apb);
        popControl1b(&branch);
    }
  label100:
    ;
}
