/*        Generated by TAPENADE     (INRIA, Ecuador team)
    Tapenade 3.16 (develop) -  6 Jan 2026 19:07
*/
#include <adStack.h>
#include <stdio.h>
#include <ctype.h>
#include "cblas_b.h"
#include "cblas_f77_b.h"
#include <adStack.h>
#include <stdc-predef.h>
#include "cblas_b.h"
#include "cblas_f77_b.h"

/* Declaration for differentiated Fortran routine */
/* void F77_dsymv_b_base(..., (size_t)1); */
/* Note: This should match the signature of dsymv_b in Fortran */

/* F77_ macros for differentiated Fortran routines */
/* These macros handle name mangling for differentiated Fortran functions */
#define F77_dsymv_b_base F77_GLOBAL_SUFFIX(dsymv_b,DSYMV_B)
#define F77_dsymv_b(...) F77_dsymv_b_base(__VA_ARGS__)


/*
  Differentiation of cblas_dsymv in reverse (adjoint) mode:
   gradient     of useful results: *Y
   with respect to varying inputs: alpha *A beta *X *Y
   RW status of diff variables: alpha:out A:(loc) *A:out beta:out
                X:(loc) *X:out Y:(loc) *Y:in-out
*/
void cblas_dsymv_b(const CBLAS_LAYOUT layout, const CBLAS_UPLO Uplo, const 
        __int32_t N, const double alpha, double *alphab, const double *A, 
        double *Ab, const __int32_t lda, const double *X, double *Xb, const 
        __int32_t incX, const double beta, double *betab, double *Y, double *
        Yb, const __int32_t incY) {
    char UL;
    int32_t F77_N = N;
    int32_t F77_lda = lda;
    int32_t F77_incX = incX;
    int32_t F77_incY = incY;
    extern int CBLAS_CallFromC;
    extern int RowMajorStrg;
    int branch;
    if (layout == CblasColMajor) {
        if (Uplo == CblasUpper) {
            pushControl1b(0);
            UL = 'U';
        } else if (Uplo == CblasLower) {
            pushControl1b(1);
            UL = 'L';
        } else {
            if (alphab)
                *alphab = 0.0;
            if (Ab)
                *Ab = 0.0;
            if (betab)
                *betab = 0.0;
            if (Xb)
                *Xb = 0.0;
            goto label100;
        }
        F77_dsymv_b(&UL, &F77_N, &alpha, &(*alphab), A, Ab, &F77_lda, X, Xb, &
                 F77_incX, &beta, &(*betab), Y, Yb, &F77_incY, (size_t)1);
        popControl1b(&branch);
    } else if (layout == CblasRowMajor) {
        if (Uplo == CblasUpper) {
            pushControl1b(0);
            UL = 'L';
        } else if (Uplo == CblasLower) {
            pushControl1b(1);
            UL = 'U';
        } else {
            if (alphab)
                *alphab = 0.0;
            if (Ab)
                *Ab = 0.0;
            if (betab)
                *betab = 0.0;
            if (Xb)
                *Xb = 0.0;
            goto label100;
        }
        F77_dsymv_b(&UL, &F77_N, &alpha, &(*alphab), A, Ab, &F77_lda, X, Xb, &
                 F77_incX, &beta, &(*betab), Y, Yb, &F77_incY, (size_t)1);
        popControl1b(&branch);
    } else {
        if (alphab)
            *alphab = 0.0;
        if (Ab)
            *Ab = 0.0;
        if (betab)
            *betab = 0.0;
        if (Xb)
            *Xb = 0.0;
    }
  label100:
    ;
}
