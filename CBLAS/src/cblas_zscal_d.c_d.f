C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of zscal in forward (tangent) mode:
C   variations   of useful results: zx
C   with respect to varying inputs: za
C> \brief \b ZSCAL
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       SUBROUTINE ZSCAL(N,ZA,ZX,INCX)
C
C       .. Scalar Arguments ..
C       COMPLEX*16 ZA
C       INTEGER INCX,N
C       ..
C       .. Array Arguments ..
C       COMPLEX*16 ZX(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C>    ZSCAL scales a vector by a constant.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>         number of elements in input vector(s)
C> \endverbatim
C>
C> \param[in] ZA
C> \verbatim
C>          ZA is COMPLEX*16
C>           On entry, ZA specifies the scalar alpha.
C> \endverbatim
C>
C> \param[in,out] ZX
C> \verbatim
C>          ZX is COMPLEX*16 array, dimension ( 1 + ( N - 1 )*abs( INCX ) )
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>         storage spacing between elements of ZX
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup scal
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>     jack dongarra, 3/11/78.
C>     modified 3/93 to return if incx .le. 0.
C>     modified 12/3/93, array(1) declarations changed to array(*)
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE ZSCAL_D(n, za, zad, zx, zxd, incx)
      IMPLICIT NONE
      INCLUDE 'DIFFSIZESF.inc'
C  Hint: ISIZE1OFzx should be the size of dimension 1 of array zx
C
C  -- Reference BLAS level1 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      COMPLEX*16 za
      COMPLEX*16 zad
      INTEGER incx, n
C     ..
C     .. Array Arguments ..
      COMPLEX*16 zx(*)
      COMPLEX*16 zxd(*)
C     ..
C
C  =====================================================================
C
C     .. Local Scalars ..
      INTEGER i, nincx
C     ..
C     .. Parameters ..
      COMPLEX*16 one
      PARAMETER (one=(1.0d+0,0.0d+0))
      INTEGER ii1
C     ..
      IF ((n .LE. 0 .OR. incx .LE. 0) .OR. za .EQ. one) THEN
        DO ii1=1,ISIZE1OFzx
C     FIXED: Removed zeroing of zxd - should accumulate from input seed
        ENDDO
        RETURN
      ELSE
        IF (incx .EQ. 1) THEN
          DO ii1=1,ISIZE1OFzx
C     FIXED: Removed zeroing of zxd - should accumulate from input seed
          ENDDO
C
C        code for increment equal to 1
C
          DO i=1,n
            zxd(i) = zx(i)*zad + za*zxd(i)
            zx(i) = za*zx(i)
          ENDDO
        ELSE
C
C        code for increment not equal to 1
C
          nincx = n*incx
          DO ii1=1,ISIZE1OFzx
C     FIXED: Removed zeroing of zxd - should accumulate from input seed
          ENDDO
          DO i=1,nincx,incx
            zxd(i) = zx(i)*zad + za*zxd(i)
            zx(i) = za*zx(i)
          ENDDO
        END IF
        RETURN
C
C     End of ZSCAL
C
      END IF
      END

