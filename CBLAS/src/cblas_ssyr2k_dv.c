/*        Generated by TAPENADE     (INRIA, Ecuador team)
    Tapenade 3.16 (develop) -  6 Jan 2026 19:07
*/
#include "DIFFSIZESC.inc"
#include <stdint.h>
#include <stdio.h>
#include <ctype.h>
#include "cblas_dv.h"
#include "cblas_f77_dv.h"
#include <stdc-predef.h>
#include "cblas_dv.h"
#include "cblas_f77_dv.h"

/* Declaration for differentiated Fortran routine */
/* void F77_ssyr2k_dv_base(...); */
/* Note: This should match the signature of ssyr2k_dv in Fortran */


/*
  Differentiation of cblas_ssyr2k in forward (tangent) mode (with options multiDirectional):
   variations   of useful results: *C
   with respect to varying inputs: alpha *A *B *C beta
   RW status of diff variables: alpha:in A:(loc) *A:in B:(loc)
                *B:in C:(loc) *C:in-out beta:in
*/
void cblas_ssyr2k_dv(const CBLAS_LAYOUT layout, const CBLAS_UPLO Uplo, const 
        CBLAS_TRANSPOSE Trans, const __int32_t N, const __int32_t K, const 
        float alpha, const float alphad[NBDirsMax], const float *A, const 
        float (*Ad)[NBDirsMax], const __int32_t lda, const float *B, const 
        float (*Bd)[NBDirsMax], const __int32_t ldb, const float beta, const 
        float betad[NBDirsMax], float *C, float (*Cd)[NBDirsMax], const 
        __int32_t ldc, int nbdirs) {
    char UL, TR;
    int32_t F77_N;
    F77_N = N;
    int32_t F77_K;
    F77_K = K;
    int32_t F77_lda;
    F77_lda = lda;
    int32_t F77_ldb;
    F77_ldb = ldb;
    int32_t F77_ldc;
    F77_ldc = ldc;
    extern int CBLAS_CallFromC;
    extern int RowMajorStrg;
    RowMajorStrg = 0;
    CBLAS_CallFromC = 1;
    if (layout == CblasColMajor) {
        if (Uplo == CblasUpper)
            UL = 'U';
        else if (Uplo == CblasLower)
            UL = 'L';
        else {
            cblas_xerbla(2, "cblas_ssyr2k", "Illegal Uplo setting, %d\n", Uplo
                        );
            CBLAS_CallFromC = 0;
            RowMajorStrg = 0;
            return;
        }
        if (Trans == CblasTrans)
            TR = 'T';
        else if (Trans == CblasConjTrans)
            TR = 'C';
        else if (Trans == CblasNoTrans)
            TR = 'N';
        else {
            cblas_xerbla(3, "cblas_ssyr2k", "Illegal Trans setting, %d\n", 
                         Trans);
            CBLAS_CallFromC = 0;
            RowMajorStrg = 0;
            return;
        }
        F77_ssyr2k_dv((float *)&UL, (float *)&TR, &F77_N, &F77_K, (float *)&alpha, (float *)alphad, (float *)A, (float *)Ad, &F77_lda, (float *)B, (float *)Bd, &F77_ldb, (float *)&beta, (float *)betad, (float *)C, (float *)Cd, &F77_ldc, &nbdirs, (size_t)1, (size_t)1);
    } else if (layout == CblasRowMajor) {
        RowMajorStrg = 1;
        if (Uplo == CblasUpper)
            UL = 'L';
        else if (Uplo == CblasLower)
            UL = 'U';
        else {
            cblas_xerbla(3, "cblas_ssyr2k", "Illegal Uplo setting, %d\n", Uplo
                        );
            CBLAS_CallFromC = 0;
            RowMajorStrg = 0;
            return;
        }
        if (Trans == CblasTrans)
            TR = 'N';
        else if (Trans == CblasConjTrans)
            TR = 'N';
        else if (Trans == CblasNoTrans)
            TR = 'T';
        else {
            cblas_xerbla(3, "cblas_ssyr2k", "Illegal Trans setting, %d\n", 
                         Trans);
            CBLAS_CallFromC = 0;
            RowMajorStrg = 0;
            return;
        }
        F77_ssyr2k_dv((float *)&UL, (float *)&TR, &F77_N, &F77_K, (float *)&alpha, (float *)alphad, (float *)A, (float *)Ad, &F77_lda, (float *)B, (float *)Bd, &F77_ldb, (float *)&beta, (float *)betad, (float *)C, (float *)Cd, &F77_ldc, &nbdirs, (size_t)1, (size_t)1);
    } else
        cblas_xerbla(1, "cblas_ssyr2k", "Illegal layout setting, %d\n", layout
                    );
    CBLAS_CallFromC = 0;
    RowMajorStrg = 0;
    return;
}
