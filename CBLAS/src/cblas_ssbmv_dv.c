/*        Generated by TAPENADE     (INRIA, Ecuador team)
    Tapenade 3.16 (develop) -  6 Jan 2026 19:07
*/
#include "DIFFSIZESC.inc"
#include <stdint.h>
#include <stdio.h>
#include <ctype.h>
#include "cblas_dv.h"
#include "cblas_f77_dv.h"
#include <stdc-predef.h>
#include "cblas_dv.h"
#include "cblas_f77_dv.h"

/* Declaration for differentiated Fortran routine */
/* void F77_ssbmv_dv_base(...); */
/* Note: This should match the signature of ssbmv_dv in Fortran */


/*
  Differentiation of cblas_ssbmv in forward (tangent) mode (with options multiDirectional):
   variations   of useful results: *Y
   with respect to varying inputs: alpha *A beta *X *Y
   RW status of diff variables: alpha:in A:(loc) *A:in beta:in
                X:(loc) *X:in Y:(loc) *Y:in-out
*/
void cblas_ssbmv_dv(const CBLAS_LAYOUT layout, const CBLAS_UPLO Uplo, const 
        __int32_t N, const __int32_t K, const float alpha, const float alphad[
        NBDirsMax], const float *A, const float (*Ad)[NBDirsMax], const 
        __int32_t lda, const float *X, const float (*Xd)[NBDirsMax], const 
        __int32_t incX, const float beta, const float betad[NBDirsMax], float 
        *Y, float (*Yd)[NBDirsMax], const __int32_t incY, int nbdirs) {
    char UL;
    int32_t F77_N;
    F77_N = N;
    int32_t F77_K;
    F77_K = K;
    int32_t F77_lda;
    F77_lda = lda;
    int32_t F77_incX;
    F77_incX = incX;
    int32_t F77_incY;
    F77_incY = incY;
    extern int CBLAS_CallFromC;
    extern int RowMajorStrg;
    RowMajorStrg = 0;
    CBLAS_CallFromC = 1;
    if (layout == CblasColMajor) {
        if (Uplo == CblasUpper)
            UL = 'U';
        else if (Uplo == CblasLower)
            UL = 'L';
        else {
            cblas_xerbla(2, "cblas_ssbmv", "Illegal Uplo setting, %d\n", Uplo)
            ;
            CBLAS_CallFromC = 0;
            RowMajorStrg = 0;
            return;
        }
        F77_ssbmv_dv((float *)&UL, &F77_N, &F77_K, (float *)&alpha, (float *)alphad, (float *)A, (float *)Ad, &F77_lda, (float *)X, (float *)Xd, &F77_incX, (float *)&beta, (float *)betad, (float *)Y, (float *)Yd, &F77_incY, &nbdirs, (size_t)1, (size_t)1);
    } else if (layout == CblasRowMajor) {
        RowMajorStrg = 1;
        if (Uplo == CblasUpper)
            UL = 'L';
        else if (Uplo == CblasLower)
            UL = 'U';
        else {
            cblas_xerbla(2, "cblas_ssbmv", "Illegal Uplo setting, %d\n", Uplo)
            ;
            CBLAS_CallFromC = 0;
            RowMajorStrg = 0;
            return;
        }
        F77_ssbmv_dv((float *)&UL, &F77_N, &F77_K, (float *)&alpha, (float *)alphad, (float *)A, (float *)Ad, &F77_lda, (float *)X, (float *)Xd, &F77_incX, (float *)&beta, (float *)betad, (float *)Y, (float *)Yd, &F77_incY, &nbdirs, (size_t)1, (size_t)1);
    } else
        cblas_xerbla(1, "cblas_ssbmv", "Illegal layout setting, %d\n", layout)
        ;
    CBLAS_CallFromC = 0;
    RowMajorStrg = 0;
    return;
}
