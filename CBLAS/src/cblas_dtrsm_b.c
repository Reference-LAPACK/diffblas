/*        Generated by TAPENADE     (INRIA, Ecuador team)
    Tapenade 3.16 (develop) -  6 Jan 2026 19:07
*/
#include <adStack.h>
#include <stdio.h>
#include <ctype.h>
#include "cblas_b.h"
#include "cblas_f77_b.h"
#include <adStack.h>
#include <stdc-predef.h>
#include "cblas_b.h"
#include "cblas_f77_b.h"

/* Declaration for differentiated Fortran routine */
/* void F77_dtrsm_b_base(..., (size_t)1, (size_t)1, (size_t)1, (size_t)1); */
/* Note: This should match the signature of dtrsm_b in Fortran */

/* F77_ macros for differentiated Fortran routines */
/* These macros handle name mangling for differentiated Fortran functions */
#define F77_dtrsm_b_base F77_GLOBAL_SUFFIX(dtrsm_b,DTRSM_B)
#define F77_dtrsm_b(...) F77_dtrsm_b_base(__VA_ARGS__)


/*
  Differentiation of cblas_dtrsm in reverse (adjoint) mode:
   gradient     of useful results: *B
   with respect to varying inputs: alpha *A *B
   RW status of diff variables: alpha:out A:(loc) *A:out B:(loc)
                *B:in-out
*/
void cblas_dtrsm_b(const CBLAS_LAYOUT layout, const CBLAS_SIDE Side, const 
        CBLAS_UPLO Uplo, const CBLAS_TRANSPOSE TransA, const CBLAS_DIAG Diag, 
        const __int32_t M, const __int32_t N, const double alpha, double *
        alphab, const double *A, double *Ab, const __int32_t lda, double *B, 
        double *Bb, const __int32_t ldb) {
    char UL, TA, SD, DI;
    int32_t F77_M = M;
    int32_t F77_N = N;
    int32_t F77_lda = lda;
    int32_t F77_ldb = ldb;
    extern int CBLAS_CallFromC;
    extern int RowMajorStrg;
    int branch;
    if (layout == CblasColMajor) {
        if (Side == CblasRight) {
            pushControl1b(0);
            SD = 'R';
        } else if (Side == CblasLeft) {
            pushControl1b(1);
            SD = 'L';
        } else {
            if (alphab)
                *alphab = 0.0;
            if (Ab)
                *Ab = 0.0;
            goto label160;
        }
        if (Uplo == CblasUpper) {
            pushControl1b(0);
            UL = 'U';
        } else if (Uplo == CblasLower) {
            pushControl1b(1);
            UL = 'L';
        } else {
            if (alphab)
                *alphab = 0.0;
            if (Ab)
                *Ab = 0.0;
            goto label120;
        }
        if (TransA == CblasTrans) {
            pushControl2b(0);
            TA = 'T';
        } else if (TransA == CblasConjTrans) {
            pushControl2b(1);
            TA = 'C';
        } else if (TransA == CblasNoTrans) {
            pushControl2b(2);
            TA = 'N';
        } else {
            if (alphab)
                *alphab = 0.0;
            if (Ab)
                *Ab = 0.0;
            goto label110;
        }
        if (Diag == CblasUnit) {
            pushControl1b(0);
            DI = 'U';
        } else if (Diag == CblasNonUnit) {
            pushControl1b(1);
            DI = 'N';
        } else {
            if (alphab)
                *alphab = 0.0;
            if (Ab)
                *Ab = 0.0;
            goto label100;
        }
        F77_dtrsm_b(&SD, &UL, &TA, &DI, &F77_M, &F77_N, &alpha, &(*alphab), A, Ab
                 , &F77_lda, B, Bb, &F77_ldb, (size_t)1, (size_t)1, (size_t)1, (size_t)1);
        popControl1b(&branch);
      label100:
        popControl2b(&branch);
      label110:
        popControl1b(&branch);
      label120:
        popControl1b(&branch);
    } else if (layout == CblasRowMajor) {
        if (Side == CblasRight) {
            pushControl1b(0);
            SD = 'L';
        } else if (Side == CblasLeft) {
            pushControl1b(1);
            SD = 'R';
        } else {
            if (alphab)
                *alphab = 0.0;
            if (Ab)
                *Ab = 0.0;
            goto label160;
        }
        if (Uplo == CblasUpper) {
            pushControl1b(0);
            UL = 'L';
        } else if (Uplo == CblasLower) {
            pushControl1b(1);
            UL = 'U';
        } else {
            if (alphab)
                *alphab = 0.0;
            if (Ab)
                *Ab = 0.0;
            goto label150;
        }
        if (TransA == CblasTrans) {
            pushControl2b(0);
            TA = 'T';
        } else if (TransA == CblasConjTrans) {
            pushControl2b(1);
            TA = 'C';
        } else if (TransA == CblasNoTrans) {
            pushControl2b(2);
            TA = 'N';
        } else {
            if (alphab)
                *alphab = 0.0;
            if (Ab)
                *Ab = 0.0;
            goto label140;
        }
        if (Diag == CblasUnit) {
            pushControl1b(0);
            DI = 'U';
        } else if (Diag == CblasNonUnit) {
            pushControl1b(1);
            DI = 'N';
        } else {
            if (alphab)
                *alphab = 0.0;
            if (Ab)
                *Ab = 0.0;
            goto label130;
        }
        F77_dtrsm_b(&SD, &UL, &TA, &DI, &F77_N, &F77_M, &alpha, &(*alphab), A, Ab
                 , &F77_lda, B, Bb, &F77_ldb, (size_t)1, (size_t)1, (size_t)1, (size_t)1);
        popControl1b(&branch);
      label130:
        popControl2b(&branch);
      label140:
        popControl1b(&branch);
      label150:
        popControl1b(&branch);
    } else {
        if (alphab)
            *alphab = 0.0;
        if (Ab)
            *Ab = 0.0;
    }
  label160:
    ;
}
