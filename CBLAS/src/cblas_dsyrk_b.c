/*        Generated by TAPENADE     (INRIA, Ecuador team)
    Tapenade 3.16 (develop) -  6 Jan 2026 19:07
*/
#include <adStack.h>
#include <stdio.h>
#include <ctype.h>
#include "cblas_b.h"
#include "cblas_f77_b.h"
#include <adStack.h>
#include <stdc-predef.h>
#include "cblas_b.h"
#include "cblas_f77_b.h"

/* Declaration for differentiated Fortran routine */
/* void F77_dsyrk_b_base(..., (size_t)1, (size_t)1); */
/* Note: This should match the signature of dsyrk_b in Fortran */

/* F77_ macros for differentiated Fortran routines */
/* These macros handle name mangling for differentiated Fortran functions */
#define F77_dsyrk_b_base F77_GLOBAL_SUFFIX(dsyrk_b,DSYRK_B)
#define F77_dsyrk_b(...) F77_dsyrk_b_base(__VA_ARGS__)


/*
  Differentiation of cblas_dsyrk in reverse (adjoint) mode:
   gradient     of useful results: *C
   with respect to varying inputs: alpha *A *C beta
   RW status of diff variables: alpha:out A:(loc) *A:out C:(loc)
                *C:in-out beta:out
*/
void cblas_dsyrk_b(const CBLAS_LAYOUT layout, const CBLAS_UPLO Uplo, const 
        CBLAS_TRANSPOSE Trans, const __int32_t N, const __int32_t K, const 
        double alpha, double *alphab, const double *A, double *Ab, const 
        __int32_t lda, const double beta, double *betab, double *C, double *Cb
        , const __int32_t ldc) {
    char UL, TR;
    int32_t F77_N = N;
    int32_t F77_K = K;
    int32_t F77_lda = lda;
    int32_t F77_ldc = ldc;
    extern int CBLAS_CallFromC;
    extern int RowMajorStrg;
    int branch;
    if (layout == CblasColMajor) {
        if (Uplo == CblasUpper) {
            pushControl1b(0);
            UL = 'U';
        } else if (Uplo == CblasLower) {
            pushControl1b(1);
            UL = 'L';
        } else {
            if (alphab)
                *alphab = 0.0;
            if (Ab)
                *Ab = 0.0;
            if (betab)
                *betab = 0.0;
            goto label120;
        }
        if (Trans == CblasTrans) {
            pushControl2b(0);
            TR = 'T';
        } else if (Trans == CblasConjTrans) {
            pushControl2b(1);
            TR = 'C';
        } else if (Trans == CblasNoTrans) {
            pushControl2b(2);
            TR = 'N';
        } else {
            if (alphab)
                *alphab = 0.0;
            if (Ab)
                *Ab = 0.0;
            if (betab)
                *betab = 0.0;
            goto label100;
        }
        F77_dsyrk_b(&UL, &TR, &F77_N, &F77_K, &alpha, &(*alphab), A, Ab, &F77_lda
                 , &beta, &(*betab), C, Cb, &F77_ldc, (size_t)1, (size_t)1);
        popControl2b(&branch);
      label100:
        popControl1b(&branch);
    } else if (layout == CblasRowMajor) {
        if (Uplo == CblasUpper) {
            pushControl1b(0);
            UL = 'L';
        } else if (Uplo == CblasLower) {
            pushControl1b(1);
            UL = 'U';
        } else {
            if (alphab)
                *alphab = 0.0;
            if (Ab)
                *Ab = 0.0;
            if (betab)
                *betab = 0.0;
            goto label120;
        }
        if (Trans == CblasTrans) {
            pushControl2b(0);
            TR = 'N';
        } else if (Trans == CblasConjTrans) {
            pushControl2b(1);
            TR = 'N';
        } else if (Trans == CblasNoTrans) {
            pushControl2b(2);
            TR = 'T';
        } else {
            if (alphab)
                *alphab = 0.0;
            if (Ab)
                *Ab = 0.0;
            if (betab)
                *betab = 0.0;
            goto label110;
        }
        F77_dsyrk_b(&UL, &TR, &F77_N, &F77_K, &alpha, &(*alphab), A, Ab, &F77_lda
                 , &beta, &(*betab), C, Cb, &F77_ldc, (size_t)1, (size_t)1);
        popControl2b(&branch);
      label110:
        popControl1b(&branch);
    } else {
        if (alphab)
            *alphab = 0.0;
        if (Ab)
            *Ab = 0.0;
        if (betab)
            *betab = 0.0;
    }
  label120:
    ;
}
