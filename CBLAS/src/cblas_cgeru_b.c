/*        Generated by TAPENADE     (INRIA, Ecuador team)
    Tapenade 3.16 (develop) -  6 Jan 2026 19:07
*/
#include <complex.h>
#include <adStack.h>
#include <stdio.h>
#include <ctype.h>
#include "cblas_b.h"
#include "cblas_f77_b.h"
#include <adStack.h>
#include <stdc-predef.h>
#include "cblas_b.h"
#include "cblas_f77_b.h"

/* Declaration for differentiated Fortran routine */
/* void F77_cgeru_b_base(...); */
/* Note: This should match the signature of cgeru_b in Fortran */

/* F77_ macros for differentiated Fortran routines */
/* These macros handle name mangling for differentiated Fortran functions */
#define F77_cgeru_b_base F77_GLOBAL_SUFFIX(cgeru_b,CGERU_B)
#define F77_cgeru_b(...) F77_cgeru_b_base(__VA_ARGS__)


/*
  Differentiation of cblas_cgeru in reverse (adjoint) mode:
   gradient     of useful results: *A
   with respect to varying inputs: *alpha *A *X *Y
   RW status of diff variables: alpha:(loc) *alpha:out A:(loc)
                *A:in-out X:(loc) *X:out Y:(loc) *Y:out
*/
void cblas_cgeru_b(const CBLAS_LAYOUT layout, const __int32_t M, const 
        __int32_t N, const void *alpha, void *alphab, const void *X, void *Xb,
        const __int32_t incX, const void *Y, void *Yb, const __int32_t incY, 
        void *A, void *Ab, const __int32_t lda) {
    int32_t F77_M = M;
    int32_t F77_N = N;
    int32_t F77_lda = lda;
    int32_t F77_incX = incX;
    int32_t F77_incY = incY;
    extern int CBLAS_CallFromC;
    extern int RowMajorStrg;
    if (layout == CblasColMajor)
        F77_cgeru_b(&F77_M, &F77_N, alpha, alphab, X, Xb, &F77_incX, Y, Yb, &
                 F77_incY, A, Ab, &F77_lda);
    else if (layout == CblasRowMajor)
        F77_cgeru_b(&F77_N, &F77_M, alpha, alphab, Y, Yb, &F77_incY, X, Xb, &
                 F77_incX, A, Ab, &F77_lda);
    else {
        if (alphab)
            *((float complex *)alphab) = 0;
        if (Xb)
            *((float complex *)Xb) = 0;
        if (Yb)
            *((float complex *)Yb) = 0;
    }
}
