/*        Generated by TAPENADE     (INRIA, Ecuador team)
    Tapenade 3.16 (develop) -  6 Jan 2026 19:07
*/
#include <adStack.h>
#include <stdio.h>
#include <ctype.h>
#include "cblas_bv.h"
#include "cblas_f77_bv.h"
#include <adStack.h>
#include <stdc-predef.h>
#include "cblas_bv.h"
#include "cblas_f77_bv.h"

/* Declaration for differentiated Fortran routine */
/* void F77_dtrmm_bv_base(...); */
/* Note: This should match the signature of dtrmm_bv in Fortran */

/* F77_ macros for differentiated Fortran routines */
/* These macros handle name mangling for differentiated Fortran functions */
#define F77_dtrmm_bv_base F77_GLOBAL_SUFFIX(dtrmm_bv,DTRMM_BV)
#define F77_dtrmm_bv(...) F77_dtrmm_bv_base(__VA_ARGS__)


/*
  Differentiation of cblas_dtrmm in reverse (adjoint) mode (with options multiDirectional):
   gradient     of useful results: *B
   with respect to varying inputs: alpha *A *B
   RW status of diff variables: alpha:out A:(loc) *A:out B:(loc)
                *B:in-out
*/
void cblas_dtrmm_bv(const CBLAS_LAYOUT layout, const CBLAS_SIDE Side, const 
        CBLAS_UPLO Uplo, const CBLAS_TRANSPOSE TransA, const CBLAS_DIAG Diag, 
        const __int32_t M, const __int32_t N, const double alpha, double (*
        alphab)[NBDirsMax], const double *A, double *Ab, const 
        __int32_t lda, double *B, double *Bb, const __int32_t ldb
        , int nbdirs) {
    char UL, TA, SD, DI;
    int32_t F77_M;
    int branch;
    int nd;
    F77_M = M;
    int32_t F77_N;
    F77_N = N;
    int32_t F77_lda;
    F77_lda = lda;
    int32_t F77_ldb;
    F77_ldb = ldb;
    extern int CBLAS_CallFromC;
    extern int RowMajorStrg;
    if (layout == CblasColMajor) {
        if (Side == CblasRight) {
            pushControl1b(0);
            SD = 'R';
        } else if (Side == CblasLeft) {
            pushControl1b(1);
            SD = 'L';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                (*alphab)[nd] = 0.0;
            for (nd = 0; nd < NBDirsMax; ++nd)
                Ab[nd] = 0.0;
            goto label160;
        }
        if (Uplo == CblasUpper) {
            pushControl1b(0);
            UL = 'U';
        } else if (Uplo == CblasLower) {
            pushControl1b(1);
            UL = 'L';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                (*alphab)[nd] = 0.0;
            for (nd = 0; nd < NBDirsMax; ++nd)
                Ab[nd] = 0.0;
            goto label120;
        }
        if (TransA == CblasTrans) {
            pushControl2b(0);
            TA = 'T';
        } else if (TransA == CblasConjTrans) {
            pushControl2b(1);
            TA = 'C';
        } else if (TransA == CblasNoTrans) {
            pushControl2b(2);
            TA = 'N';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                (*alphab)[nd] = 0.0;
            for (nd = 0; nd < NBDirsMax; ++nd)
                Ab[nd] = 0.0;
            goto label110;
        }
        if (Diag == CblasUnit) {
            pushControl1b(0);
            DI = 'U';
        } else if (Diag == CblasNonUnit) {
            pushControl1b(1);
            DI = 'N';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                (*alphab)[nd] = 0.0;
            for (nd = 0; nd < NBDirsMax; ++nd)
                Ab[nd] = 0.0;
            goto label100;
        }
        F77_dtrmm_bv(&SD, &UL, &TA, &DI, &F77_M, &F77_N, &alpha, &(*alphab), A, 
                  Ab, &F77_lda, B, Bb, &F77_ldb, &nbdirs);
        popControl1b(&branch);
      label100:
        popControl2b(&branch);
      label110:
        popControl1b(&branch);
      label120:
        popControl1b(&branch);
    } else if (layout == CblasRowMajor) {
        if (Side == CblasRight) {
            pushControl1b(0);
            SD = 'L';
        } else if (Side == CblasLeft) {
            pushControl1b(1);
            SD = 'R';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                (*alphab)[nd] = 0.0;
            for (nd = 0; nd < NBDirsMax; ++nd)
                Ab[nd] = 0.0;
            goto label160;
        }
        if (Uplo == CblasUpper) {
            pushControl1b(0);
            UL = 'L';
        } else if (Uplo == CblasLower) {
            pushControl1b(1);
            UL = 'U';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                (*alphab)[nd] = 0.0;
            for (nd = 0; nd < NBDirsMax; ++nd)
                Ab[nd] = 0.0;
            goto label150;
        }
        if (TransA == CblasTrans) {
            pushControl2b(0);
            TA = 'T';
        } else if (TransA == CblasConjTrans) {
            pushControl2b(1);
            TA = 'C';
        } else if (TransA == CblasNoTrans) {
            pushControl2b(2);
            TA = 'N';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                (*alphab)[nd] = 0.0;
            for (nd = 0; nd < NBDirsMax; ++nd)
                Ab[nd] = 0.0;
            goto label140;
        }
        if (Diag == CblasUnit) {
            pushControl1b(0);
            DI = 'U';
        } else if (Diag == CblasNonUnit) {
            pushControl1b(1);
            DI = 'N';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                (*alphab)[nd] = 0.0;
            for (nd = 0; nd < NBDirsMax; ++nd)
                Ab[nd] = 0.0;
            goto label130;
        }
        F77_dtrmm_bv(&SD, &UL, &TA, &DI, &F77_N, &F77_M, &alpha, &(*alphab), A, 
                  Ab, &F77_lda, B, Bb, &F77_ldb, &nbdirs);
        popControl1b(&branch);
      label130:
        popControl2b(&branch);
      label140:
        popControl1b(&branch);
      label150:
        popControl1b(&branch);
    } else {
        for (nd = 0; nd < NBDirsMax; ++nd)
            (*alphab)[nd] = 0.0;
        for (nd = 0; nd < NBDirsMax; ++nd)
            Ab[nd] = 0.0;
    }
  label160:
    ;
}
