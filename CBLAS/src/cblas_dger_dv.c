/*        Generated by TAPENADE     (INRIA, Ecuador team)
    Tapenade 3.16 (develop) -  6 Jan 2026 19:07
*/
#include "DIFFSIZESC.inc"
#include <stdint.h>
#include <stdio.h>
#include <ctype.h>
#include "cblas_dv.h"
#include "cblas_f77_dv.h"
#include <stdc-predef.h>
#include "cblas_dv.h"
#include "cblas_f77_dv.h"

/* Declaration for differentiated Fortran routine */
/* void F77_dger_dv_base(...); */
/* Note: This should match the signature of dger_dv in Fortran */


/*
  Differentiation of cblas_dger in forward (tangent) mode (with options multiDirectional):
   variations   of useful results: *A
   with respect to varying inputs: alpha *A *X *Y
   RW status of diff variables: alpha:in A:(loc) *A:in-out X:(loc)
                *X:in Y:(loc) *Y:in
*/
void cblas_dger_dv(const CBLAS_LAYOUT layout, const __int32_t M, const 
        __int32_t N, const double alpha, const double alphad[NBDirsMax], const
        double *X, const double (*Xd)[NBDirsMax], const __int32_t incX, const 
        double *Y, const double (*Yd)[NBDirsMax], const __int32_t incY, double
        *A, double (*Ad)[NBDirsMax], const __int32_t lda, int nbdirs) {
    int32_t F77_M;
    F77_M = M;
    int32_t F77_N;
    F77_N = N;
    int32_t F77_lda;
    F77_lda = lda;
    int32_t F77_incX;
    F77_incX = incX;
    int32_t F77_incY;
    F77_incY = incY;
    extern int CBLAS_CallFromC;
    extern int RowMajorStrg;
    RowMajorStrg = 0;
    CBLAS_CallFromC = 1;
    if (layout == CblasColMajor)
        F77_dger_dv(&F77_M, &F77_N, (double *)&alpha, (double *)alphad, (double *)X, (double *)Xd, &F77_incX, (double *)Y, (double *)Yd, &
                 F77_incY, (double *)A, (double *)Ad, &F77_lda, &nbdirs, (size_t)1, (size_t)1);
    else if (layout == CblasRowMajor) {
        RowMajorStrg = 1;
        F77_dger_dv(&F77_N, &F77_M, (double *)&alpha, (double *)alphad, (double *)Y, (double *)Yd, &F77_incY, (double *)X, (double *)Xd, &
                 F77_incX, (double *)A, (double *)Ad, &F77_lda, &nbdirs, (size_t)1, (size_t)1);
    } else
        cblas_xerbla(1, "cblas_dger", "Illegal layout setting, %d\n", layout);
    CBLAS_CallFromC = 0;
    RowMajorStrg = 0;
    return;
}
