/*        Generated by TAPENADE     (INRIA, Ecuador team)
    Tapenade 3.16 (develop) -  6 Jan 2026 19:07
*/
#include <stdio.h>
#include <ctype.h>
#include "cblas_d.h"
#include "cblas_f77_d.h"
#include <stdc-predef.h>
#include "cblas_d.h"
#include "cblas_f77_d.h"

/* Declaration for differentiated Fortran routine */
/* void F77_strsm_d_base(...); */
/* Note: This should match the signature of strsm_d in Fortran */


/*
  Differentiation of cblas_strsm in forward (tangent) mode:
   variations   of useful results: *B
   with respect to varying inputs: alpha *A *B
   RW status of diff variables: alpha:in A:(loc) *A:in B:(loc)
                *B:in-out
*/
void cblas_strsm_d(const CBLAS_LAYOUT layout, const CBLAS_SIDE Side, const 
        CBLAS_UPLO Uplo, const CBLAS_TRANSPOSE TransA, const CBLAS_DIAG Diag, 
        const __int32_t M, const __int32_t N, const float alpha, const float 
        alphad, const float *A, const float *Ad, const __int32_t lda, float *B
        , float *Bd, const __int32_t ldb) {
    char UL, TA, SD, DI;
    int32_t F77_M = M;
    int32_t F77_N = N;
    int32_t F77_lda = lda;
    int32_t F77_ldb = ldb;
    extern int CBLAS_CallFromC;
    extern int RowMajorStrg;
    RowMajorStrg = 0;
    CBLAS_CallFromC = 1;
    if (layout == CblasColMajor) {
        if (Side == CblasRight)
            SD = 'R';
        else if (Side == CblasLeft)
            SD = 'L';
        else {
            cblas_xerbla(2, "cblas_strsm", "Illegal Side setting, %d\n", Side)
            ;
            CBLAS_CallFromC = 0;
            RowMajorStrg = 0;
            return;
        }
        if (Uplo == CblasUpper)
            UL = 'U';
        else if (Uplo == CblasLower)
            UL = 'L';
        else {
            cblas_xerbla(3, "cblas_strsm", "Illegal Uplo setting, %d\n", Uplo)
            ;
            CBLAS_CallFromC = 0;
            RowMajorStrg = 0;
            return;
        }
        if (TransA == CblasTrans)
            TA = 'T';
        else if (TransA == CblasConjTrans)
            TA = 'C';
        else if (TransA == CblasNoTrans)
            TA = 'N';
        else {
            cblas_xerbla(4, "cblas_strsm", "Illegal Trans setting, %d\n", 
                         TransA);
            CBLAS_CallFromC = 0;
            RowMajorStrg = 0;
            return;
        }
        if (Diag == CblasUnit)
            DI = 'U';
        else if (Diag == CblasNonUnit)
            DI = 'N';
        else {
            cblas_xerbla(5, "cblas_strsm", "Illegal Diag setting, %d\n", Diag)
            ;
            CBLAS_CallFromC = 0;
            RowMajorStrg = 0;
            return;
        }
        F77_strsm_d(&SD, &UL, &TA, &DI, &F77_M, &F77_N, &alpha, &alphad, A, Ad, &
                 F77_lda, B, Bd, &F77_ldb);
    } else if (layout == CblasRowMajor) {
        RowMajorStrg = 1;
        if (Side == CblasRight)
            SD = 'L';
        else if (Side == CblasLeft)
            SD = 'R';
        else {
            cblas_xerbla(2, "cblas_strsm", "Illegal Side setting, %d\n", Side)
            ;
            CBLAS_CallFromC = 0;
            RowMajorStrg = 0;
            return;
        }
        if (Uplo == CblasUpper)
            UL = 'L';
        else if (Uplo == CblasLower)
            UL = 'U';
        else {
            cblas_xerbla(3, "cblas_strsm", "Illegal Uplo setting, %d\n", Uplo)
            ;
            CBLAS_CallFromC = 0;
            RowMajorStrg = 0;
            return;
        }
        if (TransA == CblasTrans)
            TA = 'T';
        else if (TransA == CblasConjTrans)
            TA = 'C';
        else if (TransA == CblasNoTrans)
            TA = 'N';
        else {
            cblas_xerbla(4, "cblas_strsm", "Illegal Trans setting, %d\n", 
                         TransA);
            CBLAS_CallFromC = 0;
            RowMajorStrg = 0;
            return;
        }
        if (Diag == CblasUnit)
            DI = 'U';
        else if (Diag == CblasNonUnit)
            DI = 'N';
        else {
            cblas_xerbla(5, "cblas_strsm", "Illegal Diag setting, %d\n", Diag)
            ;
            CBLAS_CallFromC = 0;
            RowMajorStrg = 0;
            return;
        }
        F77_strsm_d(&SD, &UL, &TA, &DI, &F77_N, &F77_M, &alpha, &alphad, A, Ad, &
                 F77_lda, B, Bd, &F77_ldb);
    } else
        cblas_xerbla(1, "cblas_strsm", "Illegal layout setting, %d\n", layout)
        ;
    CBLAS_CallFromC = 0;
    RowMajorStrg = 0;
    return;
}
