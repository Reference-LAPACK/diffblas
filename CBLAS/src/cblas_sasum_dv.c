/*        Generated by TAPENADE     (INRIA, Ecuador team)
    Tapenade 3.16 (develop) -  6 Jan 2026 19:07
*/
#include <stdint.h>
#include "DIFFSIZESC.inc"
/*  Hint: NBDirsMax should be the maximum number of differentiation directions
*/
#include <stdc-predef.h>
#include "cblas_dv.h"
#include "cblas_f77_dv.h"

/* Declaration for differentiated Fortran routine */
/* void F77_sasum_sub_dv_base(...); */
/* Note: This should match the signature of sasum_sub_dv in Fortran */


/*
  Differentiation of cblas_sasum in forward (tangent) mode (with options multiDirectional):
   variations   of useful results: cblas_sasum
   with respect to varying inputs: *X
   RW status of diff variables: cblas_sasum:out X:(loc) *X:in
*/
void cblas_sasum_dv(const __int32_t N, const float *X, const float (*Xd)[
        NBDirsMax], const __int32_t incX, float *cblas_sasum, float 
        cblas_sasumd[NBDirsMax], int nbdirs) {
    float asum;
    float asumd[NBDirsMax];
    int32_t F77_N;
    int nd;
    F77_N = N;
    int32_t F77_incX;
    F77_incX = incX;
    F77_sasumsub_dv(&F77_N, (float *)X, (float *)Xd, &F77_incX, (float *)&asum, (float *)&asumd, &nbdirs, (size_t)1, (size_t)1);
    *cblas_sasum = asum;
    for (nd = 0; nd < nbdirs; ++nd)
        cblas_sasumd[nd] = asumd[nd];
}
