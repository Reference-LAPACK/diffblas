/*        Generated by TAPENADE     (INRIA, Ecuador team)
    Tapenade 3.16 (develop) -  6 Jan 2026 19:07
*/
#include <stdio.h>
#include <ctype.h>
#include "cblas_d.h"
#include "cblas_f77_d.h"
#include <stdc-predef.h>
#include "cblas_d.h"
#include "cblas_f77_d.h"

/* Declaration for differentiated Fortran routine */
/* void F77_dsyr2k_d_base(...); */
/* Note: This should match the signature of dsyr2k_d in Fortran */


/*
  Differentiation of cblas_dsyr2k in forward (tangent) mode:
   variations   of useful results: *C
   with respect to varying inputs: alpha *A *B *C beta
   RW status of diff variables: alpha:in A:(loc) *A:in B:(loc)
                *B:in C:(loc) *C:in-out beta:in
*/
void cblas_dsyr2k_d(const CBLAS_LAYOUT layout, const CBLAS_UPLO Uplo, const 
        CBLAS_TRANSPOSE Trans, const __int32_t N, const __int32_t K, const 
        double alpha, const double alphad, const double *A, const double *Ad, 
        const __int32_t lda, const double *B, const double *Bd, const 
        __int32_t ldb, const double beta, const double betad, double *C, 
        double *Cd, const __int32_t ldc) {
    char UL, TR;
    int32_t F77_N = N;
    int32_t F77_K = K;
    int32_t F77_lda = lda;
    int32_t F77_ldb = ldb;
    int32_t F77_ldc = ldc;
    extern int CBLAS_CallFromC;
    extern int RowMajorStrg;
    RowMajorStrg = 0;
    CBLAS_CallFromC = 1;
    if (layout == CblasColMajor) {
        if (Uplo == CblasUpper)
            UL = 'U';
        else if (Uplo == CblasLower)
            UL = 'L';
        else {
            cblas_xerbla(2, "cblas_dsyr2k", "Illegal Uplo setting, %d\n", Uplo
                        );
            CBLAS_CallFromC = 0;
            RowMajorStrg = 0;
            return;
        }
        if (Trans == CblasTrans)
            TR = 'T';
        else if (Trans == CblasConjTrans)
            TR = 'C';
        else if (Trans == CblasNoTrans)
            TR = 'N';
        else {
            cblas_xerbla(3, "cblas_dsyr2k", "Illegal Trans setting, %d\n", 
                         Trans);
            CBLAS_CallFromC = 0;
            RowMajorStrg = 0;
            return;
        }
        F77_dsyr2k_d(&UL, &TR, &F77_N, &F77_K, &alpha, &alphad, A, Ad, &F77_lda, 
                  B, Bd, &F77_ldb, &beta, &betad, C, Cd, &F77_ldc);
    } else if (layout == CblasRowMajor) {
        RowMajorStrg = 1;
        if (Uplo == CblasUpper)
            UL = 'L';
        else if (Uplo == CblasLower)
            UL = 'U';
        else {
            cblas_xerbla(3, "cblas_dsyr2k", "Illegal Uplo setting, %d\n", Uplo
                        );
            CBLAS_CallFromC = 0;
            RowMajorStrg = 0;
            return;
        }
        if (Trans == CblasTrans)
            TR = 'N';
        else if (Trans == CblasConjTrans)
            TR = 'N';
        else if (Trans == CblasNoTrans)
            TR = 'T';
        else {
            cblas_xerbla(3, "cblas_dsyr2k", "Illegal Trans setting, %d\n", 
                         Trans);
            CBLAS_CallFromC = 0;
            RowMajorStrg = 0;
            return;
        }
        F77_dsyr2k_d(&UL, &TR, &F77_N, &F77_K, &alpha, &alphad, A, Ad, &F77_lda, 
                  B, Bd, &F77_ldb, &beta, &betad, C, Cd, &F77_ldc);
    } else
        cblas_xerbla(1, "cblas_dsyr2k", "Illegal layout setting, %d\n", layout
                    );
    CBLAS_CallFromC = 0;
    RowMajorStrg = 0;
    return;
}
