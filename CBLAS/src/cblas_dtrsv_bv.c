/*        Generated by TAPENADE     (INRIA, Ecuador team)
    Tapenade 3.16 (develop) -  6 Jan 2026 19:07
*/
#include <adStack.h>
#include <stdio.h>
#include <ctype.h>
#include "cblas_bv.h"
#include "cblas_f77_bv.h"
#include <adStack.h>
#include <stdc-predef.h>
#include "cblas_bv.h"
#include "cblas_f77_bv.h"

/* Declaration for differentiated Fortran routine */
/* void F77_dtrsv_bv_base(..., (size_t)1, (size_t)1, (size_t)1); */
/* Note: This should match the signature of dtrsv_bv in Fortran */

/* F77_ macros for differentiated Fortran routines */
/* These macros handle name mangling for differentiated Fortran functions */
#define F77_dtrsv_bv_base F77_GLOBAL_SUFFIX(dtrsv_bv,DTRSV_BV)
#define F77_dtrsv_bv(...) F77_dtrsv_bv_base(__VA_ARGS__)


/*
  Differentiation of cblas_dtrsv in reverse (adjoint) mode (with options multiDirectional):
   gradient     of useful results: *X
   with respect to varying inputs: *A *X
   RW status of diff variables: A:(loc) *A:out X:(loc) *X:in-out
*/
void cblas_dtrsv_bv(const CBLAS_LAYOUT layout, const CBLAS_UPLO Uplo, const 
        CBLAS_TRANSPOSE TransA, const CBLAS_DIAG Diag, const __int32_t N, 
        const double *A, double *Ab, const __int32_t lda, double 
        *X, double (*Xb)[NBDirsMax], const __int32_t incX, int nbdirs) {
    char TA;
    char UL;
    char DI;
    int32_t F77_N;
    int branch;
    int nd;
    F77_N = N;
    int32_t F77_lda;
    F77_lda = lda;
    int32_t F77_incX;
    F77_incX = incX;
    extern int CBLAS_CallFromC;
    extern int RowMajorStrg;
    if (layout == CblasColMajor) {
        if (Uplo == CblasUpper) {
            pushControl1b(0);
            UL = 'U';
        } else if (Uplo == CblasLower) {
            pushControl1b(1);
            UL = 'L';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                Ab[nd] = 0.0;
            goto label140;
        }
        if (TransA == CblasNoTrans) {
            pushControl2b(0);
            TA = 'N';
        } else if (TransA == CblasTrans) {
            pushControl2b(1);
            TA = 'T';
        } else if (TransA == CblasConjTrans) {
            pushControl2b(2);
            TA = 'C';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                Ab[nd] = 0.0;
            goto label110;
        }
        if (Diag == CblasUnit) {
            pushControl1b(0);
            DI = 'U';
        } else if (Diag == CblasNonUnit) {
            pushControl1b(1);
            DI = 'N';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                Ab[nd] = 0.0;
            goto label100;
        }
        F77_dtrsv_bv(&UL, &TA, &DI, &F77_N, A, Ab, &F77_lda, X, Xb, &F77_incX, &nbdirs, (size_t)1, (size_t)1, (size_t)1);
        popControl1b(&branch);
      label100:
        popControl2b(&branch);
      label110:
        popControl1b(&branch);
    } else if (layout == CblasRowMajor) {
        if (Uplo == CblasUpper) {
            pushControl1b(0);
            UL = 'L';
        } else if (Uplo == CblasLower) {
            pushControl1b(1);
            UL = 'U';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                Ab[nd] = 0.0;
            goto label140;
        }
        if (TransA == CblasNoTrans) {
            pushControl2b(0);
            TA = 'T';
        } else if (TransA == CblasTrans) {
            pushControl2b(1);
            TA = 'N';
        } else if (TransA == CblasConjTrans) {
            pushControl2b(2);
            TA = 'N';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                Ab[nd] = 0.0;
            goto label130;
        }
        if (Diag == CblasUnit) {
            pushControl1b(0);
            DI = 'U';
        } else if (Diag == CblasNonUnit) {
            pushControl1b(1);
            DI = 'N';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                Ab[nd] = 0.0;
            goto label120;
        }
        F77_dtrsv_bv(&UL, &TA, &DI, &F77_N, A, Ab, &F77_lda, X, Xb, &F77_incX, &nbdirs, (size_t)1, (size_t)1, (size_t)1);
        popControl1b(&branch);
      label120:
        popControl2b(&branch);
      label130:
        popControl1b(&branch);
    } else
        for (nd = 0; nd < NBDirsMax; ++nd)
            Ab[nd] = 0.0;
  label140:
    ;
}
