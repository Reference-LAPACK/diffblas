/*        Generated by TAPENADE     (INRIA, Ecuador team)
    Tapenade 3.16 (develop) -  6 Jan 2026 19:07
*/
#include <adStack.h>
#include <stdio.h>
#include <ctype.h>
#include "cblas_bv.h"
#include "cblas_f77_bv.h"
#include <adStack.h>
#include <stdc-predef.h>
#include "cblas_bv.h"
#include "cblas_f77_bv.h"

/* Declaration for differentiated Fortran routine */
/* void F77_dger_bv_base(...); */
/* Note: This should match the signature of dger_bv in Fortran */

/* F77_ macros for differentiated Fortran routines */
/* These macros handle name mangling for differentiated Fortran functions */
#define F77_dger_bv_base F77_GLOBAL_SUFFIX(dger_bv,DGER_BV)
#define F77_dger_bv(...) F77_dger_bv_base(__VA_ARGS__)


/*
  Differentiation of cblas_dger in reverse (adjoint) mode (with options multiDirectional):
   gradient     of useful results: *A
   with respect to varying inputs: alpha *A *X *Y
   RW status of diff variables: alpha:out A:(loc) *A:in-out X:(loc)
                *X:out Y:(loc) *Y:out
*/
void cblas_dger_bv(const CBLAS_LAYOUT layout, const __int32_t M, const 
        __int32_t N, const double alpha, double (*alphab)[NBDirsMax], const 
        double *X, double (*Xb)[NBDirsMax], const __int32_t incX, const double
        *Y, double (*Yb)[NBDirsMax], const __int32_t incY, double *A, double (
        *Ab)[NBDirsMax], const __int32_t lda, int nbdirs) {
    int32_t F77_M;
    int nd;
    F77_M = M;
    int32_t F77_N;
    F77_N = N;
    int32_t F77_lda;
    F77_lda = lda;
    int32_t F77_incX;
    F77_incX = incX;
    int32_t F77_incY;
    F77_incY = incY;
    extern int CBLAS_CallFromC;
    extern int RowMajorStrg;
    if (layout == CblasColMajor)
        F77_dger_bv(&F77_M, &F77_N, &alpha, &(*alphab), X, Xb, &F77_incX, Y, Yb, 
                 &F77_incY, A, Ab, &F77_lda, &nbdirs);
    else if (layout == CblasRowMajor)
        F77_dger_bv(&F77_N, &F77_M, &alpha, &(*alphab), Y, Yb, &F77_incY, X, Xb, 
                 &F77_incX, A, Ab, &F77_lda, &nbdirs);
    else {
        for (nd = 0; nd < NBDirsMax; ++nd)
            (*alphab)[nd] = 0.0;
        for (nd = 0; nd < NBDirsMax; ++nd)
            *Xb[nd] = 0.0;
        for (nd = 0; nd < NBDirsMax; ++nd)
            *Yb[nd] = 0.0;
    }
}
