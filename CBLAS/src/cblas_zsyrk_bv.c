/*        Generated by TAPENADE     (INRIA, Ecuador team)
    Tapenade 3.16 (develop) -  6 Jan 2026 19:07
*/
#include <complex.h>
#include <adStack.h>
#include <stdio.h>
#include <ctype.h>
#include "cblas_bv.h"
#include "cblas_f77_bv.h"
#include <adStack.h>
#include <stdc-predef.h>
#include "cblas_bv.h"
#include "cblas_f77_bv.h"

/* Declaration for differentiated Fortran routine */
/* void F77_zsyrk_bv_base(..., (size_t)1, (size_t)1); */
/* Note: This should match the signature of zsyrk_bv in Fortran */

/* F77_ macros for differentiated Fortran routines */
/* These macros handle name mangling for differentiated Fortran functions */
#define F77_zsyrk_bv_base F77_GLOBAL_SUFFIX(zsyrk_bv,ZSYRK_BV)
#define F77_zsyrk_bv(...) F77_zsyrk_bv_base(__VA_ARGS__)


/*
  Differentiation of cblas_zsyrk in reverse (adjoint) mode (with options multiDirectional):
   gradient     of useful results: *C
   with respect to varying inputs: *alpha *A *C *beta
   RW status of diff variables: alpha:(loc) *alpha:out A:(loc)
                *A:out C:(loc) *C:in-out beta:(loc) *beta:out
*/
void cblas_zsyrk_bv(const CBLAS_LAYOUT layout, const CBLAS_UPLO Uplo, const 
        CBLAS_TRANSPOSE Trans, const __int32_t N, const __int32_t K, const 
        void *alpha, void *alphab, const void *A, void *Ab, const __int32_t 
        lda, const void *beta, void *betab, void *C, void *Cb, const __int32_t
        ldc, int nbdirs) {
    char UL, TR;
    int32_t F77_N;
    int branch;
    int nd;
    F77_N = N;
    int32_t F77_K;
    F77_K = K;
    int32_t F77_lda;
    F77_lda = lda;
    int32_t F77_ldc;
    F77_ldc = ldc;
    extern int CBLAS_CallFromC;
    extern int RowMajorStrg;
    if (layout == CblasColMajor) {
        if (Uplo == CblasUpper) {
            pushControl1b(0);
            UL = 'U';
        } else if (Uplo == CblasLower) {
            pushControl1b(1);
            UL = 'L';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                ((double complex *)alphab)[nd] = 0.0;
            for (nd = 0; nd < NBDirsMax; ++nd)
                ((double complex *)Ab)[nd] = 0.0;
            for (nd = 0; nd < NBDirsMax; ++nd)
                ((double complex *)betab)[nd] = 0.0;
            goto label120;
        }
        if (Trans == CblasTrans) {
            pushControl2b(0);
            TR = 'T';
        } else if (Trans == CblasConjTrans) {
            pushControl2b(1);
            TR = 'C';
        } else if (Trans == CblasNoTrans) {
            pushControl2b(2);
            TR = 'N';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                ((double complex *)alphab)[nd] = 0.0;
            for (nd = 0; nd < NBDirsMax; ++nd)
                ((double complex *)Ab)[nd] = 0.0;
            for (nd = 0; nd < NBDirsMax; ++nd)
                ((double complex *)betab)[nd] = 0.0;
            goto label100;
        }
        F77_zsyrk_bv(&UL, &TR, &F77_N, &F77_K, alpha, alphab, A, Ab, &F77_lda, 
                  beta, betab, C, Cb, &F77_ldc, &nbdirs, (size_t)1, (size_t)1);
        popControl2b(&branch);
      label100:
        popControl1b(&branch);
    } else if (layout == CblasRowMajor) {
        if (Uplo == CblasUpper) {
            pushControl1b(0);
            UL = 'L';
        } else if (Uplo == CblasLower) {
            pushControl1b(1);
            UL = 'U';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                ((double complex *)alphab)[nd] = 0.0;
            for (nd = 0; nd < NBDirsMax; ++nd)
                ((double complex *)Ab)[nd] = 0.0;
            for (nd = 0; nd < NBDirsMax; ++nd)
                ((double complex *)betab)[nd] = 0.0;
            goto label120;
        }
        if (Trans == CblasTrans) {
            pushControl2b(0);
            TR = 'N';
        } else if (Trans == CblasConjTrans) {
            pushControl2b(1);
            TR = 'N';
        } else if (Trans == CblasNoTrans) {
            pushControl2b(2);
            TR = 'T';
        } else {
            for (nd = 0; nd < NBDirsMax; ++nd)
                ((double complex *)alphab)[nd] = 0.0;
            for (nd = 0; nd < NBDirsMax; ++nd)
                ((double complex *)Ab)[nd] = 0.0;
            for (nd = 0; nd < NBDirsMax; ++nd)
                ((double complex *)betab)[nd] = 0.0;
            goto label110;
        }
        F77_zsyrk_bv(&UL, &TR, &F77_N, &F77_K, alpha, alphab, A, Ab, &F77_lda, 
                  beta, betab, C, Cb, &F77_ldc, &nbdirs, (size_t)1, (size_t)1);
        popControl2b(&branch);
      label110:
        popControl1b(&branch);
    } else {
        for (nd = 0; nd < NBDirsMax; ++nd)
            ((double complex *)alphab)[nd] = 0.0;
        for (nd = 0; nd < NBDirsMax; ++nd)
            ((double complex *)Ab)[nd] = 0.0;
        for (nd = 0; nd < NBDirsMax; ++nd)
            ((double complex *)betab)[nd] = 0.0;
    }
  label120:
    ;
}
