C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of cscal in forward (tangent) mode:
C   variations   of useful results: cx
C   with respect to varying inputs: ca cx
C   RW status of diff variables: ca:in cx:in-out
C> \brief \b CSCAL
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       SUBROUTINE CSCAL(N,CA,CX,INCX)
C
C       .. Scalar Arguments ..
C       COMPLEX CA
C       INTEGER INCX,N
C       ..
C       .. Array Arguments ..
C       COMPLEX CX(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C>    CSCAL scales a vector by a constant.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>         number of elements in input vector(s)
C> \endverbatim
C>
C> \param[in] CA
C> \verbatim
C>          CA is COMPLEX
C>           On entry, CA specifies the scalar alpha.
C> \endverbatim
C>
C> \param[in,out] CX
C> \verbatim
C>          CX is COMPLEX array, dimension ( 1 + ( N - 1 )*abs( INCX ) )
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>         storage spacing between elements of CX
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup scal
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>     jack dongarra, linpack,  3/11/78.
C>     modified 3/93 to return if incx .le. 0.
C>     modified 12/3/93, array(1) declarations changed to array(*)
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE CSCAL_D(n, ca, cad, cx, cxd, incx)
      IMPLICIT NONE
C
C  -- Reference BLAS level1 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      COMPLEX ca
      COMPLEX cad
      INTEGER incx, n
C     ..
C     .. Array Arguments ..
      COMPLEX cx(*)
      COMPLEX cxd(*)
C     ..
C
C  =====================================================================
C
C     .. Local Scalars ..
      INTEGER i, nincx
C     ..
C     .. Parameters ..
      COMPLEX one
      PARAMETER (one=(1.0e+0,0.0e+0))
C     ..
      IF ((n .LE. 0 .OR. incx .LE. 0) .OR. ca .EQ. one) THEN
        RETURN
      ELSE
        IF (incx .EQ. 1) THEN
C
C        code for increment equal to 1
C
          DO i=1,n
            cxd(i) = cx(i)*cad + ca*cxd(i)
            cx(i) = ca*cx(i)
          ENDDO
        ELSE
C
C        code for increment not equal to 1
C
          nincx = n*incx
          DO i=1,nincx,incx
            cxd(i) = cx(i)*cad + ca*cxd(i)
            cx(i) = ca*cx(i)
          ENDDO
        END IF
        RETURN
C
C     End of CSCAL
C
      END IF
      END

