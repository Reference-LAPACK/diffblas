C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of zdotc in reverse (adjoint) mode:
C   gradient     of useful results: zdotc
C   with respect to varying inputs: zx zy
C   RW status of diff variables: zx:out zy:out zdotc:in-killed
C> \brief \b ZDOTC
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       COMPLEX*16 FUNCTION ZDOTC(N,ZX,INCX,ZY,INCY)
C
C       .. Scalar Arguments ..
C       INTEGER INCX,INCY,N
C       ..
C       .. Array Arguments ..
C       COMPLEX*16 ZX(*),ZY(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C> ZDOTC forms the dot product of two complex vectors
C>      ZDOTC = X^H * Y
C>
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>         number of elements in input vector(s)
C> \endverbatim
C>
C> \param[in] ZX
C> \verbatim
C>          ZX is COMPLEX*16 array, dimension ( 1 + ( N - 1 )*abs( INCX ) )
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>         storage spacing between elements of ZX
C> \endverbatim
C>
C> \param[in] ZY
C> \verbatim
C>          ZY is COMPLEX*16 array, dimension ( 1 + ( N - 1 )*abs( INCY ) )
C> \endverbatim
C>
C> \param[in] INCY
C> \verbatim
C>          INCY is INTEGER
C>         storage spacing between elements of ZY
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup dot
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>     jack dongarra, 3/11/78.
C>     modified 12/3/93, array(1) declarations changed to array(*)
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE ZDOTC_B(n, zx, zxb, incx, zy, zyb, incy, zdotcb)
      IMPLICIT NONE
      INCLUDE 'DIFFSIZES.inc'
C  Hint: ISIZE1OFzy should be the size of dimension 1 of array zy
C  Hint: ISIZE1OFzx should be the size of dimension 1 of array zx
C
C  -- Reference BLAS level1 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      INTEGER incx, incy, n
C     ..
C     .. Array Arguments ..
      COMPLEX*16 zx(*), zy(*)
      COMPLEX*16 zxb(*), zyb(*)
C     ..
C
C  =====================================================================
C
C     .. Local Scalars ..
      COMPLEX*16 ztemp
      COMPLEX*16 ztempb
      INTEGER i, ix, iy
      INTEGER ISIZE1OFZx, ISIZE1OFZy
      INTEGER get_ISIZE1OFZx, get_ISIZE1OFZy
      EXTERNAL get_ISIZE1OFZx, get_ISIZE1OFZy
C     ..
C     .. Intrinsic Functions ..
      INTRINSIC DCONJG
      INTEGER ii1
      INTEGER*4 branch
      COMPLEX*16 zdotc
      COMPLEX*16 zdotcb
C     ..
      CALL check_ISIZE1OFZx_initialized()
      CALL check_ISIZE1OFZy_initialized()
      ISIZE1OFZx = get_ISIZE1OFZx()
      ISIZE1OFZy = get_ISIZE1OFZy()
      IF (n .LE. 0) THEN
        DO ii1=1,ISIZE1OFzx
          zxb(ii1) = (0.0,0.0)
        ENDDO
        DO ii1=1,ISIZE1OFzy
          zyb(ii1) = (0.0,0.0)
        ENDDO
      ELSE
        IF (incx .EQ. 1 .AND. incy .EQ. 1) THEN
          CALL PUSHCONTROL1B(0)
        ELSE
C
C        code for unequal increments or equal increments
C          not equal to 1
C
          ix = 1
          iy = 1
          IF (incx .LT. 0) ix = (-n+1)*incx + 1
          IF (incy .LT. 0) iy = (-n+1)*incy + 1
          DO i=1,n
            CALL PUSHINTEGER4(ix)
            ix = ix + incx
            CALL PUSHINTEGER4(iy)
            iy = iy + incy
          ENDDO
          CALL PUSHCONTROL1B(1)
        END IF
        ztempb = zdotcb
        CALL POPCONTROL1B(branch)
        IF (branch .EQ. 0) THEN
          DO ii1=1,ISIZE1OFzx
            zxb(ii1) = (0.0,0.0)
          ENDDO
          DO ii1=1,ISIZE1OFzy
            zyb(ii1) = (0.0,0.0)
          ENDDO
          DO i=n,1,-1
            zxb(i) = zxb(i) + DCONJG(CONJG(zy(i))*ztempb)
            zyb(i) = zyb(i) + CONJG(DCONJG(zx(i)))*ztempb
          ENDDO
        ELSE
          DO ii1=1,ISIZE1OFzx
            zxb(ii1) = (0.0,0.0)
          ENDDO
          DO ii1=1,ISIZE1OFzy
            zyb(ii1) = (0.0,0.0)
          ENDDO
          DO i=n,1,-1
            CALL POPINTEGER4(iy)
            CALL POPINTEGER4(ix)
            zxb(ix) = zxb(ix) + DCONJG(CONJG(zy(iy))*ztempb)
            zyb(iy) = zyb(iy) + CONJG(DCONJG(zx(ix)))*ztempb
          ENDDO
        END IF
      END IF
      END

