C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of sdot in forward (tangent) mode (with options multiDirectional):
C   variations   of useful results: sdot
C   with respect to varying inputs: sx sy
C   RW status of diff variables: sdot:out sx:in sy:in
C> \brief \b SDOT
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       REAL FUNCTION SDOT(N,SX,INCX,SY,INCY)
C
C       .. Scalar Arguments ..
C       INTEGER INCX,INCY,N
C       ..
C       .. Array Arguments ..
C       REAL SX(*),SY(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C>    SDOT forms the dot product of two vectors.
C>    uses unrolled loops for increments equal to one.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>         number of elements in input vector(s)
C> \endverbatim
C>
C> \param[in] SX
C> \verbatim
C>          SX is REAL array, dimension ( 1 + ( N - 1 )*abs( INCX ) )
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>         storage spacing between elements of SX
C> \endverbatim
C>
C> \param[in] SY
C> \verbatim
C>          SY is REAL array, dimension ( 1 + ( N - 1 )*abs( INCY ) )
C> \endverbatim
C>
C> \param[in] INCY
C> \verbatim
C>          INCY is INTEGER
C>         storage spacing between elements of SY
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup dot
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>     jack dongarra, linpack, 3/11/78.
C>     modified 12/3/93, array(1) declarations changed to array(*)
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE SDOT_DV(n, sx, sxd, incx, sy, syd, incy, sdot, sdotd, 
     +                   nbdirs)
      IMPLICIT NONE
      INCLUDE 'DIFFSIZES.inc'
C  Hint: nbdirsmax should be the maximum number of differentiation directions
C
C  -- Reference BLAS level1 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      INTEGER incx, incy, n
C     ..
C     .. Array Arguments ..
      REAL sx(*), sy(*)
      REAL sxd(nbdirsmax, *), syd(nbdirsmax, *)
C     ..
C
C  =====================================================================
C
C     .. Local Scalars ..
      REAL stemp
      REAL stempd(nbdirsmax)
      INTEGER i, ix, iy, m, mp1
C     ..
C     .. Intrinsic Functions ..
      INTRINSIC MOD
      INTEGER nd
      REAL sdot
      REAL sdotd(nbdirsmax)
      INTEGER nbdirs
C     ..
C     Check 0 < nbdirs <= nbdirsmax (required by DIFFSIZES.inc)
      IF (nbdirs.LE.0 .OR. nbdirs.GT.nbdirsmax) THEN
        WRITE(*,'(A,I0,A,I0,A)') 'Error: nbdirs=', nbdirs,
     +  ' must be in 1..nbdirsmax=', nbdirsmax, '. Stopping.'
        STOP 1
      END IF
C
      stemp = 0.0e0
      sdot = 0.0e0
      IF (n .LE. 0) THEN
        DO nd=1,nbdirsmax
          sdotd(nd) = 0.0
        ENDDO
        RETURN
      ELSE
        IF (incx .EQ. 1 .AND. incy .EQ. 1) THEN
C
C        code for both increments equal to 1
C
C
C        clean-up loop
C
          m = MOD(n, 5)
          IF (m .NE. 0) THEN
            DO nd=1,nbdirsmax
              stempd(nd) = 0.0
            ENDDO
            DO i=1,m
              DO nd=1,nbdirs
                stempd(nd) = stempd(nd) + sy(i)*sxd(nd, i) + sx(i)*syd(
     +            nd, i)
              ENDDO
              stemp = stemp + sx(i)*sy(i)
            ENDDO
            IF (n .LT. 5) THEN
              DO nd=1,nbdirs
                sdotd(nd) = stempd(nd)
              ENDDO
              sdot = stemp
              RETURN
            END IF
          ELSE
            DO nd=1,nbdirsmax
              stempd(nd) = 0.0
            ENDDO
          END IF
          mp1 = m + 1
          DO i=mp1,n,5
            DO nd=1,nbdirs
              stempd(nd) = stempd(nd) + sy(i)*sxd(nd, i) + sx(i)*syd(nd
     +          , i) + sy(i+1)*sxd(nd, i+1) + sx(i+1)*syd(nd, i+1) + sy(
     +          i+2)*sxd(nd, i+2) + sx(i+2)*syd(nd, i+2) + sy(i+3)*sxd(
     +          nd, i+3) + sx(i+3)*syd(nd, i+3) + sy(i+4)*sxd(nd, i+4) +
     +          sx(i+4)*syd(nd, i+4)
            ENDDO
            stemp = stemp + sx(i)*sy(i) + sx(i+1)*sy(i+1) + sx(i+2)*sy(i
     +        +2) + sx(i+3)*sy(i+3) + sx(i+4)*sy(i+4)
          ENDDO
        ELSE
C
C        code for unequal increments or equal increments
C          not equal to 1
C
          ix = 1
          iy = 1
          IF (incx .LT. 0) ix = (-n+1)*incx + 1
          IF (incy .LT. 0) THEN
            iy = (-n+1)*incy + 1
            DO nd=1,nbdirsmax
              stempd(nd) = 0.0
            ENDDO
          ELSE
            DO nd=1,nbdirsmax
              stempd(nd) = 0.0
            ENDDO
          END IF
          DO i=1,n
            DO nd=1,nbdirs
              stempd(nd) = stempd(nd) + sy(iy)*sxd(nd, ix) + sx(ix)*syd(
     +          nd, iy)
            ENDDO
            stemp = stemp + sx(ix)*sy(iy)
            ix = ix + incx
            iy = iy + incy
          ENDDO
        END IF
        DO nd=1,nbdirs
          sdotd(nd) = stempd(nd)
        ENDDO
        sdot = stemp
        RETURN
C
C     End of SDOT
C
      END IF
      END

