C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) - 14 Mar 2025 11:54
C
C  Differentiation of ctrsm in forward (tangent) mode (with options multiDirectional):
C   variations   of useful results: b
C   with respect to varying inputs: alpha a b
C   RW status of diff variables: alpha:in a:in b:in-out
C> \brief \b CTRSM
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       SUBROUTINE CTRSM(SIDE,UPLO,TRANSA,DIAG,M,N,ALPHA,A,LDA,B,LDB)
C
C       .. Scalar Arguments ..
C       COMPLEX ALPHA
C       INTEGER LDA,LDB,M,N
C       CHARACTER DIAG,SIDE,TRANSA,UPLO
C       ..
C       .. Array Arguments ..
C       COMPLEX A(LDA,*),B(LDB,*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C> CTRSM  solves one of the matrix equations
C>
C>    op( A )*X = alpha*B,   or   X*op( A ) = alpha*B,
C>
C> where alpha is a scalar, X and B are m by n matrices, A is a unit, or
C> non-unit,  upper or lower triangular matrix  and  op( A )  is one  of
C>
C>    op( A ) = A   or   op( A ) = A**T   or   op( A ) = A**H.
C>
C> The matrix X is overwritten on B.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] SIDE
C> \verbatim
C>          SIDE is CHARACTER*1
C>           On entry, SIDE specifies whether op( A ) appears on the left
C>           or right of X as follows:
C>
C>              SIDE = 'L' or 'l'   op( A )*X = alpha*B.
C>
C>              SIDE = 'R' or 'r'   X*op( A ) = alpha*B.
C> \endverbatim
C>
C> \param[in] UPLO
C> \verbatim
C>          UPLO is CHARACTER*1
C>           On entry, UPLO specifies whether the matrix A is an upper or
C>           lower triangular matrix as follows:
C>
C>              UPLO = 'U' or 'u'   A is an upper triangular matrix.
C>
C>              UPLO = 'L' or 'l'   A is a lower triangular matrix.
C> \endverbatim
C>
C> \param[in] TRANSA
C> \verbatim
C>          TRANSA is CHARACTER*1
C>           On entry, TRANSA specifies the form of op( A ) to be used in
C>           the matrix multiplication as follows:
C>
C>              TRANSA = 'N' or 'n'   op( A ) = A.
C>
C>              TRANSA = 'T' or 't'   op( A ) = A**T.
C>
C>              TRANSA = 'C' or 'c'   op( A ) = A**H.
C> \endverbatim
C>
C> \param[in] DIAG
C> \verbatim
C>          DIAG is CHARACTER*1
C>           On entry, DIAG specifies whether or not A is unit triangular
C>           as follows:
C>
C>              DIAG = 'U' or 'u'   A is assumed to be unit triangular.
C>
C>              DIAG = 'N' or 'n'   A is not assumed to be unit
C>                                  triangular.
C> \endverbatim
C>
C> \param[in] M
C> \verbatim
C>          M is INTEGER
C>           On entry, M specifies the number of rows of B. M must be at
C>           least zero.
C> \endverbatim
C>
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>           On entry, N specifies the number of columns of B.  N must be
C>           at least zero.
C> \endverbatim
C>
C> \param[in] ALPHA
C> \verbatim
C>          ALPHA is COMPLEX
C>           On entry,  ALPHA specifies the scalar  alpha. When  alpha is
C>           zero then  A is not referenced and  B need not be set before
C>           entry.
C> \endverbatim
C>
C> \param[in] A
C> \verbatim
C>          A is COMPLEX array, dimension ( LDA, k ),
C>           where k is m when SIDE = 'L' or 'l'
C>             and k is n when SIDE = 'R' or 'r'.
C>           Before entry  with  UPLO = 'U' or 'u',  the  leading  k by k
C>           upper triangular part of the array  A must contain the upper
C>           triangular matrix  and the strictly lower triangular part of
C>           A is not referenced.
C>           Before entry  with  UPLO = 'L' or 'l',  the  leading  k by k
C>           lower triangular part of the array  A must contain the lower
C>           triangular matrix  and the strictly upper triangular part of
C>           A is not referenced.
C>           Note that when  DIAG = 'U' or 'u',  the diagonal elements of
C>           A  are not referenced either,  but are assumed to be  unity.
C> \endverbatim
C>
C> \param[in] LDA
C> \verbatim
C>          LDA is INTEGER
C>           On entry, LDA specifies the first dimension of A as declared
C>           in the calling (sub) program.  When  SIDE = 'L' or 'l'  then
C>           LDA  must be at least  max( 1, m ),  when  SIDE = 'R' or 'r'
C>           then LDA must be at least max( 1, n ).
C> \endverbatim
C>
C> \param[in,out] B
C> \verbatim
C>          B is COMPLEX array, dimension ( LDB, N )
C>           Before entry,  the leading  m by n part of the array  B must
C>           contain  the  right-hand  side  matrix  B,  and  on exit  is
C>           overwritten by the solution matrix  X.
C> \endverbatim
C>
C> \param[in] LDB
C> \verbatim
C>          LDB is INTEGER
C>           On entry, LDB specifies the first dimension of B as declared
C>           in  the  calling  (sub)  program.   LDB  must  be  at  least
C>           max( 1, m ).
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup trsm
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>  Level 3 Blas routine.
C>
C>  -- Written on 8-February-1989.
C>     Jack Dongarra, Argonne National Laboratory.
C>     Iain Duff, AERE Harwell.
C>     Jeremy Du Croz, Numerical Algorithms Group Ltd.
C>     Sven Hammarling, Numerical Algorithms Group Ltd.
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE CTRSM_DV(SIDE, UPLO, TRANSA, DIAG, M, N,
     +                    ALPHA, ALPHAD, A, AD, LDA, B, BD, LDB, NBDIRS)
!
! Vector forward-mode derivative of CTRSM, black-box/Giles-style.
! Self-contained -- does not call CTRSM_D.
!
      IMPLICIT NONE
      CHARACTER SIDE, UPLO, TRANSA, DIAG
      INTEGER M, N, LDA, LDB, NBDIRS
      COMPLEX ALPHA
      COMPLEX ALPHAD(NBDIRS)
      COMPLEX A(LDA,*), AD(NBDIRS,LDA,*)
      COMPLEX B(LDB,*), BD(NBDIRS,LDB,*)

      COMPLEX X(M,N), T(M,N)
      COMPLEX AD_SLICE(LDA,LDA), BD_SLICE(LDB,N)
      INTEGER ND, I, J

      IF (M.EQ.0 .OR. N.EQ.0) RETURN

      DO J = 1, N
         DO I = 1, M
            X(I,J) = B(I,J)
         END DO
      END DO
      CALL CTRSM(SIDE, UPLO, TRANSA, DIAG, M, N, ALPHA, A, LDA, X, M)

      DO ND = 1, NBDIRS

         DO J = 1, N
            DO I = 1, M
               AD_SLICE(I,J) = AD(ND,I,J)
               BD_SLICE(I,J) = BD(ND,I,J)
            END DO
         END DO

         DO J = 1, N
            DO I = 1, M
               T(I,J) = X(I,J)
            END DO
         END DO
C     'N' not DIAG: unit A has a constant (unit) diagonal, so AD diag=0.
         CALL CTRMM(SIDE, UPLO, TRANSA, 'N', M, N, (1.0E0,0.0E0),
     +              AD_SLICE, LDA, T, M)

         DO J = 1, N
            DO I = 1, M
               BD_SLICE(I,J) = ALPHAD(ND)*B(I,J) + ALPHA*BD_SLICE(I,J)
     +                         - T(I,J)
            END DO
         END DO

         CALL CTRSM(SIDE, UPLO, TRANSA, DIAG, M, N, (1.0E0,0.0E0),
     +              A, LDA, BD_SLICE, LDB)

         DO J = 1, N
            DO I = 1, M
               BD(ND,I,J) = BD_SLICE(I,J)
            END DO
         END DO

      END DO

      RETURN
      END SUBROUTINE CTRSM_DV

