C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of dsyrk in forward (tangent) mode:
C   variations   of useful results: c
C   with respect to varying inputs: alpha beta a c
C   RW status of diff variables: alpha:in beta:in a:in c:in-out
C> \brief \b DSYRK
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       SUBROUTINE DSYRK(UPLO,TRANS,N,K,ALPHA,A,LDA,BETA,C,LDC)
C
C       .. Scalar Arguments ..
C       DOUBLE PRECISION ALPHA,BETA
C       INTEGER K,LDA,LDC,N
C       CHARACTER TRANS,UPLO
C       ..
C       .. Array Arguments ..
C       DOUBLE PRECISION A(LDA,*),C(LDC,*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C> DSYRK  performs one of the symmetric rank k operations
C>
C>    C := alpha*A*A**T + beta*C,
C>
C> or
C>
C>    C := alpha*A**T*A + beta*C,
C>
C> where  alpha and beta  are scalars, C is an  n by n  symmetric matrix
C> and  A  is an  n by k  matrix in the first case and a  k by n  matrix
C> in the second case.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] UPLO
C> \verbatim
C>          UPLO is CHARACTER*1
C>           On  entry,   UPLO  specifies  whether  the  upper  or  lower
C>           triangular  part  of the  array  C  is to be  referenced  as
C>           follows:
C>
C>              UPLO = 'U' or 'u'   Only the  upper triangular part of  C
C>                                  is to be referenced.
C>
C>              UPLO = 'L' or 'l'   Only the  lower triangular part of  C
C>                                  is to be referenced.
C> \endverbatim
C>
C> \param[in] TRANS
C> \verbatim
C>          TRANS is CHARACTER*1
C>           On entry,  TRANS  specifies the operation to be performed as
C>           follows:
C>
C>              TRANS = 'N' or 'n'   C := alpha*A*A**T + beta*C.
C>
C>              TRANS = 'T' or 't'   C := alpha*A**T*A + beta*C.
C>
C>              TRANS = 'C' or 'c'   C := alpha*A**T*A + beta*C.
C> \endverbatim
C>
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>           On entry,  N specifies the order of the matrix C.  N must be
C>           at least zero.
C> \endverbatim
C>
C> \param[in] K
C> \verbatim
C>          K is INTEGER
C>           On entry with  TRANS = 'N' or 'n',  K  specifies  the number
C>           of  columns   of  the   matrix   A,   and  on   entry   with
C>           TRANS = 'T' or 't' or 'C' or 'c',  K  specifies  the  number
C>           of rows of the matrix  A.  K must be at least zero.
C> \endverbatim
C>
C> \param[in] ALPHA
C> \verbatim
C>          ALPHA is DOUBLE PRECISION.
C>           On entry, ALPHA specifies the scalar alpha.
C> \endverbatim
C>
C> \param[in] A
C> \verbatim
C>          A is DOUBLE PRECISION array, dimension ( LDA, ka ), where ka is
C>           k  when  TRANS = 'N' or 'n',  and is  n  otherwise.
C>           Before entry with  TRANS = 'N' or 'n',  the  leading  n by k
C>           part of the array  A  must contain the matrix  A,  otherwise
C>           the leading  k by n  part of the array  A  must contain  the
C>           matrix A.
C> \endverbatim
C>
C> \param[in] LDA
C> \verbatim
C>          LDA is INTEGER
C>           On entry, LDA specifies the first dimension of A as declared
C>           in  the  calling  (sub)  program.   When  TRANS = 'N' or 'n'
C>           then  LDA must be at least  max( 1, n ), otherwise  LDA must
C>           be at least  max( 1, k ).
C> \endverbatim
C>
C> \param[in] BETA
C> \verbatim
C>          BETA is DOUBLE PRECISION.
C>           On entry, BETA specifies the scalar beta.
C> \endverbatim
C>
C> \param[in,out] C
C> \verbatim
C>          C is DOUBLE PRECISION array, dimension ( LDC, N )
C>           Before entry  with  UPLO = 'U' or 'u',  the leading  n by n
C>           upper triangular part of the array C must contain the upper
C>           triangular part  of the  symmetric matrix  and the strictly
C>           lower triangular part of C is not referenced.  On exit, the
C>           upper triangular part of the array  C is overwritten by the
C>           upper triangular part of the updated matrix.
C>           Before entry  with  UPLO = 'L' or 'l',  the leading  n by n
C>           lower triangular part of the array C must contain the lower
C>           triangular part  of the  symmetric matrix  and the strictly
C>           upper triangular part of C is not referenced.  On exit, the
C>           lower triangular part of the array  C is overwritten by the
C>           lower triangular part of the updated matrix.
C> \endverbatim
C>
C> \param[in] LDC
C> \verbatim
C>          LDC is INTEGER
C>           On entry, LDC specifies the first dimension of C as declared
C>           in  the  calling  (sub)  program.   LDC  must  be  at  least
C>           max( 1, n ).
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup herk
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>  Level 3 Blas routine.
C>
C>  -- Written on 8-February-1989.
C>     Jack Dongarra, Argonne National Laboratory.
C>     Iain Duff, AERE Harwell.
C>     Jeremy Du Croz, Numerical Algorithms Group Ltd.
C>     Sven Hammarling, Numerical Algorithms Group Ltd.
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE DSYRK_D(uplo, trans, n, k, alpha, alphad, a, ad, lda, 
     +                   beta, betad, c, cd, ldc)
      IMPLICIT NONE
C
C  -- Reference BLAS level3 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      DOUBLE PRECISION alpha, beta
      DOUBLE PRECISION alphad, betad
      INTEGER k, lda, ldc, n
      CHARACTER trans, uplo
C     ..
C     .. Array Arguments ..
      DOUBLE PRECISION a(lda, *), c(ldc, *)
      DOUBLE PRECISION ad(lda, *), cd(ldc, *)
      EXTERNAL LSAME
C     ..
C
C  =====================================================================
C
C     .. External Functions ..
      LOGICAL LSAME
C     ..
C     .. External Subroutines ..
      EXTERNAL XERBLA
C     ..
C     .. Intrinsic Functions ..
      INTRINSIC MAX
C     ..
C     .. Local Scalars ..
      DOUBLE PRECISION temp
      DOUBLE PRECISION tempd
      INTEGER i, info, j, l, nrowa
      LOGICAL upper
C     ..
C     .. Parameters ..
      DOUBLE PRECISION one, zero
      PARAMETER (one=1.0d+0, zero=0.0d+0)
      INTEGER max1
      INTEGER max2
C     ..
C
C     Test the input parameters.
C
      IF (LSAME(trans, 'N')) THEN
        nrowa = n
      ELSE
        nrowa = k
      END IF
      upper = LSAME(uplo, 'U')
C
      info = 0
      IF (.NOT.upper .AND. (.NOT.LSAME(uplo, 'L'))) THEN
        info = 1
      ELSE IF (.NOT.LSAME(trans, 'N') .AND. (.NOT.LSAME(trans, 'T')) 
     +    .AND. (.NOT.LSAME(trans, 'C'))) THEN
        info = 2
      ELSE IF (n .LT. 0) THEN
        info = 3
      ELSE IF (k .LT. 0) THEN
        info = 4
      ELSE
        IF (1 .LT. nrowa) THEN
          max1 = nrowa
        ELSE
          max1 = 1
        END IF
        IF (lda .LT. max1) THEN
          info = 7
        ELSE
          IF (1 .LT. n) THEN
            max2 = n
          ELSE
            max2 = 1
          END IF
          IF (ldc .LT. max2) info = 10
        END IF
      END IF
      IF (info .NE. 0) THEN
        CALL XERBLA('DSYRK ', info)
        RETURN
      ELSE IF (n .EQ. 0 .OR. ((alpha .EQ. zero .OR. k .EQ. 0) .AND. beta
     +    .EQ. one)) THEN
C
C     Quick return if possible.
C
        RETURN
      ELSE IF (alpha .EQ. zero) THEN
C
C     And when  alpha.eq.zero.
C
        IF (upper) THEN
          IF (beta .EQ. zero) THEN
            DO j=1,n
              DO i=1,j
                cd(i, j) = 0.D0
                c(i, j) = zero
              ENDDO
            ENDDO
          ELSE
            DO j=1,n
              DO i=1,j
                cd(i, j) = c(i, j)*betad + beta*cd(i, j)
                c(i, j) = beta*c(i, j)
              ENDDO
            ENDDO
          END IF
        ELSE IF (beta .EQ. zero) THEN
          DO j=1,n
            DO i=j,n
              cd(i, j) = 0.D0
              c(i, j) = zero
            ENDDO
          ENDDO
        ELSE
          DO j=1,n
            DO i=j,n
              cd(i, j) = c(i, j)*betad + beta*cd(i, j)
              c(i, j) = beta*c(i, j)
            ENDDO
          ENDDO
        END IF
        RETURN
      ELSE
C
C     Start the operations.
C
        IF (LSAME(trans, 'N')) THEN
C
C        Form  C := alpha*A*A**T + beta*C.
C
          IF (upper) THEN
            DO j=1,n
              IF (beta .EQ. zero) THEN
                DO i=1,j
                  cd(i, j) = 0.D0
                  c(i, j) = zero
                ENDDO
              ELSE IF (beta .NE. one) THEN
                DO i=1,j
                  cd(i, j) = c(i, j)*betad + beta*cd(i, j)
                  c(i, j) = beta*c(i, j)
                ENDDO
              END IF
              DO l=1,k
                IF (a(j, l) .NE. zero) THEN
                  tempd = a(j, l)*alphad + alpha*ad(j, l)
                  temp = alpha*a(j, l)
                  DO i=1,j
                    cd(i, j) = cd(i, j) + a(i, l)*tempd + temp*ad(i, l)
                    c(i, j) = c(i, j) + temp*a(i, l)
                  ENDDO
                END IF
              ENDDO
            ENDDO
          ELSE
            DO j=1,n
              IF (beta .EQ. zero) THEN
                DO i=j,n
                  cd(i, j) = 0.D0
                  c(i, j) = zero
                ENDDO
              ELSE IF (beta .NE. one) THEN
                DO i=j,n
                  cd(i, j) = c(i, j)*betad + beta*cd(i, j)
                  c(i, j) = beta*c(i, j)
                ENDDO
              END IF
              DO l=1,k
                IF (a(j, l) .NE. zero) THEN
                  tempd = a(j, l)*alphad + alpha*ad(j, l)
                  temp = alpha*a(j, l)
                  DO i=j,n
                    cd(i, j) = cd(i, j) + a(i, l)*tempd + temp*ad(i, l)
                    c(i, j) = c(i, j) + temp*a(i, l)
                  ENDDO
                END IF
              ENDDO
            ENDDO
          END IF
        ELSE IF (upper) THEN
C
C        Form  C := alpha*A**T*A + beta*C.
C
          DO j=1,n
            DO i=1,j
              temp = zero
              tempd = 0.D0
              DO l=1,k
                tempd = tempd + a(l, j)*ad(l, i) + a(l, i)*ad(l, j)
                temp = temp + a(l, i)*a(l, j)
              ENDDO
              IF (beta .EQ. zero) THEN
                cd(i, j) = temp*alphad + alpha*tempd
                c(i, j) = alpha*temp
              ELSE
                cd(i, j) = temp*alphad + alpha*tempd + c(i, j)*betad + 
     +            beta*cd(i, j)
                c(i, j) = alpha*temp + beta*c(i, j)
              END IF
            ENDDO
          ENDDO
        ELSE
          DO j=1,n
            DO i=j,n
              temp = zero
              tempd = 0.D0
              DO l=1,k
                tempd = tempd + a(l, j)*ad(l, i) + a(l, i)*ad(l, j)
                temp = temp + a(l, i)*a(l, j)
              ENDDO
              IF (beta .EQ. zero) THEN
                cd(i, j) = temp*alphad + alpha*tempd
                c(i, j) = alpha*temp
              ELSE
                cd(i, j) = temp*alphad + alpha*tempd + c(i, j)*betad + 
     +            beta*cd(i, j)
                c(i, j) = alpha*temp + beta*c(i, j)
              END IF
            ENDDO
          ENDDO
        END IF
C
        RETURN
C
C     End of DSYRK
C
      END IF
      END

