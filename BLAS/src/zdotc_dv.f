C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of zdotc in forward (tangent) mode (with options multiDirectional):
C   variations   of useful results: zdotc
C   with respect to varying inputs: zx zy
C   RW status of diff variables: zx:in zy:in zdotc:out
C> \brief \b ZDOTC
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       COMPLEX*16 FUNCTION ZDOTC(N,ZX,INCX,ZY,INCY)
C
C       .. Scalar Arguments ..
C       INTEGER INCX,INCY,N
C       ..
C       .. Array Arguments ..
C       COMPLEX*16 ZX(*),ZY(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C> ZDOTC forms the dot product of two complex vectors
C>      ZDOTC = X^H * Y
C>
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>         number of elements in input vector(s)
C> \endverbatim
C>
C> \param[in] ZX
C> \verbatim
C>          ZX is COMPLEX*16 array, dimension ( 1 + ( N - 1 )*abs( INCX ) )
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>         storage spacing between elements of ZX
C> \endverbatim
C>
C> \param[in] ZY
C> \verbatim
C>          ZY is COMPLEX*16 array, dimension ( 1 + ( N - 1 )*abs( INCY ) )
C> \endverbatim
C>
C> \param[in] INCY
C> \verbatim
C>          INCY is INTEGER
C>         storage spacing between elements of ZY
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup dot
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>     jack dongarra, 3/11/78.
C>     modified 12/3/93, array(1) declarations changed to array(*)
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE ZDOTC_DV(n, zx, zxd, incx, zy, zyd, incy, zdotc, zdotcd
     +                    , nbdirs)
      IMPLICIT NONE
      INCLUDE 'DIFFSIZES.inc'
C  Hint: nbdirsmax should be the maximum number of differentiation directions
C
C  -- Reference BLAS level1 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      INTEGER incx, incy, n
C     ..
C     .. Array Arguments ..
      COMPLEX*16 zx(*), zy(*)
      COMPLEX*16 zxd(nbdirsmax, *), zyd(nbdirsmax, *)
C     ..
C
C  =====================================================================
C
C     .. Local Scalars ..
      COMPLEX*16 ztemp
      COMPLEX*16 ztempd(nbdirsmax)
      INTEGER i, ix, iy
C     ..
C     .. Intrinsic Functions ..
      INTRINSIC DCONJG
      INTEGER nd
      DOUBLE COMPLEX temp
      COMPLEX*16 zdotc
      COMPLEX*16 zdotcd(nbdirsmax)
      INTEGER nbdirs
C     ..
C     Check 0 < nbdirs <= nbdirsmax (required by DIFFSIZES.inc)
      IF (nbdirs.LE.0 .OR. nbdirs.GT.nbdirsmax) THEN
        WRITE(*,'(A,I0,A,I0,A)') 'Error: nbdirs=', nbdirs,
     +  ' must be in 1..nbdirsmax=', nbdirsmax, '. Stopping.'
        STOP 1
      END IF
C
      ztemp = (0.0d0,0.0d0)
      zdotc = (0.0d0,0.0d0)
      IF (n .LE. 0) THEN
        DO nd=1,nbdirsmax
          zdotcd(nd) = (0.0,0.0)
        ENDDO
        RETURN
      ELSE
        IF (incx .EQ. 1 .AND. incy .EQ. 1) THEN
          DO nd=1,nbdirsmax
            ztempd(nd) = (0.0,0.0)
          ENDDO
C
C        code for both increments equal to 1
C
          DO i=1,n
            temp = DCONJG(zx(i))
            DO nd=1,nbdirs
              ztempd(nd) = ztempd(nd) + zy(i)*DCONJG(zxd(nd, i)) + temp*
     +          zyd(nd, i)
            ENDDO
            ztemp = ztemp + temp*zy(i)
          ENDDO
        ELSE
C
C        code for unequal increments or equal increments
C          not equal to 1
C
          ix = 1
          iy = 1
          IF (incx .LT. 0) ix = (-n+1)*incx + 1
          IF (incy .LT. 0) THEN
            iy = (-n+1)*incy + 1
            DO nd=1,nbdirsmax
              ztempd(nd) = (0.0,0.0)
            ENDDO
          ELSE
            DO nd=1,nbdirsmax
              ztempd(nd) = (0.0,0.0)
            ENDDO
          END IF
          DO i=1,n
            temp = DCONJG(zx(ix))
            DO nd=1,nbdirs
              ztempd(nd) = ztempd(nd) + zy(iy)*DCONJG(zxd(nd, ix)) + 
     +          temp*zyd(nd, iy)
            ENDDO
            ztemp = ztemp + temp*zy(iy)
            ix = ix + incx
            iy = iy + incy
          ENDDO
        END IF
        DO nd=1,nbdirs
          zdotcd(nd) = ztempd(nd)
        ENDDO
        zdotc = ztemp
        RETURN
C
C     End of ZDOTC
C
      END IF
      END

