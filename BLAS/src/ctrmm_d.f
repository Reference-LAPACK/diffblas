C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of ctrmm in forward (tangent) mode:
C   variations   of useful results: b
C   with respect to varying inputs: alpha a b
C   RW status of diff variables: alpha:in a:in b:in-out
C> \brief \b CTRMM
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       SUBROUTINE CTRMM(SIDE,UPLO,TRANSA,DIAG,M,N,ALPHA,A,LDA,B,LDB)
C
C       .. Scalar Arguments ..
C       COMPLEX ALPHA
C       INTEGER LDA,LDB,M,N
C       CHARACTER DIAG,SIDE,TRANSA,UPLO
C       ..
C       .. Array Arguments ..
C       COMPLEX A(LDA,*),B(LDB,*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C> CTRMM  performs one of the matrix-matrix operations
C>
C>    B := alpha*op( A )*B,   or   B := alpha*B*op( A )
C>
C> where  alpha  is a scalar,  B  is an m by n matrix,  A  is a unit, or
C> non-unit,  upper or lower triangular matrix  and  op( A )  is one  of
C>
C>    op( A ) = A   or   op( A ) = A**T   or   op( A ) = A**H.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] SIDE
C> \verbatim
C>          SIDE is CHARACTER*1
C>           On entry,  SIDE specifies whether  op( A ) multiplies B from
C>           the left or right as follows:
C>
C>              SIDE = 'L' or 'l'   B := alpha*op( A )*B.
C>
C>              SIDE = 'R' or 'r'   B := alpha*B*op( A ).
C> \endverbatim
C>
C> \param[in] UPLO
C> \verbatim
C>          UPLO is CHARACTER*1
C>           On entry, UPLO specifies whether the matrix A is an upper or
C>           lower triangular matrix as follows:
C>
C>              UPLO = 'U' or 'u'   A is an upper triangular matrix.
C>
C>              UPLO = 'L' or 'l'   A is a lower triangular matrix.
C> \endverbatim
C>
C> \param[in] TRANSA
C> \verbatim
C>          TRANSA is CHARACTER*1
C>           On entry, TRANSA specifies the form of op( A ) to be used in
C>           the matrix multiplication as follows:
C>
C>              TRANSA = 'N' or 'n'   op( A ) = A.
C>
C>              TRANSA = 'T' or 't'   op( A ) = A**T.
C>
C>              TRANSA = 'C' or 'c'   op( A ) = A**H.
C> \endverbatim
C>
C> \param[in] DIAG
C> \verbatim
C>          DIAG is CHARACTER*1
C>           On entry, DIAG specifies whether or not A is unit triangular
C>           as follows:
C>
C>              DIAG = 'U' or 'u'   A is assumed to be unit triangular.
C>
C>              DIAG = 'N' or 'n'   A is not assumed to be unit
C>                                  triangular.
C> \endverbatim
C>
C> \param[in] M
C> \verbatim
C>          M is INTEGER
C>           On entry, M specifies the number of rows of B. M must be at
C>           least zero.
C> \endverbatim
C>
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>           On entry, N specifies the number of columns of B.  N must be
C>           at least zero.
C> \endverbatim
C>
C> \param[in] ALPHA
C> \verbatim
C>          ALPHA is COMPLEX
C>           On entry,  ALPHA specifies the scalar  alpha. When  alpha is
C>           zero then  A is not referenced and  B need not be set before
C>           entry.
C> \endverbatim
C>
C> \param[in] A
C> \verbatim
C>          A is COMPLEX array, dimension ( LDA, k ), where k is m
C>           when  SIDE = 'L' or 'l'  and is  n  when  SIDE = 'R' or 'r'.
C>           Before entry  with  UPLO = 'U' or 'u',  the  leading  k by k
C>           upper triangular part of the array  A must contain the upper
C>           triangular matrix  and the strictly lower triangular part of
C>           A is not referenced.
C>           Before entry  with  UPLO = 'L' or 'l',  the  leading  k by k
C>           lower triangular part of the array  A must contain the lower
C>           triangular matrix  and the strictly upper triangular part of
C>           A is not referenced.
C>           Note that when  DIAG = 'U' or 'u',  the diagonal elements of
C>           A  are not referenced either,  but are assumed to be  unity.
C> \endverbatim
C>
C> \param[in] LDA
C> \verbatim
C>          LDA is INTEGER
C>           On entry, LDA specifies the first dimension of A as declared
C>           in the calling (sub) program.  When  SIDE = 'L' or 'l'  then
C>           LDA  must be at least  max( 1, m ),  when  SIDE = 'R' or 'r'
C>           then LDA must be at least max( 1, n ).
C> \endverbatim
C>
C> \param[in,out] B
C> \verbatim
C>          B is COMPLEX array, dimension ( LDB, N ).
C>           Before entry,  the leading  m by n part of the array  B must
C>           contain the matrix  B,  and  on exit  is overwritten  by the
C>           transformed matrix.
C> \endverbatim
C>
C> \param[in] LDB
C> \verbatim
C>          LDB is INTEGER
C>           On entry, LDB specifies the first dimension of B as declared
C>           in  the  calling  (sub)  program.   LDB  must  be  at  least
C>           max( 1, m ).
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup trmm
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>  Level 3 Blas routine.
C>
C>  -- Written on 8-February-1989.
C>     Jack Dongarra, Argonne National Laboratory.
C>     Iain Duff, AERE Harwell.
C>     Jeremy Du Croz, Numerical Algorithms Group Ltd.
C>     Sven Hammarling, Numerical Algorithms Group Ltd.
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE CTRMM_D(side, uplo, transa, diag, m, n, alpha, alphad, 
     +                   a, ad, lda, b, bd, ldb)
      IMPLICIT NONE
C
C  -- Reference BLAS level3 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      COMPLEX alpha
      COMPLEX alphad
      INTEGER lda, ldb, m, n
      CHARACTER diag, side, transa, uplo
C     ..
C     .. Array Arguments ..
      COMPLEX a(lda, *), b(ldb, *)
      COMPLEX ad(lda, *), bd(ldb, *)
      EXTERNAL LSAME
C     ..
C
C  =====================================================================
C
C     .. External Functions ..
      LOGICAL LSAME
C     ..
C     .. External Subroutines ..
      EXTERNAL XERBLA
C     ..
C     .. Intrinsic Functions ..
      INTRINSIC CONJG, MAX
C     ..
C     .. Local Scalars ..
      COMPLEX temp
      COMPLEX tempd
      INTEGER i, info, j, k, nrowa
      LOGICAL lside, noconj, nounit, upper
C     ..
C     .. Parameters ..
      COMPLEX one
      PARAMETER (one=(1.0e+0,0.0e+0))
      COMPLEX zero
      PARAMETER (zero=(0.0e+0,0.0e+0))
      INTEGER max1
      INTEGER max2
      COMPLEX temp0
C     ..
C
C     Test the input parameters.
C
      lside = LSAME(side, 'L')
      IF (lside) THEN
        nrowa = m
      ELSE
        nrowa = n
      END IF
      noconj = LSAME(transa, 'T')
      nounit = LSAME(diag, 'N')
      upper = LSAME(uplo, 'U')
C
      info = 0
      IF (.NOT.lside .AND. (.NOT.LSAME(side, 'R'))) THEN
        info = 1
      ELSE IF (.NOT.upper .AND. (.NOT.LSAME(uplo, 'L'))) THEN
        info = 2
      ELSE IF (.NOT.LSAME(transa, 'N') .AND. (.NOT.LSAME(transa, 'T')) 
     +    .AND. (.NOT.LSAME(transa, 'C'))) THEN
        info = 3
      ELSE IF (.NOT.LSAME(diag, 'U') .AND. (.NOT.LSAME(diag, 'N'))) THEN
        info = 4
      ELSE IF (m .LT. 0) THEN
        info = 5
      ELSE IF (n .LT. 0) THEN
        info = 6
      ELSE
        IF (1 .LT. nrowa) THEN
          max1 = nrowa
        ELSE
          max1 = 1
        END IF
        IF (lda .LT. max1) THEN
          info = 9
        ELSE
          IF (1 .LT. m) THEN
            max2 = m
          ELSE
            max2 = 1
          END IF
          IF (ldb .LT. max2) info = 11
        END IF
      END IF
      IF (info .NE. 0) THEN
        CALL XERBLA('CTRMM ', info)
        RETURN
      ELSE IF (m .EQ. 0 .OR. n .EQ. 0) THEN
C
C     Quick return if possible.
C
        RETURN
      ELSE IF (alpha .EQ. zero) THEN
C
C     And when  alpha.eq.zero.
C
        DO j=1,n
          DO i=1,m
            bd(i, j) = (0.0,0.0)
            b(i, j) = zero
          ENDDO
        ENDDO
        RETURN
      ELSE
C
C     Start the operations.
C
        IF (lside) THEN
          IF (LSAME(transa, 'N')) THEN
C
C           Form  B := alpha*A*B.
C
            IF (upper) THEN
              DO j=1,n
                DO k=1,m
                  IF (b(k, j) .NE. zero) THEN
                    tempd = b(k, j)*alphad + alpha*bd(k, j)
                    temp = alpha*b(k, j)
                    DO i=1,k-1
                      bd(i, j) = bd(i, j) + a(i, k)*tempd + temp*ad(i, k
     +                  )
                      b(i, j) = b(i, j) + temp*a(i, k)
                    ENDDO
                    IF (nounit) THEN
                      tempd = a(k, k)*tempd + temp*ad(k, k)
                      temp = temp*a(k, k)
                    END IF
                    bd(k, j) = tempd
                    b(k, j) = temp
                  END IF
                ENDDO
              ENDDO
            ELSE
              DO j=1,n
                DO k=m,1,-1
                  IF (b(k, j) .NE. zero) THEN
                    tempd = b(k, j)*alphad + alpha*bd(k, j)
                    temp = alpha*b(k, j)
                    bd(k, j) = tempd
                    b(k, j) = temp
                    IF (nounit) THEN
                      bd(k, j) = a(k, k)*bd(k, j) + b(k, j)*ad(k, k)
                      b(k, j) = b(k, j)*a(k, k)
                    END IF
                    DO i=k+1,m
                      bd(i, j) = bd(i, j) + a(i, k)*tempd + temp*ad(i, k
     +                  )
                      b(i, j) = b(i, j) + temp*a(i, k)
                    ENDDO
                  END IF
                ENDDO
              ENDDO
            END IF
          ELSE IF (upper) THEN
C
C           Form  B := alpha*A**T*B   or   B := alpha*A**H*B.
C
            DO j=1,n
              DO i=m,1,-1
                tempd = bd(i, j)
                temp = b(i, j)
                IF (noconj) THEN
                  IF (nounit) THEN
                    tempd = a(i, i)*tempd + temp*ad(i, i)
                    temp = temp*a(i, i)
                  END IF
                  DO k=1,i-1
                    tempd = tempd + b(k, j)*ad(k, i) + a(k, i)*bd(k, j)
                    temp = temp + a(k, i)*b(k, j)
                  ENDDO
                ELSE
                  IF (nounit) THEN
                    temp0 = CONJG(a(i, i))
                    tempd = temp0*tempd + temp*CONJG(ad(i, i))
                    temp = temp*temp0
                  END IF
                  DO k=1,i-1
                    temp0 = CONJG(a(k, i))
                    tempd = tempd + b(k, j)*CONJG(ad(k, i)) + temp0*bd(k
     +                , j)
                    temp = temp + temp0*b(k, j)
                  ENDDO
                END IF
                bd(i, j) = temp*alphad + alpha*tempd
                b(i, j) = alpha*temp
              ENDDO
            ENDDO
          ELSE
            DO j=1,n
              DO i=1,m
                tempd = bd(i, j)
                temp = b(i, j)
                IF (noconj) THEN
                  IF (nounit) THEN
                    tempd = a(i, i)*tempd + temp*ad(i, i)
                    temp = temp*a(i, i)
                  END IF
                  DO k=i+1,m
                    tempd = tempd + b(k, j)*ad(k, i) + a(k, i)*bd(k, j)
                    temp = temp + a(k, i)*b(k, j)
                  ENDDO
                ELSE
                  IF (nounit) THEN
                    temp0 = CONJG(a(i, i))
                    tempd = temp0*tempd + temp*CONJG(ad(i, i))
                    temp = temp*temp0
                  END IF
                  DO k=i+1,m
                    temp0 = CONJG(a(k, i))
                    tempd = tempd + b(k, j)*CONJG(ad(k, i)) + temp0*bd(k
     +                , j)
                    temp = temp + temp0*b(k, j)
                  ENDDO
                END IF
                bd(i, j) = temp*alphad + alpha*tempd
                b(i, j) = alpha*temp
              ENDDO
            ENDDO
          END IF
        ELSE IF (LSAME(transa, 'N')) THEN
C
C           Form  B := alpha*B*A.
C
          IF (upper) THEN
            DO j=n,1,-1
              tempd = alphad
              temp = alpha
              IF (nounit) THEN
                tempd = a(j, j)*tempd + temp*ad(j, j)
                temp = temp*a(j, j)
              END IF
              DO i=1,m
                bd(i, j) = b(i, j)*tempd + temp*bd(i, j)
                b(i, j) = temp*b(i, j)
              ENDDO
              DO k=1,j-1
                IF (a(k, j) .NE. zero) THEN
                  tempd = a(k, j)*alphad + alpha*ad(k, j)
                  temp = alpha*a(k, j)
                  DO i=1,m
                    bd(i, j) = bd(i, j) + b(i, k)*tempd + temp*bd(i, k)
                    b(i, j) = b(i, j) + temp*b(i, k)
                  ENDDO
                END IF
              ENDDO
            ENDDO
          ELSE
            DO j=1,n
              tempd = alphad
              temp = alpha
              IF (nounit) THEN
                tempd = a(j, j)*tempd + temp*ad(j, j)
                temp = temp*a(j, j)
              END IF
              DO i=1,m
                bd(i, j) = b(i, j)*tempd + temp*bd(i, j)
                b(i, j) = temp*b(i, j)
              ENDDO
              DO k=j+1,n
                IF (a(k, j) .NE. zero) THEN
                  tempd = a(k, j)*alphad + alpha*ad(k, j)
                  temp = alpha*a(k, j)
                  DO i=1,m
                    bd(i, j) = bd(i, j) + b(i, k)*tempd + temp*bd(i, k)
                    b(i, j) = b(i, j) + temp*b(i, k)
                  ENDDO
                END IF
              ENDDO
            ENDDO
          END IF
        ELSE IF (upper) THEN
C
C           Form  B := alpha*B*A**T   or   B := alpha*B*A**H.
C
          DO k=1,n
            DO j=1,k-1
              IF (a(j, k) .NE. zero) THEN
                IF (noconj) THEN
                  tempd = a(j, k)*alphad + alpha*ad(j, k)
                  temp = alpha*a(j, k)
                ELSE
                  temp0 = CONJG(a(j, k))
                  tempd = temp0*alphad + alpha*CONJG(ad(j, k))
                  temp = alpha*temp0
                END IF
                DO i=1,m
                  bd(i, j) = bd(i, j) + b(i, k)*tempd + temp*bd(i, k)
                  b(i, j) = b(i, j) + temp*b(i, k)
                ENDDO
              END IF
            ENDDO
            tempd = alphad
            temp = alpha
            IF (nounit) THEN
              IF (noconj) THEN
                tempd = a(k, k)*tempd + temp*ad(k, k)
                temp = temp*a(k, k)
              ELSE
                temp0 = CONJG(a(k, k))
                tempd = temp0*tempd + temp*CONJG(ad(k, k))
                temp = temp*temp0
              END IF
            END IF
            IF (temp .NE. one) THEN
              DO i=1,m
                bd(i, k) = b(i, k)*tempd + temp*bd(i, k)
                b(i, k) = temp*b(i, k)
              ENDDO
            END IF
          ENDDO
        ELSE
          DO k=n,1,-1
            DO j=k+1,n
              IF (a(j, k) .NE. zero) THEN
                IF (noconj) THEN
                  tempd = a(j, k)*alphad + alpha*ad(j, k)
                  temp = alpha*a(j, k)
                ELSE
                  temp0 = CONJG(a(j, k))
                  tempd = temp0*alphad + alpha*CONJG(ad(j, k))
                  temp = alpha*temp0
                END IF
                DO i=1,m
                  bd(i, j) = bd(i, j) + b(i, k)*tempd + temp*bd(i, k)
                  b(i, j) = b(i, j) + temp*b(i, k)
                ENDDO
              END IF
            ENDDO
            tempd = alphad
            temp = alpha
            IF (nounit) THEN
              IF (noconj) THEN
                tempd = a(k, k)*tempd + temp*ad(k, k)
                temp = temp*a(k, k)
              ELSE
                temp0 = CONJG(a(k, k))
                tempd = temp0*tempd + temp*CONJG(ad(k, k))
                temp = temp*temp0
              END IF
            END IF
            IF (temp .NE. one) THEN
              DO i=1,m
                bd(i, k) = b(i, k)*tempd + temp*bd(i, k)
                b(i, k) = temp*b(i, k)
              ENDDO
            END IF
          ENDDO
        END IF
C
        RETURN
C
C     End of CTRMM
C
      END IF
      END

