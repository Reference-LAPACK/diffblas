C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of ddot in forward (tangent) mode:
C   variations   of useful results: ddot
C   with respect to varying inputs: dx dy
C   RW status of diff variables: dx:in dy:in ddot:out
C> \brief \b DDOT
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       DOUBLE PRECISION FUNCTION DDOT(N,DX,INCX,DY,INCY)
C
C       .. Scalar Arguments ..
C       INTEGER INCX,INCY,N
C       ..
C       .. Array Arguments ..
C       DOUBLE PRECISION DX(*),DY(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C>    DDOT forms the dot product of two vectors.
C>    uses unrolled loops for increments equal to one.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>         number of elements in input vector(s)
C> \endverbatim
C>
C> \param[in] DX
C> \verbatim
C>          DX is DOUBLE PRECISION array, dimension ( 1 + ( N - 1 )*abs( INCX ) )
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>         storage spacing between elements of DX
C> \endverbatim
C>
C> \param[in] DY
C> \verbatim
C>          DY is DOUBLE PRECISION array, dimension ( 1 + ( N - 1 )*abs( INCY ) )
C> \endverbatim
C>
C> \param[in] INCY
C> \verbatim
C>          INCY is INTEGER
C>         storage spacing between elements of DY
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup dot
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>     jack dongarra, linpack, 3/11/78.
C>     modified 12/3/93, array(1) declarations changed to array(*)
C> \endverbatim
C>
C  =====================================================================
      DOUBLE PRECISION FUNCTION DDOT_D(n, dx, dxd, incx, dy, dyd, incy, 
     +                                 ddot)
      IMPLICIT NONE
C
C  -- Reference BLAS level1 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      INTEGER incx, incy, n
C     ..
C     .. Array Arguments ..
      DOUBLE PRECISION dx(*), dy(*)
      DOUBLE PRECISION dxd(*), dyd(*)
C     ..
C
C  =====================================================================
C
C     .. Local Scalars ..
      DOUBLE PRECISION dtemp
      DOUBLE PRECISION dtempd
      INTEGER i, ix, iy, m, mp1
C     ..
C     .. Intrinsic Functions ..
      INTRINSIC MOD
      DOUBLE PRECISION ddot
C     ..
      ddot = 0.0d0
      dtemp = 0.0d0
      IF (n .LE. 0) THEN
        ddot_d = 0.D0
        RETURN
      ELSE
        IF (incx .EQ. 1 .AND. incy .EQ. 1) THEN
C
C        code for both increments equal to 1
C
C
C        clean-up loop
C
          m = MOD(n, 5)
          IF (m .NE. 0) THEN
            dtempd = 0.D0
            DO i=1,m
              dtempd = dtempd + dy(i)*dxd(i) + dx(i)*dyd(i)
              dtemp = dtemp + dx(i)*dy(i)
            ENDDO
            IF (n .LT. 5) THEN
              ddot_d = dtempd
              ddot = dtemp
              RETURN
            END IF
          ELSE
            dtempd = 0.D0
          END IF
          mp1 = m + 1
          DO i=mp1,n,5
            dtempd = dtempd + dy(i)*dxd(i) + dx(i)*dyd(i) + dy(i+1)*dxd(
     +        i+1) + dx(i+1)*dyd(i+1) + dy(i+2)*dxd(i+2) + dx(i+2)*dyd(i
     +        +2) + dy(i+3)*dxd(i+3) + dx(i+3)*dyd(i+3) + dy(i+4)*dxd(i+
     +        4) + dx(i+4)*dyd(i+4)
            dtemp = dtemp + dx(i)*dy(i) + dx(i+1)*dy(i+1) + dx(i+2)*dy(i
     +        +2) + dx(i+3)*dy(i+3) + dx(i+4)*dy(i+4)
          ENDDO
        ELSE
C
C        code for unequal increments or equal increments
C          not equal to 1
C
          ix = 1
          iy = 1
          IF (incx .LT. 0) ix = (-n+1)*incx + 1
          IF (incy .LT. 0) THEN
            iy = (-n+1)*incy + 1
            dtempd = 0.D0
          ELSE
            dtempd = 0.D0
          END IF
          DO i=1,n
            dtempd = dtempd + dy(iy)*dxd(ix) + dx(ix)*dyd(iy)
            dtemp = dtemp + dx(ix)*dy(iy)
            ix = ix + incx
            iy = iy + incy
          ENDDO
        END IF
        ddot_d = dtempd
        ddot = dtemp
        RETURN
C
C     End of DDOT
C
      END IF
      END

