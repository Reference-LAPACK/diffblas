C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of cdotc in forward (tangent) mode (with options multiDirectional):
C   variations   of useful results: cdotc
C   with respect to varying inputs: cx cy
C   RW status of diff variables: cdotc:out cx:in cy:in
C> \brief \b CDOTC
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       COMPLEX FUNCTION CDOTC(N,CX,INCX,CY,INCY)
C
C       .. Scalar Arguments ..
C       INTEGER INCX,INCY,N
C       ..
C       .. Array Arguments ..
C       COMPLEX CX(*),CY(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C> CDOTC forms the dot product of two complex vectors
C>      CDOTC = X^H * Y
C>
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>         number of elements in input vector(s)
C> \endverbatim
C>
C> \param[in] CX
C> \verbatim
C>          CX is COMPLEX array, dimension ( 1 + ( N - 1 )*abs( INCX ) )
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>         storage spacing between elements of CX
C> \endverbatim
C>
C> \param[in] CY
C> \verbatim
C>          CY is COMPLEX array, dimension ( 1 + ( N - 1 )*abs( INCY ) )
C> \endverbatim
C>
C> \param[in] INCY
C> \verbatim
C>          INCY is INTEGER
C>         storage spacing between elements of CY
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup dot
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>     jack dongarra, linpack,  3/11/78.
C>     modified 12/3/93, array(1) declarations changed to array(*)
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE CDOTC_DV(n, cx, cxd, incx, cy, cyd, incy, cdotc, cdotcd
     +                    , nbdirs)
      IMPLICIT NONE
      INCLUDE 'DIFFSIZES.inc'
C  Hint: nbdirsmax should be the maximum number of differentiation directions
C
C  -- Reference BLAS level1 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      INTEGER incx, incy, n
C     ..
C     .. Array Arguments ..
      COMPLEX cx(*), cy(*)
      COMPLEX cxd(nbdirsmax, *), cyd(nbdirsmax, *)
C     ..
C
C  =====================================================================
C
C     .. Local Scalars ..
      COMPLEX ctemp
      COMPLEX ctempd(nbdirsmax)
      INTEGER i, ix, iy
C     ..
C     .. Intrinsic Functions ..
      INTRINSIC CONJG
      INTEGER nd
      COMPLEX temp
      COMPLEX cdotc
      COMPLEX cdotcd(nbdirsmax)
      INTEGER nbdirs
C     ..
C     Check 0 < nbdirs <= nbdirsmax (required by DIFFSIZES.inc)
      IF (nbdirs.LE.0 .OR. nbdirs.GT.nbdirsmax) THEN
        WRITE(*,'(A,I0,A,I0,A)') 'Error: nbdirs=', nbdirs,
     +  ' must be in 1..nbdirsmax=', nbdirsmax, '. Stopping.'
        STOP 1
      END IF
C
      ctemp = (0.0,0.0)
      cdotc = (0.0,0.0)
      IF (n .LE. 0) THEN
        DO nd=1,nbdirsmax
          cdotcd(nd) = (0.0,0.0)
        ENDDO
        RETURN
      ELSE
        IF (incx .EQ. 1 .AND. incy .EQ. 1) THEN
          DO nd=1,nbdirsmax
            ctempd(nd) = (0.0,0.0)
          ENDDO
C
C        code for both increments equal to 1
C
          DO i=1,n
            temp = CONJG(cx(i))
            DO nd=1,nbdirs
              ctempd(nd) = ctempd(nd) + cy(i)*CONJG(cxd(nd, i)) + temp*
     +          cyd(nd, i)
            ENDDO
            ctemp = ctemp + temp*cy(i)
          ENDDO
        ELSE
C
C        code for unequal increments or equal increments
C          not equal to 1
C
          ix = 1
          iy = 1
          IF (incx .LT. 0) ix = (-n+1)*incx + 1
          IF (incy .LT. 0) THEN
            iy = (-n+1)*incy + 1
            DO nd=1,nbdirsmax
              ctempd(nd) = (0.0,0.0)
            ENDDO
          ELSE
            DO nd=1,nbdirsmax
              ctempd(nd) = (0.0,0.0)
            ENDDO
          END IF
          DO i=1,n
            temp = CONJG(cx(ix))
            DO nd=1,nbdirs
              ctempd(nd) = ctempd(nd) + cy(iy)*CONJG(cxd(nd, ix)) + temp
     +          *cyd(nd, iy)
            ENDDO
            ctemp = ctemp + temp*cy(iy)
            ix = ix + incx
            iy = iy + incy
          ENDDO
        END IF
        DO nd=1,nbdirs
          cdotcd(nd) = ctempd(nd)
        ENDDO
        cdotc = ctemp
        RETURN
C
C     End of CDOTC
C
      END IF
      END

