C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of ddot in reverse (adjoint) mode:
C   gradient     of useful results: ddot
C   with respect to varying inputs: dx dy
C   RW status of diff variables: dx:out dy:out ddot:in-killed
C> \brief \b DDOT
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       DOUBLE PRECISION FUNCTION DDOT(N,DX,INCX,DY,INCY)
C
C       .. Scalar Arguments ..
C       INTEGER INCX,INCY,N
C       ..
C       .. Array Arguments ..
C       DOUBLE PRECISION DX(*),DY(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C>    DDOT forms the dot product of two vectors.
C>    uses unrolled loops for increments equal to one.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>         number of elements in input vector(s)
C> \endverbatim
C>
C> \param[in] DX
C> \verbatim
C>          DX is DOUBLE PRECISION array, dimension ( 1 + ( N - 1 )*abs( INCX ) )
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>         storage spacing between elements of DX
C> \endverbatim
C>
C> \param[in] DY
C> \verbatim
C>          DY is DOUBLE PRECISION array, dimension ( 1 + ( N - 1 )*abs( INCY ) )
C> \endverbatim
C>
C> \param[in] INCY
C> \verbatim
C>          INCY is INTEGER
C>         storage spacing between elements of DY
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup dot
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>     jack dongarra, linpack, 3/11/78.
C>     modified 12/3/93, array(1) declarations changed to array(*)
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE DDOT_B(n, dx, dxb, incx, dy, dyb, incy, ddotb)
      IMPLICIT NONE
      INCLUDE 'DIFFSIZES.inc'
C  Hint: ISIZE1OFdy should be the size of dimension 1 of array dy
C  Hint: ISIZE1OFdx should be the size of dimension 1 of array dx
C
C  -- Reference BLAS level1 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      INTEGER incx, incy, n
C     ..
C     .. Array Arguments ..
      DOUBLE PRECISION dx(*), dy(*)
      DOUBLE PRECISION dxb(*), dyb(*)
C     ..
C
C  =====================================================================
C
C     .. Local Scalars ..
      DOUBLE PRECISION dtemp
      DOUBLE PRECISION dtempb
      INTEGER i, ix, iy, m, mp1
      INTEGER ISIZE1OFDx, ISIZE1OFDy
      INTEGER get_ISIZE1OFDx, get_ISIZE1OFDy
      EXTERNAL get_ISIZE1OFDx, get_ISIZE1OFDy
C     ..
C     .. Intrinsic Functions ..
      INTRINSIC MOD
      INTEGER ii1
      INTEGER*4 branch
      DOUBLE PRECISION ddot
      DOUBLE PRECISION ddotb
C     ..
      CALL check_ISIZE1OFDx_initialized()
      CALL check_ISIZE1OFDy_initialized()
      ISIZE1OFDx = get_ISIZE1OFDx()
      ISIZE1OFDy = get_ISIZE1OFDy()
      IF (n .LE. 0) THEN
        DO ii1=1,ISIZE1OFdx
          dxb(ii1) = 0.D0
        ENDDO
        DO ii1=1,ISIZE1OFdy
          dyb(ii1) = 0.D0
        ENDDO
      ELSE
        IF (incx .EQ. 1 .AND. incy .EQ. 1) THEN
C
C        code for both increments equal to 1
C
C
C        clean-up loop
C
          m = MOD(n, 5)
          IF (m .NE. 0) THEN
            IF (n .LT. 5) THEN
              dtempb = ddotb
              DO ii1=1,ISIZE1OFdx
                dxb(ii1) = 0.D0
              ENDDO
              DO ii1=1,ISIZE1OFdy
                dyb(ii1) = 0.D0
              ENDDO
              GOTO 100
            ELSE
              CALL PUSHCONTROL1B(0)
            END IF
          ELSE
            CALL PUSHCONTROL1B(1)
          END IF
          mp1 = m + 1
          CALL PUSHCONTROL1B(0)
        ELSE
C
C        code for unequal increments or equal increments
C          not equal to 1
C
          ix = 1
          iy = 1
          IF (incx .LT. 0) ix = (-n+1)*incx + 1
          IF (incy .LT. 0) iy = (-n+1)*incy + 1
          DO i=1,n
            CALL PUSHINTEGER4(ix)
            ix = ix + incx
            CALL PUSHINTEGER4(iy)
            iy = iy + incy
          ENDDO
          CALL PUSHCONTROL1B(1)
        END IF
        dtempb = ddotb
        CALL POPCONTROL1B(branch)
        IF (branch .EQ. 0) THEN
          DO ii1=1,ISIZE1OFdx
            dxb(ii1) = 0.D0
          ENDDO
          DO ii1=1,ISIZE1OFdy
            dyb(ii1) = 0.D0
          ENDDO
          DO i=n-MOD(n-mp1, 5),mp1,-5
            dxb(i) = dxb(i) + dy(i)*dtempb
            dyb(i) = dyb(i) + dx(i)*dtempb
            dxb(i+1) = dxb(i+1) + dy(i+1)*dtempb
            dyb(i+1) = dyb(i+1) + dx(i+1)*dtempb
            dxb(i+2) = dxb(i+2) + dy(i+2)*dtempb
            dyb(i+2) = dyb(i+2) + dx(i+2)*dtempb
            dxb(i+3) = dxb(i+3) + dy(i+3)*dtempb
            dyb(i+3) = dyb(i+3) + dx(i+3)*dtempb
            dxb(i+4) = dxb(i+4) + dy(i+4)*dtempb
            dyb(i+4) = dyb(i+4) + dx(i+4)*dtempb
          ENDDO
          CALL POPCONTROL1B(branch)
          IF (branch .NE. 0) GOTO 110
        ELSE
          DO ii1=1,ISIZE1OFdx
            dxb(ii1) = 0.D0
          ENDDO
          DO ii1=1,ISIZE1OFdy
            dyb(ii1) = 0.D0
          ENDDO
          DO i=n,1,-1
            CALL POPINTEGER4(iy)
            CALL POPINTEGER4(ix)
            dxb(ix) = dxb(ix) + dy(iy)*dtempb
            dyb(iy) = dyb(iy) + dx(ix)*dtempb
          ENDDO
          GOTO 110
        END IF
 100    DO i=m,1,-1
          dxb(i) = dxb(i) + dy(i)*dtempb
          dyb(i) = dyb(i) + dx(i)*dtempb
        ENDDO
      END IF
 110  CONTINUE
      END

