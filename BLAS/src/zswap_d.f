C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of zswap in forward (tangent) mode:
C   variations   of useful results: zx zy
C   with respect to varying inputs: zx zy
C   RW status of diff variables: zx:in-out zy:in-out
C> \brief \b ZSWAP
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       SUBROUTINE ZSWAP(N,ZX,INCX,ZY,INCY)
C
C       .. Scalar Arguments ..
C       INTEGER INCX,INCY,N
C       ..
C       .. Array Arguments ..
C       COMPLEX*16 ZX(*),ZY(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C>    ZSWAP interchanges two vectors.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>         number of elements in input vector(s)
C> \endverbatim
C>
C> \param[in,out] ZX
C> \verbatim
C>          ZX is COMPLEX*16 array, dimension ( 1 + ( N - 1 )*abs( INCX ) )
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>         storage spacing between elements of ZX
C> \endverbatim
C>
C> \param[in,out] ZY
C> \verbatim
C>          ZY is COMPLEX*16 array, dimension ( 1 + ( N - 1 )*abs( INCY ) )
C> \endverbatim
C>
C> \param[in] INCY
C> \verbatim
C>          INCY is INTEGER
C>         storage spacing between elements of ZY
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup swap
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>     jack dongarra, 3/11/78.
C>     modified 12/3/93, array(1) declarations changed to array(*)
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE ZSWAP_D(n, zx, zxd, incx, zy, zyd, incy)
      IMPLICIT NONE
C
C  -- Reference BLAS level1 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      INTEGER incx, incy, n
C     ..
C     .. Array Arguments ..
      COMPLEX*16 zx(*), zy(*)
      COMPLEX*16 zxd(*), zyd(*)
C     ..
C
C  =====================================================================
C
C     .. Local Scalars ..
      COMPLEX*16 ztemp
      COMPLEX*16 ztempd
      INTEGER i, ix, iy
C     ..
      IF (n .LE. 0) THEN
        RETURN
      ELSE
        IF (incx .EQ. 1 .AND. incy .EQ. 1) THEN
C
C       code for both increments equal to 1
          DO i=1,n
            ztempd = zxd(i)
            ztemp = zx(i)
            zxd(i) = zyd(i)
            zx(i) = zy(i)
            zyd(i) = ztempd
            zy(i) = ztemp
          ENDDO
        ELSE
C
C       code for unequal increments or equal increments not equal
C         to 1
C
          ix = 1
          iy = 1
          IF (incx .LT. 0) ix = (-n+1)*incx + 1
          IF (incy .LT. 0) iy = (-n+1)*incy + 1
          DO i=1,n
            ztempd = zxd(ix)
            ztemp = zx(ix)
            zxd(ix) = zyd(iy)
            zx(ix) = zy(iy)
            zyd(iy) = ztempd
            zy(iy) = ztemp
            ix = ix + incx
            iy = iy + incy
          ENDDO
        END IF
        RETURN
C
C     End of ZSWAP
C
      END IF
      END

