C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of zdscal in reverse (adjoint) mode:
C   gradient     of useful results: zx
C   with respect to varying inputs: zx da
C   RW status of diff variables: zx:in-out da:out
C> \brief \b ZDSCAL
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       SUBROUTINE ZDSCAL(N,DA,ZX,INCX)
C
C       .. Scalar Arguments ..
C       DOUBLE PRECISION DA
C       INTEGER INCX,N
C       ..
C       .. Array Arguments ..
C       COMPLEX*16 ZX(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C>    ZDSCAL scales a vector by a constant.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>         number of elements in input vector(s)
C> \endverbatim
C>
C> \param[in] DA
C> \verbatim
C>          DA is DOUBLE PRECISION
C>           On entry, DA specifies the scalar alpha.
C> \endverbatim
C>
C> \param[in,out] ZX
C> \verbatim
C>          ZX is COMPLEX*16 array, dimension ( 1 + ( N - 1 )*abs( INCX ) )
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>         storage spacing between elements of ZX
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup scal
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>     jack dongarra, 3/11/78.
C>     modified 3/93 to return if incx .le. 0.
C>     modified 12/3/93, array(1) declarations changed to array(*)
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE ZDSCAL_B(n, da, dab, zx, zxb, incx)
      IMPLICIT NONE
C
C  -- Reference BLAS level1 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      DOUBLE PRECISION da
      DOUBLE PRECISION dab
      INTEGER incx, n
C     ..
C     .. Array Arguments ..
      COMPLEX*16 zx(*)
      COMPLEX*16 zxb(*)
C     ..
C
C  =====================================================================
C
C     .. Local Scalars ..
      INTEGER i, nincx
C     .. Parameters ..
      DOUBLE PRECISION one
      PARAMETER (one=1.0d+0)
C     ..
C     .. Intrinsic Functions ..
      INTRINSIC DBLE, DCMPLX, DIMAG
C     ..
      IF ((n .LE. 0 .OR. incx .LE. 0) .OR. da .EQ. one) THEN
        dab = 0.D0
      ELSE IF (incx .EQ. 1) THEN
        dab = 0.D0
        DO i=n,1,-1
          dab = dab + DBLE(zx(i))*DREAL(zxb(i)) + DIMAG(zx(i))*DIMAG(zxb
     +      (i))
          zxb(i) = da*DREAL(zxb(i)) + DCMPLX(0.D0, da*DIMAG(zxb(i)))
        ENDDO
      ELSE
C
C        code for increment not equal to 1
C
        nincx = n*incx
        dab = 0.D0
        DO i=nincx-MOD(nincx-1, incx),1,-incx
          dab = dab + DBLE(zx(i))*DREAL(zxb(i)) + DIMAG(zx(i))*DIMAG(zxb
     +      (i))
          zxb(i) = da*DREAL(zxb(i)) + DCMPLX(0.D0, da*DIMAG(zxb(i)))
        ENDDO
      END IF
      END

