C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of ctrmv in reverse (adjoint) mode (with options multiDirectional):
C   gradient     of useful results: x
C   with respect to varying inputs: x a
C   RW status of diff variables: x:in-out a:out
C> \brief \b CTRMV
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       SUBROUTINE CTRMV(UPLO,TRANS,DIAG,N,A,LDA,X,INCX)
C
C       .. Scalar Arguments ..
C       INTEGER INCX,LDA,N
C       CHARACTER DIAG,TRANS,UPLO
C       ..
C       .. Array Arguments ..
C       COMPLEX A(LDA,*),X(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C> CTRMV  performs one of the matrix-vector operations
C>
C>    x := A*x,   or   x := A**T*x,   or   x := A**H*x,
C>
C> where x is an n element vector and  A is an n by n unit, or non-unit,
C> upper or lower triangular matrix.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] UPLO
C> \verbatim
C>          UPLO is CHARACTER*1
C>           On entry, UPLO specifies whether the matrix is an upper or
C>           lower triangular matrix as follows:
C>
C>              UPLO = 'U' or 'u'   A is an upper triangular matrix.
C>
C>              UPLO = 'L' or 'l'   A is a lower triangular matrix.
C> \endverbatim
C>
C> \param[in] TRANS
C> \verbatim
C>          TRANS is CHARACTER*1
C>           On entry, TRANS specifies the operation to be performed as
C>           follows:
C>
C>              TRANS = 'N' or 'n'   x := A*x.
C>
C>              TRANS = 'T' or 't'   x := A**T*x.
C>
C>              TRANS = 'C' or 'c'   x := A**H*x.
C> \endverbatim
C>
C> \param[in] DIAG
C> \verbatim
C>          DIAG is CHARACTER*1
C>           On entry, DIAG specifies whether or not A is unit
C>           triangular as follows:
C>
C>              DIAG = 'U' or 'u'   A is assumed to be unit triangular.
C>
C>              DIAG = 'N' or 'n'   A is not assumed to be unit
C>                                  triangular.
C> \endverbatim
C>
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>           On entry, N specifies the order of the matrix A.
C>           N must be at least zero.
C> \endverbatim
C>
C> \param[in] A
C> \verbatim
C>          A is COMPLEX array, dimension ( LDA, N ).
C>           Before entry with  UPLO = 'U' or 'u', the leading n by n
C>           upper triangular part of the array A must contain the upper
C>           triangular matrix and the strictly lower triangular part of
C>           A is not referenced.
C>           Before entry with UPLO = 'L' or 'l', the leading n by n
C>           lower triangular part of the array A must contain the lower
C>           triangular matrix and the strictly upper triangular part of
C>           A is not referenced.
C>           Note that when  DIAG = 'U' or 'u', the diagonal elements of
C>           A are not referenced either, but are assumed to be unity.
C> \endverbatim
C>
C> \param[in] LDA
C> \verbatim
C>          LDA is INTEGER
C>           On entry, LDA specifies the first dimension of A as declared
C>           in the calling (sub) program. LDA must be at least
C>           max( 1, n ).
C> \endverbatim
C>
C> \param[in,out] X
C> \verbatim
C>          X is COMPLEX array, dimension at least
C>           ( 1 + ( n - 1 )*abs( INCX ) ).
C>           Before entry, the incremented array X must contain the n
C>           element vector x. On exit, X is overwritten with the
C>           transformed vector x.
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>           On entry, INCX specifies the increment for the elements of
C>           X. INCX must not be zero.
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup trmv
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>  Level 2 Blas routine.
C>  The vector and matrix arguments are not referenced when N = 0, or M = 0
C>
C>  -- Written on 22-October-1986.
C>     Jack Dongarra, Argonne National Lab.
C>     Jeremy Du Croz, Nag Central Office.
C>     Sven Hammarling, Nag Central Office.
C>     Richard Hanson, Sandia National Labs.
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE CTRMV_BV(uplo, trans, diag, n, a, ab, lda, x, xb, incx
     +                    , nbdirs)
      IMPLICIT NONE
      INCLUDE 'DIFFSIZES.inc'
C  Hint: ISIZE2OFa should be the size of dimension 2 of array a
C  Hint: nbdirsmax should be the maximum number of differentiation directions
C
C  -- Reference BLAS level2 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      INTEGER incx, lda, n
      CHARACTER diag, trans, uplo
C     ..
C     .. Array Arguments ..
      COMPLEX a(lda, *), x(*)
      COMPLEX ab(nbdirsmax, lda, *), xb(nbdirsmax, *)
C     ..
C
C  =====================================================================
C
C     .. Parameters ..
      COMPLEX zero
      PARAMETER (zero=(0.0e+0,0.0e+0))
C     ..
C     .. Local Scalars ..
      COMPLEX temp
      COMPLEX tempb(nbdirsmax)
      INTEGER i, info, ix, j, jx, kx
      LOGICAL noconj, nounit
      EXTERNAL LSAME
      INTEGER ISIZE2OFA
C     ..
C     .. External Functions ..
      INTEGER get_ISIZE2OFA
      EXTERNAL get_ISIZE2OFA
      LOGICAL LSAME
C     ..
C     .. External Subroutines ..
      EXTERNAL XERBLA, check_ISIZE2OFA_initialized
C     ..
C     .. Intrinsic Functions ..
      INTRINSIC CONJG, MAX
      INTEGER max1
      INTEGER nd
      INTEGER ad_to
      INTEGER*4 branch
      INTEGER ad_to0
      INTEGER ad_to1
      INTEGER ad_to2
      INTEGER ad_from
      INTEGER ad_from0
      INTEGER ad_from1
      INTEGER ad_from2
      INTEGER ad_from3
      INTEGER ad_from4
      INTEGER ad_from5
      INTEGER ad_from6
      INTEGER ii2
      INTEGER ii1
      INTEGER nbdirs
C     ..
C
C     Test the input parameters.
C
C     Check 0 < nbdirs <= nbdirsmax (required by DIFFSIZES.inc)
      CALL check_ISIZE2OFA_initialized()
      ISIZE2OFA = get_ISIZE2OFA()
      IF (nbdirs.LE.0 .OR. nbdirs.GT.nbdirsmax) THEN
        WRITE(*,'(A,I0,A,I0,A)') 'Error: nbdirs=', nbdirs,
     +  ' must be in 1..nbdirsmax=', nbdirsmax, '. Stopping.'
        STOP 1
      END IF
C
      info = 0
      IF (.NOT.LSAME(uplo, 'U') .AND. (.NOT.LSAME(uplo, 'L'))) THEN
        CALL PUSHCONTROL3B(0)
        info = 1
      ELSE IF (.NOT.LSAME(trans, 'N') .AND. (.NOT.LSAME(trans, 'T')) 
     +    .AND. (.NOT.LSAME(trans, 'C'))) THEN
        CALL PUSHCONTROL3B(1)
        info = 2
      ELSE IF (.NOT.LSAME(diag, 'U') .AND. (.NOT.LSAME(diag, 'N'))) THEN
        CALL PUSHCONTROL3B(2)
        info = 3
      ELSE IF (n .LT. 0) THEN
        CALL PUSHCONTROL3B(3)
        info = 4
      ELSE
        IF (1 .LT. n) THEN
          max1 = n
        ELSE
          max1 = 1
        END IF
        IF (lda .LT. max1) THEN
          CALL PUSHCONTROL3B(4)
          info = 6
        ELSE IF (incx .EQ. 0) THEN
          CALL PUSHCONTROL3B(5)
          info = 8
        ELSE
          CALL PUSHCONTROL3B(5)
        END IF
      END IF
      IF (info .EQ. 0) THEN
C
C     Quick return if possible.
C
        IF (n .EQ. 0) THEN
          DO ii1=1,ISIZE2OFa
            DO ii2=1,lda
              DO nd=1,nbdirsmax
                ab(nd, ii2, ii1) = (0.0,0.0)
              ENDDO
            ENDDO
          ENDDO
        ELSE
C
          noconj = LSAME(trans, 'T')
          nounit = LSAME(diag, 'N')
C
C     Set up the start point in X if the increment is not unity. This
C     will be  ( N - 1 )*INCX  too small for descending loops.
C
          IF (incx .LE. 0) THEN
            CALL PUSHCONTROL1B(0)
            kx = 1 - (n-1)*incx
          ELSE IF (incx .NE. 1) THEN
            CALL PUSHCONTROL1B(1)
            kx = 1
          ELSE
            CALL PUSHCONTROL1B(1)
          END IF
C
C     Start the operations. In this version the elements of A are
C     accessed sequentially with one pass through A.
C
          IF (LSAME(trans, 'N')) THEN
C
C        Form  x := A*x.
C
            IF (LSAME(uplo, 'U')) THEN
              IF (incx .EQ. 1) THEN
                DO j=1,n
                  IF (x(j) .NE. zero) THEN
                    CALL PUSHCOMPLEX8(temp)
                    temp = x(j)
                    DO i=1,j-1
                      CALL PUSHCOMPLEX8(x(i))
                      x(i) = x(i) + temp*a(i, j)
                    ENDDO
                    CALL PUSHINTEGER4(i - 1)
                    IF (nounit) THEN
                      CALL PUSHCOMPLEX8(x(j))
                      x(j) = x(j)*a(j, j)
                      CALL PUSHCONTROL2B(0)
                    ELSE
                      CALL PUSHCONTROL2B(1)
                    END IF
                  ELSE
                    CALL PUSHCONTROL2B(2)
                  END IF
                ENDDO
                DO ii1=1,ISIZE2OFa
                  DO ii2=1,lda
                    DO nd=1,nbdirsmax
                      ab(nd, ii2, ii1) = (0.0,0.0)
                    ENDDO
                  ENDDO
                ENDDO
                DO 100 j=n,1,-1
                  CALL POPCONTROL2B(branch)
                  IF (branch .EQ. 0) THEN
                    CALL POPCOMPLEX8(x(j))
                    DO nd=1,nbdirs
                      ab(nd, j, j) = ab(nd, j, j) + CONJG(x(j))*xb(nd, j
     +                  )
                      xb(nd, j) = CONJG(a(j, j))*xb(nd, j)
                    ENDDO
                  ELSE IF (branch .NE. 1) THEN
                    GOTO 100
                  END IF
                  DO nd=1,nbdirsmax
                    tempb(nd) = (0.0,0.0)
                  ENDDO
                  CALL POPINTEGER4(ad_to)
                  DO i=ad_to,1,-1
                    CALL POPCOMPLEX8(x(i))
                    DO nd=1,nbdirs
                      tempb(nd) = tempb(nd) + CONJG(a(i, j))*xb(nd, i)
                      ab(nd, i, j) = ab(nd, i, j) + CONJG(temp)*xb(nd, i
     +                  )
                    ENDDO
                  ENDDO
                  CALL POPCOMPLEX8(temp)
                  DO nd=1,nbdirs
                    xb(nd, j) = xb(nd, j) + tempb(nd)
                  ENDDO
 100            CONTINUE
              ELSE
                jx = kx
                DO j=1,n
                  IF (x(jx) .NE. zero) THEN
                    CALL PUSHCOMPLEX8(temp)
                    temp = x(jx)
                    ix = kx
                    DO i=1,j-1
                      CALL PUSHCOMPLEX8(x(ix))
                      x(ix) = x(ix) + temp*a(i, j)
                      CALL PUSHINTEGER4(ix)
                      ix = ix + incx
                    ENDDO
                    CALL PUSHINTEGER4(i - 1)
                    IF (nounit) THEN
                      CALL PUSHCOMPLEX8(x(jx))
                      x(jx) = x(jx)*a(j, j)
                      CALL PUSHCONTROL2B(0)
                    ELSE
                      CALL PUSHCONTROL2B(1)
                    END IF
                  ELSE
                    CALL PUSHCONTROL2B(2)
                  END IF
                  CALL PUSHINTEGER4(jx)
                  jx = jx + incx
                ENDDO
                DO ii1=1,ISIZE2OFa
                  DO ii2=1,lda
                    DO nd=1,nbdirsmax
                      ab(nd, ii2, ii1) = (0.0,0.0)
                    ENDDO
                  ENDDO
                ENDDO
                DO 110 j=n,1,-1
                  CALL POPINTEGER4(jx)
                  CALL POPCONTROL2B(branch)
                  IF (branch .EQ. 0) THEN
                    CALL POPCOMPLEX8(x(jx))
                    DO nd=1,nbdirs
                      ab(nd, j, j) = ab(nd, j, j) + CONJG(x(jx))*xb(nd, 
     +                  jx)
                      xb(nd, jx) = CONJG(a(j, j))*xb(nd, jx)
                    ENDDO
                  ELSE IF (branch .NE. 1) THEN
                    GOTO 110
                  END IF
                  DO nd=1,nbdirsmax
                    tempb(nd) = (0.0,0.0)
                  ENDDO
                  CALL POPINTEGER4(ad_to0)
                  DO i=ad_to0,1,-1
                    CALL POPINTEGER4(ix)
                    CALL POPCOMPLEX8(x(ix))
                    DO nd=1,nbdirs
                      tempb(nd) = tempb(nd) + CONJG(a(i, j))*xb(nd, ix)
                      ab(nd, i, j) = ab(nd, i, j) + CONJG(temp)*xb(nd, 
     +                  ix)
                    ENDDO
                  ENDDO
                  CALL POPCOMPLEX8(temp)
                  DO nd=1,nbdirs
                    xb(nd, jx) = xb(nd, jx) + tempb(nd)
                  ENDDO
 110            CONTINUE
              END IF
            ELSE IF (incx .EQ. 1) THEN
              DO j=n,1,-1
                IF (x(j) .NE. zero) THEN
                  CALL PUSHCOMPLEX8(temp)
                  temp = x(j)
                  DO i=n,j+1,-1
                    CALL PUSHCOMPLEX8(x(i))
                    x(i) = x(i) + temp*a(i, j)
                  ENDDO
                  CALL PUSHINTEGER4(i + 1)
                  IF (nounit) THEN
                    CALL PUSHCOMPLEX8(x(j))
                    x(j) = x(j)*a(j, j)
                    CALL PUSHCONTROL2B(0)
                  ELSE
                    CALL PUSHCONTROL2B(1)
                  END IF
                ELSE
                  CALL PUSHCONTROL2B(2)
                END IF
              ENDDO
              DO ii1=1,ISIZE2OFa
                DO ii2=1,lda
                  DO nd=1,nbdirsmax
                    ab(nd, ii2, ii1) = (0.0,0.0)
                  ENDDO
                ENDDO
              ENDDO
              DO 120 j=1,n,1
                CALL POPCONTROL2B(branch)
                IF (branch .EQ. 0) THEN
                  CALL POPCOMPLEX8(x(j))
                  DO nd=1,nbdirs
                    ab(nd, j, j) = ab(nd, j, j) + CONJG(x(j))*xb(nd, j)
                    xb(nd, j) = CONJG(a(j, j))*xb(nd, j)
                  ENDDO
                ELSE IF (branch .NE. 1) THEN
                  GOTO 120
                END IF
                DO nd=1,nbdirsmax
                  tempb(nd) = (0.0,0.0)
                ENDDO
                CALL POPINTEGER4(ad_to1)
                DO i=ad_to1,n,1
                  CALL POPCOMPLEX8(x(i))
                  DO nd=1,nbdirs
                    tempb(nd) = tempb(nd) + CONJG(a(i, j))*xb(nd, i)
                    ab(nd, i, j) = ab(nd, i, j) + CONJG(temp)*xb(nd, i)
                  ENDDO
                ENDDO
                CALL POPCOMPLEX8(temp)
                DO nd=1,nbdirs
                  xb(nd, j) = xb(nd, j) + tempb(nd)
                ENDDO
 120          CONTINUE
            ELSE
              kx = kx + (n-1)*incx
              jx = kx
              DO j=n,1,-1
                IF (x(jx) .NE. zero) THEN
                  CALL PUSHCOMPLEX8(temp)
                  temp = x(jx)
                  ix = kx
                  DO i=n,j+1,-1
                    CALL PUSHCOMPLEX8(x(ix))
                    x(ix) = x(ix) + temp*a(i, j)
                    CALL PUSHINTEGER4(ix)
                    ix = ix - incx
                  ENDDO
                  CALL PUSHINTEGER4(i + 1)
                  IF (nounit) THEN
                    CALL PUSHCOMPLEX8(x(jx))
                    x(jx) = x(jx)*a(j, j)
                    CALL PUSHCONTROL2B(0)
                  ELSE
                    CALL PUSHCONTROL2B(1)
                  END IF
                ELSE
                  CALL PUSHCONTROL2B(2)
                END IF
                CALL PUSHINTEGER4(jx)
                jx = jx - incx
              ENDDO
              DO ii1=1,ISIZE2OFa
                DO ii2=1,lda
                  DO nd=1,nbdirsmax
                    ab(nd, ii2, ii1) = (0.0,0.0)
                  ENDDO
                ENDDO
              ENDDO
              DO 130 j=1,n,1
                CALL POPINTEGER4(jx)
                CALL POPCONTROL2B(branch)
                IF (branch .EQ. 0) THEN
                  CALL POPCOMPLEX8(x(jx))
                  DO nd=1,nbdirs
                    ab(nd, j, j) = ab(nd, j, j) + CONJG(x(jx))*xb(nd, jx
     +                )
                    xb(nd, jx) = CONJG(a(j, j))*xb(nd, jx)
                  ENDDO
                ELSE IF (branch .NE. 1) THEN
                  GOTO 130
                END IF
                DO nd=1,nbdirsmax
                  tempb(nd) = (0.0,0.0)
                ENDDO
                CALL POPINTEGER4(ad_to2)
                DO i=ad_to2,n,1
                  CALL POPINTEGER4(ix)
                  CALL POPCOMPLEX8(x(ix))
                  DO nd=1,nbdirs
                    tempb(nd) = tempb(nd) + CONJG(a(i, j))*xb(nd, ix)
                    ab(nd, i, j) = ab(nd, i, j) + CONJG(temp)*xb(nd, ix)
                  ENDDO
                ENDDO
                CALL POPCOMPLEX8(temp)
                DO nd=1,nbdirs
                  xb(nd, jx) = xb(nd, jx) + tempb(nd)
                ENDDO
 130          CONTINUE
            END IF
          ELSE IF (LSAME(uplo, 'U')) THEN
C
C        Form  x := A**T*x  or  x := A**H*x.
C
            IF (incx .EQ. 1) THEN
              DO j=n,1,-1
                temp = x(j)
                IF (noconj) THEN
                  IF (nounit) THEN
                    CALL PUSHCOMPLEX8(temp)
                    temp = temp*a(j, j)
                    CALL PUSHCONTROL1B(1)
                  ELSE
                    CALL PUSHCONTROL1B(0)
                  END IF
                  ad_from = j - 1
                  DO i=ad_from,1,-1
                    temp = temp + a(i, j)*x(i)
                  ENDDO
                  CALL PUSHINTEGER4(ad_from)
                  CALL PUSHCONTROL1B(0)
                ELSE
                  IF (nounit) THEN
                    CALL PUSHCOMPLEX8(temp)
                    temp = temp*CONJG(a(j, j))
                    CALL PUSHCONTROL1B(1)
                  ELSE
                    CALL PUSHCONTROL1B(0)
                  END IF
                  ad_from0 = j - 1
                  DO i=ad_from0,1,-1
                    temp = temp + CONJG(a(i, j))*x(i)
                  ENDDO
                  CALL PUSHINTEGER4(ad_from0)
                  CALL PUSHCONTROL1B(1)
                END IF
                CALL PUSHCOMPLEX8(x(j))
                x(j) = temp
              ENDDO
              DO ii1=1,ISIZE2OFa
                DO ii2=1,lda
                  DO nd=1,nbdirsmax
                    ab(nd, ii2, ii1) = (0.0,0.0)
                  ENDDO
                ENDDO
              ENDDO
              DO j=1,n,1
                CALL POPCOMPLEX8(x(j))
                DO nd=1,nbdirs
                  tempb(nd) = xb(nd, j)
                  xb(nd, j) = (0.0,0.0)
                ENDDO
                CALL POPCONTROL1B(branch)
                IF (branch .EQ. 0) THEN
                  CALL POPINTEGER4(ad_from)
                  DO i=1,ad_from,1
                    DO nd=1,nbdirs
                      ab(nd, i, j) = ab(nd, i, j) + CONJG(x(i))*tempb(nd
     +                  )
                      xb(nd, i) = xb(nd, i) + CONJG(a(i, j))*tempb(nd)
                    ENDDO
                  ENDDO
                  CALL POPCONTROL1B(branch)
                  IF (branch .NE. 0) THEN
                    CALL POPCOMPLEX8(temp)
                    DO nd=1,nbdirs
                      ab(nd, j, j) = ab(nd, j, j) + CONJG(temp)*tempb(nd
     +                  )
                      tempb(nd) = CONJG(a(j, j))*tempb(nd)
                    ENDDO
                  END IF
                ELSE
                  CALL POPINTEGER4(ad_from0)
                  DO i=1,ad_from0,1
                    DO nd=1,nbdirs
                      ab(nd, i, j) = ab(nd, i, j) + CONJG(CONJG(x(i))*
     +                  tempb(nd))
                      xb(nd, i) = xb(nd, i) + CONJG(CONJG(a(i, j)))*
     +                  tempb(nd)
                    ENDDO
                  ENDDO
                  CALL POPCONTROL1B(branch)
                  IF (branch .NE. 0) THEN
                    CALL POPCOMPLEX8(temp)
                    DO nd=1,nbdirs
                      ab(nd, j, j) = ab(nd, j, j) + CONJG(CONJG(temp)*
     +                  tempb(nd))
                      tempb(nd) = CONJG(CONJG(a(j, j)))*tempb(nd)
                    ENDDO
                  END IF
                END IF
                DO nd=1,nbdirs
                  xb(nd, j) = xb(nd, j) + tempb(nd)
                ENDDO
              ENDDO
            ELSE
              jx = kx + (n-1)*incx
              DO j=n,1,-1
                temp = x(jx)
                CALL PUSHINTEGER4(ix)
                ix = jx
                IF (noconj) THEN
                  IF (nounit) THEN
                    CALL PUSHCOMPLEX8(temp)
                    temp = temp*a(j, j)
                    CALL PUSHCONTROL1B(1)
                  ELSE
                    CALL PUSHCONTROL1B(0)
                  END IF
                  ad_from1 = j - 1
                  DO i=ad_from1,1,-1
                    CALL PUSHINTEGER4(ix)
                    ix = ix - incx
                    temp = temp + a(i, j)*x(ix)
                  ENDDO
                  CALL PUSHINTEGER4(ad_from1)
                  CALL PUSHCONTROL1B(0)
                ELSE
                  IF (nounit) THEN
                    CALL PUSHCOMPLEX8(temp)
                    temp = temp*CONJG(a(j, j))
                    CALL PUSHCONTROL1B(1)
                  ELSE
                    CALL PUSHCONTROL1B(0)
                  END IF
                  ad_from2 = j - 1
                  DO i=ad_from2,1,-1
                    CALL PUSHINTEGER4(ix)
                    ix = ix - incx
                    temp = temp + CONJG(a(i, j))*x(ix)
                  ENDDO
                  CALL PUSHINTEGER4(ad_from2)
                  CALL PUSHCONTROL1B(1)
                END IF
                CALL PUSHCOMPLEX8(x(jx))
                x(jx) = temp
                CALL PUSHINTEGER4(jx)
                jx = jx - incx
              ENDDO
              DO ii1=1,ISIZE2OFa
                DO ii2=1,lda
                  DO nd=1,nbdirsmax
                    ab(nd, ii2, ii1) = (0.0,0.0)
                  ENDDO
                ENDDO
              ENDDO
              DO j=1,n,1
                CALL POPINTEGER4(jx)
                CALL POPCOMPLEX8(x(jx))
                DO nd=1,nbdirs
                  tempb(nd) = xb(nd, jx)
                  xb(nd, jx) = (0.0,0.0)
                ENDDO
                CALL POPCONTROL1B(branch)
                IF (branch .EQ. 0) THEN
                  CALL POPINTEGER4(ad_from1)
                  DO i=1,ad_from1,1
                    DO nd=1,nbdirs
                      ab(nd, i, j) = ab(nd, i, j) + CONJG(x(ix))*tempb(
     +                  nd)
                      xb(nd, ix) = xb(nd, ix) + CONJG(a(i, j))*tempb(nd)
                    ENDDO
                    CALL POPINTEGER4(ix)
                  ENDDO
                  CALL POPCONTROL1B(branch)
                  IF (branch .NE. 0) THEN
                    CALL POPCOMPLEX8(temp)
                    DO nd=1,nbdirs
                      ab(nd, j, j) = ab(nd, j, j) + CONJG(temp)*tempb(nd
     +                  )
                      tempb(nd) = CONJG(a(j, j))*tempb(nd)
                    ENDDO
                  END IF
                ELSE
                  CALL POPINTEGER4(ad_from2)
                  DO i=1,ad_from2,1
                    DO nd=1,nbdirs
                      ab(nd, i, j) = ab(nd, i, j) + CONJG(CONJG(x(ix))*
     +                  tempb(nd))
                      xb(nd, ix) = xb(nd, ix) + CONJG(CONJG(a(i, j)))*
     +                  tempb(nd)
                    ENDDO
                    CALL POPINTEGER4(ix)
                  ENDDO
                  CALL POPCONTROL1B(branch)
                  IF (branch .NE. 0) THEN
                    CALL POPCOMPLEX8(temp)
                    DO nd=1,nbdirs
                      ab(nd, j, j) = ab(nd, j, j) + CONJG(CONJG(temp)*
     +                  tempb(nd))
                      tempb(nd) = CONJG(CONJG(a(j, j)))*tempb(nd)
                    ENDDO
                  END IF
                END IF
                CALL POPINTEGER4(ix)
                DO nd=1,nbdirs
                  xb(nd, jx) = xb(nd, jx) + tempb(nd)
                ENDDO
              ENDDO
            END IF
          ELSE IF (incx .EQ. 1) THEN
            DO j=1,n
              temp = x(j)
              IF (noconj) THEN
                IF (nounit) THEN
                  CALL PUSHCOMPLEX8(temp)
                  temp = temp*a(j, j)
                  CALL PUSHCONTROL1B(1)
                ELSE
                  CALL PUSHCONTROL1B(0)
                END IF
                ad_from3 = j + 1
                DO i=ad_from3,n
                  temp = temp + a(i, j)*x(i)
                ENDDO
                CALL PUSHINTEGER4(ad_from3)
                CALL PUSHCONTROL1B(0)
              ELSE
                IF (nounit) THEN
                  CALL PUSHCOMPLEX8(temp)
                  temp = temp*CONJG(a(j, j))
                  CALL PUSHCONTROL1B(1)
                ELSE
                  CALL PUSHCONTROL1B(0)
                END IF
                ad_from4 = j + 1
                DO i=ad_from4,n
                  temp = temp + CONJG(a(i, j))*x(i)
                ENDDO
                CALL PUSHINTEGER4(ad_from4)
                CALL PUSHCONTROL1B(1)
              END IF
              CALL PUSHCOMPLEX8(x(j))
              x(j) = temp
            ENDDO
            DO ii1=1,ISIZE2OFa
              DO ii2=1,lda
                DO nd=1,nbdirsmax
                  ab(nd, ii2, ii1) = (0.0,0.0)
                ENDDO
              ENDDO
            ENDDO
            DO j=n,1,-1
              CALL POPCOMPLEX8(x(j))
              DO nd=1,nbdirs
                tempb(nd) = xb(nd, j)
                xb(nd, j) = (0.0,0.0)
              ENDDO
              CALL POPCONTROL1B(branch)
              IF (branch .EQ. 0) THEN
                CALL POPINTEGER4(ad_from3)
                DO i=n,ad_from3,-1
                  DO nd=1,nbdirs
                    ab(nd, i, j) = ab(nd, i, j) + CONJG(x(i))*tempb(nd)
                    xb(nd, i) = xb(nd, i) + CONJG(a(i, j))*tempb(nd)
                  ENDDO
                ENDDO
                CALL POPCONTROL1B(branch)
                IF (branch .NE. 0) THEN
                  CALL POPCOMPLEX8(temp)
                  DO nd=1,nbdirs
                    ab(nd, j, j) = ab(nd, j, j) + CONJG(temp)*tempb(nd)
                    tempb(nd) = CONJG(a(j, j))*tempb(nd)
                  ENDDO
                END IF
              ELSE
                CALL POPINTEGER4(ad_from4)
                DO i=n,ad_from4,-1
                  DO nd=1,nbdirs
                    ab(nd, i, j) = ab(nd, i, j) + CONJG(CONJG(x(i))*
     +                tempb(nd))
                    xb(nd, i) = xb(nd, i) + CONJG(CONJG(a(i, j)))*tempb(
     +                nd)
                  ENDDO
                ENDDO
                CALL POPCONTROL1B(branch)
                IF (branch .NE. 0) THEN
                  CALL POPCOMPLEX8(temp)
                  DO nd=1,nbdirs
                    ab(nd, j, j) = ab(nd, j, j) + CONJG(CONJG(temp)*
     +                tempb(nd))
                    tempb(nd) = CONJG(CONJG(a(j, j)))*tempb(nd)
                  ENDDO
                END IF
              END IF
              DO nd=1,nbdirs
                xb(nd, j) = xb(nd, j) + tempb(nd)
              ENDDO
            ENDDO
          ELSE
            jx = kx
            DO j=1,n
              temp = x(jx)
              CALL PUSHINTEGER4(ix)
              ix = jx
              IF (noconj) THEN
                IF (nounit) THEN
                  CALL PUSHCOMPLEX8(temp)
                  temp = temp*a(j, j)
                  CALL PUSHCONTROL1B(1)
                ELSE
                  CALL PUSHCONTROL1B(0)
                END IF
                ad_from5 = j + 1
                DO i=ad_from5,n
                  CALL PUSHINTEGER4(ix)
                  ix = ix + incx
                  temp = temp + a(i, j)*x(ix)
                ENDDO
                CALL PUSHINTEGER4(ad_from5)
                CALL PUSHCONTROL1B(0)
              ELSE
                IF (nounit) THEN
                  CALL PUSHCOMPLEX8(temp)
                  temp = temp*CONJG(a(j, j))
                  CALL PUSHCONTROL1B(1)
                ELSE
                  CALL PUSHCONTROL1B(0)
                END IF
                ad_from6 = j + 1
                DO i=ad_from6,n
                  CALL PUSHINTEGER4(ix)
                  ix = ix + incx
                  temp = temp + CONJG(a(i, j))*x(ix)
                ENDDO
                CALL PUSHINTEGER4(ad_from6)
                CALL PUSHCONTROL1B(1)
              END IF
              CALL PUSHCOMPLEX8(x(jx))
              x(jx) = temp
              CALL PUSHINTEGER4(jx)
              jx = jx + incx
            ENDDO
            DO ii1=1,ISIZE2OFa
              DO ii2=1,lda
                DO nd=1,nbdirsmax
                  ab(nd, ii2, ii1) = (0.0,0.0)
                ENDDO
              ENDDO
            ENDDO
            DO j=n,1,-1
              CALL POPINTEGER4(jx)
              CALL POPCOMPLEX8(x(jx))
              DO nd=1,nbdirs
                tempb(nd) = xb(nd, jx)
                xb(nd, jx) = (0.0,0.0)
              ENDDO
              CALL POPCONTROL1B(branch)
              IF (branch .EQ. 0) THEN
                CALL POPINTEGER4(ad_from5)
                DO i=n,ad_from5,-1
                  DO nd=1,nbdirs
                    ab(nd, i, j) = ab(nd, i, j) + CONJG(x(ix))*tempb(nd)
                    xb(nd, ix) = xb(nd, ix) + CONJG(a(i, j))*tempb(nd)
                  ENDDO
                  CALL POPINTEGER4(ix)
                ENDDO
                CALL POPCONTROL1B(branch)
                IF (branch .NE. 0) THEN
                  CALL POPCOMPLEX8(temp)
                  DO nd=1,nbdirs
                    ab(nd, j, j) = ab(nd, j, j) + CONJG(temp)*tempb(nd)
                    tempb(nd) = CONJG(a(j, j))*tempb(nd)
                  ENDDO
                END IF
              ELSE
                CALL POPINTEGER4(ad_from6)
                DO i=n,ad_from6,-1
                  DO nd=1,nbdirs
                    ab(nd, i, j) = ab(nd, i, j) + CONJG(CONJG(x(ix))*
     +                tempb(nd))
                    xb(nd, ix) = xb(nd, ix) + CONJG(CONJG(a(i, j)))*
     +                tempb(nd)
                  ENDDO
                  CALL POPINTEGER4(ix)
                ENDDO
                CALL POPCONTROL1B(branch)
                IF (branch .NE. 0) THEN
                  CALL POPCOMPLEX8(temp)
                  DO nd=1,nbdirs
                    ab(nd, j, j) = ab(nd, j, j) + CONJG(CONJG(temp)*
     +                tempb(nd))
                    tempb(nd) = CONJG(CONJG(a(j, j)))*tempb(nd)
                  ENDDO
                END IF
              END IF
              CALL POPINTEGER4(ix)
              DO nd=1,nbdirs
                xb(nd, jx) = xb(nd, jx) + tempb(nd)
              ENDDO
            ENDDO
          END IF
          CALL POPCONTROL1B(branch)
        END IF
      END IF
      CALL POPCONTROL3B(branch)
      END

