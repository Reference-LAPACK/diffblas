C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of dspmv in forward (tangent) mode (with options multiDirectional):
C   variations   of useful results: y
C   with respect to varying inputs: alpha ap x y beta
C   RW status of diff variables: alpha:in ap:in x:in y:in-out beta:in
C> \brief \b DSPMV
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       SUBROUTINE DSPMV(UPLO,N,ALPHA,AP,X,INCX,BETA,Y,INCY)
C
C       .. Scalar Arguments ..
C       DOUBLE PRECISION ALPHA,BETA
C       INTEGER INCX,INCY,N
C       CHARACTER UPLO
C       ..
C       .. Array Arguments ..
C       DOUBLE PRECISION AP(*),X(*),Y(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C> DSPMV  performs the matrix-vector operation
C>
C>    y := alpha*A*x + beta*y,
C>
C> where alpha and beta are scalars, x and y are n element vectors and
C> A is an n by n symmetric matrix, supplied in packed form.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] UPLO
C> \verbatim
C>          UPLO is CHARACTER*1
C>           On entry, UPLO specifies whether the upper or lower
C>           triangular part of the matrix A is supplied in the packed
C>           array AP as follows:
C>
C>              UPLO = 'U' or 'u'   The upper triangular part of A is
C>                                  supplied in AP.
C>
C>              UPLO = 'L' or 'l'   The lower triangular part of A is
C>                                  supplied in AP.
C> \endverbatim
C>
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>           On entry, N specifies the order of the matrix A.
C>           N must be at least zero.
C> \endverbatim
C>
C> \param[in] ALPHA
C> \verbatim
C>          ALPHA is DOUBLE PRECISION.
C>           On entry, ALPHA specifies the scalar alpha.
C> \endverbatim
C>
C> \param[in] AP
C> \verbatim
C>          AP is DOUBLE PRECISION array, dimension at least
C>           ( ( n*( n + 1 ) )/2 ).
C>           Before entry with UPLO = 'U' or 'u', the array AP must
C>           contain the upper triangular part of the symmetric matrix
C>           packed sequentially, column by column, so that AP( 1 )
C>           contains a( 1, 1 ), AP( 2 ) and AP( 3 ) contain a( 1, 2 )
C>           and a( 2, 2 ) respectively, and so on.
C>           Before entry with UPLO = 'L' or 'l', the array AP must
C>           contain the lower triangular part of the symmetric matrix
C>           packed sequentially, column by column, so that AP( 1 )
C>           contains a( 1, 1 ), AP( 2 ) and AP( 3 ) contain a( 2, 1 )
C>           and a( 3, 1 ) respectively, and so on.
C> \endverbatim
C>
C> \param[in] X
C> \verbatim
C>          X is DOUBLE PRECISION array, dimension at least
C>           ( 1 + ( n - 1 )*abs( INCX ) ).
C>           Before entry, the incremented array X must contain the n
C>           element vector x.
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>           On entry, INCX specifies the increment for the elements of
C>           X. INCX must not be zero.
C> \endverbatim
C>
C> \param[in] BETA
C> \verbatim
C>          BETA is DOUBLE PRECISION.
C>           On entry, BETA specifies the scalar beta. When BETA is
C>           supplied as zero then Y need not be set on input.
C> \endverbatim
C>
C> \param[in,out] Y
C> \verbatim
C>          Y is DOUBLE PRECISION array, dimension at least
C>           ( 1 + ( n - 1 )*abs( INCY ) ).
C>           Before entry, the incremented array Y must contain the n
C>           element vector y. On exit, Y is overwritten by the updated
C>           vector y.
C> \endverbatim
C>
C> \param[in] INCY
C> \verbatim
C>          INCY is INTEGER
C>           On entry, INCY specifies the increment for the elements of
C>           Y. INCY must not be zero.
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup hpmv
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>  Level 2 Blas routine.
C>  The vector and matrix arguments are not referenced when N = 0, or M = 0
C>
C>  -- Written on 22-October-1986.
C>     Jack Dongarra, Argonne National Lab.
C>     Jeremy Du Croz, Nag Central Office.
C>     Sven Hammarling, Nag Central Office.
C>     Richard Hanson, Sandia National Labs.
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE DSPMV_DV(uplo, n, alpha, alphad, ap, apd, x, xd, incx, 
     +                    beta, betad, y, yd, incy, nbdirs)
      IMPLICIT NONE
      INCLUDE 'DIFFSIZES.inc'
C  Hint: nbdirsmax should be the maximum number of differentiation directions
C
C  -- Reference BLAS level2 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      DOUBLE PRECISION alpha, beta
      DOUBLE PRECISION alphad(nbdirsmax), betad(nbdirsmax)
      INTEGER incx, incy, n
      CHARACTER uplo
C     ..
C     .. Array Arguments ..
      DOUBLE PRECISION ap(*), x(*), y(*)
      DOUBLE PRECISION apd(nbdirsmax, *), xd(nbdirsmax, *), yd(nbdirsmax
     +                 , *)
C     ..
C
C  =====================================================================
C
C     .. Parameters ..
      DOUBLE PRECISION one, zero
      PARAMETER (one=1.0d+0, zero=0.0d+0)
C     ..
C     .. Local Scalars ..
      DOUBLE PRECISION temp1, temp2
      DOUBLE PRECISION temp1d(nbdirsmax), temp2d(nbdirsmax)
      INTEGER i, info, ix, iy, j, jx, jy, k, kk, kx, ky
      EXTERNAL LSAME
C     ..
C     .. External Functions ..
      LOGICAL LSAME
C     ..
C     .. External Subroutines ..
      EXTERNAL XERBLA
      INTEGER nd
      INTEGER nbdirs
C     ..
C
C     Test the input parameters.
C
C     Check 0 < nbdirs <= nbdirsmax (required by DIFFSIZES.inc)
      IF (nbdirs.LE.0 .OR. nbdirs.GT.nbdirsmax) THEN
        WRITE(*,'(A,I0,A,I0,A)') 'Error: nbdirs=', nbdirs,
     +  ' must be in 1..nbdirsmax=', nbdirsmax, '. Stopping.'
        STOP 1
      END IF
C
      info = 0
      IF (.NOT.LSAME(uplo, 'U') .AND. (.NOT.LSAME(uplo, 'L'))) THEN
        info = 1
      ELSE IF (n .LT. 0) THEN
        info = 2
      ELSE IF (incx .EQ. 0) THEN
        info = 6
      ELSE IF (incy .EQ. 0) THEN
        info = 9
      END IF
      IF (info .NE. 0) THEN
        CALL XERBLA('DSPMV ', info)
        RETURN
      ELSE IF (n .EQ. 0 .OR. (alpha .EQ. zero .AND. beta .EQ. one)) THEN
C
C     Quick return if possible.
C
        RETURN
      ELSE
C
C     Set up the start points in  X  and  Y.
C
        IF (incx .GT. 0) THEN
          kx = 1
        ELSE
          kx = 1 - (n-1)*incx
        END IF
        IF (incy .GT. 0) THEN
          ky = 1
        ELSE
          ky = 1 - (n-1)*incy
        END IF
C
C     Start the operations. In this version the elements of the array AP
C     are accessed sequentially with one pass through AP.
C
C     First form  y := beta*y.
C
        IF (beta .NE. one) THEN
          IF (incy .EQ. 1) THEN
            IF (beta .EQ. zero) THEN
              DO i=1,n
                DO nd=1,nbdirs
                  yd(nd, i) = 0.D0
                ENDDO
                y(i) = zero
              ENDDO
            ELSE
              DO i=1,n
                DO nd=1,nbdirs
                  yd(nd, i) = y(i)*betad(nd) + beta*yd(nd, i)
                ENDDO
                y(i) = beta*y(i)
              ENDDO
            END IF
          ELSE
            iy = ky
            IF (beta .EQ. zero) THEN
              DO i=1,n
                DO nd=1,nbdirs
                  yd(nd, iy) = 0.D0
                ENDDO
                y(iy) = zero
                iy = iy + incy
              ENDDO
            ELSE
              DO i=1,n
                DO nd=1,nbdirs
                  yd(nd, iy) = y(iy)*betad(nd) + beta*yd(nd, iy)
                ENDDO
                y(iy) = beta*y(iy)
                iy = iy + incy
              ENDDO
            END IF
          END IF
        END IF
        IF (alpha .EQ. zero) THEN
          RETURN
        ELSE
          kk = 1
          IF (LSAME(uplo, 'U')) THEN
C
C        Form  y  when AP contains the upper triangle.
C
            IF (incx .EQ. 1 .AND. incy .EQ. 1) THEN
              DO j=1,n
                DO nd=1,nbdirs
                  temp1d(nd) = x(j)*alphad(nd) + alpha*xd(nd, j)
                ENDDO
                temp1 = alpha*x(j)
                temp2 = zero
                k = kk
                DO nd=1,nbdirsmax
                  temp2d(nd) = 0.D0
                ENDDO
                DO i=1,j-1
                  DO nd=1,nbdirs
                    yd(nd, i) = yd(nd, i) + ap(k)*temp1d(nd) + temp1*apd
     +                (nd, k)
                    temp2d(nd) = temp2d(nd) + x(i)*apd(nd, k) + ap(k)*xd
     +                (nd, i)
                  ENDDO
                  y(i) = y(i) + temp1*ap(k)
                  temp2 = temp2 + ap(k)*x(i)
                  k = k + 1
                ENDDO
                DO nd=1,nbdirs
                  yd(nd, j) = yd(nd, j) + ap(kk+j-1)*temp1d(nd) + temp1*
     +              apd(nd, kk+j-1) + temp2*alphad(nd) + alpha*temp2d(nd
     +              )
                ENDDO
                y(j) = y(j) + temp1*ap(kk+j-1) + alpha*temp2
                kk = kk + j
              ENDDO
            ELSE
              jx = kx
              jy = ky
              DO j=1,n
                DO nd=1,nbdirs
                  temp1d(nd) = x(jx)*alphad(nd) + alpha*xd(nd, jx)
                ENDDO
                temp1 = alpha*x(jx)
                temp2 = zero
                ix = kx
                iy = ky
                DO nd=1,nbdirsmax
                  temp2d(nd) = 0.D0
                ENDDO
                DO k=kk,kk+j-2
                  DO nd=1,nbdirs
                    yd(nd, iy) = yd(nd, iy) + ap(k)*temp1d(nd) + temp1*
     +                apd(nd, k)
                    temp2d(nd) = temp2d(nd) + x(ix)*apd(nd, k) + ap(k)*
     +                xd(nd, ix)
                  ENDDO
                  y(iy) = y(iy) + temp1*ap(k)
                  temp2 = temp2 + ap(k)*x(ix)
                  ix = ix + incx
                  iy = iy + incy
                ENDDO
                DO nd=1,nbdirs
                  yd(nd, jy) = yd(nd, jy) + ap(kk+j-1)*temp1d(nd) + 
     +              temp1*apd(nd, kk+j-1) + temp2*alphad(nd) + alpha*
     +              temp2d(nd)
                ENDDO
                y(jy) = y(jy) + temp1*ap(kk+j-1) + alpha*temp2
                jx = jx + incx
                jy = jy + incy
                kk = kk + j
              ENDDO
            END IF
          ELSE IF (incx .EQ. 1 .AND. incy .EQ. 1) THEN
C
C        Form  y  when AP contains the lower triangle.
C
            DO j=1,n
              temp1 = alpha*x(j)
              DO nd=1,nbdirs
                temp1d(nd) = x(j)*alphad(nd) + alpha*xd(nd, j)
                yd(nd, j) = yd(nd, j) + ap(kk)*temp1d(nd) + temp1*apd(nd
     +            , kk)
              ENDDO
              temp2 = zero
              y(j) = y(j) + temp1*ap(kk)
              k = kk + 1
              DO nd=1,nbdirsmax
                temp2d(nd) = 0.D0
              ENDDO
              DO i=j+1,n
                DO nd=1,nbdirs
                  yd(nd, i) = yd(nd, i) + ap(k)*temp1d(nd) + temp1*apd(
     +              nd, k)
                  temp2d(nd) = temp2d(nd) + x(i)*apd(nd, k) + ap(k)*xd(
     +              nd, i)
                ENDDO
                y(i) = y(i) + temp1*ap(k)
                temp2 = temp2 + ap(k)*x(i)
                k = k + 1
              ENDDO
              DO nd=1,nbdirs
                yd(nd, j) = yd(nd, j) + temp2*alphad(nd) + alpha*temp2d(
     +            nd)
              ENDDO
              y(j) = y(j) + alpha*temp2
              kk = kk + (n-j+1)
            ENDDO
          ELSE
            jx = kx
            jy = ky
            DO j=1,n
              temp1 = alpha*x(jx)
              DO nd=1,nbdirs
                temp1d(nd) = x(jx)*alphad(nd) + alpha*xd(nd, jx)
                yd(nd, jy) = yd(nd, jy) + ap(kk)*temp1d(nd) + temp1*apd(
     +            nd, kk)
              ENDDO
              temp2 = zero
              y(jy) = y(jy) + temp1*ap(kk)
              ix = jx
              iy = jy
              DO nd=1,nbdirsmax
                temp2d(nd) = 0.D0
              ENDDO
              DO k=kk+1,kk+n-j
                ix = ix + incx
                iy = iy + incy
                DO nd=1,nbdirs
                  yd(nd, iy) = yd(nd, iy) + ap(k)*temp1d(nd) + temp1*apd
     +              (nd, k)
                  temp2d(nd) = temp2d(nd) + x(ix)*apd(nd, k) + ap(k)*xd(
     +              nd, ix)
                ENDDO
                y(iy) = y(iy) + temp1*ap(k)
                temp2 = temp2 + ap(k)*x(ix)
              ENDDO
              DO nd=1,nbdirs
                yd(nd, jy) = yd(nd, jy) + temp2*alphad(nd) + alpha*
     +            temp2d(nd)
              ENDDO
              y(jy) = y(jy) + alpha*temp2
              jx = jx + incx
              jy = jy + incy
              kk = kk + (n-j+1)
            ENDDO
          END IF
C
          RETURN
C
C     End of DSPMV
C
        END IF
      END IF
      END

