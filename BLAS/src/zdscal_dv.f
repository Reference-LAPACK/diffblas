C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of zdscal in forward (tangent) mode (with options multiDirectional):
C   variations   of useful results: zx
C   with respect to varying inputs: zx da
C   RW status of diff variables: zx:in-out da:in
C> \brief \b ZDSCAL
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       SUBROUTINE ZDSCAL(N,DA,ZX,INCX)
C
C       .. Scalar Arguments ..
C       DOUBLE PRECISION DA
C       INTEGER INCX,N
C       ..
C       .. Array Arguments ..
C       COMPLEX*16 ZX(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C>    ZDSCAL scales a vector by a constant.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>         number of elements in input vector(s)
C> \endverbatim
C>
C> \param[in] DA
C> \verbatim
C>          DA is DOUBLE PRECISION
C>           On entry, DA specifies the scalar alpha.
C> \endverbatim
C>
C> \param[in,out] ZX
C> \verbatim
C>          ZX is COMPLEX*16 array, dimension ( 1 + ( N - 1 )*abs( INCX ) )
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>         storage spacing between elements of ZX
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup scal
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>     jack dongarra, 3/11/78.
C>     modified 3/93 to return if incx .le. 0.
C>     modified 12/3/93, array(1) declarations changed to array(*)
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE ZDSCAL_DV(n, da, dad, zx, zxd, incx, nbdirs)
      IMPLICIT NONE
      INCLUDE 'DIFFSIZES.inc'
C  Hint: nbdirsmax should be the maximum number of differentiation directions
C
C  -- Reference BLAS level1 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      DOUBLE PRECISION da
      DOUBLE PRECISION dad(nbdirsmax)
      INTEGER incx, n
C     ..
C     .. Array Arguments ..
      COMPLEX*16 zx(*)
      COMPLEX*16 zxd(nbdirsmax, *)
C     ..
C
C  =====================================================================
C
C     .. Local Scalars ..
      INTEGER i, nincx
C     .. Parameters ..
      DOUBLE PRECISION one
      PARAMETER (one=1.0d+0)
C     ..
C     .. Intrinsic Functions ..
      INTRINSIC DBLE, DCMPLX, DIMAG
      DOUBLE PRECISION arg1
      DOUBLE PRECISION arg1d(nbdirsmax)
      DOUBLE PRECISION arg2
      DOUBLE PRECISION arg2d(nbdirsmax)
      INTEGER nd
      DOUBLE PRECISION temp
      INTEGER nbdirs
C     ..
C     Check 0 < nbdirs <= nbdirsmax (required by DIFFSIZES.inc)
      IF (nbdirs.LE.0 .OR. nbdirs.GT.nbdirsmax) THEN
        WRITE(*,'(A,I0,A,I0,A)') 'Error: nbdirs=', nbdirs,
     +  ' must be in 1..nbdirsmax=', nbdirsmax, '. Stopping.'
        STOP 1
      END IF
C
      IF ((n .LE. 0 .OR. incx .LE. 0) .OR. da .EQ. one) THEN
        RETURN
      ELSE
        IF (incx .EQ. 1) THEN
C
C        code for increment equal to 1
C
          DO i=1,n
            temp = DBLE(zx(i))
            DO nd=1,nbdirs
              arg1d(nd) = temp*dad(nd) + da*DBLE(zxd(nd, i))
            ENDDO
            arg1 = da*temp
            temp = DIMAG(zx(i))
            arg2 = da*temp
            DO nd=1,nbdirs
              arg2d(nd) = temp*dad(nd) + da*DIMAG(zxd(nd, i))
              zxd(nd, i) = DCMPLX(arg1d(nd), arg2d(nd))
            ENDDO
            zx(i) = DCMPLX(arg1, arg2)
          ENDDO
        ELSE
C
C        code for increment not equal to 1
C
          nincx = n*incx
          DO i=1,nincx,incx
            temp = DBLE(zx(i))
            DO nd=1,nbdirs
              arg1d(nd) = temp*dad(nd) + da*DBLE(zxd(nd, i))
            ENDDO
            arg1 = da*temp
            temp = DIMAG(zx(i))
            arg2 = da*temp
            DO nd=1,nbdirs
              arg2d(nd) = temp*dad(nd) + da*DIMAG(zxd(nd, i))
              zxd(nd, i) = DCMPLX(arg1d(nd), arg2d(nd))
            ENDDO
            zx(i) = DCMPLX(arg1, arg2)
          ENDDO
        END IF
        RETURN
C
C     End of ZDSCAL
C
      END IF
      END

