C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of ssbmv in reverse (adjoint) mode (with options multiDirectional):
C   gradient     of useful results: y
C   with respect to varying inputs: alpha x y beta a
C   RW status of diff variables: alpha:out x:out y:in-out beta:out
C                a:out
C> \brief \b SSBMV
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       SUBROUTINE SSBMV(UPLO,N,K,ALPHA,A,LDA,X,INCX,BETA,Y,INCY)
C
C       .. Scalar Arguments ..
C       REAL ALPHA,BETA
C       INTEGER INCX,INCY,K,LDA,N
C       CHARACTER UPLO
C       ..
C       .. Array Arguments ..
C       REAL A(LDA,*),X(*),Y(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C> SSBMV  performs the matrix-vector  operation
C>
C>    y := alpha*A*x + beta*y,
C>
C> where alpha and beta are scalars, x and y are n element vectors and
C> A is an n by n symmetric band matrix, with k super-diagonals.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] UPLO
C> \verbatim
C>          UPLO is CHARACTER*1
C>           On entry, UPLO specifies whether the upper or lower
C>           triangular part of the band matrix A is being supplied as
C>           follows:
C>
C>              UPLO = 'U' or 'u'   The upper triangular part of A is
C>                                  being supplied.
C>
C>              UPLO = 'L' or 'l'   The lower triangular part of A is
C>                                  being supplied.
C> \endverbatim
C>
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>           On entry, N specifies the order of the matrix A.
C>           N must be at least zero.
C> \endverbatim
C>
C> \param[in] K
C> \verbatim
C>          K is INTEGER
C>           On entry, K specifies the number of super-diagonals of the
C>           matrix A. K must satisfy  0 .le. K.
C> \endverbatim
C>
C> \param[in] ALPHA
C> \verbatim
C>          ALPHA is REAL
C>           On entry, ALPHA specifies the scalar alpha.
C> \endverbatim
C>
C> \param[in] A
C> \verbatim
C>          A is REAL array, dimension ( LDA, N )
C>           Before entry with UPLO = 'U' or 'u', the leading ( k + 1 )
C>           by n part of the array A must contain the upper triangular
C>           band part of the symmetric matrix, supplied column by
C>           column, with the leading diagonal of the matrix in row
C>           ( k + 1 ) of the array, the first super-diagonal starting at
C>           position 2 in row k, and so on. The top left k by k triangle
C>           of the array A is not referenced.
C>           The following program segment will transfer the upper
C>           triangular part of a symmetric band matrix from conventional
C>           full matrix storage to band storage:
C>
C>                 DO 20, J = 1, N
C>                    M = K + 1 - J
C>                    DO 10, I = MAX( 1, J - K ), J
C>                       A( M + I, J ) = matrix( I, J )
C>              10    CONTINUE
C>              20 CONTINUE
C>
C>           Before entry with UPLO = 'L' or 'l', the leading ( k + 1 )
C>           by n part of the array A must contain the lower triangular
C>           band part of the symmetric matrix, supplied column by
C>           column, with the leading diagonal of the matrix in row 1 of
C>           the array, the first sub-diagonal starting at position 1 in
C>           row 2, and so on. The bottom right k by k triangle of the
C>           array A is not referenced.
C>           The following program segment will transfer the lower
C>           triangular part of a symmetric band matrix from conventional
C>           full matrix storage to band storage:
C>
C>                 DO 20, J = 1, N
C>                    M = 1 - J
C>                    DO 10, I = J, MIN( N, J + K )
C>                       A( M + I, J ) = matrix( I, J )
C>              10    CONTINUE
C>              20 CONTINUE
C> \endverbatim
C>
C> \param[in] LDA
C> \verbatim
C>          LDA is INTEGER
C>           On entry, LDA specifies the first dimension of A as declared
C>           in the calling (sub) program. LDA must be at least
C>           ( k + 1 ).
C> \endverbatim
C>
C> \param[in] X
C> \verbatim
C>          X is REAL array, dimension at least
C>           ( 1 + ( n - 1 )*abs( INCX ) ).
C>           Before entry, the incremented array X must contain the
C>           vector x.
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>           On entry, INCX specifies the increment for the elements of
C>           X. INCX must not be zero.
C> \endverbatim
C>
C> \param[in] BETA
C> \verbatim
C>          BETA is REAL
C>           On entry, BETA specifies the scalar beta.
C> \endverbatim
C>
C> \param[in,out] Y
C> \verbatim
C>          Y is REAL array, dimension at least
C>           ( 1 + ( n - 1 )*abs( INCY ) ).
C>           Before entry, the incremented array Y must contain the
C>           vector y. On exit, Y is overwritten by the updated vector y.
C> \endverbatim
C>
C> \param[in] INCY
C> \verbatim
C>          INCY is INTEGER
C>           On entry, INCY specifies the increment for the elements of
C>           Y. INCY must not be zero.
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup hbmv
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>  Level 2 Blas routine.
C>  The vector and matrix arguments are not referenced when N = 0, or M = 0
C>
C>  -- Written on 22-October-1986.
C>     Jack Dongarra, Argonne National Lab.
C>     Jeremy Du Croz, Nag Central Office.
C>     Sven Hammarling, Nag Central Office.
C>     Richard Hanson, Sandia National Labs.
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE SSBMV_BV(uplo, n, k, alpha, alphab, a, ab, lda, x, xb, 
     +                    incx, beta, betab, y, yb, incy, nbdirs)
      IMPLICIT NONE
      INCLUDE 'DIFFSIZES.inc'
C  Hint: ISIZE2OFa should be the size of dimension 2 of array a
C  Hint: ISIZE1OFx should be the size of dimension 1 of array x
C  Hint: nbdirsmax should be the maximum number of differentiation directions
C
C  -- Reference BLAS level2 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      REAL alpha, beta
      REAL alphab(nbdirsmax), betab(nbdirsmax)
      INTEGER incx, incy, k, lda, n
      CHARACTER uplo
C     ..
C     .. Array Arguments ..
      REAL a(lda, *), x(*), y(*)
      REAL ab(nbdirsmax, lda, *), xb(nbdirsmax, *), yb(nbdirsmax, *)
C     ..
C
C  =====================================================================
C
C     .. Parameters ..
      REAL one, zero
      PARAMETER (one=1.0e+0, zero=0.0e+0)
C     ..
C     .. Local Scalars ..
      REAL temp1, temp2
      REAL temp1b(nbdirsmax), temp2b(nbdirsmax)
      INTEGER i, info, ix, iy, j, jx, jy, kplus1, kx, ky, l
      EXTERNAL LSAME
      INTEGER ISIZE1OFX, ISIZE2OFA
C     ..
C     .. External Functions ..
      INTEGER get_ISIZE1OFX, get_ISIZE2OFA
      EXTERNAL get_ISIZE1OFX, get_ISIZE2OFA
      LOGICAL LSAME
C     ..
C     .. External Subroutines ..
      EXTERNAL XERBLA, check_ISIZE1OFX_initialized, check_ISIZE2OFA_initialized
C     ..
C     .. Intrinsic Functions ..
      INTRINSIC MAX, MIN
      INTEGER max1
      INTEGER max2
      INTEGER min1
      INTEGER min2
      INTEGER nd
      INTEGER ad_from
      INTEGER ad_to
      INTEGER ad_from0
      INTEGER ad_to0
      INTEGER*4 branch
      INTEGER ad_from1
      INTEGER ad_to1
      INTEGER ad_from2
      INTEGER ad_to2
      INTEGER ii2
      INTEGER ii1
      INTEGER nbdirs
C     ..
C
C     Test the input parameters.
C
C     Check 0 < nbdirs <= nbdirsmax (required by DIFFSIZES.inc)
      CALL check_ISIZE1OFX_initialized()
      CALL check_ISIZE2OFA_initialized()
      ISIZE1OFX = get_ISIZE1OFX()
      ISIZE2OFA = get_ISIZE2OFA()
      IF (nbdirs.LE.0 .OR. nbdirs.GT.nbdirsmax) THEN
        WRITE(*,'(A,I0,A,I0,A)') 'Error: nbdirs=', nbdirs,
     +  ' must be in 1..nbdirsmax=', nbdirsmax, '. Stopping.'
        STOP 1
      END IF
C
      info = 0
      IF (.NOT.LSAME(uplo, 'U') .AND. (.NOT.LSAME(uplo, 'L'))) THEN
        CALL PUSHCONTROL3B(0)
        info = 1
      ELSE IF (n .LT. 0) THEN
        CALL PUSHCONTROL3B(1)
        info = 2
      ELSE IF (k .LT. 0) THEN
        CALL PUSHCONTROL3B(2)
        info = 3
      ELSE IF (lda .LT. k + 1) THEN
        CALL PUSHCONTROL3B(3)
        info = 6
      ELSE IF (incx .EQ. 0) THEN
        CALL PUSHCONTROL3B(4)
        info = 8
      ELSE IF (incy .EQ. 0) THEN
        CALL PUSHCONTROL3B(5)
        info = 11
      ELSE
        CALL PUSHCONTROL3B(5)
      END IF
      IF (info .EQ. 0) THEN
C
C     Quick return if possible.
C
        IF (n .EQ. 0 .OR. (alpha .EQ. zero .AND. beta .EQ. one)) THEN
          DO nd=1,nbdirsmax
            alphab(nd) = 0.0
          ENDDO
          DO ii1=1,ISIZE1OFx
            DO nd=1,nbdirsmax
              xb(nd, ii1) = 0.0
            ENDDO
          ENDDO
          DO nd=1,nbdirsmax
            betab(nd) = 0.0
          ENDDO
          DO ii1=1,ISIZE2OFa
            DO ii2=1,lda
              DO nd=1,nbdirsmax
                ab(nd, ii2, ii1) = 0.0
              ENDDO
            ENDDO
          ENDDO
        ELSE
C
C     Set up the start points in  X  and  Y.
C
          IF (incx .GT. 0) THEN
            kx = 1
          ELSE
            kx = 1 - (n-1)*incx
          END IF
          IF (incy .GT. 0) THEN
            ky = 1
          ELSE
            ky = 1 - (n-1)*incy
          END IF
C
C     Start the operations. In this version the elements of the array A
C     are accessed sequentially with one pass through A.
C
C     First form  y := beta*y.
C
          IF (beta .NE. one) THEN
            IF (incy .EQ. 1) THEN
              IF (beta .EQ. zero) THEN
                CALL PUSHCONTROL3B(0)
              ELSE
                CALL PUSHCONTROL3B(1)
              END IF
            ELSE
              iy = ky
              IF (beta .EQ. zero) THEN
                DO i=1,n
                  CALL PUSHINTEGER4(iy)
                  iy = iy + incy
                ENDDO
                CALL PUSHCONTROL3B(2)
              ELSE
                DO i=1,n
                  CALL PUSHREAL4(y(iy))
                  y(iy) = beta*y(iy)
                  CALL PUSHINTEGER4(iy)
                  iy = iy + incy
                ENDDO
                CALL PUSHCONTROL3B(3)
              END IF
            END IF
          ELSE
            CALL PUSHCONTROL3B(4)
          END IF
          IF (alpha .EQ. zero) THEN
            DO nd=1,nbdirsmax
              alphab(nd) = 0.0
            ENDDO
            DO ii1=1,ISIZE1OFx
              DO nd=1,nbdirsmax
                xb(nd, ii1) = 0.0
              ENDDO
            ENDDO
            DO ii1=1,ISIZE2OFa
              DO ii2=1,lda
                DO nd=1,nbdirsmax
                  ab(nd, ii2, ii1) = 0.0
                ENDDO
              ENDDO
            ENDDO
          ELSE IF (LSAME(uplo, 'U')) THEN
C
C        Form  y  when upper triangle of A is stored.
C
            kplus1 = k + 1
            IF (incx .EQ. 1 .AND. incy .EQ. 1) THEN
              DO j=1,n
                CALL PUSHREAL4(temp2)
                temp2 = zero
                l = kplus1 - j
                IF (1 .LT. j - k) THEN
                  max1 = j - k
                ELSE
                  max1 = 1
                END IF
                ad_from = max1
                DO i=ad_from,j-1
                  temp2 = temp2 + a(l+i, j)*x(i)
                ENDDO
                CALL PUSHINTEGER4(i - 1)
                CALL PUSHINTEGER4(ad_from)
              ENDDO
              DO nd=1,nbdirsmax
                alphab(nd) = 0.0
              ENDDO
              DO ii1=1,ISIZE1OFx
                DO nd=1,nbdirsmax
                  xb(nd, ii1) = 0.0
                ENDDO
              ENDDO
              DO ii1=1,ISIZE2OFa
                DO ii2=1,lda
                  DO nd=1,nbdirsmax
                    ab(nd, ii2, ii1) = 0.0
                  ENDDO
                ENDDO
              ENDDO
              DO j=n,1,-1
                temp1 = alpha*x(j)
                l = kplus1 - j
                DO nd=1,nbdirs
                  temp1b(nd) = a(kplus1, j)*yb(nd, j)
                  ab(nd, kplus1, j) = ab(nd, kplus1, j) + temp1*yb(nd, j
     +              )
                  alphab(nd) = alphab(nd) + temp2*yb(nd, j)
                  temp2b(nd) = alpha*yb(nd, j)
                ENDDO
                CALL POPINTEGER4(ad_from)
                CALL POPINTEGER4(ad_to)
                DO i=ad_to,ad_from,-1
                  DO nd=1,nbdirs
                    ab(nd, l+i, j) = ab(nd, l+i, j) + x(i)*temp2b(nd) + 
     +                temp1*yb(nd, i)
                    xb(nd, i) = xb(nd, i) + a(l+i, j)*temp2b(nd)
                    temp1b(nd) = temp1b(nd) + a(l+i, j)*yb(nd, i)
                  ENDDO
                ENDDO
                CALL POPREAL4(temp2)
                DO nd=1,nbdirs
                  alphab(nd) = alphab(nd) + x(j)*temp1b(nd)
                  xb(nd, j) = xb(nd, j) + alpha*temp1b(nd)
                ENDDO
              ENDDO
            ELSE
              jx = kx
              jy = ky
              DO j=1,n
                CALL PUSHREAL4(temp2)
                temp2 = zero
                ix = kx
                iy = ky
                l = kplus1 - j
                IF (1 .LT. j - k) THEN
                  max2 = j - k
                ELSE
                  max2 = 1
                END IF
                ad_from0 = max2
                DO i=ad_from0,j-1
                  temp2 = temp2 + a(l+i, j)*x(ix)
                  CALL PUSHINTEGER4(ix)
                  ix = ix + incx
                  CALL PUSHINTEGER4(iy)
                  iy = iy + incy
                ENDDO
                CALL PUSHINTEGER4(i - 1)
                CALL PUSHINTEGER4(ad_from0)
                CALL PUSHINTEGER4(jx)
                jx = jx + incx
                CALL PUSHINTEGER4(jy)
                jy = jy + incy
                IF (j .GT. k) THEN
                  CALL PUSHCONTROL1B(0)
                  kx = kx + incx
                  ky = ky + incy
                ELSE
                  CALL PUSHCONTROL1B(1)
                END IF
              ENDDO
              DO nd=1,nbdirsmax
                alphab(nd) = 0.0
              ENDDO
              DO ii1=1,ISIZE1OFx
                DO nd=1,nbdirsmax
                  xb(nd, ii1) = 0.0
                ENDDO
              ENDDO
              DO ii1=1,ISIZE2OFa
                DO ii2=1,lda
                  DO nd=1,nbdirsmax
                    ab(nd, ii2, ii1) = 0.0
                  ENDDO
                ENDDO
              ENDDO
              DO j=n,1,-1
                CALL POPCONTROL1B(branch)
                CALL POPINTEGER4(jy)
                CALL POPINTEGER4(jx)
                temp1 = alpha*x(jx)
                l = kplus1 - j
                DO nd=1,nbdirs
                  temp1b(nd) = a(kplus1, j)*yb(nd, jy)
                  ab(nd, kplus1, j) = ab(nd, kplus1, j) + temp1*yb(nd, 
     +              jy)
                  alphab(nd) = alphab(nd) + temp2*yb(nd, jy)
                  temp2b(nd) = alpha*yb(nd, jy)
                ENDDO
                CALL POPINTEGER4(ad_from0)
                CALL POPINTEGER4(ad_to0)
                DO i=ad_to0,ad_from0,-1
                  CALL POPINTEGER4(iy)
                  CALL POPINTEGER4(ix)
                  DO nd=1,nbdirs
                    ab(nd, l+i, j) = ab(nd, l+i, j) + x(ix)*temp2b(nd) +
     +                temp1*yb(nd, iy)
                    xb(nd, ix) = xb(nd, ix) + a(l+i, j)*temp2b(nd)
                    temp1b(nd) = temp1b(nd) + a(l+i, j)*yb(nd, iy)
                  ENDDO
                ENDDO
                CALL POPREAL4(temp2)
                DO nd=1,nbdirs
                  alphab(nd) = alphab(nd) + x(jx)*temp1b(nd)
                  xb(nd, jx) = xb(nd, jx) + alpha*temp1b(nd)
                ENDDO
              ENDDO
            END IF
          ELSE IF (incx .EQ. 1 .AND. incy .EQ. 1) THEN
C
C        Form  y  when lower triangle of A is stored.
C
            DO j=1,n
              CALL PUSHREAL4(temp2)
              temp2 = zero
              l = 1 - j
              IF (n .GT. j + k) THEN
                min1 = j + k
              ELSE
                min1 = n
              END IF
              ad_from1 = j + 1
              DO i=ad_from1,min1
                temp2 = temp2 + a(l+i, j)*x(i)
              ENDDO
              CALL PUSHINTEGER4(i - 1)
              CALL PUSHINTEGER4(ad_from1)
            ENDDO
            DO nd=1,nbdirsmax
              alphab(nd) = 0.0
            ENDDO
            DO ii1=1,ISIZE1OFx
              DO nd=1,nbdirsmax
                xb(nd, ii1) = 0.0
              ENDDO
            ENDDO
            DO ii1=1,ISIZE2OFa
              DO ii2=1,lda
                DO nd=1,nbdirsmax
                  ab(nd, ii2, ii1) = 0.0
                ENDDO
              ENDDO
            ENDDO
            DO j=n,1,-1
              DO nd=1,nbdirs
                alphab(nd) = alphab(nd) + temp2*yb(nd, j)
                temp2b(nd) = alpha*yb(nd, j)
              ENDDO
              l = 1 - j
              temp1 = alpha*x(j)
              DO nd=1,nbdirsmax
                temp1b(nd) = 0.0
              ENDDO
              CALL POPINTEGER4(ad_from1)
              CALL POPINTEGER4(ad_to1)
              DO i=ad_to1,ad_from1,-1
                DO nd=1,nbdirs
                  ab(nd, l+i, j) = ab(nd, l+i, j) + x(i)*temp2b(nd) + 
     +              temp1*yb(nd, i)
                  xb(nd, i) = xb(nd, i) + a(l+i, j)*temp2b(nd)
                  temp1b(nd) = temp1b(nd) + a(l+i, j)*yb(nd, i)
                ENDDO
              ENDDO
              DO nd=1,nbdirs
                temp1b(nd) = temp1b(nd) + a(1, j)*yb(nd, j)
                ab(nd, 1, j) = ab(nd, 1, j) + temp1*yb(nd, j)
                alphab(nd) = alphab(nd) + x(j)*temp1b(nd)
                xb(nd, j) = xb(nd, j) + alpha*temp1b(nd)
              ENDDO
              CALL POPREAL4(temp2)
            ENDDO
          ELSE
            jx = kx
            jy = ky
            DO j=1,n
              CALL PUSHREAL4(temp2)
              temp2 = zero
              l = 1 - j
              CALL PUSHINTEGER4(ix)
              ix = jx
              CALL PUSHINTEGER4(iy)
              iy = jy
              IF (n .GT. j + k) THEN
                min2 = j + k
              ELSE
                min2 = n
              END IF
              ad_from2 = j + 1
              DO i=ad_from2,min2
                CALL PUSHINTEGER4(ix)
                ix = ix + incx
                CALL PUSHINTEGER4(iy)
                iy = iy + incy
                temp2 = temp2 + a(l+i, j)*x(ix)
              ENDDO
              CALL PUSHINTEGER4(i - 1)
              CALL PUSHINTEGER4(ad_from2)
              CALL PUSHINTEGER4(jx)
              jx = jx + incx
              CALL PUSHINTEGER4(jy)
              jy = jy + incy
            ENDDO
            DO nd=1,nbdirsmax
              alphab(nd) = 0.0
            ENDDO
            DO ii1=1,ISIZE1OFx
              DO nd=1,nbdirsmax
                xb(nd, ii1) = 0.0
              ENDDO
            ENDDO
            DO ii1=1,ISIZE2OFa
              DO ii2=1,lda
                DO nd=1,nbdirsmax
                  ab(nd, ii2, ii1) = 0.0
                ENDDO
              ENDDO
            ENDDO
            DO j=n,1,-1
              CALL POPINTEGER4(jy)
              CALL POPINTEGER4(jx)
              l = 1 - j
              temp1 = alpha*x(jx)
              DO nd=1,nbdirs
                alphab(nd) = alphab(nd) + temp2*yb(nd, jy)
                temp2b(nd) = alpha*yb(nd, jy)
              ENDDO
              DO nd=1,nbdirsmax
                temp1b(nd) = 0.0
              ENDDO
              CALL POPINTEGER4(ad_from2)
              CALL POPINTEGER4(ad_to2)
              DO i=ad_to2,ad_from2,-1
                DO nd=1,nbdirs
                  ab(nd, l+i, j) = ab(nd, l+i, j) + x(ix)*temp2b(nd) + 
     +              temp1*yb(nd, iy)
                  xb(nd, ix) = xb(nd, ix) + a(l+i, j)*temp2b(nd)
                  temp1b(nd) = temp1b(nd) + a(l+i, j)*yb(nd, iy)
                ENDDO
                CALL POPINTEGER4(iy)
                CALL POPINTEGER4(ix)
              ENDDO
              CALL POPINTEGER4(iy)
              CALL POPINTEGER4(ix)
              CALL POPREAL4(temp2)
              DO nd=1,nbdirs
                temp1b(nd) = temp1b(nd) + a(1, j)*yb(nd, jy)
                ab(nd, 1, j) = ab(nd, 1, j) + temp1*yb(nd, jy)
                alphab(nd) = alphab(nd) + x(jx)*temp1b(nd)
                xb(nd, jx) = xb(nd, jx) + alpha*temp1b(nd)
              ENDDO
            ENDDO
          END IF
          CALL POPCONTROL3B(branch)
          IF (branch .LT. 2) THEN
            IF (branch .EQ. 0) THEN
              DO i=n,1,-1
                DO nd=1,nbdirs
                  yb(nd, i) = 0.0
                ENDDO
              ENDDO
              DO nd=1,nbdirsmax
                betab(nd) = 0.0
              ENDDO
            ELSE
              DO nd=1,nbdirsmax
                betab(nd) = 0.0
              ENDDO
              DO i=n,1,-1
                DO nd=1,nbdirs
                  betab(nd) = betab(nd) + y(i)*yb(nd, i)
                  yb(nd, i) = beta*yb(nd, i)
                ENDDO
              ENDDO
            END IF
          ELSE IF (branch .EQ. 2) THEN
            DO i=n,1,-1
              CALL POPINTEGER4(iy)
              DO nd=1,nbdirs
                yb(nd, iy) = 0.0
              ENDDO
            ENDDO
            DO nd=1,nbdirsmax
              betab(nd) = 0.0
            ENDDO
          ELSE IF (branch .EQ. 3) THEN
            DO nd=1,nbdirsmax
              betab(nd) = 0.0
            ENDDO
            DO i=n,1,-1
              CALL POPINTEGER4(iy)
              CALL POPREAL4(y(iy))
              DO nd=1,nbdirs
                betab(nd) = betab(nd) + y(iy)*yb(nd, iy)
                yb(nd, iy) = beta*yb(nd, iy)
              ENDDO
            ENDDO
          ELSE
            DO nd=1,nbdirsmax
              betab(nd) = 0.0
            ENDDO
          END IF
        END IF
      END IF
      CALL POPCONTROL3B(branch)
      END

