C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of dspmv in reverse (adjoint) mode (with options multiDirectional):
C   gradient     of useful results: y
C   with respect to varying inputs: alpha ap x y beta
C   RW status of diff variables: alpha:out ap:out x:out y:in-out
C                beta:out
C> \brief \b DSPMV
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       SUBROUTINE DSPMV(UPLO,N,ALPHA,AP,X,INCX,BETA,Y,INCY)
C
C       .. Scalar Arguments ..
C       DOUBLE PRECISION ALPHA,BETA
C       INTEGER INCX,INCY,N
C       CHARACTER UPLO
C       ..
C       .. Array Arguments ..
C       DOUBLE PRECISION AP(*),X(*),Y(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C> DSPMV  performs the matrix-vector operation
C>
C>    y := alpha*A*x + beta*y,
C>
C> where alpha and beta are scalars, x and y are n element vectors and
C> A is an n by n symmetric matrix, supplied in packed form.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] UPLO
C> \verbatim
C>          UPLO is CHARACTER*1
C>           On entry, UPLO specifies whether the upper or lower
C>           triangular part of the matrix A is supplied in the packed
C>           array AP as follows:
C>
C>              UPLO = 'U' or 'u'   The upper triangular part of A is
C>                                  supplied in AP.
C>
C>              UPLO = 'L' or 'l'   The lower triangular part of A is
C>                                  supplied in AP.
C> \endverbatim
C>
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>           On entry, N specifies the order of the matrix A.
C>           N must be at least zero.
C> \endverbatim
C>
C> \param[in] ALPHA
C> \verbatim
C>          ALPHA is DOUBLE PRECISION.
C>           On entry, ALPHA specifies the scalar alpha.
C> \endverbatim
C>
C> \param[in] AP
C> \verbatim
C>          AP is DOUBLE PRECISION array, dimension at least
C>           ( ( n*( n + 1 ) )/2 ).
C>           Before entry with UPLO = 'U' or 'u', the array AP must
C>           contain the upper triangular part of the symmetric matrix
C>           packed sequentially, column by column, so that AP( 1 )
C>           contains a( 1, 1 ), AP( 2 ) and AP( 3 ) contain a( 1, 2 )
C>           and a( 2, 2 ) respectively, and so on.
C>           Before entry with UPLO = 'L' or 'l', the array AP must
C>           contain the lower triangular part of the symmetric matrix
C>           packed sequentially, column by column, so that AP( 1 )
C>           contains a( 1, 1 ), AP( 2 ) and AP( 3 ) contain a( 2, 1 )
C>           and a( 3, 1 ) respectively, and so on.
C> \endverbatim
C>
C> \param[in] X
C> \verbatim
C>          X is DOUBLE PRECISION array, dimension at least
C>           ( 1 + ( n - 1 )*abs( INCX ) ).
C>           Before entry, the incremented array X must contain the n
C>           element vector x.
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>           On entry, INCX specifies the increment for the elements of
C>           X. INCX must not be zero.
C> \endverbatim
C>
C> \param[in] BETA
C> \verbatim
C>          BETA is DOUBLE PRECISION.
C>           On entry, BETA specifies the scalar beta. When BETA is
C>           supplied as zero then Y need not be set on input.
C> \endverbatim
C>
C> \param[in,out] Y
C> \verbatim
C>          Y is DOUBLE PRECISION array, dimension at least
C>           ( 1 + ( n - 1 )*abs( INCY ) ).
C>           Before entry, the incremented array Y must contain the n
C>           element vector y. On exit, Y is overwritten by the updated
C>           vector y.
C> \endverbatim
C>
C> \param[in] INCY
C> \verbatim
C>          INCY is INTEGER
C>           On entry, INCY specifies the increment for the elements of
C>           Y. INCY must not be zero.
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup hpmv
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>  Level 2 Blas routine.
C>  The vector and matrix arguments are not referenced when N = 0, or M = 0
C>
C>  -- Written on 22-October-1986.
C>     Jack Dongarra, Argonne National Lab.
C>     Jeremy Du Croz, Nag Central Office.
C>     Sven Hammarling, Nag Central Office.
C>     Richard Hanson, Sandia National Labs.
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE DSPMV_BV(uplo, n, alpha, alphab, ap, apb, x, xb, incx, 
     +                    beta, betab, y, yb, incy, nbdirs)
      IMPLICIT NONE
      INCLUDE 'DIFFSIZES.inc'
C  Hint: ISIZE1OFx should be the size of dimension 1 of array x
C  Hint: ISIZE1OFap should be the size of dimension 1 of array ap
C  Hint: nbdirsmax should be the maximum number of differentiation directions
C
C  -- Reference BLAS level2 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      DOUBLE PRECISION alpha, beta
      DOUBLE PRECISION alphab(nbdirsmax), betab(nbdirsmax)
      INTEGER incx, incy, n
      CHARACTER uplo
C     ..
C     .. Array Arguments ..
      DOUBLE PRECISION ap(*), x(*), y(*)
      DOUBLE PRECISION apb(nbdirsmax, *), xb(nbdirsmax, *), yb(nbdirsmax
     +                 , *)
C     ..
C
C  =====================================================================
C
C     .. Parameters ..
      DOUBLE PRECISION one, zero
      PARAMETER (one=1.0d+0, zero=0.0d+0)
C     ..
C     .. Local Scalars ..
      DOUBLE PRECISION temp1, temp2
      DOUBLE PRECISION temp1b(nbdirsmax), temp2b(nbdirsmax)
      INTEGER i, info, ix, iy, j, jx, jy, k, kk, kx, ky
      EXTERNAL LSAME
      INTEGER ISIZE1OFAp, ISIZE1OFX
C     ..
C     .. External Functions ..
      INTEGER get_ISIZE1OFAp, get_ISIZE1OFX
      EXTERNAL get_ISIZE1OFAp, get_ISIZE1OFX
      LOGICAL LSAME
C     ..
C     .. External Subroutines ..
      EXTERNAL XERBLA, check_ISIZE1OFAp_initialized, check_ISIZE1OFX_initialized
      INTEGER nd
      INTEGER ad_to
      INTEGER ad_from
      INTEGER ad_to0
      INTEGER ad_from0
      INTEGER ad_from1
      INTEGER ad_to1
      INTEGER*4 branch
      INTEGER ii1
      INTEGER nbdirs
C     ..
C
C     Test the input parameters.
C
C     Check 0 < nbdirs <= nbdirsmax (required by DIFFSIZES.inc)
      CALL check_ISIZE1OFAp_initialized()
      CALL check_ISIZE1OFX_initialized()
      ISIZE1OFAp = get_ISIZE1OFAp()
      ISIZE1OFX = get_ISIZE1OFX()
      IF (nbdirs.LE.0 .OR. nbdirs.GT.nbdirsmax) THEN
        WRITE(*,'(A,I0,A,I0,A)') 'Error: nbdirs=', nbdirs,
     +  ' must be in 1..nbdirsmax=', nbdirsmax, '. Stopping.'
        STOP 1
      END IF
C
      info = 0
      IF (.NOT.LSAME(uplo, 'U') .AND. (.NOT.LSAME(uplo, 'L'))) THEN
        CALL PUSHCONTROL2B(0)
        info = 1
      ELSE IF (n .LT. 0) THEN
        CALL PUSHCONTROL2B(1)
        info = 2
      ELSE IF (incx .EQ. 0) THEN
        CALL PUSHCONTROL2B(2)
        info = 6
      ELSE IF (incy .EQ. 0) THEN
        CALL PUSHCONTROL2B(3)
        info = 9
      ELSE
        CALL PUSHCONTROL2B(3)
      END IF
      IF (info .EQ. 0) THEN
C
C     Quick return if possible.
C
        IF (n .EQ. 0 .OR. (alpha .EQ. zero .AND. beta .EQ. one)) THEN
          DO nd=1,nbdirsmax
            alphab(nd) = 0.D0
          ENDDO
          DO ii1=1,ISIZE1OFap
            DO nd=1,nbdirsmax
              apb(nd, ii1) = 0.D0
            ENDDO
          ENDDO
          DO ii1=1,ISIZE1OFx
            DO nd=1,nbdirsmax
              xb(nd, ii1) = 0.D0
            ENDDO
          ENDDO
          DO nd=1,nbdirsmax
            betab(nd) = 0.D0
          ENDDO
        ELSE
C
C     Set up the start points in  X  and  Y.
C
          IF (incx .GT. 0) THEN
            kx = 1
          ELSE
            kx = 1 - (n-1)*incx
          END IF
          IF (incy .GT. 0) THEN
            ky = 1
          ELSE
            ky = 1 - (n-1)*incy
          END IF
C
C     Start the operations. In this version the elements of the array AP
C     are accessed sequentially with one pass through AP.
C
C     First form  y := beta*y.
C
          IF (beta .NE. one) THEN
            IF (incy .EQ. 1) THEN
              IF (beta .EQ. zero) THEN
                CALL PUSHCONTROL3B(0)
              ELSE
                CALL PUSHCONTROL3B(1)
              END IF
            ELSE
              iy = ky
              IF (beta .EQ. zero) THEN
                DO i=1,n
                  CALL PUSHINTEGER4(iy)
                  iy = iy + incy
                ENDDO
                CALL PUSHCONTROL3B(2)
              ELSE
                DO i=1,n
                  CALL PUSHREAL8(y(iy))
                  y(iy) = beta*y(iy)
                  CALL PUSHINTEGER4(iy)
                  iy = iy + incy
                ENDDO
                CALL PUSHCONTROL3B(3)
              END IF
            END IF
          ELSE
            CALL PUSHCONTROL3B(4)
          END IF
          IF (alpha .EQ. zero) THEN
            DO nd=1,nbdirsmax
              alphab(nd) = 0.D0
            ENDDO
            DO ii1=1,ISIZE1OFap
              DO nd=1,nbdirsmax
                apb(nd, ii1) = 0.D0
              ENDDO
            ENDDO
            DO ii1=1,ISIZE1OFx
              DO nd=1,nbdirsmax
                xb(nd, ii1) = 0.D0
              ENDDO
            ENDDO
          ELSE
            kk = 1
            IF (LSAME(uplo, 'U')) THEN
C
C        Form  y  when AP contains the upper triangle.
C
              IF (incx .EQ. 1 .AND. incy .EQ. 1) THEN
                DO j=1,n
                  CALL PUSHREAL8(temp2)
                  temp2 = zero
                  k = kk
                  DO i=1,j-1
                    temp2 = temp2 + ap(k)*x(i)
                    CALL PUSHINTEGER4(k)
                    k = k + 1
                  ENDDO
                  CALL PUSHINTEGER4(i - 1)
                  CALL PUSHINTEGER4(kk)
                  kk = kk + j
                ENDDO
                DO nd=1,nbdirsmax
                  alphab(nd) = 0.D0
                ENDDO
                DO ii1=1,ISIZE1OFap
                  DO nd=1,nbdirsmax
                    apb(nd, ii1) = 0.D0
                  ENDDO
                ENDDO
                DO ii1=1,ISIZE1OFx
                  DO nd=1,nbdirsmax
                    xb(nd, ii1) = 0.D0
                  ENDDO
                ENDDO
                DO j=n,1,-1
                  CALL POPINTEGER4(kk)
                  temp1 = alpha*x(j)
                  DO nd=1,nbdirs
                    temp1b(nd) = ap(kk+j-1)*yb(nd, j)
                    apb(nd, kk+j-1) = apb(nd, kk+j-1) + temp1*yb(nd, j)
                    alphab(nd) = alphab(nd) + temp2*yb(nd, j)
                    temp2b(nd) = alpha*yb(nd, j)
                  ENDDO
                  CALL POPINTEGER4(ad_to)
                  DO i=ad_to,1,-1
                    CALL POPINTEGER4(k)
                    DO nd=1,nbdirs
                      apb(nd, k) = apb(nd, k) + x(i)*temp2b(nd) + temp1*
     +                  yb(nd, i)
                      xb(nd, i) = xb(nd, i) + ap(k)*temp2b(nd)
                      temp1b(nd) = temp1b(nd) + ap(k)*yb(nd, i)
                    ENDDO
                  ENDDO
                  CALL POPREAL8(temp2)
                  DO nd=1,nbdirs
                    alphab(nd) = alphab(nd) + x(j)*temp1b(nd)
                    xb(nd, j) = xb(nd, j) + alpha*temp1b(nd)
                  ENDDO
                ENDDO
              ELSE
                jx = kx
                jy = ky
                DO j=1,n
                  CALL PUSHREAL8(temp2)
                  temp2 = zero
                  ix = kx
                  iy = ky
                  ad_from = kk
                  DO k=ad_from,kk+j-2
                    temp2 = temp2 + ap(k)*x(ix)
                    CALL PUSHINTEGER4(ix)
                    ix = ix + incx
                    CALL PUSHINTEGER4(iy)
                    iy = iy + incy
                  ENDDO
                  CALL PUSHINTEGER4(k - 1)
                  CALL PUSHINTEGER4(ad_from)
                  CALL PUSHINTEGER4(jx)
                  jx = jx + incx
                  CALL PUSHINTEGER4(jy)
                  jy = jy + incy
                  CALL PUSHINTEGER4(kk)
                  kk = kk + j
                ENDDO
                DO nd=1,nbdirsmax
                  alphab(nd) = 0.D0
                ENDDO
                DO ii1=1,ISIZE1OFap
                  DO nd=1,nbdirsmax
                    apb(nd, ii1) = 0.D0
                  ENDDO
                ENDDO
                DO ii1=1,ISIZE1OFx
                  DO nd=1,nbdirsmax
                    xb(nd, ii1) = 0.D0
                  ENDDO
                ENDDO
                DO j=n,1,-1
                  CALL POPINTEGER4(kk)
                  CALL POPINTEGER4(jy)
                  CALL POPINTEGER4(jx)
                  temp1 = alpha*x(jx)
                  DO nd=1,nbdirs
                    temp1b(nd) = ap(kk+j-1)*yb(nd, jy)
                    apb(nd, kk+j-1) = apb(nd, kk+j-1) + temp1*yb(nd, jy)
                    alphab(nd) = alphab(nd) + temp2*yb(nd, jy)
                    temp2b(nd) = alpha*yb(nd, jy)
                  ENDDO
                  CALL POPINTEGER4(ad_from)
                  CALL POPINTEGER4(ad_to0)
                  DO k=ad_to0,ad_from,-1
                    CALL POPINTEGER4(iy)
                    CALL POPINTEGER4(ix)
                    DO nd=1,nbdirs
                      apb(nd, k) = apb(nd, k) + x(ix)*temp2b(nd) + temp1
     +                  *yb(nd, iy)
                      xb(nd, ix) = xb(nd, ix) + ap(k)*temp2b(nd)
                      temp1b(nd) = temp1b(nd) + ap(k)*yb(nd, iy)
                    ENDDO
                  ENDDO
                  CALL POPREAL8(temp2)
                  DO nd=1,nbdirs
                    alphab(nd) = alphab(nd) + x(jx)*temp1b(nd)
                    xb(nd, jx) = xb(nd, jx) + alpha*temp1b(nd)
                  ENDDO
                ENDDO
              END IF
            ELSE IF (incx .EQ. 1 .AND. incy .EQ. 1) THEN
C
C        Form  y  when AP contains the lower triangle.
C
              DO j=1,n
                CALL PUSHREAL8(temp2)
                temp2 = zero
                k = kk + 1
                ad_from0 = j + 1
                DO i=ad_from0,n
                  temp2 = temp2 + ap(k)*x(i)
                  CALL PUSHINTEGER4(k)
                  k = k + 1
                ENDDO
                CALL PUSHINTEGER4(ad_from0)
                CALL PUSHINTEGER4(kk)
                kk = kk + (n-j+1)
              ENDDO
              DO nd=1,nbdirsmax
                alphab(nd) = 0.D0
              ENDDO
              DO ii1=1,ISIZE1OFap
                DO nd=1,nbdirsmax
                  apb(nd, ii1) = 0.D0
                ENDDO
              ENDDO
              DO ii1=1,ISIZE1OFx
                DO nd=1,nbdirsmax
                  xb(nd, ii1) = 0.D0
                ENDDO
              ENDDO
              DO j=n,1,-1
                CALL POPINTEGER4(kk)
                temp1 = alpha*x(j)
                DO nd=1,nbdirs
                  alphab(nd) = alphab(nd) + temp2*yb(nd, j)
                  temp2b(nd) = alpha*yb(nd, j)
                ENDDO
                DO nd=1,nbdirsmax
                  temp1b(nd) = 0.D0
                ENDDO
                CALL POPINTEGER4(ad_from0)
                DO i=n,ad_from0,-1
                  CALL POPINTEGER4(k)
                  DO nd=1,nbdirs
                    apb(nd, k) = apb(nd, k) + x(i)*temp2b(nd) + temp1*yb
     +                (nd, i)
                    xb(nd, i) = xb(nd, i) + ap(k)*temp2b(nd)
                    temp1b(nd) = temp1b(nd) + ap(k)*yb(nd, i)
                  ENDDO
                ENDDO
                DO nd=1,nbdirs
                  temp1b(nd) = temp1b(nd) + ap(kk)*yb(nd, j)
                  apb(nd, kk) = apb(nd, kk) + temp1*yb(nd, j)
                  alphab(nd) = alphab(nd) + x(j)*temp1b(nd)
                  xb(nd, j) = xb(nd, j) + alpha*temp1b(nd)
                ENDDO
                CALL POPREAL8(temp2)
              ENDDO
            ELSE
              jx = kx
              jy = ky
              DO j=1,n
                CALL PUSHREAL8(temp2)
                temp2 = zero
                CALL PUSHINTEGER4(ix)
                ix = jx
                CALL PUSHINTEGER4(iy)
                iy = jy
                ad_from1 = kk + 1
                DO k=ad_from1,kk+n-j
                  CALL PUSHINTEGER4(ix)
                  ix = ix + incx
                  CALL PUSHINTEGER4(iy)
                  iy = iy + incy
                  temp2 = temp2 + ap(k)*x(ix)
                ENDDO
                CALL PUSHINTEGER4(k - 1)
                CALL PUSHINTEGER4(ad_from1)
                CALL PUSHINTEGER4(jx)
                jx = jx + incx
                CALL PUSHINTEGER4(jy)
                jy = jy + incy
                CALL PUSHINTEGER4(kk)
                kk = kk + (n-j+1)
              ENDDO
              DO nd=1,nbdirsmax
                alphab(nd) = 0.D0
              ENDDO
              DO ii1=1,ISIZE1OFap
                DO nd=1,nbdirsmax
                  apb(nd, ii1) = 0.D0
                ENDDO
              ENDDO
              DO ii1=1,ISIZE1OFx
                DO nd=1,nbdirsmax
                  xb(nd, ii1) = 0.D0
                ENDDO
              ENDDO
              DO j=n,1,-1
                CALL POPINTEGER4(kk)
                CALL POPINTEGER4(jy)
                CALL POPINTEGER4(jx)
                temp1 = alpha*x(jx)
                DO nd=1,nbdirs
                  alphab(nd) = alphab(nd) + temp2*yb(nd, jy)
                  temp2b(nd) = alpha*yb(nd, jy)
                ENDDO
                DO nd=1,nbdirsmax
                  temp1b(nd) = 0.D0
                ENDDO
                CALL POPINTEGER4(ad_from1)
                CALL POPINTEGER4(ad_to1)
                DO k=ad_to1,ad_from1,-1
                  DO nd=1,nbdirs
                    apb(nd, k) = apb(nd, k) + x(ix)*temp2b(nd) + temp1*
     +                yb(nd, iy)
                    xb(nd, ix) = xb(nd, ix) + ap(k)*temp2b(nd)
                    temp1b(nd) = temp1b(nd) + ap(k)*yb(nd, iy)
                  ENDDO
                  CALL POPINTEGER4(iy)
                  CALL POPINTEGER4(ix)
                ENDDO
                CALL POPINTEGER4(iy)
                CALL POPINTEGER4(ix)
                CALL POPREAL8(temp2)
                DO nd=1,nbdirs
                  temp1b(nd) = temp1b(nd) + ap(kk)*yb(nd, jy)
                  apb(nd, kk) = apb(nd, kk) + temp1*yb(nd, jy)
                  alphab(nd) = alphab(nd) + x(jx)*temp1b(nd)
                  xb(nd, jx) = xb(nd, jx) + alpha*temp1b(nd)
                ENDDO
              ENDDO
            END IF
          END IF
          CALL POPCONTROL3B(branch)
          IF (branch .LT. 2) THEN
            IF (branch .EQ. 0) THEN
              DO i=n,1,-1
                DO nd=1,nbdirs
                  yb(nd, i) = 0.D0
                ENDDO
              ENDDO
              DO nd=1,nbdirsmax
                betab(nd) = 0.D0
              ENDDO
            ELSE
              DO nd=1,nbdirsmax
                betab(nd) = 0.D0
              ENDDO
              DO i=n,1,-1
                DO nd=1,nbdirs
                  betab(nd) = betab(nd) + y(i)*yb(nd, i)
                  yb(nd, i) = beta*yb(nd, i)
                ENDDO
              ENDDO
            END IF
          ELSE IF (branch .EQ. 2) THEN
            DO i=n,1,-1
              CALL POPINTEGER4(iy)
              DO nd=1,nbdirs
                yb(nd, iy) = 0.D0
              ENDDO
            ENDDO
            DO nd=1,nbdirsmax
              betab(nd) = 0.D0
            ENDDO
          ELSE IF (branch .EQ. 3) THEN
            DO nd=1,nbdirsmax
              betab(nd) = 0.D0
            ENDDO
            DO i=n,1,-1
              CALL POPINTEGER4(iy)
              CALL POPREAL8(y(iy))
              DO nd=1,nbdirs
                betab(nd) = betab(nd) + y(iy)*yb(nd, iy)
                yb(nd, iy) = beta*yb(nd, iy)
              ENDDO
            ENDDO
          ELSE
            DO nd=1,nbdirsmax
              betab(nd) = 0.D0
            ENDDO
          END IF
        END IF
      END IF
      CALL POPCONTROL2B(branch)
      END

