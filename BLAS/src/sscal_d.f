C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of sscal in forward (tangent) mode:
C   variations   of useful results: sx
C   with respect to varying inputs: sa sx
C   RW status of diff variables: sa:in sx:in-out
C> \brief \b SSCAL
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       SUBROUTINE SSCAL(N,SA,SX,INCX)
C
C       .. Scalar Arguments ..
C       REAL SA
C       INTEGER INCX,N
C       ..
C       .. Array Arguments ..
C       REAL SX(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C>    SSCAL scales a vector by a constant.
C>    uses unrolled loops for increment equal to 1.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>         number of elements in input vector(s)
C> \endverbatim
C>
C> \param[in] SA
C> \verbatim
C>          SA is REAL
C>           On entry, SA specifies the scalar alpha.
C> \endverbatim
C>
C> \param[in,out] SX
C> \verbatim
C>          SX is REAL array, dimension ( 1 + ( N - 1 )*abs( INCX ) )
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>         storage spacing between elements of SX
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup scal
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>     jack dongarra, linpack, 3/11/78.
C>     modified 3/93 to return if incx .le. 0.
C>     modified 12/3/93, array(1) declarations changed to array(*)
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE SSCAL_D(n, sa, sad, sx, sxd, incx)
      IMPLICIT NONE
C
C  -- Reference BLAS level1 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      REAL sa
      REAL sad
      INTEGER incx, n
C     ..
C     .. Array Arguments ..
      REAL sx(*)
      REAL sxd(*)
C     ..
C
C  =====================================================================
C
C     .. Local Scalars ..
      INTEGER i, m, mp1, nincx
C     ..
C     .. Parameters ..
      REAL one
      PARAMETER (one=1.0e+0)
C     ..
C     .. Intrinsic Functions ..
      INTRINSIC MOD
C     ..
      IF ((n .LE. 0 .OR. incx .LE. 0) .OR. sa .EQ. one) THEN
        RETURN
      ELSE
        IF (incx .EQ. 1) THEN
C
C        code for increment equal to 1
C
C
C        clean-up loop
C
          m = MOD(n, 5)
          IF (m .NE. 0) THEN
            DO i=1,m
              sxd(i) = sx(i)*sad + sa*sxd(i)
              sx(i) = sa*sx(i)
            ENDDO
            IF (n .LT. 5) RETURN
          END IF
          mp1 = m + 1
          DO i=mp1,n,5
            sxd(i) = sx(i)*sad + sa*sxd(i)
            sx(i) = sa*sx(i)
            sxd(i+1) = sx(i+1)*sad + sa*sxd(i+1)
            sx(i+1) = sa*sx(i+1)
            sxd(i+2) = sx(i+2)*sad + sa*sxd(i+2)
            sx(i+2) = sa*sx(i+2)
            sxd(i+3) = sx(i+3)*sad + sa*sxd(i+3)
            sx(i+3) = sa*sx(i+3)
            sxd(i+4) = sx(i+4)*sad + sa*sxd(i+4)
            sx(i+4) = sa*sx(i+4)
          ENDDO
        ELSE
C
C        code for increment not equal to 1
C
          nincx = n*incx
          DO i=1,nincx,incx
            sxd(i) = sx(i)*sad + sa*sxd(i)
            sx(i) = sa*sx(i)
          ENDDO
        END IF
        RETURN
C
C     End of SSCAL
C
      END IF
      END

