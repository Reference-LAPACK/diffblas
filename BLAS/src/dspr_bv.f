C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of dspr in reverse (adjoint) mode (with options multiDirectional):
C   gradient     of useful results: ap
C   with respect to varying inputs: alpha ap x
C   RW status of diff variables: alpha:out ap:in-out x:out
C> \brief \b DSPR
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       SUBROUTINE DSPR(UPLO,N,ALPHA,X,INCX,AP)
C
C       .. Scalar Arguments ..
C       DOUBLE PRECISION ALPHA
C       INTEGER INCX,N
C       CHARACTER UPLO
C       ..
C       .. Array Arguments ..
C       DOUBLE PRECISION AP(*),X(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C> DSPR    performs the symmetric rank 1 operation
C>
C>    A := alpha*x*x**T + A,
C>
C> where alpha is a real scalar, x is an n element vector and A is an
C> n by n symmetric matrix, supplied in packed form.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] UPLO
C> \verbatim
C>          UPLO is CHARACTER*1
C>           On entry, UPLO specifies whether the upper or lower
C>           triangular part of the matrix A is supplied in the packed
C>           array AP as follows:
C>
C>              UPLO = 'U' or 'u'   The upper triangular part of A is
C>                                  supplied in AP.
C>
C>              UPLO = 'L' or 'l'   The lower triangular part of A is
C>                                  supplied in AP.
C> \endverbatim
C>
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>           On entry, N specifies the order of the matrix A.
C>           N must be at least zero.
C> \endverbatim
C>
C> \param[in] ALPHA
C> \verbatim
C>          ALPHA is DOUBLE PRECISION.
C>           On entry, ALPHA specifies the scalar alpha.
C> \endverbatim
C>
C> \param[in] X
C> \verbatim
C>          X is DOUBLE PRECISION array, dimension at least
C>           ( 1 + ( n - 1 )*abs( INCX ) ).
C>           Before entry, the incremented array X must contain the n
C>           element vector x.
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>           On entry, INCX specifies the increment for the elements of
C>           X. INCX must not be zero.
C> \endverbatim
C>
C> \param[in,out] AP
C> \verbatim
C>          AP is DOUBLE PRECISION array, dimension at least
C>           ( ( n*( n + 1 ) )/2 ).
C>           Before entry with  UPLO = 'U' or 'u', the array AP must
C>           contain the upper triangular part of the symmetric matrix
C>           packed sequentially, column by column, so that AP( 1 )
C>           contains a( 1, 1 ), AP( 2 ) and AP( 3 ) contain a( 1, 2 )
C>           and a( 2, 2 ) respectively, and so on. On exit, the array
C>           AP is overwritten by the upper triangular part of the
C>           updated matrix.
C>           Before entry with UPLO = 'L' or 'l', the array AP must
C>           contain the lower triangular part of the symmetric matrix
C>           packed sequentially, column by column, so that AP( 1 )
C>           contains a( 1, 1 ), AP( 2 ) and AP( 3 ) contain a( 2, 1 )
C>           and a( 3, 1 ) respectively, and so on. On exit, the array
C>           AP is overwritten by the lower triangular part of the
C>           updated matrix.
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup hpr
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>  Level 2 Blas routine.
C>
C>  -- Written on 22-October-1986.
C>     Jack Dongarra, Argonne National Lab.
C>     Jeremy Du Croz, Nag Central Office.
C>     Sven Hammarling, Nag Central Office.
C>     Richard Hanson, Sandia National Labs.
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE DSPR_BV(uplo, n, alpha, alphab, x, xb, incx, ap, apb, 
     +                   nbdirs)
      IMPLICIT NONE
      INCLUDE 'DIFFSIZES.inc'
C  Hint: ISIZE1OFx should be the size of dimension 1 of array x
C  Hint: nbdirsmax should be the maximum number of differentiation directions
C
C  -- Reference BLAS level2 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      DOUBLE PRECISION alpha
      DOUBLE PRECISION alphab(nbdirsmax)
      INTEGER incx, n
      CHARACTER uplo
C     ..
C     .. Array Arguments ..
      DOUBLE PRECISION ap(*), x(*)
      DOUBLE PRECISION apb(nbdirsmax, *), xb(nbdirsmax, *)
C     ..
C
C  =====================================================================
C
C     .. Parameters ..
      DOUBLE PRECISION zero
      PARAMETER (zero=0.0d+0)
C     ..
C     .. Local Scalars ..
      DOUBLE PRECISION temp
      DOUBLE PRECISION tempb(nbdirsmax)
      INTEGER i, info, ix, j, jx, k, kk, kx
      EXTERNAL LSAME
      INTEGER ISIZE1OFX
C     ..
C     .. External Functions ..
      INTEGER get_ISIZE1OFX
      EXTERNAL get_ISIZE1OFX
      LOGICAL LSAME
C     ..
C     .. External Subroutines ..
      EXTERNAL XERBLA, check_ISIZE1OFX_initialized
      INTEGER nd
      INTEGER ad_to
      INTEGER*4 branch
      INTEGER ad_from
      INTEGER ad_to0
      INTEGER ad_from0
      INTEGER ad_from1
      INTEGER ad_to1
      INTEGER ii1
      INTEGER nbdirs
C     ..
C
C     Test the input parameters.
C
C     Check 0 < nbdirs <= nbdirsmax (required by DIFFSIZES.inc)
      CALL check_ISIZE1OFX_initialized()
      ISIZE1OFX = get_ISIZE1OFX()
      IF (nbdirs.LE.0 .OR. nbdirs.GT.nbdirsmax) THEN
        WRITE(*,'(A,I0,A,I0,A)') 'Error: nbdirs=', nbdirs,
     +  ' must be in 1..nbdirsmax=', nbdirsmax, '. Stopping.'
        STOP 1
      END IF
C
      info = 0
      IF (.NOT.LSAME(uplo, 'U') .AND. (.NOT.LSAME(uplo, 'L'))) THEN
        CALL PUSHCONTROL2B(0)
        info = 1
      ELSE IF (n .LT. 0) THEN
        CALL PUSHCONTROL2B(1)
        info = 2
      ELSE IF (incx .EQ. 0) THEN
        CALL PUSHCONTROL2B(2)
        info = 5
      ELSE
        CALL PUSHCONTROL2B(2)
      END IF
      IF (info .EQ. 0) THEN
C
C     Quick return if possible.
C
        IF (n .EQ. 0 .OR. alpha .EQ. zero) THEN
          DO nd=1,nbdirsmax
            alphab(nd) = 0.D0
          ENDDO
          DO ii1=1,ISIZE1OFx
            DO nd=1,nbdirsmax
              xb(nd, ii1) = 0.D0
            ENDDO
          ENDDO
        ELSE
C
C     Set the start point in X if the increment is not unity.
C
          IF (incx .LE. 0) THEN
            CALL PUSHCONTROL1B(0)
            kx = 1 - (n-1)*incx
          ELSE IF (incx .NE. 1) THEN
            CALL PUSHCONTROL1B(1)
            kx = 1
          ELSE
            CALL PUSHCONTROL1B(1)
          END IF
C
C     Start the operations. In this version the elements of the array AP
C     are accessed sequentially with one pass through AP.
C
          kk = 1
          IF (LSAME(uplo, 'U')) THEN
C
C        Form  A  when upper triangle is stored in AP.
C
            IF (incx .EQ. 1) THEN
              DO j=1,n
                IF (x(j) .NE. zero) THEN
                  CALL PUSHREAL8(temp)
                  temp = alpha*x(j)
                  k = kk
                  DO i=1,j
                    CALL PUSHINTEGER4(k)
                    k = k + 1
                  ENDDO
                  CALL PUSHINTEGER4(i - 1)
                  CALL PUSHCONTROL1B(0)
                ELSE
                  CALL PUSHCONTROL1B(1)
                END IF
                kk = kk + j
              ENDDO
              DO nd=1,nbdirsmax
                alphab(nd) = 0.D0
              ENDDO
              DO ii1=1,ISIZE1OFx
                DO nd=1,nbdirsmax
                  xb(nd, ii1) = 0.D0
                ENDDO
              ENDDO
              DO j=n,1,-1
                CALL POPCONTROL1B(branch)
                IF (branch .EQ. 0) THEN
                  DO nd=1,nbdirsmax
                    tempb(nd) = 0.D0
                  ENDDO
                  CALL POPINTEGER4(ad_to)
                  DO i=ad_to,1,-1
                    CALL POPINTEGER4(k)
                    DO nd=1,nbdirs
                      xb(nd, i) = xb(nd, i) + temp*apb(nd, k)
                      tempb(nd) = tempb(nd) + x(i)*apb(nd, k)
                    ENDDO
                  ENDDO
                  CALL POPREAL8(temp)
                  DO nd=1,nbdirs
                    alphab(nd) = alphab(nd) + x(j)*tempb(nd)
                    xb(nd, j) = xb(nd, j) + alpha*tempb(nd)
                  ENDDO
                END IF
              ENDDO
            ELSE
              jx = kx
              DO j=1,n
                IF (x(jx) .NE. zero) THEN
                  CALL PUSHREAL8(temp)
                  temp = alpha*x(jx)
                  ix = kx
                  ad_from = kk
                  DO k=ad_from,kk+j-1
                    CALL PUSHINTEGER4(ix)
                    ix = ix + incx
                  ENDDO
                  CALL PUSHINTEGER4(k - 1)
                  CALL PUSHINTEGER4(ad_from)
                  CALL PUSHCONTROL1B(0)
                ELSE
                  CALL PUSHCONTROL1B(1)
                END IF
                CALL PUSHINTEGER4(jx)
                jx = jx + incx
                kk = kk + j
              ENDDO
              DO nd=1,nbdirsmax
                alphab(nd) = 0.D0
              ENDDO
              DO ii1=1,ISIZE1OFx
                DO nd=1,nbdirsmax
                  xb(nd, ii1) = 0.D0
                ENDDO
              ENDDO
              DO j=n,1,-1
                CALL POPINTEGER4(jx)
                CALL POPCONTROL1B(branch)
                IF (branch .EQ. 0) THEN
                  DO nd=1,nbdirsmax
                    tempb(nd) = 0.D0
                  ENDDO
                  CALL POPINTEGER4(ad_from)
                  CALL POPINTEGER4(ad_to0)
                  DO k=ad_to0,ad_from,-1
                    CALL POPINTEGER4(ix)
                    DO nd=1,nbdirs
                      xb(nd, ix) = xb(nd, ix) + temp*apb(nd, k)
                      tempb(nd) = tempb(nd) + x(ix)*apb(nd, k)
                    ENDDO
                  ENDDO
                  CALL POPREAL8(temp)
                  DO nd=1,nbdirs
                    alphab(nd) = alphab(nd) + x(jx)*tempb(nd)
                    xb(nd, jx) = xb(nd, jx) + alpha*tempb(nd)
                  ENDDO
                END IF
              ENDDO
            END IF
          ELSE IF (incx .EQ. 1) THEN
C
C        Form  A  when lower triangle is stored in AP.
C
            DO j=1,n
              IF (x(j) .NE. zero) THEN
                CALL PUSHREAL8(temp)
                temp = alpha*x(j)
                k = kk
                ad_from0 = j
                DO i=ad_from0,n
                  CALL PUSHINTEGER4(k)
                  k = k + 1
                ENDDO
                CALL PUSHINTEGER4(ad_from0)
                CALL PUSHCONTROL1B(0)
              ELSE
                CALL PUSHCONTROL1B(1)
              END IF
              kk = kk + n - j + 1
            ENDDO
            DO nd=1,nbdirsmax
              alphab(nd) = 0.D0
            ENDDO
            DO ii1=1,ISIZE1OFx
              DO nd=1,nbdirsmax
                xb(nd, ii1) = 0.D0
              ENDDO
            ENDDO
            DO j=n,1,-1
              CALL POPCONTROL1B(branch)
              IF (branch .EQ. 0) THEN
                DO nd=1,nbdirsmax
                  tempb(nd) = 0.D0
                ENDDO
                CALL POPINTEGER4(ad_from0)
                DO i=n,ad_from0,-1
                  CALL POPINTEGER4(k)
                  DO nd=1,nbdirs
                    xb(nd, i) = xb(nd, i) + temp*apb(nd, k)
                    tempb(nd) = tempb(nd) + x(i)*apb(nd, k)
                  ENDDO
                ENDDO
                CALL POPREAL8(temp)
                DO nd=1,nbdirs
                  alphab(nd) = alphab(nd) + x(j)*tempb(nd)
                  xb(nd, j) = xb(nd, j) + alpha*tempb(nd)
                ENDDO
              END IF
            ENDDO
          ELSE
            jx = kx
            DO j=1,n
              IF (x(jx) .NE. zero) THEN
                CALL PUSHREAL8(temp)
                temp = alpha*x(jx)
                ix = jx
                ad_from1 = kk
                DO k=ad_from1,kk+n-j
                  CALL PUSHINTEGER4(ix)
                  ix = ix + incx
                ENDDO
                CALL PUSHINTEGER4(k - 1)
                CALL PUSHINTEGER4(ad_from1)
                CALL PUSHCONTROL1B(0)
              ELSE
                CALL PUSHCONTROL1B(1)
              END IF
              CALL PUSHINTEGER4(jx)
              jx = jx + incx
              kk = kk + n - j + 1
            ENDDO
            DO nd=1,nbdirsmax
              alphab(nd) = 0.D0
            ENDDO
            DO ii1=1,ISIZE1OFx
              DO nd=1,nbdirsmax
                xb(nd, ii1) = 0.D0
              ENDDO
            ENDDO
            DO j=n,1,-1
              CALL POPINTEGER4(jx)
              CALL POPCONTROL1B(branch)
              IF (branch .EQ. 0) THEN
                DO nd=1,nbdirsmax
                  tempb(nd) = 0.D0
                ENDDO
                CALL POPINTEGER4(ad_from1)
                CALL POPINTEGER4(ad_to1)
                DO k=ad_to1,ad_from1,-1
                  CALL POPINTEGER4(ix)
                  DO nd=1,nbdirs
                    xb(nd, ix) = xb(nd, ix) + temp*apb(nd, k)
                    tempb(nd) = tempb(nd) + x(ix)*apb(nd, k)
                  ENDDO
                ENDDO
                CALL POPREAL8(temp)
                DO nd=1,nbdirs
                  alphab(nd) = alphab(nd) + x(jx)*tempb(nd)
                  xb(nd, jx) = xb(nd, jx) + alpha*tempb(nd)
                ENDDO
              END IF
            ENDDO
          END IF
          CALL POPCONTROL1B(branch)
        END IF
      END IF
      CALL POPCONTROL2B(branch)
      END

