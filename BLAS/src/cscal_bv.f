C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of cscal in reverse (adjoint) mode (with options multiDirectional):
C   gradient     of useful results: cx
C   with respect to varying inputs: ca cx
C   RW status of diff variables: ca:out cx:in-out
C> \brief \b CSCAL
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       SUBROUTINE CSCAL(N,CA,CX,INCX)
C
C       .. Scalar Arguments ..
C       COMPLEX CA
C       INTEGER INCX,N
C       ..
C       .. Array Arguments ..
C       COMPLEX CX(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C>    CSCAL scales a vector by a constant.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>         number of elements in input vector(s)
C> \endverbatim
C>
C> \param[in] CA
C> \verbatim
C>          CA is COMPLEX
C>           On entry, CA specifies the scalar alpha.
C> \endverbatim
C>
C> \param[in,out] CX
C> \verbatim
C>          CX is COMPLEX array, dimension ( 1 + ( N - 1 )*abs( INCX ) )
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>         storage spacing between elements of CX
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup scal
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>     jack dongarra, linpack,  3/11/78.
C>     modified 3/93 to return if incx .le. 0.
C>     modified 12/3/93, array(1) declarations changed to array(*)
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE CSCAL_BV(n, ca, cab, cx, cxb, incx, nbdirs)
      IMPLICIT NONE
      INCLUDE 'DIFFSIZES.inc'
C  Hint: nbdirsmax should be the maximum number of differentiation directions
C
C  -- Reference BLAS level1 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      COMPLEX ca
      COMPLEX cab(nbdirsmax)
      INTEGER incx, n
C     ..
C     .. Array Arguments ..
      COMPLEX cx(*)
      COMPLEX cxb(nbdirsmax, *)
C     ..
C
C  =====================================================================
C
C     .. Local Scalars ..
      INTEGER i, nincx
C     ..
C     .. Parameters ..
      COMPLEX one
      PARAMETER (one=(1.0e+0,0.0e+0))
      INTEGER nd
      INTEGER nbdirs
C     ..
C     Check 0 < nbdirs <= nbdirsmax (required by DIFFSIZES.inc)
      IF (nbdirs.LE.0 .OR. nbdirs.GT.nbdirsmax) THEN
        WRITE(*,'(A,I0,A,I0,A)') 'Error: nbdirs=', nbdirs,
     +  ' must be in 1..nbdirsmax=', nbdirsmax, '. Stopping.'
        STOP 1
      END IF
C
      IF ((n .LE. 0 .OR. incx .LE. 0) .OR. ca .EQ. one) THEN
        DO nd=1,nbdirsmax
          cab(nd) = (0.0,0.0)
        ENDDO
      ELSE IF (incx .EQ. 1) THEN
        DO nd=1,nbdirsmax
          cab(nd) = (0.0,0.0)
        ENDDO
        DO i=n,1,-1
          DO nd=1,nbdirs
            cab(nd) = cab(nd) + CONJG(cx(i))*cxb(nd, i)
            cxb(nd, i) = CONJG(ca)*cxb(nd, i)
          ENDDO
        ENDDO
      ELSE
C
C        code for increment not equal to 1
C
        nincx = n*incx
        DO nd=1,nbdirsmax
          cab(nd) = (0.0,0.0)
        ENDDO
        DO i=nincx-MOD(nincx-1, incx),1,-incx
          DO nd=1,nbdirs
            cab(nd) = cab(nd) + CONJG(cx(i))*cxb(nd, i)
            cxb(nd, i) = CONJG(ca)*cxb(nd, i)
          ENDDO
        ENDDO
      END IF
      END

