C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of sdot in reverse (adjoint) mode:
C   gradient     of useful results: sdot
C   with respect to varying inputs: sx sy
C   RW status of diff variables: sdot:in-killed sx:out sy:out
C> \brief \b SDOT
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       REAL FUNCTION SDOT(N,SX,INCX,SY,INCY)
C
C       .. Scalar Arguments ..
C       INTEGER INCX,INCY,N
C       ..
C       .. Array Arguments ..
C       REAL SX(*),SY(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C>    SDOT forms the dot product of two vectors.
C>    uses unrolled loops for increments equal to one.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>         number of elements in input vector(s)
C> \endverbatim
C>
C> \param[in] SX
C> \verbatim
C>          SX is REAL array, dimension ( 1 + ( N - 1 )*abs( INCX ) )
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>         storage spacing between elements of SX
C> \endverbatim
C>
C> \param[in] SY
C> \verbatim
C>          SY is REAL array, dimension ( 1 + ( N - 1 )*abs( INCY ) )
C> \endverbatim
C>
C> \param[in] INCY
C> \verbatim
C>          INCY is INTEGER
C>         storage spacing between elements of SY
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup dot
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>     jack dongarra, linpack, 3/11/78.
C>     modified 12/3/93, array(1) declarations changed to array(*)
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE SDOT_B(n, sx, sxb, incx, sy, syb, incy, sdotb)
      IMPLICIT NONE
      INCLUDE 'DIFFSIZES.inc'
C  Hint: ISIZE1OFsy should be the size of dimension 1 of array sy
C  Hint: ISIZE1OFsx should be the size of dimension 1 of array sx
C
C  -- Reference BLAS level1 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      INTEGER incx, incy, n
C     ..
C     .. Array Arguments ..
      REAL sx(*), sy(*)
      REAL sxb(*), syb(*)
C     ..
C
C  =====================================================================
C
C     .. Local Scalars ..
      REAL stemp
      REAL stempb
      INTEGER i, ix, iy, m, mp1
      INTEGER ISIZE1OFSx, ISIZE1OFSy
      INTEGER get_ISIZE1OFSx, get_ISIZE1OFSy
      EXTERNAL get_ISIZE1OFSx, get_ISIZE1OFSy
C     ..
C     .. Intrinsic Functions ..
      INTRINSIC MOD
      INTEGER ii1
      INTEGER*4 branch
      REAL sdot
      REAL sdotb
C     ..
      CALL check_ISIZE1OFSx_initialized()
      CALL check_ISIZE1OFSy_initialized()
      ISIZE1OFSx = get_ISIZE1OFSx()
      ISIZE1OFSy = get_ISIZE1OFSy()
      IF (n .LE. 0) THEN
        DO ii1=1,ISIZE1OFsx
          sxb(ii1) = 0.0
        ENDDO
        DO ii1=1,ISIZE1OFsy
          syb(ii1) = 0.0
        ENDDO
      ELSE
        IF (incx .EQ. 1 .AND. incy .EQ. 1) THEN
C
C        code for both increments equal to 1
C
C
C        clean-up loop
C
          m = MOD(n, 5)
          IF (m .NE. 0) THEN
            IF (n .LT. 5) THEN
              stempb = sdotb
              DO ii1=1,ISIZE1OFsx
                sxb(ii1) = 0.0
              ENDDO
              DO ii1=1,ISIZE1OFsy
                syb(ii1) = 0.0
              ENDDO
              GOTO 100
            ELSE
              CALL PUSHCONTROL1B(0)
            END IF
          ELSE
            CALL PUSHCONTROL1B(1)
          END IF
          mp1 = m + 1
          CALL PUSHCONTROL1B(0)
        ELSE
C
C        code for unequal increments or equal increments
C          not equal to 1
C
          ix = 1
          iy = 1
          IF (incx .LT. 0) ix = (-n+1)*incx + 1
          IF (incy .LT. 0) iy = (-n+1)*incy + 1
          DO i=1,n
            CALL PUSHINTEGER4(ix)
            ix = ix + incx
            CALL PUSHINTEGER4(iy)
            iy = iy + incy
          ENDDO
          CALL PUSHCONTROL1B(1)
        END IF
        stempb = sdotb
        CALL POPCONTROL1B(branch)
        IF (branch .EQ. 0) THEN
          DO ii1=1,ISIZE1OFsx
            sxb(ii1) = 0.0
          ENDDO
          DO ii1=1,ISIZE1OFsy
            syb(ii1) = 0.0
          ENDDO
          DO i=n-MOD(n-mp1, 5),mp1,-5
            sxb(i) = sxb(i) + sy(i)*stempb
            syb(i) = syb(i) + sx(i)*stempb
            sxb(i+1) = sxb(i+1) + sy(i+1)*stempb
            syb(i+1) = syb(i+1) + sx(i+1)*stempb
            sxb(i+2) = sxb(i+2) + sy(i+2)*stempb
            syb(i+2) = syb(i+2) + sx(i+2)*stempb
            sxb(i+3) = sxb(i+3) + sy(i+3)*stempb
            syb(i+3) = syb(i+3) + sx(i+3)*stempb
            sxb(i+4) = sxb(i+4) + sy(i+4)*stempb
            syb(i+4) = syb(i+4) + sx(i+4)*stempb
          ENDDO
          CALL POPCONTROL1B(branch)
          IF (branch .NE. 0) GOTO 110
        ELSE
          DO ii1=1,ISIZE1OFsx
            sxb(ii1) = 0.0
          ENDDO
          DO ii1=1,ISIZE1OFsy
            syb(ii1) = 0.0
          ENDDO
          DO i=n,1,-1
            CALL POPINTEGER4(iy)
            CALL POPINTEGER4(ix)
            sxb(ix) = sxb(ix) + sy(iy)*stempb
            syb(iy) = syb(iy) + sx(ix)*stempb
          ENDDO
          GOTO 110
        END IF
 100    DO i=m,1,-1
          sxb(i) = sxb(i) + sy(i)*stempb
          syb(i) = syb(i) + sx(i)*stempb
        ENDDO
      END IF
 110  CONTINUE
      END

