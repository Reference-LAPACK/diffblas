C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of zaxpy in reverse (adjoint) mode:
C   gradient     of useful results: zy
C   with respect to varying inputs: zx zy za
C   RW status of diff variables: zx:out zy:in-out za:out
C> \brief \b ZAXPY
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       SUBROUTINE ZAXPY(N,ZA,ZX,INCX,ZY,INCY)
C
C       .. Scalar Arguments ..
C       COMPLEX*16 ZA
C       INTEGER INCX,INCY,N
C       ..
C       .. Array Arguments ..
C       COMPLEX*16 ZX(*),ZY(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C>    ZAXPY constant times a vector plus a vector.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>         number of elements in input vector(s)
C> \endverbatim
C>
C> \param[in] ZA
C> \verbatim
C>          ZA is COMPLEX*16
C>           On entry, ZA specifies the scalar alpha.
C> \endverbatim
C>
C> \param[in] ZX
C> \verbatim
C>          ZX is COMPLEX*16 array, dimension ( 1 + ( N - 1 )*abs( INCX ) )
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>         storage spacing between elements of ZX
C> \endverbatim
C>
C> \param[in,out] ZY
C> \verbatim
C>          ZY is COMPLEX*16 array, dimension ( 1 + ( N - 1 )*abs( INCY ) )
C> \endverbatim
C>
C> \param[in] INCY
C> \verbatim
C>          INCY is INTEGER
C>         storage spacing between elements of ZY
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup axpy
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>     jack dongarra, 3/11/78.
C>     modified 12/3/93, array(1) declarations changed to array(*)
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE ZAXPY_B(n, za, zab, zx, zxb, incx, zy, zyb, incy)
      IMPLICIT NONE
      INCLUDE 'DIFFSIZES.inc'
C  Hint: ISIZE1OFzx should be the size of dimension 1 of array zx
C
C  -- Reference BLAS level1 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      COMPLEX*16 za
      COMPLEX*16 zab
      INTEGER incx, incy, n
C     ..
C     .. Array Arguments ..
      COMPLEX*16 zx(*), zy(*)
      COMPLEX*16 zxb(*), zyb(*)
C     ..
C
C  =====================================================================
C
C     .. Local Scalars ..
      INTEGER i, ix, iy
      EXTERNAL DCABS1
      INTEGER ISIZE1OFZx
C     ..
C     .. External Functions ..
      INTEGER get_ISIZE1OFZx
      EXTERNAL get_ISIZE1OFZx
      DOUBLE PRECISION DCABS1
      DOUBLE PRECISION result1
      INTEGER ii1
C     ..
      CALL check_ISIZE1OFZx_initialized()
      ISIZE1OFZx = get_ISIZE1OFZx()
      IF (n .LE. 0) THEN
        DO ii1=1,ISIZE1OFzx
          zxb(ii1) = (0.0,0.0)
        ENDDO
        zab = (0.0,0.0)
      ELSE
        result1 = DCABS1(za)
        IF (result1 .EQ. 0.0d0) THEN
          DO ii1=1,ISIZE1OFzx
            zxb(ii1) = (0.0,0.0)
          ENDDO
          zab = (0.0,0.0)
        ELSE IF (incx .EQ. 1 .AND. incy .EQ. 1) THEN
          DO ii1=1,ISIZE1OFzx
            zxb(ii1) = (0.0,0.0)
          ENDDO
          zab = (0.0,0.0)
          DO i=n,1,-1
            zab = zab + CONJG(zx(i))*zyb(i)
            zxb(i) = zxb(i) + CONJG(za)*zyb(i)
          ENDDO
        ELSE
C
C        code for unequal increments or equal increments
C          not equal to 1
C
          ix = 1
          iy = 1
          IF (incx .LT. 0) ix = (-n+1)*incx + 1
          IF (incy .LT. 0) iy = (-n+1)*incy + 1
          DO i=1,n
            CALL PUSHINTEGER4(ix)
            ix = ix + incx
            CALL PUSHINTEGER4(iy)
            iy = iy + incy
          ENDDO
          DO ii1=1,ISIZE1OFzx
            zxb(ii1) = (0.0,0.0)
          ENDDO
          zab = (0.0,0.0)
          DO i=n,1,-1
            CALL POPINTEGER4(iy)
            CALL POPINTEGER4(ix)
            zab = zab + CONJG(zx(ix))*zyb(iy)
            zxb(ix) = zxb(ix) + CONJG(za)*zyb(iy)
          ENDDO
        END IF
      END IF
      END

