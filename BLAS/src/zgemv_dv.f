C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of zgemv in forward (tangent) mode (with options multiDirectional):
C   variations   of useful results: y
C   with respect to varying inputs: alpha x y beta a
C   RW status of diff variables: alpha:in x:in y:in-out beta:in
C                a:in
C> \brief \b ZGEMV
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       SUBROUTINE ZGEMV(TRANS,M,N,ALPHA,A,LDA,X,INCX,BETA,Y,INCY)
C
C       .. Scalar Arguments ..
C       COMPLEX*16 ALPHA,BETA
C       INTEGER INCX,INCY,LDA,M,N
C       CHARACTER TRANS
C       ..
C       .. Array Arguments ..
C       COMPLEX*16 A(LDA,*),X(*),Y(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C> ZGEMV  performs one of the matrix-vector operations
C>
C>    y := alpha*A*x + beta*y,   or   y := alpha*A**T*x + beta*y,   or
C>
C>    y := alpha*A**H*x + beta*y,
C>
C> where alpha and beta are scalars, x and y are vectors and A is an
C> m by n matrix.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] TRANS
C> \verbatim
C>          TRANS is CHARACTER*1
C>           On entry, TRANS specifies the operation to be performed as
C>           follows:
C>
C>              TRANS = 'N' or 'n'   y := alpha*A*x + beta*y.
C>
C>              TRANS = 'T' or 't'   y := alpha*A**T*x + beta*y.
C>
C>              TRANS = 'C' or 'c'   y := alpha*A**H*x + beta*y.
C> \endverbatim
C>
C> \param[in] M
C> \verbatim
C>          M is INTEGER
C>           On entry, M specifies the number of rows of the matrix A.
C>           M must be at least zero.
C> \endverbatim
C>
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>           On entry, N specifies the number of columns of the matrix A.
C>           N must be at least zero.
C> \endverbatim
C>
C> \param[in] ALPHA
C> \verbatim
C>          ALPHA is COMPLEX*16
C>           On entry, ALPHA specifies the scalar alpha.
C> \endverbatim
C>
C> \param[in] A
C> \verbatim
C>          A is COMPLEX*16 array, dimension ( LDA, N )
C>           Before entry, the leading m by n part of the array A must
C>           contain the matrix of coefficients.
C> \endverbatim
C>
C> \param[in] LDA
C> \verbatim
C>          LDA is INTEGER
C>           On entry, LDA specifies the first dimension of A as declared
C>           in the calling (sub) program. LDA must be at least
C>           max( 1, m ).
C> \endverbatim
C>
C> \param[in] X
C> \verbatim
C>          X is COMPLEX*16 array, dimension at least
C>           ( 1 + ( n - 1 )*abs( INCX ) ) when TRANS = 'N' or 'n'
C>           and at least
C>           ( 1 + ( m - 1 )*abs( INCX ) ) otherwise.
C>           Before entry, the incremented array X must contain the
C>           vector x.
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>           On entry, INCX specifies the increment for the elements of
C>           X. INCX must not be zero.
C> \endverbatim
C>
C> \param[in] BETA
C> \verbatim
C>          BETA is COMPLEX*16
C>           On entry, BETA specifies the scalar beta. When BETA is
C>           supplied as zero then Y need not be set on input.
C> \endverbatim
C>
C> \param[in,out] Y
C> \verbatim
C>          Y is COMPLEX*16 array, dimension at least
C>           ( 1 + ( m - 1 )*abs( INCY ) ) when TRANS = 'N' or 'n'
C>           and at least
C>           ( 1 + ( n - 1 )*abs( INCY ) ) otherwise.
C>           Before entry with BETA non-zero, the incremented array Y
C>           must contain the vector y. On exit, Y is overwritten by the
C>           updated vector y.
C>           If either m or n is zero, then Y not referenced and the function
C>           performs a quick return.
C> \endverbatim
C>
C> \param[in] INCY
C> \verbatim
C>          INCY is INTEGER
C>           On entry, INCY specifies the increment for the elements of
C>           Y. INCY must not be zero.
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup gemv
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>  Level 2 Blas routine.
C>  The vector and matrix arguments are not referenced when N = 0, or M = 0
C>
C>  -- Written on 22-October-1986.
C>     Jack Dongarra, Argonne National Lab.
C>     Jeremy Du Croz, Nag Central Office.
C>     Sven Hammarling, Nag Central Office.
C>     Richard Hanson, Sandia National Labs.
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE ZGEMV_DV(trans, m, n, alpha, alphad, a, ad, lda, x, xd
     +                    , incx, beta, betad, y, yd, incy, nbdirs)
      IMPLICIT NONE
      INCLUDE 'DIFFSIZES.inc'
C  Hint: nbdirsmax should be the maximum number of differentiation directions
C
C  -- Reference BLAS level2 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      COMPLEX*16 alpha, beta
      COMPLEX*16 alphad(nbdirsmax), betad(nbdirsmax)
      INTEGER incx, incy, lda, m, n
      CHARACTER trans
C     ..
C     .. Array Arguments ..
      COMPLEX*16 a(lda, *), x(*), y(*)
      COMPLEX*16 ad(nbdirsmax, lda, *), xd(nbdirsmax, *), yd(nbdirsmax, 
     +           *)
C     ..
C
C  =====================================================================
C
C     .. Parameters ..
      COMPLEX*16 one
      PARAMETER (one=(1.0d+0,0.0d+0))
      COMPLEX*16 zero
      PARAMETER (zero=(0.0d+0,0.0d+0))
C     ..
C     .. Local Scalars ..
      COMPLEX*16 temp
      COMPLEX*16 tempd(nbdirsmax)
      INTEGER i, info, ix, iy, j, jx, jy, kx, ky, lenx, leny
      LOGICAL noconj
      EXTERNAL LSAME
C     ..
C     .. External Functions ..
      LOGICAL LSAME
C     ..
C     .. External Subroutines ..
      EXTERNAL XERBLA
C     ..
C     .. Intrinsic Functions ..
      INTRINSIC DCONJG, MAX
      INTEGER max1
      INTEGER nd
      DOUBLE COMPLEX temp0
      INTEGER nbdirs
C     ..
C
C     Test the input parameters.
C
C     Check 0 < nbdirs <= nbdirsmax (required by DIFFSIZES.inc)
      IF (nbdirs.LE.0 .OR. nbdirs.GT.nbdirsmax) THEN
        WRITE(*,'(A,I0,A,I0,A)') 'Error: nbdirs=', nbdirs,
     +  ' must be in 1..nbdirsmax=', nbdirsmax, '. Stopping.'
        STOP 1
      END IF
C
      info = 0
      IF (.NOT.LSAME(trans, 'N') .AND. (.NOT.LSAME(trans, 'T')) .AND. (
     +    .NOT.LSAME(trans, 'C'))) THEN
        info = 1
      ELSE IF (m .LT. 0) THEN
        info = 2
      ELSE IF (n .LT. 0) THEN
        info = 3
      ELSE
        IF (1 .LT. m) THEN
          max1 = m
        ELSE
          max1 = 1
        END IF
        IF (lda .LT. max1) THEN
          info = 6
        ELSE IF (incx .EQ. 0) THEN
          info = 8
        ELSE IF (incy .EQ. 0) THEN
          info = 11
        END IF
      END IF
      IF (info .NE. 0) THEN
        CALL XERBLA('ZGEMV ', info)
        RETURN
      ELSE IF ((m .EQ. 0 .OR. n .EQ. 0) .OR. (alpha .EQ. zero .AND. beta
     +    .EQ. one)) THEN
C
C     Quick return if possible.
C
        RETURN
      ELSE
C
        noconj = LSAME(trans, 'T')
C
C     Set  LENX  and  LENY, the lengths of the vectors x and y, and set
C     up the start points in  X  and  Y.
C
        IF (LSAME(trans, 'N')) THEN
          lenx = n
          leny = m
        ELSE
          lenx = m
          leny = n
        END IF
        IF (incx .GT. 0) THEN
          kx = 1
        ELSE
          kx = 1 - (lenx-1)*incx
        END IF
        IF (incy .GT. 0) THEN
          ky = 1
        ELSE
          ky = 1 - (leny-1)*incy
        END IF
C
C     Start the operations. In this version the elements of A are
C     accessed sequentially with one pass through A.
C
C     First form  y := beta*y.
C
        IF (beta .NE. one) THEN
          IF (incy .EQ. 1) THEN
            IF (beta .EQ. zero) THEN
              DO i=1,leny
                DO nd=1,nbdirs
                  yd(nd, i) = 0.0
                ENDDO
                y(i) = zero
              ENDDO
            ELSE
              DO i=1,leny
                DO nd=1,nbdirs
                  yd(nd, i) = y(i)*betad(nd) + beta*yd(nd, i)
                ENDDO
                y(i) = beta*y(i)
              ENDDO
            END IF
          ELSE
            iy = ky
            IF (beta .EQ. zero) THEN
              DO i=1,leny
                DO nd=1,nbdirs
                  yd(nd, iy) = 0.0
                ENDDO
                y(iy) = zero
                iy = iy + incy
              ENDDO
            ELSE
              DO i=1,leny
                DO nd=1,nbdirs
                  yd(nd, iy) = y(iy)*betad(nd) + beta*yd(nd, iy)
                ENDDO
                y(iy) = beta*y(iy)
                iy = iy + incy
              ENDDO
            END IF
          END IF
        END IF
        IF (alpha .EQ. zero) THEN
          RETURN
        ELSE
          IF (LSAME(trans, 'N')) THEN
C
C        Form  y := alpha*A*x + y.
C
            jx = kx
            IF (incy .EQ. 1) THEN
              DO j=1,n
                DO nd=1,nbdirs
                  tempd(nd) = x(jx)*alphad(nd) + alpha*xd(nd, jx)
                ENDDO
                temp = alpha*x(jx)
                DO i=1,m
                  DO nd=1,nbdirs
                    yd(nd, i) = yd(nd, i) + a(i, j)*tempd(nd) + temp*ad(
     +                nd, i, j)
                  ENDDO
                  y(i) = y(i) + temp*a(i, j)
                ENDDO
                jx = jx + incx
              ENDDO
            ELSE
              DO j=1,n
                DO nd=1,nbdirs
                  tempd(nd) = x(jx)*alphad(nd) + alpha*xd(nd, jx)
                ENDDO
                temp = alpha*x(jx)
                iy = ky
                DO i=1,m
                  DO nd=1,nbdirs
                    yd(nd, iy) = yd(nd, iy) + a(i, j)*tempd(nd) + temp*
     +                ad(nd, i, j)
                  ENDDO
                  y(iy) = y(iy) + temp*a(i, j)
                  iy = iy + incy
                ENDDO
                jx = jx + incx
              ENDDO
            END IF
          ELSE
C
C        Form  y := alpha*A**T*x + y  or  y := alpha*A**H*x + y.
C
            jy = ky
            IF (incx .EQ. 1) THEN
              DO j=1,n
                temp = zero
                IF (noconj) THEN
                  DO nd=1,nbdirsmax
                    tempd(nd) = (0.0,0.0)
                  ENDDO
                  DO i=1,m
                    DO nd=1,nbdirs
                      tempd(nd) = tempd(nd) + x(i)*ad(nd, i, j) + a(i, j
     +                  )*xd(nd, i)
                    ENDDO
                    temp = temp + a(i, j)*x(i)
                  ENDDO
                ELSE
                  DO nd=1,nbdirsmax
                    tempd(nd) = (0.0,0.0)
                  ENDDO
                  DO i=1,m
                    temp0 = DCONJG(a(i, j))
                    DO nd=1,nbdirs
                      tempd(nd) = tempd(nd) + x(i)*DCONJG(ad(nd, i, j)) 
     +                  + temp0*xd(nd, i)
                    ENDDO
                    temp = temp + temp0*x(i)
                  ENDDO
                END IF
                DO nd=1,nbdirs
                  yd(nd, jy) = yd(nd, jy) + temp*alphad(nd) + alpha*
     +              tempd(nd)
                ENDDO
                y(jy) = y(jy) + alpha*temp
                jy = jy + incy
              ENDDO
            ELSE
              DO j=1,n
                temp = zero
                ix = kx
                IF (noconj) THEN
                  DO nd=1,nbdirsmax
                    tempd(nd) = (0.0,0.0)
                  ENDDO
                  DO i=1,m
                    DO nd=1,nbdirs
                      tempd(nd) = tempd(nd) + x(ix)*ad(nd, i, j) + a(i, 
     +                  j)*xd(nd, ix)
                    ENDDO
                    temp = temp + a(i, j)*x(ix)
                    ix = ix + incx
                  ENDDO
                ELSE
                  DO nd=1,nbdirsmax
                    tempd(nd) = (0.0,0.0)
                  ENDDO
                  DO i=1,m
                    temp0 = DCONJG(a(i, j))
                    DO nd=1,nbdirs
                      tempd(nd) = tempd(nd) + x(ix)*DCONJG(ad(nd, i, j))
     +                  + temp0*xd(nd, ix)
                    ENDDO
                    temp = temp + temp0*x(ix)
                    ix = ix + incx
                  ENDDO
                END IF
                DO nd=1,nbdirs
                  yd(nd, jy) = yd(nd, jy) + temp*alphad(nd) + alpha*
     +              tempd(nd)
                ENDDO
                y(jy) = y(jy) + alpha*temp
                jy = jy + incy
              ENDDO
            END IF
          END IF
C
          RETURN
C
C     End of ZGEMV
C
        END IF
      END IF
      END

