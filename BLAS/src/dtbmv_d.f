C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of dtbmv in forward (tangent) mode:
C   variations   of useful results: x
C   with respect to varying inputs: x a
C   RW status of diff variables: x:in-out a:in
C> \brief \b DTBMV
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       SUBROUTINE DTBMV(UPLO,TRANS,DIAG,N,K,A,LDA,X,INCX)
C
C       .. Scalar Arguments ..
C       INTEGER INCX,K,LDA,N
C       CHARACTER DIAG,TRANS,UPLO
C       ..
C       .. Array Arguments ..
C       DOUBLE PRECISION A(LDA,*),X(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C> DTBMV  performs one of the matrix-vector operations
C>
C>    x := A*x,   or   x := A**T*x,
C>
C> where x is an n element vector and  A is an n by n unit, or non-unit,
C> upper or lower triangular band matrix, with ( k + 1 ) diagonals.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] UPLO
C> \verbatim
C>          UPLO is CHARACTER*1
C>           On entry, UPLO specifies whether the matrix is an upper or
C>           lower triangular matrix as follows:
C>
C>              UPLO = 'U' or 'u'   A is an upper triangular matrix.
C>
C>              UPLO = 'L' or 'l'   A is a lower triangular matrix.
C> \endverbatim
C>
C> \param[in] TRANS
C> \verbatim
C>          TRANS is CHARACTER*1
C>           On entry, TRANS specifies the operation to be performed as
C>           follows:
C>
C>              TRANS = 'N' or 'n'   x := A*x.
C>
C>              TRANS = 'T' or 't'   x := A**T*x.
C>
C>              TRANS = 'C' or 'c'   x := A**T*x.
C> \endverbatim
C>
C> \param[in] DIAG
C> \verbatim
C>          DIAG is CHARACTER*1
C>           On entry, DIAG specifies whether or not A is unit
C>           triangular as follows:
C>
C>              DIAG = 'U' or 'u'   A is assumed to be unit triangular.
C>
C>              DIAG = 'N' or 'n'   A is not assumed to be unit
C>                                  triangular.
C> \endverbatim
C>
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>           On entry, N specifies the order of the matrix A.
C>           N must be at least zero.
C> \endverbatim
C>
C> \param[in] K
C> \verbatim
C>          K is INTEGER
C>           On entry with UPLO = 'U' or 'u', K specifies the number of
C>           super-diagonals of the matrix A.
C>           On entry with UPLO = 'L' or 'l', K specifies the number of
C>           sub-diagonals of the matrix A.
C>           K must satisfy  0 .le. K.
C> \endverbatim
C>
C> \param[in] A
C> \verbatim
C>          A is DOUBLE PRECISION array, dimension ( LDA, N )
C>           Before entry with UPLO = 'U' or 'u', the leading ( k + 1 )
C>           by n part of the array A must contain the upper triangular
C>           band part of the matrix of coefficients, supplied column by
C>           column, with the leading diagonal of the matrix in row
C>           ( k + 1 ) of the array, the first super-diagonal starting at
C>           position 2 in row k, and so on. The top left k by k triangle
C>           of the array A is not referenced.
C>           The following program segment will transfer an upper
C>           triangular band matrix from conventional full matrix storage
C>           to band storage:
C>
C>                 DO 20, J = 1, N
C>                    M = K + 1 - J
C>                    DO 10, I = MAX( 1, J - K ), J
C>                       A( M + I, J ) = matrix( I, J )
C>              10    CONTINUE
C>              20 CONTINUE
C>
C>           Before entry with UPLO = 'L' or 'l', the leading ( k + 1 )
C>           by n part of the array A must contain the lower triangular
C>           band part of the matrix of coefficients, supplied column by
C>           column, with the leading diagonal of the matrix in row 1 of
C>           the array, the first sub-diagonal starting at position 1 in
C>           row 2, and so on. The bottom right k by k triangle of the
C>           array A is not referenced.
C>           The following program segment will transfer a lower
C>           triangular band matrix from conventional full matrix storage
C>           to band storage:
C>
C>                 DO 20, J = 1, N
C>                    M = 1 - J
C>                    DO 10, I = J, MIN( N, J + K )
C>                       A( M + I, J ) = matrix( I, J )
C>              10    CONTINUE
C>              20 CONTINUE
C>
C>           Note that when DIAG = 'U' or 'u' the elements of the array A
C>           corresponding to the diagonal elements of the matrix are not
C>           referenced, but are assumed to be unity.
C> \endverbatim
C>
C> \param[in] LDA
C> \verbatim
C>          LDA is INTEGER
C>           On entry, LDA specifies the first dimension of A as declared
C>           in the calling (sub) program. LDA must be at least
C>           ( k + 1 ).
C> \endverbatim
C>
C> \param[in,out] X
C> \verbatim
C>          X is DOUBLE PRECISION array, dimension at least
C>           ( 1 + ( n - 1 )*abs( INCX ) ).
C>           Before entry, the incremented array X must contain the n
C>           element vector x. On exit, X is overwritten with the
C>           transformed vector x.
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>           On entry, INCX specifies the increment for the elements of
C>           X. INCX must not be zero.
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup tbmv
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>  Level 2 Blas routine.
C>  The vector and matrix arguments are not referenced when N = 0, or M = 0
C>
C>  -- Written on 22-October-1986.
C>     Jack Dongarra, Argonne National Lab.
C>     Jeremy Du Croz, Nag Central Office.
C>     Sven Hammarling, Nag Central Office.
C>     Richard Hanson, Sandia National Labs.
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE DTBMV_D(uplo, trans, diag, n, k, a, ad, lda, x, xd, 
     +                   incx)
      IMPLICIT NONE
C
C  -- Reference BLAS level2 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      INTEGER incx, k, lda, n
      CHARACTER diag, trans, uplo
C     ..
C     .. Array Arguments ..
      DOUBLE PRECISION a(lda, *), x(*)
      DOUBLE PRECISION ad(lda, *), xd(*)
C     ..
C
C  =====================================================================
C
C     .. Parameters ..
      DOUBLE PRECISION zero
      PARAMETER (zero=0.0d+0)
C     ..
C     .. Local Scalars ..
      DOUBLE PRECISION temp
      DOUBLE PRECISION tempd
      INTEGER i, info, ix, j, jx, kplus1, kx, l
      LOGICAL nounit
      EXTERNAL LSAME
C     ..
C     .. External Functions ..
      LOGICAL LSAME
C     ..
C     .. External Subroutines ..
      EXTERNAL XERBLA
C     ..
C     .. Intrinsic Functions ..
      INTRINSIC MAX, MIN
      INTEGER max1
      INTEGER max2
      INTEGER min1
      INTEGER min2
      INTEGER max3
      INTEGER max4
      INTEGER min3
      INTEGER min4
C     ..
C
C     Test the input parameters.
C
      info = 0
      IF (.NOT.LSAME(uplo, 'U') .AND. (.NOT.LSAME(uplo, 'L'))) THEN
        info = 1
      ELSE IF (.NOT.LSAME(trans, 'N') .AND. (.NOT.LSAME(trans, 'T')) 
     +    .AND. (.NOT.LSAME(trans, 'C'))) THEN
        info = 2
      ELSE IF (.NOT.LSAME(diag, 'U') .AND. (.NOT.LSAME(diag, 'N'))) THEN
        info = 3
      ELSE IF (n .LT. 0) THEN
        info = 4
      ELSE IF (k .LT. 0) THEN
        info = 5
      ELSE IF (lda .LT. k + 1) THEN
        info = 7
      ELSE IF (incx .EQ. 0) THEN
        info = 9
      END IF
      IF (info .NE. 0) THEN
        CALL XERBLA('DTBMV ', info)
        RETURN
      ELSE IF (n .EQ. 0) THEN
C
C     Quick return if possible.
C
        RETURN
      ELSE
C
        nounit = LSAME(diag, 'N')
C
C     Set up the start point in X if the increment is not unity. This
C     will be  ( N - 1 )*INCX   too small for descending loops.
C
        IF (incx .LE. 0) THEN
          kx = 1 - (n-1)*incx
        ELSE IF (incx .NE. 1) THEN
          kx = 1
        END IF
C
C     Start the operations. In this version the elements of A are
C     accessed sequentially with one pass through A.
C
        IF (LSAME(trans, 'N')) THEN
C
C         Form  x := A*x.
C
          IF (LSAME(uplo, 'U')) THEN
            kplus1 = k + 1
            IF (incx .EQ. 1) THEN
              DO j=1,n
                IF (x(j) .NE. zero) THEN
                  tempd = xd(j)
                  temp = x(j)
                  l = kplus1 - j
                  IF (1 .LT. j - k) THEN
                    max1 = j - k
                  ELSE
                    max1 = 1
                  END IF
                  DO i=max1,j-1
                    xd(i) = xd(i) + a(l+i, j)*tempd + temp*ad(l+i, j)
                    x(i) = x(i) + temp*a(l+i, j)
                  ENDDO
                  IF (nounit) THEN
                    xd(j) = a(kplus1, j)*xd(j) + x(j)*ad(kplus1, j)
                    x(j) = x(j)*a(kplus1, j)
                  END IF
                END IF
              ENDDO
            ELSE
              jx = kx
              DO j=1,n
                IF (x(jx) .NE. zero) THEN
                  tempd = xd(jx)
                  temp = x(jx)
                  ix = kx
                  l = kplus1 - j
                  IF (1 .LT. j - k) THEN
                    max2 = j - k
                  ELSE
                    max2 = 1
                  END IF
                  DO i=max2,j-1
                    xd(ix) = xd(ix) + a(l+i, j)*tempd + temp*ad(l+i, j)
                    x(ix) = x(ix) + temp*a(l+i, j)
                    ix = ix + incx
                  ENDDO
                  IF (nounit) THEN
                    xd(jx) = a(kplus1, j)*xd(jx) + x(jx)*ad(kplus1, j)
                    x(jx) = x(jx)*a(kplus1, j)
                  END IF
                END IF
                jx = jx + incx
                IF (j .GT. k) kx = kx + incx
              ENDDO
            END IF
          ELSE IF (incx .EQ. 1) THEN
            DO j=n,1,-1
              IF (x(j) .NE. zero) THEN
                tempd = xd(j)
                temp = x(j)
                l = 1 - j
                IF (n .GT. j + k) THEN
                  min1 = j + k
                ELSE
                  min1 = n
                END IF
                DO i=min1,j+1,-1
                  xd(i) = xd(i) + a(l+i, j)*tempd + temp*ad(l+i, j)
                  x(i) = x(i) + temp*a(l+i, j)
                ENDDO
                IF (nounit) THEN
                  xd(j) = a(1, j)*xd(j) + x(j)*ad(1, j)
                  x(j) = x(j)*a(1, j)
                END IF
              END IF
            ENDDO
          ELSE
            kx = kx + (n-1)*incx
            jx = kx
            DO j=n,1,-1
              IF (x(jx) .NE. zero) THEN
                tempd = xd(jx)
                temp = x(jx)
                ix = kx
                l = 1 - j
                IF (n .GT. j + k) THEN
                  min2 = j + k
                ELSE
                  min2 = n
                END IF
                DO i=min2,j+1,-1
                  xd(ix) = xd(ix) + a(l+i, j)*tempd + temp*ad(l+i, j)
                  x(ix) = x(ix) + temp*a(l+i, j)
                  ix = ix - incx
                ENDDO
                IF (nounit) THEN
                  xd(jx) = a(1, j)*xd(jx) + x(jx)*ad(1, j)
                  x(jx) = x(jx)*a(1, j)
                END IF
              END IF
              jx = jx - incx
              IF (n - j .GE. k) kx = kx - incx
            ENDDO
          END IF
        ELSE IF (LSAME(uplo, 'U')) THEN
C
C        Form  x := A**T*x.
C
          kplus1 = k + 1
          IF (incx .EQ. 1) THEN
            DO j=n,1,-1
              tempd = xd(j)
              temp = x(j)
              l = kplus1 - j
              IF (nounit) THEN
                tempd = a(kplus1, j)*tempd + temp*ad(kplus1, j)
                temp = temp*a(kplus1, j)
              END IF
              IF (1 .LT. j - k) THEN
                max3 = j - k
              ELSE
                max3 = 1
              END IF
              DO i=j-1,max3,-1
                tempd = tempd + x(i)*ad(l+i, j) + a(l+i, j)*xd(i)
                temp = temp + a(l+i, j)*x(i)
              ENDDO
              xd(j) = tempd
              x(j) = temp
            ENDDO
          ELSE
            kx = kx + (n-1)*incx
            jx = kx
            DO j=n,1,-1
              tempd = xd(jx)
              temp = x(jx)
              kx = kx - incx
              ix = kx
              l = kplus1 - j
              IF (nounit) THEN
                tempd = a(kplus1, j)*tempd + temp*ad(kplus1, j)
                temp = temp*a(kplus1, j)
              END IF
              IF (1 .LT. j - k) THEN
                max4 = j - k
              ELSE
                max4 = 1
              END IF
              DO i=j-1,max4,-1
                tempd = tempd + x(ix)*ad(l+i, j) + a(l+i, j)*xd(ix)
                temp = temp + a(l+i, j)*x(ix)
                ix = ix - incx
              ENDDO
              xd(jx) = tempd
              x(jx) = temp
              jx = jx - incx
            ENDDO
          END IF
        ELSE IF (incx .EQ. 1) THEN
          DO j=1,n
            tempd = xd(j)
            temp = x(j)
            l = 1 - j
            IF (nounit) THEN
              tempd = a(1, j)*tempd + temp*ad(1, j)
              temp = temp*a(1, j)
            END IF
            IF (n .GT. j + k) THEN
              min3 = j + k
            ELSE
              min3 = n
            END IF
            DO i=j+1,min3
              tempd = tempd + x(i)*ad(l+i, j) + a(l+i, j)*xd(i)
              temp = temp + a(l+i, j)*x(i)
            ENDDO
            xd(j) = tempd
            x(j) = temp
          ENDDO
        ELSE
          jx = kx
          DO j=1,n
            tempd = xd(jx)
            temp = x(jx)
            kx = kx + incx
            ix = kx
            l = 1 - j
            IF (nounit) THEN
              tempd = a(1, j)*tempd + temp*ad(1, j)
              temp = temp*a(1, j)
            END IF
            IF (n .GT. j + k) THEN
              min4 = j + k
            ELSE
              min4 = n
            END IF
            DO i=j+1,min4
              tempd = tempd + x(ix)*ad(l+i, j) + a(l+i, j)*xd(ix)
              temp = temp + a(l+i, j)*x(ix)
              ix = ix + incx
            ENDDO
            xd(jx) = tempd
            x(jx) = temp
            jx = jx + incx
          ENDDO
        END IF
C
        RETURN
C
C     End of DTBMV
C
      END IF
      END

