C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of dsymv in forward (tangent) mode:
C   variations   of useful results: y
C   with respect to varying inputs: alpha x y beta a
C   RW status of diff variables: alpha:in x:in y:in-out beta:in
C                a:in
C> \brief \b DSYMV
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       SUBROUTINE DSYMV(UPLO,N,ALPHA,A,LDA,X,INCX,BETA,Y,INCY)
C
C       .. Scalar Arguments ..
C       DOUBLE PRECISION ALPHA,BETA
C       INTEGER INCX,INCY,LDA,N
C       CHARACTER UPLO
C       ..
C       .. Array Arguments ..
C       DOUBLE PRECISION A(LDA,*),X(*),Y(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C> DSYMV  performs the matrix-vector  operation
C>
C>    y := alpha*A*x + beta*y,
C>
C> where alpha and beta are scalars, x and y are n element vectors and
C> A is an n by n symmetric matrix.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] UPLO
C> \verbatim
C>          UPLO is CHARACTER*1
C>           On entry, UPLO specifies whether the upper or lower
C>           triangular part of the array A is to be referenced as
C>           follows:
C>
C>              UPLO = 'U' or 'u'   Only the upper triangular part of A
C>                                  is to be referenced.
C>
C>              UPLO = 'L' or 'l'   Only the lower triangular part of A
C>                                  is to be referenced.
C> \endverbatim
C>
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>           On entry, N specifies the order of the matrix A.
C>           N must be at least zero.
C> \endverbatim
C>
C> \param[in] ALPHA
C> \verbatim
C>          ALPHA is DOUBLE PRECISION.
C>           On entry, ALPHA specifies the scalar alpha.
C> \endverbatim
C>
C> \param[in] A
C> \verbatim
C>          A is DOUBLE PRECISION array, dimension ( LDA, N )
C>           Before entry with  UPLO = 'U' or 'u', the leading n by n
C>           upper triangular part of the array A must contain the upper
C>           triangular part of the symmetric matrix and the strictly
C>           lower triangular part of A is not referenced.
C>           Before entry with UPLO = 'L' or 'l', the leading n by n
C>           lower triangular part of the array A must contain the lower
C>           triangular part of the symmetric matrix and the strictly
C>           upper triangular part of A is not referenced.
C> \endverbatim
C>
C> \param[in] LDA
C> \verbatim
C>          LDA is INTEGER
C>           On entry, LDA specifies the first dimension of A as declared
C>           in the calling (sub) program. LDA must be at least
C>           max( 1, n ).
C> \endverbatim
C>
C> \param[in] X
C> \verbatim
C>          X is DOUBLE PRECISION array, dimension at least
C>           ( 1 + ( n - 1 )*abs( INCX ) ).
C>           Before entry, the incremented array X must contain the n
C>           element vector x.
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>           On entry, INCX specifies the increment for the elements of
C>           X. INCX must not be zero.
C> \endverbatim
C>
C> \param[in] BETA
C> \verbatim
C>          BETA is DOUBLE PRECISION.
C>           On entry, BETA specifies the scalar beta. When BETA is
C>           supplied as zero then Y need not be set on input.
C> \endverbatim
C>
C> \param[in,out] Y
C> \verbatim
C>          Y is DOUBLE PRECISION array, dimension at least
C>           ( 1 + ( n - 1 )*abs( INCY ) ).
C>           Before entry, the incremented array Y must contain the n
C>           element vector y. On exit, Y is overwritten by the updated
C>           vector y.
C> \endverbatim
C>
C> \param[in] INCY
C> \verbatim
C>          INCY is INTEGER
C>           On entry, INCY specifies the increment for the elements of
C>           Y. INCY must not be zero.
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup hemv
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>  Level 2 Blas routine.
C>  The vector and matrix arguments are not referenced when N = 0, or M = 0
C>
C>  -- Written on 22-October-1986.
C>     Jack Dongarra, Argonne National Lab.
C>     Jeremy Du Croz, Nag Central Office.
C>     Sven Hammarling, Nag Central Office.
C>     Richard Hanson, Sandia National Labs.
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE DSYMV_D(uplo, n, alpha, alphad, a, ad, lda, x, xd, incx
     +                   , beta, betad, y, yd, incy)
      IMPLICIT NONE
C
C  -- Reference BLAS level2 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      DOUBLE PRECISION alpha, beta
      DOUBLE PRECISION alphad, betad
      INTEGER incx, incy, lda, n
      CHARACTER uplo
C     ..
C     .. Array Arguments ..
      DOUBLE PRECISION a(lda, *), x(*), y(*)
      DOUBLE PRECISION ad(lda, *), xd(*), yd(*)
C     ..
C
C  =====================================================================
C
C     .. Parameters ..
      DOUBLE PRECISION one, zero
      PARAMETER (one=1.0d+0, zero=0.0d+0)
C     ..
C     .. Local Scalars ..
      DOUBLE PRECISION temp1, temp2
      DOUBLE PRECISION temp1d, temp2d
      INTEGER i, info, ix, iy, j, jx, jy, kx, ky
      EXTERNAL LSAME
C     ..
C     .. External Functions ..
      LOGICAL LSAME
C     ..
C     .. External Subroutines ..
      EXTERNAL XERBLA
C     ..
C     .. Intrinsic Functions ..
      INTRINSIC MAX
      INTEGER max1
C     ..
C
C     Test the input parameters.
C
      info = 0
      IF (.NOT.LSAME(uplo, 'U') .AND. (.NOT.LSAME(uplo, 'L'))) THEN
        info = 1
      ELSE IF (n .LT. 0) THEN
        info = 2
      ELSE
        IF (1 .LT. n) THEN
          max1 = n
        ELSE
          max1 = 1
        END IF
        IF (lda .LT. max1) THEN
          info = 5
        ELSE IF (incx .EQ. 0) THEN
          info = 7
        ELSE IF (incy .EQ. 0) THEN
          info = 10
        END IF
      END IF
      IF (info .NE. 0) THEN
        CALL XERBLA('DSYMV ', info)
        RETURN
      ELSE IF (n .EQ. 0 .OR. (alpha .EQ. zero .AND. beta .EQ. one)) THEN
C
C     Quick return if possible.
C
        RETURN
      ELSE
C
C     Set up the start points in  X  and  Y.
C
        IF (incx .GT. 0) THEN
          kx = 1
        ELSE
          kx = 1 - (n-1)*incx
        END IF
        IF (incy .GT. 0) THEN
          ky = 1
        ELSE
          ky = 1 - (n-1)*incy
        END IF
C
C     Start the operations. In this version the elements of A are
C     accessed sequentially with one pass through the triangular part
C     of A.
C
C     First form  y := beta*y.
C
        IF (beta .NE. one) THEN
          IF (incy .EQ. 1) THEN
            IF (beta .EQ. zero) THEN
              DO i=1,n
                yd(i) = 0.D0
                y(i) = zero
              ENDDO
            ELSE
              DO i=1,n
                yd(i) = y(i)*betad + beta*yd(i)
                y(i) = beta*y(i)
              ENDDO
            END IF
          ELSE
            iy = ky
            IF (beta .EQ. zero) THEN
              DO i=1,n
                yd(iy) = 0.D0
                y(iy) = zero
                iy = iy + incy
              ENDDO
            ELSE
              DO i=1,n
                yd(iy) = y(iy)*betad + beta*yd(iy)
                y(iy) = beta*y(iy)
                iy = iy + incy
              ENDDO
            END IF
          END IF
        END IF
        IF (alpha .EQ. zero) THEN
          RETURN
        ELSE
          IF (LSAME(uplo, 'U')) THEN
C
C        Form  y  when A is stored in upper triangle.
C
            IF (incx .EQ. 1 .AND. incy .EQ. 1) THEN
              DO j=1,n
                temp1d = x(j)*alphad + alpha*xd(j)
                temp1 = alpha*x(j)
                temp2 = zero
                temp2d = 0.D0
                DO i=1,j-1
                  yd(i) = yd(i) + a(i, j)*temp1d + temp1*ad(i, j)
                  y(i) = y(i) + temp1*a(i, j)
                  temp2d = temp2d + x(i)*ad(i, j) + a(i, j)*xd(i)
                  temp2 = temp2 + a(i, j)*x(i)
                ENDDO
                yd(j) = yd(j) + a(j, j)*temp1d + temp1*ad(j, j) + temp2*
     +            alphad + alpha*temp2d
                y(j) = y(j) + temp1*a(j, j) + alpha*temp2
              ENDDO
            ELSE
              jx = kx
              jy = ky
              DO j=1,n
                temp1d = x(jx)*alphad + alpha*xd(jx)
                temp1 = alpha*x(jx)
                temp2 = zero
                ix = kx
                iy = ky
                temp2d = 0.D0
                DO i=1,j-1
                  yd(iy) = yd(iy) + a(i, j)*temp1d + temp1*ad(i, j)
                  y(iy) = y(iy) + temp1*a(i, j)
                  temp2d = temp2d + x(ix)*ad(i, j) + a(i, j)*xd(ix)
                  temp2 = temp2 + a(i, j)*x(ix)
                  ix = ix + incx
                  iy = iy + incy
                ENDDO
                yd(jy) = yd(jy) + a(j, j)*temp1d + temp1*ad(j, j) + 
     +            temp2*alphad + alpha*temp2d
                y(jy) = y(jy) + temp1*a(j, j) + alpha*temp2
                jx = jx + incx
                jy = jy + incy
              ENDDO
            END IF
          ELSE IF (incx .EQ. 1 .AND. incy .EQ. 1) THEN
C
C        Form  y  when A is stored in lower triangle.
C
            DO j=1,n
              temp1d = x(j)*alphad + alpha*xd(j)
              temp1 = alpha*x(j)
              temp2 = zero
              yd(j) = yd(j) + a(j, j)*temp1d + temp1*ad(j, j)
              y(j) = y(j) + temp1*a(j, j)
              temp2d = 0.D0
              DO i=j+1,n
                yd(i) = yd(i) + a(i, j)*temp1d + temp1*ad(i, j)
                y(i) = y(i) + temp1*a(i, j)
                temp2d = temp2d + x(i)*ad(i, j) + a(i, j)*xd(i)
                temp2 = temp2 + a(i, j)*x(i)
              ENDDO
              yd(j) = yd(j) + temp2*alphad + alpha*temp2d
              y(j) = y(j) + alpha*temp2
            ENDDO
          ELSE
            jx = kx
            jy = ky
            DO j=1,n
              temp1d = x(jx)*alphad + alpha*xd(jx)
              temp1 = alpha*x(jx)
              temp2 = zero
              yd(jy) = yd(jy) + a(j, j)*temp1d + temp1*ad(j, j)
              y(jy) = y(jy) + temp1*a(j, j)
              ix = jx
              iy = jy
              temp2d = 0.D0
              DO i=j+1,n
                ix = ix + incx
                iy = iy + incy
                yd(iy) = yd(iy) + a(i, j)*temp1d + temp1*ad(i, j)
                y(iy) = y(iy) + temp1*a(i, j)
                temp2d = temp2d + x(ix)*ad(i, j) + a(i, j)*xd(ix)
                temp2 = temp2 + a(i, j)*x(ix)
              ENDDO
              yd(jy) = yd(jy) + temp2*alphad + alpha*temp2d
              y(jy) = y(jy) + alpha*temp2
              jx = jx + incx
              jy = jy + incy
            ENDDO
          END IF
C
          RETURN
C
C     End of DSYMV
C
        END IF
      END IF
      END

