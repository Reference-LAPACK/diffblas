C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of ztrsm in forward (tangent) mode (with options multiDirectional):
C   variations   of useful results: b
C   with respect to varying inputs: alpha a b
C   RW status of diff variables: alpha:in a:in b:in-out
C> \brief \b ZTRSM
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       SUBROUTINE ZTRSM(SIDE,UPLO,TRANSA,DIAG,M,N,ALPHA,A,LDA,B,LDB)
C
C       .. Scalar Arguments ..
C       COMPLEX*16 ALPHA
C       INTEGER LDA,LDB,M,N
C       CHARACTER DIAG,SIDE,TRANSA,UPLO
C       ..
C       .. Array Arguments ..
C       COMPLEX*16 A(LDA,*),B(LDB,*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C> ZTRSM  solves one of the matrix equations
C>
C>    op( A )*X = alpha*B,   or   X*op( A ) = alpha*B,
C>
C> where alpha is a scalar, X and B are m by n matrices, A is a unit, or
C> non-unit,  upper or lower triangular matrix  and  op( A )  is one  of
C>
C>    op( A ) = A   or   op( A ) = A**T   or   op( A ) = A**H.
C>
C> The matrix X is overwritten on B.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] SIDE
C> \verbatim
C>          SIDE is CHARACTER*1
C>           On entry, SIDE specifies whether op( A ) appears on the left
C>           or right of X as follows:
C>
C>              SIDE = 'L' or 'l'   op( A )*X = alpha*B.
C>
C>              SIDE = 'R' or 'r'   X*op( A ) = alpha*B.
C> \endverbatim
C>
C> \param[in] UPLO
C> \verbatim
C>          UPLO is CHARACTER*1
C>           On entry, UPLO specifies whether the matrix A is an upper or
C>           lower triangular matrix as follows:
C>
C>              UPLO = 'U' or 'u'   A is an upper triangular matrix.
C>
C>              UPLO = 'L' or 'l'   A is a lower triangular matrix.
C> \endverbatim
C>
C> \param[in] TRANSA
C> \verbatim
C>          TRANSA is CHARACTER*1
C>           On entry, TRANSA specifies the form of op( A ) to be used in
C>           the matrix multiplication as follows:
C>
C>              TRANSA = 'N' or 'n'   op( A ) = A.
C>
C>              TRANSA = 'T' or 't'   op( A ) = A**T.
C>
C>              TRANSA = 'C' or 'c'   op( A ) = A**H.
C> \endverbatim
C>
C> \param[in] DIAG
C> \verbatim
C>          DIAG is CHARACTER*1
C>           On entry, DIAG specifies whether or not A is unit triangular
C>           as follows:
C>
C>              DIAG = 'U' or 'u'   A is assumed to be unit triangular.
C>
C>              DIAG = 'N' or 'n'   A is not assumed to be unit
C>                                  triangular.
C> \endverbatim
C>
C> \param[in] M
C> \verbatim
C>          M is INTEGER
C>           On entry, M specifies the number of rows of B. M must be at
C>           least zero.
C> \endverbatim
C>
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>           On entry, N specifies the number of columns of B.  N must be
C>           at least zero.
C> \endverbatim
C>
C> \param[in] ALPHA
C> \verbatim
C>          ALPHA is COMPLEX*16
C>           On entry,  ALPHA specifies the scalar  alpha. When  alpha is
C>           zero then  A is not referenced and  B need not be set before
C>           entry.
C> \endverbatim
C>
C> \param[in] A
C> \verbatim
C>          A is COMPLEX*16 array, dimension ( LDA, k ),
C>           where k is m when SIDE = 'L' or 'l'
C>             and k is n when SIDE = 'R' or 'r'.
C>           Before entry  with  UPLO = 'U' or 'u',  the  leading  k by k
C>           upper triangular part of the array  A must contain the upper
C>           triangular matrix  and the strictly lower triangular part of
C>           A is not referenced.
C>           Before entry  with  UPLO = 'L' or 'l',  the  leading  k by k
C>           lower triangular part of the array  A must contain the lower
C>           triangular matrix  and the strictly upper triangular part of
C>           A is not referenced.
C>           Note that when  DIAG = 'U' or 'u',  the diagonal elements of
C>           A  are not referenced either,  but are assumed to be  unity.
C> \endverbatim
C>
C> \param[in] LDA
C> \verbatim
C>          LDA is INTEGER
C>           On entry, LDA specifies the first dimension of A as declared
C>           in the calling (sub) program.  When  SIDE = 'L' or 'l'  then
C>           LDA  must be at least  max( 1, m ),  when  SIDE = 'R' or 'r'
C>           then LDA must be at least max( 1, n ).
C> \endverbatim
C>
C> \param[in,out] B
C> \verbatim
C>          B is COMPLEX*16 array, dimension ( LDB, N )
C>           Before entry,  the leading  m by n part of the array  B must
C>           contain  the  right-hand  side  matrix  B,  and  on exit  is
C>           overwritten by the solution matrix  X.
C> \endverbatim
C>
C> \param[in] LDB
C> \verbatim
C>          LDB is INTEGER
C>           On entry, LDB specifies the first dimension of B as declared
C>           in  the  calling  (sub)  program.   LDB  must  be  at  least
C>           max( 1, m ).
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup trsm
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>  Level 3 Blas routine.
C>
C>  -- Written on 8-February-1989.
C>     Jack Dongarra, Argonne National Laboratory.
C>     Iain Duff, AERE Harwell.
C>     Jeremy Du Croz, Numerical Algorithms Group Ltd.
C>     Sven Hammarling, Numerical Algorithms Group Ltd.
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE ZTRSM_DV(side, uplo, transa, diag, m, n, alpha, alphad
     +                    , a, ad, lda, b, bd, ldb, nbdirs)
      IMPLICIT NONE
      INCLUDE 'DIFFSIZES.inc'
C  Hint: nbdirsmax should be the maximum number of differentiation directions
C
C  -- Reference BLAS level3 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      COMPLEX*16 alpha
      COMPLEX*16 alphad(nbdirsmax)
      INTEGER lda, ldb, m, n
      CHARACTER diag, side, transa, uplo
C     ..
C     .. Array Arguments ..
      COMPLEX*16 a(lda, *), b(ldb, *)
      COMPLEX*16 ad(nbdirsmax, lda, *), bd(nbdirsmax, ldb, *)
      EXTERNAL LSAME
C     ..
C
C  =====================================================================
C
C     .. External Functions ..
      LOGICAL LSAME
C     ..
C     .. External Subroutines ..
      EXTERNAL XERBLA
C     ..
C     .. Intrinsic Functions ..
      INTRINSIC DCONJG, MAX
C     ..
C     .. Local Scalars ..
      COMPLEX*16 temp
      COMPLEX*16 tempd(nbdirsmax)
      INTEGER i, info, j, k, nrowa
      LOGICAL lside, noconj, nounit, upper
C     ..
C     .. Parameters ..
      COMPLEX*16 one
      PARAMETER (one=(1.0d+0,0.0d+0))
      COMPLEX*16 zero
      PARAMETER (zero=(0.0d+0,0.0d+0))
      INTEGER max1
      INTEGER max2
      INTEGER nd
      COMPLEX*16 temp0
      DOUBLE COMPLEX temp1
      INTEGER nbdirs
C     ..
C
C     Test the input parameters.
C
C     Check 0 < nbdirs <= nbdirsmax (required by DIFFSIZES.inc)
      IF (nbdirs.LE.0 .OR. nbdirs.GT.nbdirsmax) THEN
        WRITE(*,'(A,I0,A,I0,A)') 'Error: nbdirs=', nbdirs,
     +  ' must be in 1..nbdirsmax=', nbdirsmax, '. Stopping.'
        STOP 1
      END IF
C
      lside = LSAME(side, 'L')
      IF (lside) THEN
        nrowa = m
      ELSE
        nrowa = n
      END IF
      noconj = LSAME(transa, 'T')
      nounit = LSAME(diag, 'N')
      upper = LSAME(uplo, 'U')
C
      info = 0
      IF (.NOT.lside .AND. (.NOT.LSAME(side, 'R'))) THEN
        info = 1
      ELSE IF (.NOT.upper .AND. (.NOT.LSAME(uplo, 'L'))) THEN
        info = 2
      ELSE IF (.NOT.LSAME(transa, 'N') .AND. (.NOT.LSAME(transa, 'T')) 
     +    .AND. (.NOT.LSAME(transa, 'C'))) THEN
        info = 3
      ELSE IF (.NOT.LSAME(diag, 'U') .AND. (.NOT.LSAME(diag, 'N'))) THEN
        info = 4
      ELSE IF (m .LT. 0) THEN
        info = 5
      ELSE IF (n .LT. 0) THEN
        info = 6
      ELSE
        IF (1 .LT. nrowa) THEN
          max1 = nrowa
        ELSE
          max1 = 1
        END IF
        IF (lda .LT. max1) THEN
          info = 9
        ELSE
          IF (1 .LT. m) THEN
            max2 = m
          ELSE
            max2 = 1
          END IF
          IF (ldb .LT. max2) info = 11
        END IF
      END IF
      IF (info .NE. 0) THEN
        CALL XERBLA('ZTRSM ', info)
        RETURN
      ELSE IF (m .EQ. 0 .OR. n .EQ. 0) THEN
C
C     Quick return if possible.
C
        RETURN
      ELSE IF (alpha .EQ. zero) THEN
C
C     And when  alpha.eq.zero.
C
        DO j=1,n
          DO i=1,m
            DO nd=1,nbdirs
              bd(nd, i, j) = 0.0
            ENDDO
            b(i, j) = zero
          ENDDO
        ENDDO
        RETURN
      ELSE
C
C     Start the operations.
C
        IF (lside) THEN
          IF (LSAME(transa, 'N')) THEN
C
C           Form  B := alpha*inv( A )*B.
C
            IF (upper) THEN
              DO j=1,n
                IF (alpha .NE. one) THEN
                  DO i=1,m
                    DO nd=1,nbdirs
                      bd(nd, i, j) = b(i, j)*alphad(nd) + alpha*bd(nd, i
     +                  , j)
                    ENDDO
                    b(i, j) = alpha*b(i, j)
                  ENDDO
                END IF
                DO k=m,1,-1
                  IF (b(k, j) .NE. zero) THEN
                    IF (nounit) THEN
                      temp0 = b(k, j)/a(k, k)
                      DO nd=1,nbdirs
                        bd(nd, k, j) = (bd(nd, k, j)-temp0*ad(nd, k, k))
     +                    /a(k, k)
                      ENDDO
                      b(k, j) = temp0
                    END IF
                    DO i=1,k-1
                      DO nd=1,nbdirs
                        bd(nd, i, j) = bd(nd, i, j) - a(i, k)*bd(nd, k, 
     +                    j) - b(k, j)*ad(nd, i, k)
                      ENDDO
                      b(i, j) = b(i, j) - b(k, j)*a(i, k)
                    ENDDO
                  END IF
                ENDDO
              ENDDO
            ELSE
              DO j=1,n
                IF (alpha .NE. one) THEN
                  DO i=1,m
                    DO nd=1,nbdirs
                      bd(nd, i, j) = b(i, j)*alphad(nd) + alpha*bd(nd, i
     +                  , j)
                    ENDDO
                    b(i, j) = alpha*b(i, j)
                  ENDDO
                END IF
                DO k=1,m
                  IF (b(k, j) .NE. zero) THEN
                    IF (nounit) THEN
                      temp0 = b(k, j)/a(k, k)
                      DO nd=1,nbdirs
                        bd(nd, k, j) = (bd(nd, k, j)-temp0*ad(nd, k, k))
     +                    /a(k, k)
                      ENDDO
                      b(k, j) = temp0
                    END IF
                    DO i=k+1,m
                      DO nd=1,nbdirs
                        bd(nd, i, j) = bd(nd, i, j) - a(i, k)*bd(nd, k, 
     +                    j) - b(k, j)*ad(nd, i, k)
                      ENDDO
                      b(i, j) = b(i, j) - b(k, j)*a(i, k)
                    ENDDO
                  END IF
                ENDDO
              ENDDO
            END IF
          ELSE IF (upper) THEN
C
C           Form  B := alpha*inv( A**T )*B
C           or    B := alpha*inv( A**H )*B.
C
            DO j=1,n
              DO i=1,m
                DO nd=1,nbdirs
                  tempd(nd) = b(i, j)*alphad(nd) + alpha*bd(nd, i, j)
                ENDDO
                temp = alpha*b(i, j)
                IF (noconj) THEN
                  DO k=1,i-1
                    DO nd=1,nbdirs
                      tempd(nd) = tempd(nd) - b(k, j)*ad(nd, k, i) - a(k
     +                  , i)*bd(nd, k, j)
                    ENDDO
                    temp = temp - a(k, i)*b(k, j)
                  ENDDO
                  IF (nounit) THEN
                    temp0 = temp/a(i, i)
                    DO nd=1,nbdirs
                      tempd(nd) = (tempd(nd)-temp0*ad(nd, i, i))/a(i, i)
                    ENDDO
                    temp = temp0
                  END IF
                ELSE
                  DO k=1,i-1
                    temp1 = DCONJG(a(k, i))
                    DO nd=1,nbdirs
                      tempd(nd) = tempd(nd) - b(k, j)*DCONJG(ad(nd, k, i
     +                  )) - temp1*bd(nd, k, j)
                    ENDDO
                    temp = temp - temp1*b(k, j)
                  ENDDO
                  IF (nounit) THEN
                    temp1 = DCONJG(a(i, i))
                    DO nd=1,nbdirs
                      tempd(nd) = (tempd(nd)-temp*DCONJG(ad(nd, i, i))/
     +                  temp1)/temp1
                    ENDDO
                    temp = temp/temp1
                  END IF
                END IF
                DO nd=1,nbdirs
                  bd(nd, i, j) = tempd(nd)
                ENDDO
                b(i, j) = temp
              ENDDO
            ENDDO
          ELSE
            DO j=1,n
              DO i=m,1,-1
                DO nd=1,nbdirs
                  tempd(nd) = b(i, j)*alphad(nd) + alpha*bd(nd, i, j)
                ENDDO
                temp = alpha*b(i, j)
                IF (noconj) THEN
                  DO k=i+1,m
                    DO nd=1,nbdirs
                      tempd(nd) = tempd(nd) - b(k, j)*ad(nd, k, i) - a(k
     +                  , i)*bd(nd, k, j)
                    ENDDO
                    temp = temp - a(k, i)*b(k, j)
                  ENDDO
                  IF (nounit) THEN
                    temp0 = temp/a(i, i)
                    DO nd=1,nbdirs
                      tempd(nd) = (tempd(nd)-temp0*ad(nd, i, i))/a(i, i)
                    ENDDO
                    temp = temp0
                  END IF
                ELSE
                  DO k=i+1,m
                    temp1 = DCONJG(a(k, i))
                    DO nd=1,nbdirs
                      tempd(nd) = tempd(nd) - b(k, j)*DCONJG(ad(nd, k, i
     +                  )) - temp1*bd(nd, k, j)
                    ENDDO
                    temp = temp - temp1*b(k, j)
                  ENDDO
                  IF (nounit) THEN
                    temp1 = DCONJG(a(i, i))
                    DO nd=1,nbdirs
                      tempd(nd) = (tempd(nd)-temp*DCONJG(ad(nd, i, i))/
     +                  temp1)/temp1
                    ENDDO
                    temp = temp/temp1
                  END IF
                END IF
                DO nd=1,nbdirs
                  bd(nd, i, j) = tempd(nd)
                ENDDO
                b(i, j) = temp
              ENDDO
            ENDDO
          END IF
        ELSE IF (LSAME(transa, 'N')) THEN
C
C           Form  B := alpha*B*inv( A ).
C
          IF (upper) THEN
            DO j=1,n
              IF (alpha .NE. one) THEN
                DO i=1,m
                  DO nd=1,nbdirs
                    bd(nd, i, j) = b(i, j)*alphad(nd) + alpha*bd(nd, i, 
     +                j)
                  ENDDO
                  b(i, j) = alpha*b(i, j)
                ENDDO
              END IF
              DO k=1,j-1
                IF (a(k, j) .NE. zero) THEN
                  DO i=1,m
                    DO nd=1,nbdirs
                      bd(nd, i, j) = bd(nd, i, j) - b(i, k)*ad(nd, k, j)
     +                  - a(k, j)*bd(nd, i, k)
                    ENDDO
                    b(i, j) = b(i, j) - a(k, j)*b(i, k)
                  ENDDO
                END IF
              ENDDO
              IF (nounit) THEN
                temp0 = one/a(j, j)
                DO nd=1,nbdirs
                  tempd(nd) = -(temp0*ad(nd, j, j)/a(j, j))
                ENDDO
                temp = temp0
                DO i=1,m
                  DO nd=1,nbdirs
                    bd(nd, i, j) = b(i, j)*tempd(nd) + temp*bd(nd, i, j)
                  ENDDO
                  b(i, j) = temp*b(i, j)
                ENDDO
              END IF
            ENDDO
          ELSE
            DO j=n,1,-1
              IF (alpha .NE. one) THEN
                DO i=1,m
                  DO nd=1,nbdirs
                    bd(nd, i, j) = b(i, j)*alphad(nd) + alpha*bd(nd, i, 
     +                j)
                  ENDDO
                  b(i, j) = alpha*b(i, j)
                ENDDO
              END IF
              DO k=j+1,n
                IF (a(k, j) .NE. zero) THEN
                  DO i=1,m
                    DO nd=1,nbdirs
                      bd(nd, i, j) = bd(nd, i, j) - b(i, k)*ad(nd, k, j)
     +                  - a(k, j)*bd(nd, i, k)
                    ENDDO
                    b(i, j) = b(i, j) - a(k, j)*b(i, k)
                  ENDDO
                END IF
              ENDDO
              IF (nounit) THEN
                temp0 = one/a(j, j)
                DO nd=1,nbdirs
                  tempd(nd) = -(temp0*ad(nd, j, j)/a(j, j))
                ENDDO
                temp = temp0
                DO i=1,m
                  DO nd=1,nbdirs
                    bd(nd, i, j) = b(i, j)*tempd(nd) + temp*bd(nd, i, j)
                  ENDDO
                  b(i, j) = temp*b(i, j)
                ENDDO
              END IF
            ENDDO
          END IF
        ELSE IF (upper) THEN
C
C           Form  B := alpha*B*inv( A**T )
C           or    B := alpha*B*inv( A**H ).
C
          DO k=n,1,-1
            IF (nounit) THEN
              IF (noconj) THEN
                temp0 = one/a(k, k)
                DO nd=1,nbdirs
                  tempd(nd) = -(temp0*ad(nd, k, k)/a(k, k))
                ENDDO
                temp = temp0
              ELSE
                temp0 = one/DCONJG(a(k, k))
                DO nd=1,nbdirs
                  tempd(nd) = -(temp0*DCONJG(ad(nd, k, k))/DCONJG(a(k, k
     +              )))
                ENDDO
                temp = temp0
              END IF
              DO i=1,m
                DO nd=1,nbdirs
                  bd(nd, i, k) = b(i, k)*tempd(nd) + temp*bd(nd, i, k)
                ENDDO
                b(i, k) = temp*b(i, k)
              ENDDO
            END IF
            DO j=1,k-1
              IF (a(j, k) .NE. zero) THEN
                IF (noconj) THEN
                  DO nd=1,nbdirs
                    tempd(nd) = ad(nd, j, k)
                  ENDDO
                  temp = a(j, k)
                ELSE
                  DO nd=1,nbdirs
                    tempd(nd) = DCONJG(ad(nd, j, k))
                  ENDDO
                  temp = DCONJG(a(j, k))
                END IF
                DO i=1,m
                  DO nd=1,nbdirs
                    bd(nd, i, j) = bd(nd, i, j) - b(i, k)*tempd(nd) - 
     +                temp*bd(nd, i, k)
                  ENDDO
                  b(i, j) = b(i, j) - temp*b(i, k)
                ENDDO
              END IF
            ENDDO
            IF (alpha .NE. one) THEN
              DO i=1,m
                DO nd=1,nbdirs
                  bd(nd, i, k) = b(i, k)*alphad(nd) + alpha*bd(nd, i, k)
                ENDDO
                b(i, k) = alpha*b(i, k)
              ENDDO
            END IF
          ENDDO
        ELSE
          DO k=1,n
            IF (nounit) THEN
              IF (noconj) THEN
                temp0 = one/a(k, k)
                DO nd=1,nbdirs
                  tempd(nd) = -(temp0*ad(nd, k, k)/a(k, k))
                ENDDO
                temp = temp0
              ELSE
                temp0 = one/DCONJG(a(k, k))
                DO nd=1,nbdirs
                  tempd(nd) = -(temp0*DCONJG(ad(nd, k, k))/DCONJG(a(k, k
     +              )))
                ENDDO
                temp = temp0
              END IF
              DO i=1,m
                DO nd=1,nbdirs
                  bd(nd, i, k) = b(i, k)*tempd(nd) + temp*bd(nd, i, k)
                ENDDO
                b(i, k) = temp*b(i, k)
              ENDDO
            END IF
            DO j=k+1,n
              IF (a(j, k) .NE. zero) THEN
                IF (noconj) THEN
                  DO nd=1,nbdirs
                    tempd(nd) = ad(nd, j, k)
                  ENDDO
                  temp = a(j, k)
                ELSE
                  DO nd=1,nbdirs
                    tempd(nd) = DCONJG(ad(nd, j, k))
                  ENDDO
                  temp = DCONJG(a(j, k))
                END IF
                DO i=1,m
                  DO nd=1,nbdirs
                    bd(nd, i, j) = bd(nd, i, j) - b(i, k)*tempd(nd) - 
     +                temp*bd(nd, i, k)
                  ENDDO
                  b(i, j) = b(i, j) - temp*b(i, k)
                ENDDO
              END IF
            ENDDO
            IF (alpha .NE. one) THEN
              DO i=1,m
                DO nd=1,nbdirs
                  bd(nd, i, k) = b(i, k)*alphad(nd) + alpha*bd(nd, i, k)
                ENDDO
                b(i, k) = alpha*b(i, k)
              ENDDO
            END IF
          ENDDO
        END IF
C
        RETURN
C
C     End of ZTRSM
C
      END IF
      END

