C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of ssyr2 in reverse (adjoint) mode:
C   gradient     of useful results: a
C   with respect to varying inputs: alpha x y a
C   RW status of diff variables: alpha:out x:out y:out a:in-out
C> \brief \b SSYR2
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       SUBROUTINE SSYR2(UPLO,N,ALPHA,X,INCX,Y,INCY,A,LDA)
C
C       .. Scalar Arguments ..
C       REAL ALPHA
C       INTEGER INCX,INCY,LDA,N
C       CHARACTER UPLO
C       ..
C       .. Array Arguments ..
C       REAL A(LDA,*),X(*),Y(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C> SSYR2  performs the symmetric rank 2 operation
C>
C>    A := alpha*x*y**T + alpha*y*x**T + A,
C>
C> where alpha is a scalar, x and y are n element vectors and A is an n
C> by n symmetric matrix.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] UPLO
C> \verbatim
C>          UPLO is CHARACTER*1
C>           On entry, UPLO specifies whether the upper or lower
C>           triangular part of the array A is to be referenced as
C>           follows:
C>
C>              UPLO = 'U' or 'u'   Only the upper triangular part of A
C>                                  is to be referenced.
C>
C>              UPLO = 'L' or 'l'   Only the lower triangular part of A
C>                                  is to be referenced.
C> \endverbatim
C>
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>           On entry, N specifies the order of the matrix A.
C>           N must be at least zero.
C> \endverbatim
C>
C> \param[in] ALPHA
C> \verbatim
C>          ALPHA is REAL
C>           On entry, ALPHA specifies the scalar alpha.
C> \endverbatim
C>
C> \param[in] X
C> \verbatim
C>          X is REAL array, dimension at least
C>           ( 1 + ( n - 1 )*abs( INCX ) ).
C>           Before entry, the incremented array X must contain the n
C>           element vector x.
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>           On entry, INCX specifies the increment for the elements of
C>           X. INCX must not be zero.
C> \endverbatim
C>
C> \param[in] Y
C> \verbatim
C>          Y is REAL array, dimension at least
C>           ( 1 + ( n - 1 )*abs( INCY ) ).
C>           Before entry, the incremented array Y must contain the n
C>           element vector y.
C> \endverbatim
C>
C> \param[in] INCY
C> \verbatim
C>          INCY is INTEGER
C>           On entry, INCY specifies the increment for the elements of
C>           Y. INCY must not be zero.
C> \endverbatim
C>
C> \param[in,out] A
C> \verbatim
C>          A is REAL array, dimension ( LDA, N )
C>           Before entry with  UPLO = 'U' or 'u', the leading n by n
C>           upper triangular part of the array A must contain the upper
C>           triangular part of the symmetric matrix and the strictly
C>           lower triangular part of A is not referenced. On exit, the
C>           upper triangular part of the array A is overwritten by the
C>           upper triangular part of the updated matrix.
C>           Before entry with UPLO = 'L' or 'l', the leading n by n
C>           lower triangular part of the array A must contain the lower
C>           triangular part of the symmetric matrix and the strictly
C>           upper triangular part of A is not referenced. On exit, the
C>           lower triangular part of the array A is overwritten by the
C>           lower triangular part of the updated matrix.
C> \endverbatim
C>
C> \param[in] LDA
C> \verbatim
C>          LDA is INTEGER
C>           On entry, LDA specifies the first dimension of A as declared
C>           in the calling (sub) program. LDA must be at least
C>           max( 1, n ).
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup her2
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>  Level 2 Blas routine.
C>
C>  -- Written on 22-October-1986.
C>     Jack Dongarra, Argonne National Lab.
C>     Jeremy Du Croz, Nag Central Office.
C>     Sven Hammarling, Nag Central Office.
C>     Richard Hanson, Sandia National Labs.
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE SSYR2_B(uplo, n, alpha, alphab, x, xb, incx, y, yb, 
     +                   incy, a, ab, lda)
      IMPLICIT NONE
      INCLUDE 'DIFFSIZES.inc'
C  Hint: ISIZE1OFy should be the size of dimension 1 of array y
C  Hint: ISIZE1OFx should be the size of dimension 1 of array x
C
C  -- Reference BLAS level2 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      REAL alpha
      REAL alphab
      INTEGER incx, incy, lda, n
      CHARACTER uplo
C     ..
C     .. Array Arguments ..
      REAL a(lda, *), x(*), y(*)
      REAL ab(lda, *), xb(*), yb(*)
C     ..
C
C  =====================================================================
C
C     .. Parameters ..
      REAL zero
      PARAMETER (zero=0.0e+0)
C     ..
C     .. Local Scalars ..
      REAL temp1, temp2
      REAL temp1b, temp2b
      INTEGER i, info, ix, iy, j, jx, jy, kx, ky
      EXTERNAL LSAME
      INTEGER ISIZE1OFX, ISIZE1OFY
C     ..
C     .. External Functions ..
      INTEGER get_ISIZE1OFX, get_ISIZE1OFY
      EXTERNAL get_ISIZE1OFX, get_ISIZE1OFY
      LOGICAL LSAME
C     ..
C     .. External Subroutines ..
      EXTERNAL XERBLA, check_ISIZE1OFX_initialized, check_ISIZE1OFY_initialized
C     ..
C     .. Intrinsic Functions ..
      INTRINSIC MAX
      INTEGER max1
      INTEGER ad_to
      INTEGER*4 branch
      INTEGER ad_to0
      INTEGER ad_from
      INTEGER ad_from0
      INTEGER ii1
C     ..
C
C     Test the input parameters.
C
      CALL check_ISIZE1OFX_initialized()
      CALL check_ISIZE1OFY_initialized()
      ISIZE1OFX = get_ISIZE1OFX()
      ISIZE1OFY = get_ISIZE1OFY()
      info = 0
      IF (.NOT.LSAME(uplo, 'U') .AND. (.NOT.LSAME(uplo, 'L'))) THEN
        CALL PUSHCONTROL3B(0)
        info = 1
      ELSE IF (n .LT. 0) THEN
        CALL PUSHCONTROL3B(1)
        info = 2
      ELSE IF (incx .EQ. 0) THEN
        CALL PUSHCONTROL3B(2)
        info = 5
      ELSE IF (incy .EQ. 0) THEN
        CALL PUSHCONTROL3B(3)
        info = 7
      ELSE
        IF (1 .LT. n) THEN
          max1 = n
        ELSE
          max1 = 1
        END IF
        IF (lda .LT. max1) THEN
          CALL PUSHCONTROL3B(4)
          info = 9
        ELSE
          CALL PUSHCONTROL3B(4)
        END IF
      END IF
      IF (info .EQ. 0) THEN
C
C     Quick return if possible.
C
        IF (n .EQ. 0 .OR. alpha .EQ. zero) THEN
          alphab = 0.0
          DO ii1=1,ISIZE1OFx
            xb(ii1) = 0.0
          ENDDO
          DO ii1=1,ISIZE1OFy
            yb(ii1) = 0.0
          ENDDO
        ELSE
C
C     Set up the start points in X and Y if the increments are not both
C     unity.
C
          IF (incx .NE. 1 .OR. incy .NE. 1) THEN
            IF (incx .GT. 0) THEN
              kx = 1
            ELSE
              kx = 1 - (n-1)*incx
            END IF
            IF (incy .GT. 0) THEN
              CALL PUSHCONTROL1B(0)
              ky = 1
            ELSE
              CALL PUSHCONTROL1B(0)
              ky = 1 - (n-1)*incy
            END IF
            jx = kx
            jy = ky
          ELSE
            CALL PUSHCONTROL1B(1)
          END IF
C
C     Start the operations. In this version the elements of A are
C     accessed sequentially with one pass through the triangular part
C     of A.
C
          IF (LSAME(uplo, 'U')) THEN
C
C        Form  A  when A is stored in the upper triangle.
C
            IF (incx .EQ. 1 .AND. incy .EQ. 1) THEN
              DO j=1,n
                IF (x(j) .NE. zero .OR. y(j) .NE. zero) THEN
                  CALL PUSHREAL4(temp1)
                  temp1 = alpha*y(j)
                  CALL PUSHREAL4(temp2)
                  temp2 = alpha*x(j)
                  i = j + 1
                  CALL PUSHINTEGER4(i - 1)
                  CALL PUSHCONTROL1B(0)
                ELSE
                  CALL PUSHCONTROL1B(1)
                END IF
              ENDDO
              alphab = 0.0
              DO ii1=1,ISIZE1OFx
                xb(ii1) = 0.0
              ENDDO
              DO ii1=1,ISIZE1OFy
                yb(ii1) = 0.0
              ENDDO
              DO j=n,1,-1
                CALL POPCONTROL1B(branch)
                IF (branch .EQ. 0) THEN
                  temp1b = 0.0
                  temp2b = 0.0
                  CALL POPINTEGER4(ad_to)
                  DO i=ad_to,1,-1
                    xb(i) = xb(i) + temp1*ab(i, j)
                    temp1b = temp1b + x(i)*ab(i, j)
                    yb(i) = yb(i) + temp2*ab(i, j)
                    temp2b = temp2b + y(i)*ab(i, j)
                  ENDDO
                  CALL POPREAL4(temp2)
                  alphab = alphab + x(j)*temp2b + y(j)*temp1b
                  xb(j) = xb(j) + alpha*temp2b
                  CALL POPREAL4(temp1)
                  yb(j) = yb(j) + alpha*temp1b
                END IF
              ENDDO
            ELSE
              DO j=1,n
                IF (x(jx) .NE. zero .OR. y(jy) .NE. zero) THEN
                  CALL PUSHREAL4(temp1)
                  temp1 = alpha*y(jy)
                  CALL PUSHREAL4(temp2)
                  temp2 = alpha*x(jx)
                  ix = kx
                  iy = ky
                  DO i=1,j
                    CALL PUSHINTEGER4(ix)
                    ix = ix + incx
                    CALL PUSHINTEGER4(iy)
                    iy = iy + incy
                  ENDDO
                  CALL PUSHINTEGER4(i - 1)
                  CALL PUSHCONTROL1B(0)
                ELSE
                  CALL PUSHCONTROL1B(1)
                END IF
                CALL PUSHINTEGER4(jx)
                jx = jx + incx
                CALL PUSHINTEGER4(jy)
                jy = jy + incy
              ENDDO
              alphab = 0.0
              DO ii1=1,ISIZE1OFx
                xb(ii1) = 0.0
              ENDDO
              DO ii1=1,ISIZE1OFy
                yb(ii1) = 0.0
              ENDDO
              DO j=n,1,-1
                CALL POPINTEGER4(jy)
                CALL POPINTEGER4(jx)
                CALL POPCONTROL1B(branch)
                IF (branch .EQ. 0) THEN
                  temp1b = 0.0
                  temp2b = 0.0
                  CALL POPINTEGER4(ad_to0)
                  DO i=ad_to0,1,-1
                    CALL POPINTEGER4(iy)
                    CALL POPINTEGER4(ix)
                    xb(ix) = xb(ix) + temp1*ab(i, j)
                    temp1b = temp1b + x(ix)*ab(i, j)
                    yb(iy) = yb(iy) + temp2*ab(i, j)
                    temp2b = temp2b + y(iy)*ab(i, j)
                  ENDDO
                  CALL POPREAL4(temp2)
                  alphab = alphab + x(jx)*temp2b + y(jy)*temp1b
                  xb(jx) = xb(jx) + alpha*temp2b
                  CALL POPREAL4(temp1)
                  yb(jy) = yb(jy) + alpha*temp1b
                END IF
              ENDDO
            END IF
          ELSE IF (incx .EQ. 1 .AND. incy .EQ. 1) THEN
C
C        Form  A  when A is stored in the lower triangle.
C
            DO j=1,n
              IF (x(j) .NE. zero .OR. y(j) .NE. zero) THEN
                CALL PUSHREAL4(temp1)
                temp1 = alpha*y(j)
                CALL PUSHREAL4(temp2)
                temp2 = alpha*x(j)
                ad_from = j
                CALL PUSHINTEGER4(ad_from)
                CALL PUSHCONTROL1B(0)
              ELSE
                CALL PUSHCONTROL1B(1)
              END IF
            ENDDO
            alphab = 0.0
            DO ii1=1,ISIZE1OFx
              xb(ii1) = 0.0
            ENDDO
            DO ii1=1,ISIZE1OFy
              yb(ii1) = 0.0
            ENDDO
            DO j=n,1,-1
              CALL POPCONTROL1B(branch)
              IF (branch .EQ. 0) THEN
                temp1b = 0.0
                temp2b = 0.0
                CALL POPINTEGER4(ad_from)
                DO i=n,ad_from,-1
                  xb(i) = xb(i) + temp1*ab(i, j)
                  temp1b = temp1b + x(i)*ab(i, j)
                  yb(i) = yb(i) + temp2*ab(i, j)
                  temp2b = temp2b + y(i)*ab(i, j)
                ENDDO
                CALL POPREAL4(temp2)
                alphab = alphab + x(j)*temp2b + y(j)*temp1b
                xb(j) = xb(j) + alpha*temp2b
                CALL POPREAL4(temp1)
                yb(j) = yb(j) + alpha*temp1b
              END IF
            ENDDO
          ELSE
            DO j=1,n
              IF (x(jx) .NE. zero .OR. y(jy) .NE. zero) THEN
                CALL PUSHREAL4(temp1)
                temp1 = alpha*y(jy)
                CALL PUSHREAL4(temp2)
                temp2 = alpha*x(jx)
                ix = jx
                iy = jy
                ad_from0 = j
                DO i=ad_from0,n
                  CALL PUSHINTEGER4(ix)
                  ix = ix + incx
                  CALL PUSHINTEGER4(iy)
                  iy = iy + incy
                ENDDO
                CALL PUSHINTEGER4(ad_from0)
                CALL PUSHCONTROL1B(0)
              ELSE
                CALL PUSHCONTROL1B(1)
              END IF
              CALL PUSHINTEGER4(jx)
              jx = jx + incx
              CALL PUSHINTEGER4(jy)
              jy = jy + incy
            ENDDO
            alphab = 0.0
            DO ii1=1,ISIZE1OFx
              xb(ii1) = 0.0
            ENDDO
            DO ii1=1,ISIZE1OFy
              yb(ii1) = 0.0
            ENDDO
            DO j=n,1,-1
              CALL POPINTEGER4(jy)
              CALL POPINTEGER4(jx)
              CALL POPCONTROL1B(branch)
              IF (branch .EQ. 0) THEN
                temp1b = 0.0
                temp2b = 0.0
                CALL POPINTEGER4(ad_from0)
                DO i=n,ad_from0,-1
                  CALL POPINTEGER4(iy)
                  CALL POPINTEGER4(ix)
                  xb(ix) = xb(ix) + temp1*ab(i, j)
                  temp1b = temp1b + x(ix)*ab(i, j)
                  yb(iy) = yb(iy) + temp2*ab(i, j)
                  temp2b = temp2b + y(iy)*ab(i, j)
                ENDDO
                CALL POPREAL4(temp2)
                alphab = alphab + x(jx)*temp2b + y(jy)*temp1b
                xb(jx) = xb(jx) + alpha*temp2b
                CALL POPREAL4(temp1)
                yb(jy) = yb(jy) + alpha*temp1b
              END IF
            ENDDO
          END IF
          CALL POPCONTROL1B(branch)
        END IF
      END IF
      CALL POPCONTROL3B(branch)
      END

