C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of zgemm in forward (tangent) mode:
C   variations   of useful results: c
C   with respect to varying inputs: alpha beta a b c
C   RW status of diff variables: alpha:in beta:in a:in b:in c:in-out
C> \brief \b ZGEMM
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       SUBROUTINE ZGEMM(TRANSA,TRANSB,M,N,K,ALPHA,A,LDA,B,LDB,BETA,C,LDC)
C
C       .. Scalar Arguments ..
C       COMPLEX*16 ALPHA,BETA
C       INTEGER K,LDA,LDB,LDC,M,N
C       CHARACTER TRANSA,TRANSB
C       ..
C       .. Array Arguments ..
C       COMPLEX*16 A(LDA,*),B(LDB,*),C(LDC,*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C> ZGEMM  performs one of the matrix-matrix operations
C>
C>    C := alpha*op( A )*op( B ) + beta*C,
C>
C> where  op( X ) is one of
C>
C>    op( X ) = X   or   op( X ) = X**T   or   op( X ) = X**H,
C>
C> alpha and beta are scalars, and A, B and C are matrices, with op( A )
C> an m by k matrix,  op( B )  a  k by n matrix and  C an m by n matrix.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] TRANSA
C> \verbatim
C>          TRANSA is CHARACTER*1
C>           On entry, TRANSA specifies the form of op( A ) to be used in
C>           the matrix multiplication as follows:
C>
C>              TRANSA = 'N' or 'n',  op( A ) = A.
C>
C>              TRANSA = 'T' or 't',  op( A ) = A**T.
C>
C>              TRANSA = 'C' or 'c',  op( A ) = A**H.
C> \endverbatim
C>
C> \param[in] TRANSB
C> \verbatim
C>          TRANSB is CHARACTER*1
C>           On entry, TRANSB specifies the form of op( B ) to be used in
C>           the matrix multiplication as follows:
C>
C>              TRANSB = 'N' or 'n',  op( B ) = B.
C>
C>              TRANSB = 'T' or 't',  op( B ) = B**T.
C>
C>              TRANSB = 'C' or 'c',  op( B ) = B**H.
C> \endverbatim
C>
C> \param[in] M
C> \verbatim
C>          M is INTEGER
C>           On entry,  M  specifies  the number  of rows  of the  matrix
C>           op( A )  and of the  matrix  C.  M  must  be at least  zero.
C> \endverbatim
C>
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>           On entry,  N  specifies the number  of columns of the matrix
C>           op( B ) and the number of columns of the matrix C. N must be
C>           at least zero.
C> \endverbatim
C>
C> \param[in] K
C> \verbatim
C>          K is INTEGER
C>           On entry,  K  specifies  the number of columns of the matrix
C>           op( A ) and the number of rows of the matrix op( B ). K must
C>           be at least  zero.
C> \endverbatim
C>
C> \param[in] ALPHA
C> \verbatim
C>          ALPHA is COMPLEX*16
C>           On entry, ALPHA specifies the scalar alpha.
C> \endverbatim
C>
C> \param[in] A
C> \verbatim
C>          A is COMPLEX*16 array, dimension ( LDA, ka ), where ka is
C>           k  when  TRANSA = 'N' or 'n',  and is  m  otherwise.
C>           Before entry with  TRANSA = 'N' or 'n',  the leading  m by k
C>           part of the array  A  must contain the matrix  A,  otherwise
C>           the leading  k by m  part of the array  A  must contain  the
C>           matrix A.
C> \endverbatim
C>
C> \param[in] LDA
C> \verbatim
C>          LDA is INTEGER
C>           On entry, LDA specifies the first dimension of A as declared
C>           in the calling (sub) program. When  TRANSA = 'N' or 'n' then
C>           LDA must be at least  max( 1, m ), otherwise  LDA must be at
C>           least  max( 1, k ).
C> \endverbatim
C>
C> \param[in] B
C> \verbatim
C>          B is COMPLEX*16 array, dimension ( LDB, kb ), where kb is
C>           n  when  TRANSB = 'N' or 'n',  and is  k  otherwise.
C>           Before entry with  TRANSB = 'N' or 'n',  the leading  k by n
C>           part of the array  B  must contain the matrix  B,  otherwise
C>           the leading  n by k  part of the array  B  must contain  the
C>           matrix B.
C> \endverbatim
C>
C> \param[in] LDB
C> \verbatim
C>          LDB is INTEGER
C>           On entry, LDB specifies the first dimension of B as declared
C>           in the calling (sub) program. When  TRANSB = 'N' or 'n' then
C>           LDB must be at least  max( 1, k ), otherwise  LDB must be at
C>           least  max( 1, n ).
C> \endverbatim
C>
C> \param[in] BETA
C> \verbatim
C>          BETA is COMPLEX*16
C>           On entry,  BETA  specifies the scalar  beta.  When  BETA  is
C>           supplied as zero then C need not be set on input.
C> \endverbatim
C>
C> \param[in,out] C
C> \verbatim
C>          C is COMPLEX*16 array, dimension ( LDC, N )
C>           Before entry, the leading  m by n  part of the array  C must
C>           contain the matrix  C,  except when  beta  is zero, in which
C>           case C need not be set on entry.
C>           On exit, the array  C  is overwritten by the  m by n  matrix
C>           ( alpha*op( A )*op( B ) + beta*C ).
C> \endverbatim
C>
C> \param[in] LDC
C> \verbatim
C>          LDC is INTEGER
C>           On entry, LDC specifies the first dimension of C as declared
C>           in  the  calling  (sub)  program.   LDC  must  be  at  least
C>           max( 1, m ).
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup gemm
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>  Level 3 Blas routine.
C>
C>  -- Written on 8-February-1989.
C>     Jack Dongarra, Argonne National Laboratory.
C>     Iain Duff, AERE Harwell.
C>     Jeremy Du Croz, Numerical Algorithms Group Ltd.
C>     Sven Hammarling, Numerical Algorithms Group Ltd.
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE ZGEMM_D(transa, transb, m, n, k, alpha, alphad, a, ad, 
     +                   lda, b, bd, ldb, beta, betad, c, cd, ldc)
      IMPLICIT NONE
C
C  -- Reference BLAS level3 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      COMPLEX*16 alpha, beta
      COMPLEX*16 alphad, betad
      INTEGER k, lda, ldb, ldc, m, n
      CHARACTER transa, transb
C     ..
C     .. Array Arguments ..
      COMPLEX*16 a(lda, *), b(ldb, *), c(ldc, *)
      COMPLEX*16 ad(lda, *), bd(ldb, *), cd(ldc, *)
      EXTERNAL LSAME
C     ..
C
C  =====================================================================
C
C     .. External Functions ..
      LOGICAL LSAME
C     ..
C     .. External Subroutines ..
      EXTERNAL XERBLA
C     ..
C     .. Intrinsic Functions ..
      INTRINSIC DCONJG, MAX
C     ..
C     .. Local Scalars ..
      COMPLEX*16 temp
      COMPLEX*16 tempd
      INTEGER i, info, j, l, nrowa, nrowb
      LOGICAL conja, conjb, nota, notb
C     ..
C     .. Parameters ..
      COMPLEX*16 one
      PARAMETER (one=(1.0d+0,0.0d+0))
      COMPLEX*16 zero
      PARAMETER (zero=(0.0d+0,0.0d+0))
      INTEGER max1
      INTEGER max2
      INTEGER max3
      DOUBLE COMPLEX temp0
      DOUBLE COMPLEX temp1
C     ..
C
C     Set  NOTA  and  NOTB  as  true if  A  and  B  respectively are not
C     conjugated or transposed, set  CONJA and CONJB  as true if  A  and
C     B  respectively are to be  transposed but  not conjugated  and set
C     NROWA and NROWB  as the number of rows  of  A  and  B  respectively.
C
      nota = LSAME(transa, 'N')
      notb = LSAME(transb, 'N')
      conja = LSAME(transa, 'C')
      conjb = LSAME(transb, 'C')
      IF (nota) THEN
        nrowa = m
      ELSE
        nrowa = k
      END IF
      IF (notb) THEN
        nrowb = k
      ELSE
        nrowb = n
      END IF
C
C     Test the input parameters.
C
      info = 0
      IF (.NOT.nota .AND. (.NOT.conja) .AND. (.NOT.LSAME(transa, 'T'))) 
     +THEN
        info = 1
      ELSE IF (.NOT.notb .AND. (.NOT.conjb) .AND. (.NOT.LSAME(transb, 
     +    'T'))) THEN
        info = 2
      ELSE IF (m .LT. 0) THEN
        info = 3
      ELSE IF (n .LT. 0) THEN
        info = 4
      ELSE IF (k .LT. 0) THEN
        info = 5
      ELSE
        IF (1 .LT. nrowa) THEN
          max1 = nrowa
        ELSE
          max1 = 1
        END IF
        IF (lda .LT. max1) THEN
          info = 8
        ELSE
          IF (1 .LT. nrowb) THEN
            max2 = nrowb
          ELSE
            max2 = 1
          END IF
          IF (ldb .LT. max2) THEN
            info = 10
          ELSE
            IF (1 .LT. m) THEN
              max3 = m
            ELSE
              max3 = 1
            END IF
            IF (ldc .LT. max3) info = 13
          END IF
        END IF
      END IF
      IF (info .NE. 0) THEN
        CALL XERBLA('ZGEMM ', info)
        RETURN
      ELSE IF ((m .EQ. 0 .OR. n .EQ. 0) .OR. ((alpha .EQ. zero .OR. k 
     +    .EQ. 0) .AND. beta .EQ. one)) THEN
C
C     Quick return if possible.
C
        RETURN
      ELSE IF (alpha .EQ. zero) THEN
C
C     And when  alpha.eq.zero.
C
        IF (beta .EQ. zero) THEN
          DO j=1,n
            DO i=1,m
              cd(i, j) = 0.0
              c(i, j) = zero
            ENDDO
          ENDDO
        ELSE
          DO j=1,n
            DO i=1,m
              cd(i, j) = c(i, j)*betad + beta*cd(i, j)
              c(i, j) = beta*c(i, j)
            ENDDO
          ENDDO
        END IF
        RETURN
      ELSE
C
C     Start the operations.
C
        IF (notb) THEN
          IF (nota) THEN
C
C           Form  C := alpha*A*B + beta*C.
C
            DO j=1,n
              IF (beta .EQ. zero) THEN
                DO i=1,m
                  cd(i, j) = 0.0
                  c(i, j) = zero
                ENDDO
              ELSE IF (beta .NE. one) THEN
                DO i=1,m
                  cd(i, j) = c(i, j)*betad + beta*cd(i, j)
                  c(i, j) = beta*c(i, j)
                ENDDO
              END IF
              DO l=1,k
                tempd = b(l, j)*alphad + alpha*bd(l, j)
                temp = alpha*b(l, j)
                DO i=1,m
                  cd(i, j) = cd(i, j) + a(i, l)*tempd + temp*ad(i, l)
                  c(i, j) = c(i, j) + temp*a(i, l)
                ENDDO
              ENDDO
            ENDDO
          ELSE IF (conja) THEN
C
C           Form  C := alpha*A**H*B + beta*C.
C
            DO j=1,n
              DO i=1,m
                temp = zero
                tempd = (0.0,0.0)
                DO l=1,k
                  temp0 = DCONJG(a(l, i))
                  tempd = tempd + b(l, j)*DCONJG(ad(l, i)) + temp0*bd(l
     +              , j)
                  temp = temp + temp0*b(l, j)
                ENDDO
                IF (beta .EQ. zero) THEN
                  cd(i, j) = temp*alphad + alpha*tempd
                  c(i, j) = alpha*temp
                ELSE
                  cd(i, j) = temp*alphad + alpha*tempd + c(i, j)*betad +
     +              beta*cd(i, j)
                  c(i, j) = alpha*temp + beta*c(i, j)
                END IF
              ENDDO
            ENDDO
          ELSE
C
C           Form  C := alpha*A**T*B + beta*C
C
            DO j=1,n
              DO i=1,m
                temp = zero
                tempd = (0.0,0.0)
                DO l=1,k
                  tempd = tempd + b(l, j)*ad(l, i) + a(l, i)*bd(l, j)
                  temp = temp + a(l, i)*b(l, j)
                ENDDO
                IF (beta .EQ. zero) THEN
                  cd(i, j) = temp*alphad + alpha*tempd
                  c(i, j) = alpha*temp
                ELSE
                  cd(i, j) = temp*alphad + alpha*tempd + c(i, j)*betad +
     +              beta*cd(i, j)
                  c(i, j) = alpha*temp + beta*c(i, j)
                END IF
              ENDDO
            ENDDO
          END IF
        ELSE IF (nota) THEN
          IF (conjb) THEN
C
C           Form  C := alpha*A*B**H + beta*C.
C
            DO j=1,n
              IF (beta .EQ. zero) THEN
                DO i=1,m
                  cd(i, j) = 0.0
                  c(i, j) = zero
                ENDDO
              ELSE IF (beta .NE. one) THEN
                DO i=1,m
                  cd(i, j) = c(i, j)*betad + beta*cd(i, j)
                  c(i, j) = beta*c(i, j)
                ENDDO
              END IF
              DO l=1,k
                temp0 = DCONJG(b(j, l))
                tempd = temp0*alphad + alpha*DCONJG(bd(j, l))
                temp = alpha*temp0
                DO i=1,m
                  cd(i, j) = cd(i, j) + a(i, l)*tempd + temp*ad(i, l)
                  c(i, j) = c(i, j) + temp*a(i, l)
                ENDDO
              ENDDO
            ENDDO
          ELSE
C
C           Form  C := alpha*A*B**T + beta*C
C
            DO j=1,n
              IF (beta .EQ. zero) THEN
                DO i=1,m
                  cd(i, j) = 0.0
                  c(i, j) = zero
                ENDDO
              ELSE IF (beta .NE. one) THEN
                DO i=1,m
                  cd(i, j) = c(i, j)*betad + beta*cd(i, j)
                  c(i, j) = beta*c(i, j)
                ENDDO
              END IF
              DO l=1,k
                tempd = b(j, l)*alphad + alpha*bd(j, l)
                temp = alpha*b(j, l)
                DO i=1,m
                  cd(i, j) = cd(i, j) + a(i, l)*tempd + temp*ad(i, l)
                  c(i, j) = c(i, j) + temp*a(i, l)
                ENDDO
              ENDDO
            ENDDO
          END IF
        ELSE IF (conja) THEN
          IF (conjb) THEN
C
C           Form  C := alpha*A**H*B**H + beta*C.
C
            DO j=1,n
              DO i=1,m
                temp = zero
                tempd = (0.0,0.0)
                DO l=1,k
                  temp0 = DCONJG(b(j, l))
                  temp1 = DCONJG(a(l, i))
                  tempd = tempd + temp0*DCONJG(ad(l, i)) + temp1*DCONJG(
     +              bd(j, l))
                  temp = temp + temp1*temp0
                ENDDO
                IF (beta .EQ. zero) THEN
                  cd(i, j) = temp*alphad + alpha*tempd
                  c(i, j) = alpha*temp
                ELSE
                  cd(i, j) = temp*alphad + alpha*tempd + c(i, j)*betad +
     +              beta*cd(i, j)
                  c(i, j) = alpha*temp + beta*c(i, j)
                END IF
              ENDDO
            ENDDO
          ELSE
C
C           Form  C := alpha*A**H*B**T + beta*C
C
            DO j=1,n
              DO i=1,m
                temp = zero
                tempd = (0.0,0.0)
                DO l=1,k
                  temp1 = DCONJG(a(l, i))
                  tempd = tempd + b(j, l)*DCONJG(ad(l, i)) + temp1*bd(j
     +              , l)
                  temp = temp + temp1*b(j, l)
                ENDDO
                IF (beta .EQ. zero) THEN
                  cd(i, j) = temp*alphad + alpha*tempd
                  c(i, j) = alpha*temp
                ELSE
                  cd(i, j) = temp*alphad + alpha*tempd + c(i, j)*betad +
     +              beta*cd(i, j)
                  c(i, j) = alpha*temp + beta*c(i, j)
                END IF
              ENDDO
            ENDDO
          END IF
        ELSE IF (conjb) THEN
C
C           Form  C := alpha*A**T*B**H + beta*C
C
          DO j=1,n
            DO i=1,m
              temp = zero
              tempd = (0.0,0.0)
              DO l=1,k
                temp1 = DCONJG(b(j, l))
                tempd = tempd + temp1*ad(l, i) + a(l, i)*DCONJG(bd(j, l)
     +            )
                temp = temp + a(l, i)*temp1
              ENDDO
              IF (beta .EQ. zero) THEN
                cd(i, j) = temp*alphad + alpha*tempd
                c(i, j) = alpha*temp
              ELSE
                cd(i, j) = temp*alphad + alpha*tempd + c(i, j)*betad + 
     +            beta*cd(i, j)
                c(i, j) = alpha*temp + beta*c(i, j)
              END IF
            ENDDO
          ENDDO
        ELSE
C
C           Form  C := alpha*A**T*B**T + beta*C
C
          DO j=1,n
            DO i=1,m
              temp = zero
              tempd = (0.0,0.0)
              DO l=1,k
                tempd = tempd + b(j, l)*ad(l, i) + a(l, i)*bd(j, l)
                temp = temp + a(l, i)*b(j, l)
              ENDDO
              IF (beta .EQ. zero) THEN
                cd(i, j) = temp*alphad + alpha*tempd
                c(i, j) = alpha*temp
              ELSE
                cd(i, j) = temp*alphad + alpha*tempd + c(i, j)*betad + 
     +            beta*cd(i, j)
                c(i, j) = alpha*temp + beta*c(i, j)
              END IF
            ENDDO
          ENDDO
        END IF
C
        RETURN
C
C     End of ZGEMM
C
      END IF
      END

