C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of cdotu in forward (tangent) mode:
C   variations   of useful results: cdotu
C   with respect to varying inputs: cx cy
C   RW status of diff variables: cdotu:out cx:in cy:in
C> \brief \b CDOTU
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       COMPLEX FUNCTION CDOTU(N,CX,INCX,CY,INCY)
C
C       .. Scalar Arguments ..
C       INTEGER INCX,INCY,N
C       ..
C       .. Array Arguments ..
C       COMPLEX CX(*),CY(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C> CDOTU forms the dot product of two complex vectors
C>      CDOTU = X^T * Y
C>
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>         number of elements in input vector(s)
C> \endverbatim
C>
C> \param[in] CX
C> \verbatim
C>          CX is COMPLEX array, dimension ( 1 + ( N - 1 )*abs( INCX ) )
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>         storage spacing between elements of CX
C> \endverbatim
C>
C> \param[in] CY
C> \verbatim
C>          CY is COMPLEX array, dimension ( 1 + ( N - 1 )*abs( INCY ) )
C> \endverbatim
C>
C> \param[in] INCY
C> \verbatim
C>          INCY is INTEGER
C>         storage spacing between elements of CY
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup dot
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>     jack dongarra, linpack, 3/11/78.
C>     modified 12/3/93, array(1) declarations changed to array(*)
C> \endverbatim
C>
C  =====================================================================
      COMPLEX FUNCTION CDOTU_D(n, cx, cxd, incx, cy, cyd, incy, cdotu)
      IMPLICIT NONE
C
C  -- Reference BLAS level1 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      INTEGER incx, incy, n
C     ..
C     .. Array Arguments ..
      COMPLEX cx(*), cy(*)
      COMPLEX cxd(*), cyd(*)
C     ..
C
C  =====================================================================
C
C     .. Local Scalars ..
      COMPLEX ctemp
      COMPLEX ctempd
      INTEGER i, ix, iy
      COMPLEX cdotu
C     ..
      ctemp = (0.0,0.0)
      cdotu = (0.0,0.0)
      IF (n .LE. 0) THEN
        cdotu_d = (0.0,0.0)
        RETURN
      ELSE
        IF (incx .EQ. 1 .AND. incy .EQ. 1) THEN
          ctempd = (0.0,0.0)
C
C        code for both increments equal to 1
C
          DO i=1,n
            ctempd = ctempd + cy(i)*cxd(i) + cx(i)*cyd(i)
            ctemp = ctemp + cx(i)*cy(i)
          ENDDO
        ELSE
C
C        code for unequal increments or equal increments
C          not equal to 1
C
          ix = 1
          iy = 1
          IF (incx .LT. 0) ix = (-n+1)*incx + 1
          IF (incy .LT. 0) THEN
            iy = (-n+1)*incy + 1
            ctempd = (0.0,0.0)
          ELSE
            ctempd = (0.0,0.0)
          END IF
          DO i=1,n
            ctempd = ctempd + cy(iy)*cxd(ix) + cx(ix)*cyd(iy)
            ctemp = ctemp + cx(ix)*cy(iy)
            ix = ix + incx
            iy = iy + incy
          ENDDO
        END IF
        cdotu_d = ctempd
        cdotu = ctemp
        RETURN
C
C     End of CDOTU
C
      END IF
      END

