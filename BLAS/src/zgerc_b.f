C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of zgerc in reverse (adjoint) mode:
C   gradient     of useful results: a
C   with respect to varying inputs: alpha x y a
C   RW status of diff variables: alpha:out x:out y:out a:in-out
C> \brief \b ZGERC
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       SUBROUTINE ZGERC(M,N,ALPHA,X,INCX,Y,INCY,A,LDA)
C
C       .. Scalar Arguments ..
C       COMPLEX*16 ALPHA
C       INTEGER INCX,INCY,LDA,M,N
C       ..
C       .. Array Arguments ..
C       COMPLEX*16 A(LDA,*),X(*),Y(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C> ZGERC  performs the rank 1 operation
C>
C>    A := alpha*x*y**H + A,
C>
C> where alpha is a scalar, x is an m element vector, y is an n element
C> vector and A is an m by n matrix.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] M
C> \verbatim
C>          M is INTEGER
C>           On entry, M specifies the number of rows of the matrix A.
C>           M must be at least zero.
C> \endverbatim
C>
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>           On entry, N specifies the number of columns of the matrix A.
C>           N must be at least zero.
C> \endverbatim
C>
C> \param[in] ALPHA
C> \verbatim
C>          ALPHA is COMPLEX*16
C>           On entry, ALPHA specifies the scalar alpha.
C> \endverbatim
C>
C> \param[in] X
C> \verbatim
C>          X is COMPLEX*16 array, dimension at least
C>           ( 1 + ( m - 1 )*abs( INCX ) ).
C>           Before entry, the incremented array X must contain the m
C>           element vector x.
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>           On entry, INCX specifies the increment for the elements of
C>           X. INCX must not be zero.
C> \endverbatim
C>
C> \param[in] Y
C> \verbatim
C>          Y is COMPLEX*16 array, dimension at least
C>           ( 1 + ( n - 1 )*abs( INCY ) ).
C>           Before entry, the incremented array Y must contain the n
C>           element vector y.
C> \endverbatim
C>
C> \param[in] INCY
C> \verbatim
C>          INCY is INTEGER
C>           On entry, INCY specifies the increment for the elements of
C>           Y. INCY must not be zero.
C> \endverbatim
C>
C> \param[in,out] A
C> \verbatim
C>          A is COMPLEX*16 array, dimension ( LDA, N )
C>           Before entry, the leading m by n part of the array A must
C>           contain the matrix of coefficients. On exit, A is
C>           overwritten by the updated matrix.
C> \endverbatim
C>
C> \param[in] LDA
C> \verbatim
C>          LDA is INTEGER
C>           On entry, LDA specifies the first dimension of A as declared
C>           in the calling (sub) program. LDA must be at least
C>           max( 1, m ).
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup ger
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>  Level 2 Blas routine.
C>
C>  -- Written on 22-October-1986.
C>     Jack Dongarra, Argonne National Lab.
C>     Jeremy Du Croz, Nag Central Office.
C>     Sven Hammarling, Nag Central Office.
C>     Richard Hanson, Sandia National Labs.
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE ZGERC_B(m, n, alpha, alphab, x, xb, incx, y, yb, incy, 
     +                   a, ab, lda)
      IMPLICIT NONE
      INCLUDE 'DIFFSIZES.inc'
C  Hint: ISIZE1OFy should be the size of dimension 1 of array y
C  Hint: ISIZE1OFx should be the size of dimension 1 of array x
C
C  -- Reference BLAS level2 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      COMPLEX*16 alpha
      COMPLEX*16 alphab
      INTEGER incx, incy, lda, m, n
C     ..
C     .. Array Arguments ..
      COMPLEX*16 a(lda, *), x(*), y(*)
      COMPLEX*16 ab(lda, *), xb(*), yb(*)
C     ..
C
C  =====================================================================
C
C     .. Parameters ..
      COMPLEX*16 zero
      PARAMETER (zero=(0.0d+0,0.0d+0))
C     ..
C     .. Local Scalars ..
      COMPLEX*16 temp
      COMPLEX*16 tempb
      INTEGER i, info, ix, j, jy, kx
      INTEGER ISIZE1OFX, ISIZE1OFY
      INTEGER get_ISIZE1OFX, get_ISIZE1OFY
      EXTERNAL get_ISIZE1OFX, get_ISIZE1OFY
C     ..
C     .. External Subroutines ..
      EXTERNAL XERBLA, check_ISIZE1OFX_initialized, check_ISIZE1OFY_initialized
C     ..
C     .. Intrinsic Functions ..
      INTRINSIC DCONJG, MAX
      INTEGER max1
      INTEGER*4 branch
      INTEGER ii1
C     ..
C
C     Test the input parameters.
C
      CALL check_ISIZE1OFX_initialized()
      CALL check_ISIZE1OFY_initialized()
      ISIZE1OFX = get_ISIZE1OFX()
      ISIZE1OFY = get_ISIZE1OFY()
      info = 0
      IF (m .LT. 0) THEN
        CALL PUSHCONTROL3B(0)
        info = 1
      ELSE IF (n .LT. 0) THEN
        CALL PUSHCONTROL3B(1)
        info = 2
      ELSE IF (incx .EQ. 0) THEN
        CALL PUSHCONTROL3B(2)
        info = 5
      ELSE IF (incy .EQ. 0) THEN
        CALL PUSHCONTROL3B(3)
        info = 7
      ELSE
        IF (1 .LT. m) THEN
          max1 = m
        ELSE
          max1 = 1
        END IF
        IF (lda .LT. max1) THEN
          CALL PUSHCONTROL3B(4)
          info = 9
        ELSE
          CALL PUSHCONTROL3B(4)
        END IF
      END IF
      IF (info .EQ. 0) THEN
C
C     Quick return if possible.
C
        IF ((m .EQ. 0 .OR. n .EQ. 0) .OR. alpha .EQ. zero) THEN
          alphab = (0.0,0.0)
          DO ii1=1,ISIZE1OFx
            xb(ii1) = (0.0,0.0)
          ENDDO
          DO ii1=1,ISIZE1OFy
            yb(ii1) = (0.0,0.0)
          ENDDO
        ELSE
C
C     Start the operations. In this version the elements of A are
C     accessed sequentially with one pass through A.
C
          IF (incy .GT. 0) THEN
            jy = 1
          ELSE
            jy = 1 - (n-1)*incy
          END IF
          IF (incx .EQ. 1) THEN
            DO j=1,n
              IF (y(jy) .NE. zero) THEN
                CALL PUSHCOMPLEX16(temp)
                temp = alpha*DCONJG(y(jy))
                CALL PUSHCONTROL1B(0)
              ELSE
                CALL PUSHCONTROL1B(1)
              END IF
              CALL PUSHINTEGER4(jy)
              jy = jy + incy
            ENDDO
            alphab = (0.0,0.0)
            DO ii1=1,ISIZE1OFx
              xb(ii1) = (0.0,0.0)
            ENDDO
            DO ii1=1,ISIZE1OFy
              yb(ii1) = (0.0,0.0)
            ENDDO
            DO j=n,1,-1
              CALL POPINTEGER4(jy)
              CALL POPCONTROL1B(branch)
              IF (branch .EQ. 0) THEN
                tempb = (0.0,0.0)
                DO i=m,1,-1
                  xb(i) = xb(i) + CONJG(temp)*ab(i, j)
                  tempb = tempb + CONJG(x(i))*ab(i, j)
                ENDDO
                CALL POPCOMPLEX16(temp)
                alphab = alphab + CONJG(DCONJG(y(jy)))*tempb
                yb(jy) = yb(jy) + DCONJG(CONJG(alpha)*tempb)
              END IF
            ENDDO
          ELSE
            IF (incx .GT. 0) THEN
              kx = 1
            ELSE
              kx = 1 - (m-1)*incx
            END IF
            DO j=1,n
              IF (y(jy) .NE. zero) THEN
                CALL PUSHCOMPLEX16(temp)
                temp = alpha*DCONJG(y(jy))
                ix = kx
                DO i=1,m
                  CALL PUSHINTEGER4(ix)
                  ix = ix + incx
                ENDDO
                CALL PUSHCONTROL1B(0)
              ELSE
                CALL PUSHCONTROL1B(1)
              END IF
              CALL PUSHINTEGER4(jy)
              jy = jy + incy
            ENDDO
            alphab = (0.0,0.0)
            DO ii1=1,ISIZE1OFx
              xb(ii1) = (0.0,0.0)
            ENDDO
            DO ii1=1,ISIZE1OFy
              yb(ii1) = (0.0,0.0)
            ENDDO
            DO j=n,1,-1
              CALL POPINTEGER4(jy)
              CALL POPCONTROL1B(branch)
              IF (branch .EQ. 0) THEN
                tempb = (0.0,0.0)
                DO i=m,1,-1
                  CALL POPINTEGER4(ix)
                  xb(ix) = xb(ix) + CONJG(temp)*ab(i, j)
                  tempb = tempb + CONJG(x(ix))*ab(i, j)
                ENDDO
                CALL POPCOMPLEX16(temp)
                alphab = alphab + CONJG(DCONJG(y(jy)))*tempb
                yb(jy) = yb(jy) + DCONJG(CONJG(alpha)*tempb)
              END IF
            ENDDO
          END IF
        END IF
      END IF
      CALL POPCONTROL3B(branch)
      END

