C        Generated by TAPENADE     (INRIA, Ecuador team)
C  Tapenade 3.16 (develop) -  6 Jan 2026 19:07
C
C  Differentiation of stpmv in reverse (adjoint) mode (with options multiDirectional):
C   gradient     of useful results: x
C   with respect to varying inputs: ap x
C   RW status of diff variables: ap:out x:in-out
C> \brief \b STPMV
C
C  =========== DOCUMENTATION ===========
C
C Online html documentation available at
C            http://www.netlib.org/lapack/explore-html/
C
C  Definition:
C  ===========
C
C       SUBROUTINE STPMV(UPLO,TRANS,DIAG,N,AP,X,INCX)
C
C       .. Scalar Arguments ..
C       INTEGER INCX,N
C       CHARACTER DIAG,TRANS,UPLO
C       ..
C       .. Array Arguments ..
C       REAL AP(*),X(*)
C       ..
C
C
C> \par Purpose:
C  =============
C>
C> \verbatim
C>
C> STPMV  performs one of the matrix-vector operations
C>
C>    x := A*x,   or   x := A**T*x,
C>
C> where x is an n element vector and  A is an n by n unit, or non-unit,
C> upper or lower triangular matrix, supplied in packed form.
C> \endverbatim
C
C  Arguments:
C  ==========
C
C> \param[in] UPLO
C> \verbatim
C>          UPLO is CHARACTER*1
C>           On entry, UPLO specifies whether the matrix is an upper or
C>           lower triangular matrix as follows:
C>
C>              UPLO = 'U' or 'u'   A is an upper triangular matrix.
C>
C>              UPLO = 'L' or 'l'   A is a lower triangular matrix.
C> \endverbatim
C>
C> \param[in] TRANS
C> \verbatim
C>          TRANS is CHARACTER*1
C>           On entry, TRANS specifies the operation to be performed as
C>           follows:
C>
C>              TRANS = 'N' or 'n'   x := A*x.
C>
C>              TRANS = 'T' or 't'   x := A**T*x.
C>
C>              TRANS = 'C' or 'c'   x := A**T*x.
C> \endverbatim
C>
C> \param[in] DIAG
C> \verbatim
C>          DIAG is CHARACTER*1
C>           On entry, DIAG specifies whether or not A is unit
C>           triangular as follows:
C>
C>              DIAG = 'U' or 'u'   A is assumed to be unit triangular.
C>
C>              DIAG = 'N' or 'n'   A is not assumed to be unit
C>                                  triangular.
C> \endverbatim
C>
C> \param[in] N
C> \verbatim
C>          N is INTEGER
C>           On entry, N specifies the order of the matrix A.
C>           N must be at least zero.
C> \endverbatim
C>
C> \param[in] AP
C> \verbatim
C>          AP is REAL array, dimension at least
C>           ( ( n*( n + 1 ) )/2 ).
C>           Before entry with  UPLO = 'U' or 'u', the array AP must
C>           contain the upper triangular matrix packed sequentially,
C>           column by column, so that AP( 1 ) contains a( 1, 1 ),
C>           AP( 2 ) and AP( 3 ) contain a( 1, 2 ) and a( 2, 2 )
C>           respectively, and so on.
C>           Before entry with UPLO = 'L' or 'l', the array AP must
C>           contain the lower triangular matrix packed sequentially,
C>           column by column, so that AP( 1 ) contains a( 1, 1 ),
C>           AP( 2 ) and AP( 3 ) contain a( 2, 1 ) and a( 3, 1 )
C>           respectively, and so on.
C>           Note that when  DIAG = 'U' or 'u', the diagonal elements of
C>           A are not referenced, but are assumed to be unity.
C> \endverbatim
C>
C> \param[in,out] X
C> \verbatim
C>          X is REAL array, dimension at least
C>           ( 1 + ( n - 1 )*abs( INCX ) ).
C>           Before entry, the incremented array X must contain the n
C>           element vector x. On exit, X is overwritten with the
C>           transformed vector x.
C> \endverbatim
C>
C> \param[in] INCX
C> \verbatim
C>          INCX is INTEGER
C>           On entry, INCX specifies the increment for the elements of
C>           X. INCX must not be zero.
C> \endverbatim
C
C  Authors:
C  ========
C
C> \author Univ. of Tennessee
C> \author Univ. of California Berkeley
C> \author Univ. of Colorado Denver
C> \author NAG Ltd.
C
C> \ingroup tpmv
C
C> \par Further Details:
C  =====================
C>
C> \verbatim
C>
C>  Level 2 Blas routine.
C>  The vector and matrix arguments are not referenced when N = 0, or M = 0
C>
C>  -- Written on 22-October-1986.
C>     Jack Dongarra, Argonne National Lab.
C>     Jeremy Du Croz, Nag Central Office.
C>     Sven Hammarling, Nag Central Office.
C>     Richard Hanson, Sandia National Labs.
C> \endverbatim
C>
C  =====================================================================
      SUBROUTINE STPMV_BV(uplo, trans, diag, n, ap, apb, x, xb, incx, 
     +                    nbdirs)
      IMPLICIT NONE
      INCLUDE 'DIFFSIZES.inc'
C  Hint: ISIZE1OFap should be the size of dimension 1 of array ap
C  Hint: nbdirsmax should be the maximum number of differentiation directions
C
C  -- Reference BLAS level2 routine --
C  -- Reference BLAS is a software package provided by Univ. of Tennessee,    --
C  -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
C
C     .. Scalar Arguments ..
      INTEGER incx, n
      CHARACTER diag, trans, uplo
C     ..
C     .. Array Arguments ..
      REAL ap(*), x(*)
      REAL apb(nbdirsmax, *), xb(nbdirsmax, *)
C     ..
C
C  =====================================================================
C
C     .. Parameters ..
      REAL zero
      PARAMETER (zero=0.0e+0)
C     ..
C     .. Local Scalars ..
      REAL temp
      REAL tempb(nbdirsmax)
      INTEGER i, info, ix, j, jx, k, kk, kx
      LOGICAL nounit
      EXTERNAL LSAME
      INTEGER ISIZE1OFAp
C     ..
C     .. External Functions ..
      INTEGER get_ISIZE1OFAp
      EXTERNAL get_ISIZE1OFAp
      LOGICAL LSAME
C     ..
C     .. External Subroutines ..
      EXTERNAL XERBLA, check_ISIZE1OFAp_initialized
      INTEGER nd
      INTEGER ad_to
      INTEGER*4 branch
      INTEGER ad_from
      INTEGER ad_to0
      INTEGER ad_to1
      INTEGER ad_from0
      INTEGER ad_to2
      INTEGER ad_from1
      INTEGER ad_from2
      INTEGER ad_to3
      INTEGER ad_from3
      INTEGER ad_from4
      INTEGER ad_to4
      INTEGER ii1
      INTEGER nbdirs
C     ..
C
C     Test the input parameters.
C
C     Check 0 < nbdirs <= nbdirsmax (required by DIFFSIZES.inc)
      CALL check_ISIZE1OFAp_initialized()
      ISIZE1OFAp = get_ISIZE1OFAp()
      IF (nbdirs.LE.0 .OR. nbdirs.GT.nbdirsmax) THEN
        WRITE(*,'(A,I0,A,I0,A)') 'Error: nbdirs=', nbdirs,
     +  ' must be in 1..nbdirsmax=', nbdirsmax, '. Stopping.'
        STOP 1
      END IF
C
      info = 0
      IF (.NOT.LSAME(uplo, 'U') .AND. (.NOT.LSAME(uplo, 'L'))) THEN
        CALL PUSHCONTROL3B(0)
        info = 1
      ELSE IF (.NOT.LSAME(trans, 'N') .AND. (.NOT.LSAME(trans, 'T')) 
     +    .AND. (.NOT.LSAME(trans, 'C'))) THEN
        CALL PUSHCONTROL3B(1)
        info = 2
      ELSE IF (.NOT.LSAME(diag, 'U') .AND. (.NOT.LSAME(diag, 'N'))) THEN
        CALL PUSHCONTROL3B(2)
        info = 3
      ELSE IF (n .LT. 0) THEN
        CALL PUSHCONTROL3B(3)
        info = 4
      ELSE IF (incx .EQ. 0) THEN
        CALL PUSHCONTROL3B(4)
        info = 7
      ELSE
        CALL PUSHCONTROL3B(4)
      END IF
      IF (info .EQ. 0) THEN
C
C     Quick return if possible.
C
        IF (n .EQ. 0) THEN
          DO ii1=1,ISIZE1OFap
            DO nd=1,nbdirsmax
              apb(nd, ii1) = 0.0
            ENDDO
          ENDDO
        ELSE
C
          nounit = LSAME(diag, 'N')
C
C     Set up the start point in X if the increment is not unity. This
C     will be  ( N - 1 )*INCX  too small for descending loops.
C
          IF (incx .LE. 0) THEN
            CALL PUSHCONTROL1B(0)
            kx = 1 - (n-1)*incx
          ELSE IF (incx .NE. 1) THEN
            CALL PUSHCONTROL1B(1)
            kx = 1
          ELSE
            CALL PUSHCONTROL1B(1)
          END IF
C
C     Start the operations. In this version the elements of AP are
C     accessed sequentially with one pass through AP.
C
          IF (LSAME(trans, 'N')) THEN
C
C        Form  x:= A*x.
C
            IF (LSAME(uplo, 'U')) THEN
              kk = 1
              IF (incx .EQ. 1) THEN
                DO j=1,n
                  IF (x(j) .NE. zero) THEN
                    CALL PUSHREAL4(temp)
                    temp = x(j)
                    k = kk
                    DO i=1,j-1
                      CALL PUSHREAL4(x(i))
                      x(i) = x(i) + temp*ap(k)
                      CALL PUSHINTEGER4(k)
                      k = k + 1
                    ENDDO
                    CALL PUSHINTEGER4(i - 1)
                    IF (nounit) THEN
                      CALL PUSHREAL4(x(j))
                      x(j) = x(j)*ap(kk+j-1)
                      CALL PUSHCONTROL2B(0)
                    ELSE
                      CALL PUSHCONTROL2B(1)
                    END IF
                  ELSE
                    CALL PUSHCONTROL2B(2)
                  END IF
                  CALL PUSHINTEGER4(kk)
                  kk = kk + j
                ENDDO
                DO ii1=1,ISIZE1OFap
                  DO nd=1,nbdirsmax
                    apb(nd, ii1) = 0.0
                  ENDDO
                ENDDO
                DO 100 j=n,1,-1
                  CALL POPINTEGER4(kk)
                  CALL POPCONTROL2B(branch)
                  IF (branch .EQ. 0) THEN
                    CALL POPREAL4(x(j))
                    DO nd=1,nbdirs
                      apb(nd, kk+j-1) = apb(nd, kk+j-1) + x(j)*xb(nd, j)
                      xb(nd, j) = ap(kk+j-1)*xb(nd, j)
                    ENDDO
                  ELSE IF (branch .NE. 1) THEN
                    GOTO 100
                  END IF
                  DO nd=1,nbdirsmax
                    tempb(nd) = 0.0
                  ENDDO
                  CALL POPINTEGER4(ad_to)
                  DO i=ad_to,1,-1
                    CALL POPINTEGER4(k)
                    CALL POPREAL4(x(i))
                    DO nd=1,nbdirs
                      tempb(nd) = tempb(nd) + ap(k)*xb(nd, i)
                      apb(nd, k) = apb(nd, k) + temp*xb(nd, i)
                    ENDDO
                  ENDDO
                  CALL POPREAL4(temp)
                  DO nd=1,nbdirs
                    xb(nd, j) = xb(nd, j) + tempb(nd)
                  ENDDO
 100            CONTINUE
              ELSE
                jx = kx
                DO j=1,n
                  IF (x(jx) .NE. zero) THEN
                    CALL PUSHREAL4(temp)
                    temp = x(jx)
                    ix = kx
                    ad_from = kk
                    DO k=ad_from,kk+j-2
                      CALL PUSHREAL4(x(ix))
                      x(ix) = x(ix) + temp*ap(k)
                      CALL PUSHINTEGER4(ix)
                      ix = ix + incx
                    ENDDO
                    CALL PUSHINTEGER4(k - 1)
                    CALL PUSHINTEGER4(ad_from)
                    IF (nounit) THEN
                      CALL PUSHREAL4(x(jx))
                      x(jx) = x(jx)*ap(kk+j-1)
                      CALL PUSHCONTROL2B(0)
                    ELSE
                      CALL PUSHCONTROL2B(1)
                    END IF
                  ELSE
                    CALL PUSHCONTROL2B(2)
                  END IF
                  CALL PUSHINTEGER4(jx)
                  jx = jx + incx
                  CALL PUSHINTEGER4(kk)
                  kk = kk + j
                ENDDO
                DO ii1=1,ISIZE1OFap
                  DO nd=1,nbdirsmax
                    apb(nd, ii1) = 0.0
                  ENDDO
                ENDDO
                DO 110 j=n,1,-1
                  CALL POPINTEGER4(kk)
                  CALL POPINTEGER4(jx)
                  CALL POPCONTROL2B(branch)
                  IF (branch .EQ. 0) THEN
                    CALL POPREAL4(x(jx))
                    DO nd=1,nbdirs
                      apb(nd, kk+j-1) = apb(nd, kk+j-1) + x(jx)*xb(nd, 
     +                  jx)
                      xb(nd, jx) = ap(kk+j-1)*xb(nd, jx)
                    ENDDO
                  ELSE IF (branch .NE. 1) THEN
                    GOTO 110
                  END IF
                  DO nd=1,nbdirsmax
                    tempb(nd) = 0.0
                  ENDDO
                  CALL POPINTEGER4(ad_from)
                  CALL POPINTEGER4(ad_to0)
                  DO k=ad_to0,ad_from,-1
                    CALL POPINTEGER4(ix)
                    CALL POPREAL4(x(ix))
                    DO nd=1,nbdirs
                      tempb(nd) = tempb(nd) + ap(k)*xb(nd, ix)
                      apb(nd, k) = apb(nd, k) + temp*xb(nd, ix)
                    ENDDO
                  ENDDO
                  CALL POPREAL4(temp)
                  DO nd=1,nbdirs
                    xb(nd, jx) = xb(nd, jx) + tempb(nd)
                  ENDDO
 110            CONTINUE
              END IF
            ELSE
              kk = n*(n+1)/2
              IF (incx .EQ. 1) THEN
                DO j=n,1,-1
                  IF (x(j) .NE. zero) THEN
                    CALL PUSHREAL4(temp)
                    temp = x(j)
                    k = kk
                    DO i=n,j+1,-1
                      CALL PUSHREAL4(x(i))
                      x(i) = x(i) + temp*ap(k)
                      CALL PUSHINTEGER4(k)
                      k = k - 1
                    ENDDO
                    CALL PUSHINTEGER4(i + 1)
                    IF (nounit) THEN
                      CALL PUSHREAL4(x(j))
                      x(j) = x(j)*ap(kk-n+j)
                      CALL PUSHCONTROL2B(0)
                    ELSE
                      CALL PUSHCONTROL2B(1)
                    END IF
                  ELSE
                    CALL PUSHCONTROL2B(2)
                  END IF
                  CALL PUSHINTEGER4(kk)
                  kk = kk - (n-j+1)
                ENDDO
                DO ii1=1,ISIZE1OFap
                  DO nd=1,nbdirsmax
                    apb(nd, ii1) = 0.0
                  ENDDO
                ENDDO
                DO 120 j=1,n,1
                  CALL POPINTEGER4(kk)
                  CALL POPCONTROL2B(branch)
                  IF (branch .EQ. 0) THEN
                    CALL POPREAL4(x(j))
                    DO nd=1,nbdirs
                      apb(nd, kk-n+j) = apb(nd, kk-n+j) + x(j)*xb(nd, j)
                      xb(nd, j) = ap(kk-n+j)*xb(nd, j)
                    ENDDO
                  ELSE IF (branch .NE. 1) THEN
                    GOTO 120
                  END IF
                  DO nd=1,nbdirsmax
                    tempb(nd) = 0.0
                  ENDDO
                  CALL POPINTEGER4(ad_to1)
                  DO i=ad_to1,n,1
                    CALL POPINTEGER4(k)
                    CALL POPREAL4(x(i))
                    DO nd=1,nbdirs
                      tempb(nd) = tempb(nd) + ap(k)*xb(nd, i)
                      apb(nd, k) = apb(nd, k) + temp*xb(nd, i)
                    ENDDO
                  ENDDO
                  CALL POPREAL4(temp)
                  DO nd=1,nbdirs
                    xb(nd, j) = xb(nd, j) + tempb(nd)
                  ENDDO
 120            CONTINUE
              ELSE
                kx = kx + (n-1)*incx
                jx = kx
                DO j=n,1,-1
                  IF (x(jx) .NE. zero) THEN
                    CALL PUSHREAL4(temp)
                    temp = x(jx)
                    ix = kx
                    ad_from0 = kk
                    DO k=ad_from0,kk-(n-(j+1)),-1
                      CALL PUSHREAL4(x(ix))
                      x(ix) = x(ix) + temp*ap(k)
                      CALL PUSHINTEGER4(ix)
                      ix = ix - incx
                    ENDDO
                    CALL PUSHINTEGER4(k + 1)
                    CALL PUSHINTEGER4(ad_from0)
                    IF (nounit) THEN
                      CALL PUSHREAL4(x(jx))
                      x(jx) = x(jx)*ap(kk-n+j)
                      CALL PUSHCONTROL2B(0)
                    ELSE
                      CALL PUSHCONTROL2B(1)
                    END IF
                  ELSE
                    CALL PUSHCONTROL2B(2)
                  END IF
                  CALL PUSHINTEGER4(jx)
                  jx = jx - incx
                  CALL PUSHINTEGER4(kk)
                  kk = kk - (n-j+1)
                ENDDO
                DO ii1=1,ISIZE1OFap
                  DO nd=1,nbdirsmax
                    apb(nd, ii1) = 0.0
                  ENDDO
                ENDDO
                DO 130 j=1,n,1
                  CALL POPINTEGER4(kk)
                  CALL POPINTEGER4(jx)
                  CALL POPCONTROL2B(branch)
                  IF (branch .EQ. 0) THEN
                    CALL POPREAL4(x(jx))
                    DO nd=1,nbdirs
                      apb(nd, kk-n+j) = apb(nd, kk-n+j) + x(jx)*xb(nd, 
     +                  jx)
                      xb(nd, jx) = ap(kk-n+j)*xb(nd, jx)
                    ENDDO
                  ELSE IF (branch .NE. 1) THEN
                    GOTO 130
                  END IF
                  DO nd=1,nbdirsmax
                    tempb(nd) = 0.0
                  ENDDO
                  CALL POPINTEGER4(ad_from0)
                  CALL POPINTEGER4(ad_to2)
                  DO k=ad_to2,ad_from0,1
                    CALL POPINTEGER4(ix)
                    CALL POPREAL4(x(ix))
                    DO nd=1,nbdirs
                      tempb(nd) = tempb(nd) + ap(k)*xb(nd, ix)
                      apb(nd, k) = apb(nd, k) + temp*xb(nd, ix)
                    ENDDO
                  ENDDO
                  CALL POPREAL4(temp)
                  DO nd=1,nbdirs
                    xb(nd, jx) = xb(nd, jx) + tempb(nd)
                  ENDDO
 130            CONTINUE
              END IF
            END IF
          ELSE IF (LSAME(uplo, 'U')) THEN
C
C        Form  x := A**T*x.
C
            kk = n*(n+1)/2
            IF (incx .EQ. 1) THEN
              DO j=n,1,-1
                temp = x(j)
                IF (nounit) THEN
                  CALL PUSHREAL4(temp)
                  temp = temp*ap(kk)
                  CALL PUSHCONTROL1B(0)
                ELSE
                  CALL PUSHCONTROL1B(1)
                END IF
                k = kk - 1
                ad_from1 = j - 1
                DO i=ad_from1,1,-1
                  temp = temp + ap(k)*x(i)
                  CALL PUSHINTEGER4(k)
                  k = k - 1
                ENDDO
                CALL PUSHINTEGER4(ad_from1)
                CALL PUSHREAL4(x(j))
                x(j) = temp
                CALL PUSHINTEGER4(kk)
                kk = kk - j
              ENDDO
              DO ii1=1,ISIZE1OFap
                DO nd=1,nbdirsmax
                  apb(nd, ii1) = 0.0
                ENDDO
              ENDDO
              DO j=1,n,1
                CALL POPINTEGER4(kk)
                CALL POPREAL4(x(j))
                DO nd=1,nbdirs
                  tempb(nd) = xb(nd, j)
                  xb(nd, j) = 0.0
                ENDDO
                CALL POPINTEGER4(ad_from1)
                DO i=1,ad_from1,1
                  CALL POPINTEGER4(k)
                  DO nd=1,nbdirs
                    apb(nd, k) = apb(nd, k) + x(i)*tempb(nd)
                    xb(nd, i) = xb(nd, i) + ap(k)*tempb(nd)
                  ENDDO
                ENDDO
                CALL POPCONTROL1B(branch)
                IF (branch .EQ. 0) THEN
                  CALL POPREAL4(temp)
                  DO nd=1,nbdirs
                    apb(nd, kk) = apb(nd, kk) + temp*tempb(nd)
                    tempb(nd) = ap(kk)*tempb(nd)
                  ENDDO
                END IF
                DO nd=1,nbdirs
                  xb(nd, j) = xb(nd, j) + tempb(nd)
                ENDDO
              ENDDO
            ELSE
              jx = kx + (n-1)*incx
              DO j=n,1,-1
                temp = x(jx)
                CALL PUSHINTEGER4(ix)
                ix = jx
                IF (nounit) THEN
                  CALL PUSHREAL4(temp)
                  temp = temp*ap(kk)
                  CALL PUSHCONTROL1B(1)
                ELSE
                  CALL PUSHCONTROL1B(0)
                END IF
                ad_from2 = kk - 1
                DO k=ad_from2,kk-j+1,-1
                  CALL PUSHINTEGER4(ix)
                  ix = ix - incx
                  temp = temp + ap(k)*x(ix)
                ENDDO
                CALL PUSHINTEGER4(k + 1)
                CALL PUSHINTEGER4(ad_from2)
                CALL PUSHREAL4(x(jx))
                x(jx) = temp
                CALL PUSHINTEGER4(jx)
                jx = jx - incx
                CALL PUSHINTEGER4(kk)
                kk = kk - j
              ENDDO
              DO ii1=1,ISIZE1OFap
                DO nd=1,nbdirsmax
                  apb(nd, ii1) = 0.0
                ENDDO
              ENDDO
              DO j=1,n,1
                CALL POPINTEGER4(kk)
                CALL POPINTEGER4(jx)
                CALL POPREAL4(x(jx))
                DO nd=1,nbdirs
                  tempb(nd) = xb(nd, jx)
                  xb(nd, jx) = 0.0
                ENDDO
                CALL POPINTEGER4(ad_from2)
                CALL POPINTEGER4(ad_to3)
                DO k=ad_to3,ad_from2,1
                  DO nd=1,nbdirs
                    apb(nd, k) = apb(nd, k) + x(ix)*tempb(nd)
                    xb(nd, ix) = xb(nd, ix) + ap(k)*tempb(nd)
                  ENDDO
                  CALL POPINTEGER4(ix)
                ENDDO
                CALL POPCONTROL1B(branch)
                IF (branch .NE. 0) THEN
                  CALL POPREAL4(temp)
                  DO nd=1,nbdirs
                    apb(nd, kk) = apb(nd, kk) + temp*tempb(nd)
                    tempb(nd) = ap(kk)*tempb(nd)
                  ENDDO
                END IF
                CALL POPINTEGER4(ix)
                DO nd=1,nbdirs
                  xb(nd, jx) = xb(nd, jx) + tempb(nd)
                ENDDO
              ENDDO
            END IF
          ELSE
            kk = 1
            IF (incx .EQ. 1) THEN
              DO j=1,n
                temp = x(j)
                IF (nounit) THEN
                  CALL PUSHREAL4(temp)
                  temp = temp*ap(kk)
                  CALL PUSHCONTROL1B(0)
                ELSE
                  CALL PUSHCONTROL1B(1)
                END IF
                k = kk + 1
                ad_from3 = j + 1
                DO i=ad_from3,n
                  temp = temp + ap(k)*x(i)
                  CALL PUSHINTEGER4(k)
                  k = k + 1
                ENDDO
                CALL PUSHINTEGER4(ad_from3)
                CALL PUSHREAL4(x(j))
                x(j) = temp
                CALL PUSHINTEGER4(kk)
                kk = kk + (n-j+1)
              ENDDO
              DO ii1=1,ISIZE1OFap
                DO nd=1,nbdirsmax
                  apb(nd, ii1) = 0.0
                ENDDO
              ENDDO
              DO j=n,1,-1
                CALL POPINTEGER4(kk)
                CALL POPREAL4(x(j))
                DO nd=1,nbdirs
                  tempb(nd) = xb(nd, j)
                  xb(nd, j) = 0.0
                ENDDO
                CALL POPINTEGER4(ad_from3)
                DO i=n,ad_from3,-1
                  CALL POPINTEGER4(k)
                  DO nd=1,nbdirs
                    apb(nd, k) = apb(nd, k) + x(i)*tempb(nd)
                    xb(nd, i) = xb(nd, i) + ap(k)*tempb(nd)
                  ENDDO
                ENDDO
                CALL POPCONTROL1B(branch)
                IF (branch .EQ. 0) THEN
                  CALL POPREAL4(temp)
                  DO nd=1,nbdirs
                    apb(nd, kk) = apb(nd, kk) + temp*tempb(nd)
                    tempb(nd) = ap(kk)*tempb(nd)
                  ENDDO
                END IF
                DO nd=1,nbdirs
                  xb(nd, j) = xb(nd, j) + tempb(nd)
                ENDDO
              ENDDO
            ELSE
              jx = kx
              DO j=1,n
                temp = x(jx)
                CALL PUSHINTEGER4(ix)
                ix = jx
                IF (nounit) THEN
                  CALL PUSHREAL4(temp)
                  temp = temp*ap(kk)
                  CALL PUSHCONTROL1B(1)
                ELSE
                  CALL PUSHCONTROL1B(0)
                END IF
                ad_from4 = kk + 1
                DO k=ad_from4,kk+n-j
                  CALL PUSHINTEGER4(ix)
                  ix = ix + incx
                  temp = temp + ap(k)*x(ix)
                ENDDO
                CALL PUSHINTEGER4(k - 1)
                CALL PUSHINTEGER4(ad_from4)
                CALL PUSHREAL4(x(jx))
                x(jx) = temp
                CALL PUSHINTEGER4(jx)
                jx = jx + incx
                CALL PUSHINTEGER4(kk)
                kk = kk + (n-j+1)
              ENDDO
              DO ii1=1,ISIZE1OFap
                DO nd=1,nbdirsmax
                  apb(nd, ii1) = 0.0
                ENDDO
              ENDDO
              DO j=n,1,-1
                CALL POPINTEGER4(kk)
                CALL POPINTEGER4(jx)
                CALL POPREAL4(x(jx))
                DO nd=1,nbdirs
                  tempb(nd) = xb(nd, jx)
                  xb(nd, jx) = 0.0
                ENDDO
                CALL POPINTEGER4(ad_from4)
                CALL POPINTEGER4(ad_to4)
                DO k=ad_to4,ad_from4,-1
                  DO nd=1,nbdirs
                    apb(nd, k) = apb(nd, k) + x(ix)*tempb(nd)
                    xb(nd, ix) = xb(nd, ix) + ap(k)*tempb(nd)
                  ENDDO
                  CALL POPINTEGER4(ix)
                ENDDO
                CALL POPCONTROL1B(branch)
                IF (branch .NE. 0) THEN
                  CALL POPREAL4(temp)
                  DO nd=1,nbdirs
                    apb(nd, kk) = apb(nd, kk) + temp*tempb(nd)
                    tempb(nd) = ap(kk)*tempb(nd)
                  ENDDO
                END IF
                CALL POPINTEGER4(ix)
                DO nd=1,nbdirs
                  xb(nd, jx) = xb(nd, jx) + tempb(nd)
                ENDDO
              ENDDO
            END IF
          END IF
          CALL POPCONTROL1B(branch)
        END IF
      END IF
      CALL POPCONTROL3B(branch)
      END

